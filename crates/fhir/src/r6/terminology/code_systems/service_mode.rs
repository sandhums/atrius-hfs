// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Service Mode
//!
//! An example set of Service Modes that could be applicable to use to characterize HealthcareServices or PractitionerRoles while searching

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ServiceMode {
    InPerson,
    Telephone,
    Videoconference,
    Chat,
}

impl ServiceMode {
    pub const URL: &'static str = "http://hl7.org/fhir/service-mode";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::InPerson => "in-person",
            Self::Telephone => "telephone",
            Self::Videoconference => "videoconference",
            Self::Chat => "chat",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "in-person" => Self::InPerson,
            "telephone" => Self::Telephone,
            "videoconference" => Self::Videoconference,
            "chat" => Self::Chat,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::InPerson => Some("In Person"),
            Self::Telephone => Some("Telephone"),
            Self::Videoconference => Some("Video Conference"),
            Self::Chat => Some("Chat/Messaging"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::InPerson => Some("The service will be provided in person"),
            Self::Telephone => Some(
                "The service will be provided by a teleconferencing facility or regular telephone",
            ),
            Self::Videoconference => {
                Some("The service will be provided over a video-conference facility")
            }
            Self::Chat => {
                Some("This service will be provided via a realtime chat/messaging conversation")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::InPerson => None,
            Self::Telephone => None,
            Self::Videoconference => None,
            Self::Chat => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::InPerson => 0,
            Self::Telephone => 0,
            Self::Videoconference => 0,
            Self::Chat => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
