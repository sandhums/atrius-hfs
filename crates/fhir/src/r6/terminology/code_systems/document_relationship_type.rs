// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Document Relationship Type
//!
//! The type of relationship between documents.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DocumentRelationshipType {
    Replaces,
    Transforms,
    Signs,
    Appends,
    Incorporates,
    Summarizes,
}

impl DocumentRelationshipType {
    pub const URL: &'static str = "http://hl7.org/fhir/document-relationship-type";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Replaces => "replaces",
            Self::Transforms => "transforms",
            Self::Signs => "signs",
            Self::Appends => "appends",
            Self::Incorporates => "incorporates",
            Self::Summarizes => "summarizes",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "replaces" => Self::Replaces,
            "transforms" => Self::Transforms,
            "signs" => Self::Signs,
            "appends" => Self::Appends,
            "incorporates" => Self::Incorporates,
            "summarizes" => Self::Summarizes,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Replaces => Some("Replaces"),
            Self::Transforms => Some("Transforms"),
            Self::Signs => Some("Signs"),
            Self::Appends => Some("Appends"),
            Self::Incorporates => Some("Incorporates"),
            Self::Summarizes => Some("Summarizes"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Replaces => {
                Some("This document logically replaces or supersedes the target document.")
            }
            Self::Transforms => Some(
                "This document was generated by transforming the target document (e.g. format or language conversion).",
            ),
            Self::Signs => Some("This document is a signature of the target document."),
            Self::Appends => {
                Some("This document adds additional information to the target document.")
            }
            Self::Incorporates => {
                Some("This document embeds the content from the (source) target document.")
            }
            Self::Summarizes => {
                Some("This document summarizes the content from the (source) target document.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Replaces => None,
            Self::Transforms => None,
            Self::Signs => None,
            Self::Appends => None,
            Self::Incorporates => None,
            Self::Summarizes => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Replaces => 0,
            Self::Transforms => 0,
            Self::Signs => 0,
            Self::Appends => 0,
            Self::Incorporates => 0,
            Self::Summarizes => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
