// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Group Type
//!
//! A code that describes the use of the group. The use of the group usually dictates what kind of entities can be members of the group.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum GroupType_a53ddbfe {
    PatientList,
    Family,
    Household,
    Workplace,
    EventAttendee,
    ExposureGroup,
    Herd,
    Locality,
    Organization,
    Collection,
}

impl GroupType_a53ddbfe {
    pub const URL: &'static str = "http://hl7.org/fhir/group-code";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::PatientList => "patient-list",
            Self::Family => "family",
            Self::Household => "household",
            Self::Workplace => "workplace",
            Self::EventAttendee => "event-attendee",
            Self::ExposureGroup => "exposure-group",
            Self::Herd => "herd",
            Self::Locality => "locality",
            Self::Organization => "organization",
            Self::Collection => "collection",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "patient-list" => Self::PatientList,
            "family" => Self::Family,
            "household" => Self::Household,
            "workplace" => Self::Workplace,
            "event-attendee" => Self::EventAttendee,
            "exposure-group" => Self::ExposureGroup,
            "herd" => Self::Herd,
            "locality" => Self::Locality,
            "organization" => Self::Organization,
            "collection" => Self::Collection,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::PatientList => Some("Patient-List"),
            Self::Family => Some("Family"),
            Self::Household => Some("Household"),
            Self::Workplace => Some("Workplace"),
            Self::EventAttendee => Some("Event Attendee"),
            Self::ExposureGroup => Some("Exposure Group"),
            Self::Herd => Some("Herd"),
            Self::Locality => Some("Locality"),
            Self::Organization => Some("Organization"),
            Self::Collection => Some("Collection"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::PatientList => {
                Some("A list of patients for the purpose of some kind of clinical treatment.")
            }
            Self::Family => Some(
                "A group of persons and/or animals that constitute a family. The definition of family is not precisely fixed and subject to much variableness in interpretation.",
            ),
            Self::Household => Some(
                "A group of persons and/or animals that reside in a singe house. Both the definitions of \'reside\' and \'house\' are not precisely fixed.",
            ),
            Self::Workplace => Some("A group of persons who work together in a single place."),
            Self::EventAttendee => Some(
                "Group contains a set of persons and/or animals who were at the same place at the same time. This is typically used for an infection event",
            ),
            Self::ExposureGroup => Some(
                "Group contains a set of persons and/or animals who were exposed to some antigen or infectious agent.",
            ),
            Self::Herd => {
                Some("Group contains a set of animals that are treated as a coherent group.")
            }
            Self::Locality => Some(
                "Group contains HealthcareService resources that are found in a single geographical location.",
            ),
            Self::Organization => Some("A collection of healthcare services."),
            Self::Collection => Some("A collection of entities for an arbitrary reason."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::PatientList => None,
            Self::Family => None,
            Self::Household => None,
            Self::Workplace => None,
            Self::EventAttendee => None,
            Self::ExposureGroup => None,
            Self::Herd => None,
            Self::Locality => None,
            Self::Organization => None,
            Self::Collection => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::PatientList => 0,
            Self::Family => 0,
            Self::Household => 0,
            Self::Workplace => 0,
            Self::EventAttendee => 0,
            Self::ExposureGroup => 0,
            Self::Herd => 0,
            Self::Locality => 0,
            Self::Organization => 0,
            Self::Collection => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
