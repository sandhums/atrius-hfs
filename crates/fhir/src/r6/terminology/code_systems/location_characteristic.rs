// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Location Characteristic
//!
//! Example Set of Location Characteristics.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum LocationCharacteristic {
    Wheelchair,
    HasTranslation,
    HasOxyNitro,
    HasNegPress,
    HasIsoWard,
    HasIcu,
}

impl LocationCharacteristic {
    pub const URL: &'static str = "http://hl7.org/fhir/location-characteristic";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Wheelchair => "wheelchair",
            Self::HasTranslation => "has-translation",
            Self::HasOxyNitro => "has-oxy-nitro",
            Self::HasNegPress => "has-neg-press",
            Self::HasIsoWard => "has-iso-ward",
            Self::HasIcu => "has-icu",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "wheelchair" => Self::Wheelchair,
            "has-translation" => Self::HasTranslation,
            "has-oxy-nitro" => Self::HasOxyNitro,
            "has-neg-press" => Self::HasNegPress,
            "has-iso-ward" => Self::HasIsoWard,
            "has-icu" => Self::HasIcu,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Wheelchair => Some("Wheelchair accessible"),
            Self::HasTranslation => Some("Translation services available"),
            Self::HasOxyNitro => Some("Oxygen/nitrogen available"),
            Self::HasNegPress => Some("Negative pressure rooms available"),
            Self::HasIsoWard => Some("Isolation ward"),
            Self::HasIcu => Some("Has ICU"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Wheelchair => {
                Some("The location is accessible to those requiring wheelchair access")
            }
            Self::HasTranslation => Some("The location has translation services available"),
            Self::HasOxyNitro => Some("The location has oxygen and nitrogen services available"),
            Self::HasNegPress => Some("The location has negative pressure rooms available"),
            Self::HasIsoWard => Some("The location is or has an isolation ward"),
            Self::HasIcu => Some("The location is or has an intensive care unit"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Wheelchair => None,
            Self::HasTranslation => None,
            Self::HasOxyNitro => None,
            Self::HasNegPress => None,
            Self::HasIsoWard => None,
            Self::HasIcu => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Wheelchair => 0,
            Self::HasTranslation => 0,
            Self::HasOxyNitro => 0,
            Self::HasNegPress => 0,
            Self::HasIsoWard => 0,
            Self::HasIcu => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
