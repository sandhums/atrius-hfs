// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Definition Method
//!
//! The method used to define, describe, or determine a characteristic value.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DefinitionMethod {
    SystematicAssessment,
    NonSystematicAssessment,
    Mean,
    Median,
    MeanOfMean,
    MeanOfMedian,
    MedianOfMean,
    MedianOfMedian,
}

impl DefinitionMethod {
    pub const URL: &'static str = "http://hl7.org/fhir/definition-method";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::SystematicAssessment => "systematic-assessment",
            Self::NonSystematicAssessment => "non-systematic-assessment",
            Self::Mean => "mean",
            Self::Median => "median",
            Self::MeanOfMean => "mean-of-mean",
            Self::MeanOfMedian => "mean-of-median",
            Self::MedianOfMean => "median-of-mean",
            Self::MedianOfMedian => "median-of-median",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "systematic-assessment" => Self::SystematicAssessment,
            "non-systematic-assessment" => Self::NonSystematicAssessment,
            "mean" => Self::Mean,
            "median" => Self::Median,
            "mean-of-mean" => Self::MeanOfMean,
            "mean-of-median" => Self::MeanOfMedian,
            "median-of-mean" => Self::MedianOfMean,
            "median-of-median" => Self::MedianOfMedian,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::SystematicAssessment => Some("Systematic Assessment"),
            Self::NonSystematicAssessment => Some("Non-Systematic Assessment"),
            Self::Mean => Some("Mean"),
            Self::Median => Some("Median"),
            Self::MeanOfMean => Some("Mean of Means"),
            Self::MeanOfMedian => Some("Mean of Medians"),
            Self::MedianOfMean => Some("Median of Means"),
            Self::MedianOfMedian => Some("Median of Medians"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::SystematicAssessment => Some(
                "Any method of routinely determining whether or not specific outcomes (e.g. adverse events) have occurred, for example through a standard questionnaire, regular investigator assessment, regular laboratory testing, or other method",
            ),
            Self::NonSystematicAssessment => Some(
                "Any non-systematic method for determining whether or not adverse events have occurred, such as self-reporting by participants or occasional assessment/testing",
            ),
            Self::Mean => Some("Aggregated using mean of observed values."),
            Self::Median => Some("Aggregated using median of observed values."),
            Self::MeanOfMean => Some("Aggregated using mean of means (e.g. study mean values)."),
            Self::MeanOfMedian => {
                Some("Aggregated using mean of medians (e.g. study median values).")
            }
            Self::MedianOfMean => {
                Some("Aggregated using median of means (e.g. study mean values).")
            }
            Self::MedianOfMedian => {
                Some("Aggregated using median of medians (e.g. study median values).")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::SystematicAssessment => None,
            Self::NonSystematicAssessment => None,
            Self::Mean => None,
            Self::Median => None,
            Self::MeanOfMean => None,
            Self::MeanOfMedian => None,
            Self::MedianOfMean => None,
            Self::MedianOfMedian => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::SystematicAssessment => 0,
            Self::NonSystematicAssessment => 0,
            Self::Mean => 0,
            Self::Median => 0,
            Self::MeanOfMean => 0,
            Self::MeanOfMedian => 0,
            Self::MedianOfMean => 0,
            Self::MedianOfMedian => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
