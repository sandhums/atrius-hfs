// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! MedicationRequest dispenser instructions
//!
//! MedicationRequest dispenser instructions

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MedicationRequestDispenserInstructions {
    DispenseAsWritten,
    SplitPackAllowed,
    CounselPatient,
    AdditionalMonitoringRequired,
}

impl MedicationRequestDispenserInstructions {
    pub const URL: &'static str =
        "http://hl7.org/fhir/CodeSystem/medicationrequest-dispenser-instructions";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::DispenseAsWritten => "dispense-as-written",
            Self::SplitPackAllowed => "split-pack-allowed",
            Self::CounselPatient => "counsel-patient",
            Self::AdditionalMonitoringRequired => "additional-monitoring-required",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "dispense-as-written" => Self::DispenseAsWritten,
            "split-pack-allowed" => Self::SplitPackAllowed,
            "counsel-patient" => Self::CounselPatient,
            "additional-monitoring-required" => Self::AdditionalMonitoringRequired,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::DispenseAsWritten => Some("Dispenser as written"),
            Self::SplitPackAllowed => Some("Split pack allowed"),
            Self::CounselPatient => Some("Counsel patient"),
            Self::AdditionalMonitoringRequired => Some("Additional monitoring required"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::DispenseAsWritten => {
                Some("Medication should be dispensed exactly as indicated in the request.")
            }
            Self::SplitPackAllowed => Some("Split pack allowed."),
            Self::CounselPatient => Some("Dispenser should provide counseling to the patient."),
            Self::AdditionalMonitoringRequired => {
                Some("Flag that extra monitoring is needed when dispensing.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::DispenseAsWritten => None,
            Self::SplitPackAllowed => None,
            Self::CounselPatient => None,
            Self::AdditionalMonitoringRequired => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::DispenseAsWritten => 0,
            Self::SplitPackAllowed => 0,
            Self::CounselPatient => 0,
            Self::AdditionalMonitoringRequired => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
