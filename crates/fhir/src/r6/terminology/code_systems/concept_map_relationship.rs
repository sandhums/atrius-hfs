// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! ConceptMapRelationship
//!
//! The relationship between concepts.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ConceptMapRelationship {
    RelatedTo,
    Equivalent,
    SourceIsNarrowerThanTarget,
    SourceIsBroaderThanTarget,
    NotRelatedTo,
}

impl ConceptMapRelationship {
    pub const URL: &'static str = "http://hl7.org/fhir/concept-map-relationship";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::RelatedTo => "related-to",
            Self::Equivalent => "equivalent",
            Self::SourceIsNarrowerThanTarget => "source-is-narrower-than-target",
            Self::SourceIsBroaderThanTarget => "source-is-broader-than-target",
            Self::NotRelatedTo => "not-related-to",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "related-to" => Self::RelatedTo,
            "equivalent" => Self::Equivalent,
            "source-is-narrower-than-target" => Self::SourceIsNarrowerThanTarget,
            "source-is-broader-than-target" => Self::SourceIsBroaderThanTarget,
            "not-related-to" => Self::NotRelatedTo,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::RelatedTo => Some("Related To"),
            Self::Equivalent => Some("Equivalent"),
            Self::SourceIsNarrowerThanTarget => Some("Source Is Narrower Than Target"),
            Self::SourceIsBroaderThanTarget => Some("Source Is Broader Than Target"),
            Self::NotRelatedTo => Some("Not Related To"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::RelatedTo => Some(
                "The concepts are related to each other, but the exact relationship is not known.",
            ),
            Self::Equivalent => Some("The definitions of the concepts mean the same thing."),
            Self::SourceIsNarrowerThanTarget => {
                Some("The source concept is narrower in meaning than the target concept.")
            }
            Self::SourceIsBroaderThanTarget => {
                Some("The source concept is broader in meaning than the target concept.")
            }
            Self::NotRelatedTo => Some(
                "This is an explicit assertion that the target concept is not related to the source concept.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::RelatedTo => None,
            Self::Equivalent => Some("related-to"),
            Self::SourceIsNarrowerThanTarget => Some("related-to"),
            Self::SourceIsBroaderThanTarget => Some("related-to"),
            Self::NotRelatedTo => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::RelatedTo => 0,
            Self::Equivalent => 1,
            Self::SourceIsNarrowerThanTarget => 1,
            Self::SourceIsBroaderThanTarget => 1,
            Self::NotRelatedTo => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
