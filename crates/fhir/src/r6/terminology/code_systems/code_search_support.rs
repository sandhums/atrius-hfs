// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Code Search Support
//!
//! The degree to which the server supports the code search parameter on ValueSet, if it is supported.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum CodeSearchSupport {
    InCompose,
    InExpansion,
    InComposeOrExpansion,
}

impl CodeSearchSupport {
    pub const URL: &'static str = "http://hl7.org/fhir/code-search-support";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::InCompose => "in-compose",
            Self::InExpansion => "in-expansion",
            Self::InComposeOrExpansion => "in-compose-or-expansion",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "in-compose" => Self::InCompose,
            "in-expansion" => Self::InExpansion,
            "in-compose-or-expansion" => Self::InComposeOrExpansion,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::InCompose => Some("In Compose"),
            Self::InExpansion => Some("In Expansion"),
            Self::InComposeOrExpansion => Some("In Compose Or Expansion"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::InCompose => Some(
                "The search for code on ValueSet returns ValueSet resources where the code is included in the extensional definition of the ValueSet.",
            ),
            Self::InExpansion => Some(
                "The search for code on ValueSet returns ValueSet resources where the code is contained in the  ValueSet expansion.",
            ),
            Self::InComposeOrExpansion => Some(
                "The search for code on ValueSet returns ValueSet resources where the code is included in the extensional definition or contained in the ValueSet expansion.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::InCompose => None,
            Self::InExpansion => None,
            Self::InComposeOrExpansion => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::InCompose => 0,
            Self::InExpansion => 0,
            Self::InComposeOrExpansion => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
