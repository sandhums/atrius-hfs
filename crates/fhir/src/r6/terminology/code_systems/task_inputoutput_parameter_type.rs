// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Task Input/Output Parameter Type
//!
//! The input and output types for the task.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum TaskInputOutputParameterType {
    CollectedSpecimen,
    ReferenceResults,
    PriorResults,
    ConcomitantMedications,
    FillerOrder,
    DataQuery,
    DataCode,
    DataValue,
}

impl TaskInputOutputParameterType {
    pub const URL: &'static str = "http://hl7.org/fhir/task-inputoutput-parameter-type";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::CollectedSpecimen => "collected-specimen",
            Self::ReferenceResults => "reference-results",
            Self::PriorResults => "prior-results",
            Self::ConcomitantMedications => "concomitant-medications",
            Self::FillerOrder => "filler-order",
            Self::DataQuery => "data-query",
            Self::DataCode => "data-code",
            Self::DataValue => "data-value",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "collected-specimen" => Self::CollectedSpecimen,
            "reference-results" => Self::ReferenceResults,
            "prior-results" => Self::PriorResults,
            "concomitant-medications" => Self::ConcomitantMedications,
            "filler-order" => Self::FillerOrder,
            "data-query" => Self::DataQuery,
            "data-code" => Self::DataCode,
            "data-value" => Self::DataValue,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::CollectedSpecimen => Some("Collected Specimen"),
            Self::ReferenceResults => Some("Reference Results"),
            Self::PriorResults => Some("Prior Results"),
            Self::ConcomitantMedications => Some("Concomitant Medications"),
            Self::FillerOrder => Some("Filler Order"),
            Self::DataQuery => Some("data query input"),
            Self::DataCode => Some("data code input"),
            Self::DataValue => Some("data values output"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::CollectedSpecimen => Some("A specimen that has been collected."),
            Self::ReferenceResults => Some("Reference results to compare against."),
            Self::PriorResults => Some("Results from a previous order."),
            Self::ConcomitantMedications => Some(
                "Medications, supplements or drugs the patient consumes at the same time as something else.",
            ),
            Self::FillerOrder => Some("The order number associated with the filler."),
            Self::DataQuery => Some(
                "Indicates the parameters of a FHIR RESTful search to be executed whose results are requested by a data-request Task. The content will be the portion of the query URL following the \'base\'. E.g.  `Observation?code=foo`.",
            ),
            Self::DataCode => Some(
                "A code describing the information requested by a data-request Task - often a code for a type of document",
            ),
            Self::DataValue => Some(
                "A FHIR resource, typically a search-set Bundle, containing the requested information in response to a data-request Task",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::CollectedSpecimen => None,
            Self::ReferenceResults => None,
            Self::PriorResults => None,
            Self::ConcomitantMedications => None,
            Self::FillerOrder => None,
            Self::DataQuery => None,
            Self::DataCode => None,
            Self::DataValue => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::CollectedSpecimen => 0,
            Self::ReferenceResults => 0,
            Self::PriorResults => 0,
            Self::ConcomitantMedications => 0,
            Self::FillerOrder => 0,
            Self::DataQuery => 0,
            Self::DataCode => 0,
            Self::DataValue => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
