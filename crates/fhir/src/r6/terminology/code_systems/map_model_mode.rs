// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Structure Map Model Mode
//!
//! How the referenced structure is used in this mapping.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum StructureMapModelMode {
    Source,
    Queried,
    Target,
    Produced,
}

impl StructureMapModelMode {
    pub const URL: &'static str = "http://hl7.org/fhir/map-model-mode";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Source => "source",
            Self::Queried => "queried",
            Self::Target => "target",
            Self::Produced => "produced",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "source" => Self::Source,
            "queried" => Self::Queried,
            "target" => Self::Target,
            "produced" => Self::Produced,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Source => Some("Source Structure Definition"),
            Self::Queried => Some("Queried Structure Definition"),
            Self::Target => Some("Target Structure Definition"),
            Self::Produced => Some("Produced Structure Definition"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Source => Some(
                "This structure describes an instance passed to the mapping engine that is used a source of data.",
            ),
            Self::Queried => Some(
                "This structure describes an instance that the mapping engine may ask for that is used a source of data.",
            ),
            Self::Target => Some(
                "This structure describes an instance passed to the mapping engine that is used a target of data.",
            ),
            Self::Produced => Some(
                "This structure describes an instance that the mapping engine may ask to create that is used a target of data.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Source => None,
            Self::Queried => None,
            Self::Target => None,
            Self::Produced => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Source => 0,
            Self::Queried => 0,
            Self::Target => 0,
            Self::Produced => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
