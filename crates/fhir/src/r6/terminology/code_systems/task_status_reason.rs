// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Task Status Reason
//!
//! The current status reason of the task.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum TaskStatusReason {
    Missing,
    Misidentified,
    EquipmentIssue,
    EnvironmentalIssue,
    PersonnelIssue,
}

impl TaskStatusReason {
    pub const URL: &'static str = "http://hl7.org/fhir/task-status-reason";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Missing => "missing",
            Self::Misidentified => "misidentified",
            Self::EquipmentIssue => "equipment-issue",
            Self::EnvironmentalIssue => "environmental-issue",
            Self::PersonnelIssue => "personnel-issue",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "missing" => Self::Missing,
            "misidentified" => Self::Misidentified,
            "equipment-issue" => Self::EquipmentIssue,
            "environmental-issue" => Self::EnvironmentalIssue,
            "personnel-issue" => Self::PersonnelIssue,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Missing => Some("Missing"),
            Self::Misidentified => Some("Misidentified"),
            Self::EquipmentIssue => Some("Equipment-issue"),
            Self::EnvironmentalIssue => Some("Environmental-issue"),
            Self::PersonnelIssue => Some("Personnel-issue"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Missing => Some("An item nessary for task completion is missing."),
            Self::Misidentified => Some("Something related to the task was misidentified."),
            Self::EquipmentIssue => {
                Some("A piece of equipment necessary for completion of the task is malfunctioning.")
            }
            Self::EnvironmentalIssue => {
                Some("Something in the environment is preventing task completion.")
            }
            Self::PersonnelIssue => {
                Some("Key personnel necessary for task completion are not present.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Missing => None,
            Self::Misidentified => None,
            Self::EquipmentIssue => None,
            Self::EnvironmentalIssue => None,
            Self::PersonnelIssue => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Missing => 0,
            Self::Misidentified => 0,
            Self::EquipmentIssue => 0,
            Self::EnvironmentalIssue => 0,
            Self::PersonnelIssue => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
