// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Task Business Status
//!
//! Task execution business status.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum TaskBusinessStatus {
    SpecimenCollected,
    IvPrepped,
    SpecimenInTransit,
    WaitingForFinalReview,
}

impl TaskBusinessStatus {
    pub const URL: &'static str = "http://hl7.org/fhir/task-business-status";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::SpecimenCollected => "specimen-collected",
            Self::IvPrepped => "iv-prepped",
            Self::SpecimenInTransit => "specimen-in-transit",
            Self::WaitingForFinalReview => "waiting-for-final-review",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "specimen-collected" => Self::SpecimenCollected,
            "iv-prepped" => Self::IvPrepped,
            "specimen-in-transit" => Self::SpecimenInTransit,
            "waiting-for-final-review" => Self::WaitingForFinalReview,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::SpecimenCollected => Some("Specimen Collected"),
            Self::IvPrepped => Some("IV Prepped"),
            Self::SpecimenInTransit => Some("Specimen In Transit"),
            Self::WaitingForFinalReview => Some("Waiting For Final Review"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::SpecimenCollected => Some("The specimen has been collected."),
            Self::IvPrepped => Some("The patient IV has been prepared."),
            Self::SpecimenInTransit => Some("The specimen is in transit."),
            Self::WaitingForFinalReview => Some("Waiting for final review."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::SpecimenCollected => None,
            Self::IvPrepped => None,
            Self::SpecimenInTransit => None,
            Self::WaitingForFinalReview => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::SpecimenCollected => 0,
            Self::IvPrepped => 0,
            Self::SpecimenInTransit => 0,
            Self::WaitingForFinalReview => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
