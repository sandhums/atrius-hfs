// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Administration SubPotent Reason
//!
//! This value set is provided as an example. The value set to instantiate this attribute should be drawn from a robust terminology code system that consists of or contains concepts to support the medication administration process.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum AdministrationSubPotentReason {
    Partialdose,
    Vomited,
    Coldchainbreak,
    Recall,
    Adversestorage,
    Expired,
}

impl AdministrationSubPotentReason {
    pub const URL: &'static str = "http://hl7.org/fhir/CodeSystem/administration-subpotent-reason";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Partialdose => "partialdose",
            Self::Vomited => "vomited",
            Self::Coldchainbreak => "coldchainbreak",
            Self::Recall => "recall",
            Self::Adversestorage => "adversestorage",
            Self::Expired => "expired",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "partialdose" => Self::Partialdose,
            "vomited" => Self::Vomited,
            "coldchainbreak" => Self::Coldchainbreak,
            "recall" => Self::Recall,
            "adversestorage" => Self::Adversestorage,
            "expired" => Self::Expired,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Partialdose => Some("Partial Dose"),
            Self::Vomited => Some("Vomited"),
            Self::Coldchainbreak => Some("Cold Chain Break"),
            Self::Recall => Some("Manufacturer Recall"),
            Self::Adversestorage => Some("Adverse Storage"),
            Self::Expired => Some("Expired Product"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Partialdose => {
                Some("The full amount of the dose was not administered to the patient.")
            }
            Self::Vomited => Some("The patient vomited part of the dose."),
            Self::Coldchainbreak => Some("The medication experienced a cold chain break."),
            Self::Recall => Some("The medication was recalled by the manufacturer."),
            Self::Adversestorage => Some("The medication experienced adverse storage conditions."),
            Self::Expired => Some("The medication was expired at the time of administration."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Partialdose => None,
            Self::Vomited => Some("partialdose"),
            Self::Coldchainbreak => None,
            Self::Recall => None,
            Self::Adversestorage => None,
            Self::Expired => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Partialdose => 0,
            Self::Vomited => 1,
            Self::Coldchainbreak => 0,
            Self::Recall => 0,
            Self::Adversestorage => 0,
            Self::Expired => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
