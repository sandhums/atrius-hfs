// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Task Performer Function Code
//!
//! The performer of the task.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum TaskPerformerFunctionCode {
    Performer,
    Verifier,
    Witness,
    Referrer,
}

impl TaskPerformerFunctionCode {
    pub const URL: &'static str = "http://hl7.org/fhir/task-performer-function-code";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Performer => "performer",
            Self::Verifier => "verifier",
            Self::Witness => "witness",
            Self::Referrer => "referrer",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "performer" => Self::Performer,
            "verifier" => Self::Verifier,
            "witness" => Self::Witness,
            "referrer" => Self::Referrer,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Performer => Some("Performer"),
            Self::Verifier => Some("Verifier"),
            Self::Witness => Some("Witness"),
            Self::Referrer => Some("Referrer"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Performer => Some(
                "A person, non-person living subject, organization or device that who actually and principally carries out the Task action.",
            ),
            Self::Verifier => Some(
                "A person who verifies the correctness and appropriateness of the Task action (plan, order, event, etc.) and hence takes on accountability.",
            ),
            Self::Witness => Some(
                "A person witnessing the action happening without doing anything. A witness is not necessarily aware, much less approves of anything stated in the service event.",
            ),
            Self::Referrer => Some(
                "A person having referred the action of the Task to the performer (referring physician).",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Performer => None,
            Self::Verifier => None,
            Self::Witness => None,
            Self::Referrer => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Performer => 0,
            Self::Verifier => 0,
            Self::Witness => 0,
            Self::Referrer => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
