// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Clinical Use Definition Type
//!
//! Overall defining type of this clinical use definition.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ClinicalUseDefinitionType {
    Indication,
    Contraindication,
    Interaction,
    UndesirableEffect,
    Warning,
}

impl ClinicalUseDefinitionType {
    pub const URL: &'static str = "http://hl7.org/fhir/clinical-use-definition-type";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Indication => "indication",
            Self::Contraindication => "contraindication",
            Self::Interaction => "interaction",
            Self::UndesirableEffect => "undesirable-effect",
            Self::Warning => "warning",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "indication" => Self::Indication,
            "contraindication" => Self::Contraindication,
            "interaction" => Self::Interaction,
            "undesirable-effect" => Self::UndesirableEffect,
            "warning" => Self::Warning,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Indication => Some("Indication"),
            Self::Contraindication => Some("Contraindication"),
            Self::Interaction => Some("Interaction"),
            Self::UndesirableEffect => Some("Undesirable Effect"),
            Self::Warning => Some("Warning"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Indication => Some("A reason for giving the medication."),
            Self::Contraindication => Some("A reason for not giving the medication."),
            Self::Interaction => Some("Interactions between the medication and other substances."),
            Self::UndesirableEffect => {
                Some("Side effects or adverse effects associated with the medication.")
            }
            Self::Warning => {
                Some("A general warning or issue that is not specifically one of the other types.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Indication => None,
            Self::Contraindication => None,
            Self::Interaction => None,
            Self::UndesirableEffect => None,
            Self::Warning => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Indication => 0,
            Self::Contraindication => 0,
            Self::Interaction => 0,
            Self::UndesirableEffect => 0,
            Self::Warning => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
