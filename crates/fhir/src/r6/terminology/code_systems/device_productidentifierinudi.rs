// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Device Production Identifier In UDI
//!
//! The production identifier(s) that are expected to appear in the UDI carrier.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceProductionIdentifierInUDI {
    LotNumber,
    ManufacturedDate,
    SerialNumber,
    ExpirationDate,
    BiologicalSource,
    SoftwareVersion,
}

impl DeviceProductionIdentifierInUDI {
    pub const URL: &'static str = "http://hl7.org/fhir/device-productidentifierinudi";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::LotNumber => "lot-number",
            Self::ManufacturedDate => "manufactured-date",
            Self::SerialNumber => "serial-number",
            Self::ExpirationDate => "expiration-date",
            Self::BiologicalSource => "biological-source",
            Self::SoftwareVersion => "software-version",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "lot-number" => Self::LotNumber,
            "manufactured-date" => Self::ManufacturedDate,
            "serial-number" => Self::SerialNumber,
            "expiration-date" => Self::ExpirationDate,
            "biological-source" => Self::BiologicalSource,
            "software-version" => Self::SoftwareVersion,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::LotNumber => Some("Lot Number"),
            Self::ManufacturedDate => Some("Manufactured date"),
            Self::SerialNumber => Some("Serial Number"),
            Self::ExpirationDate => Some("Expiration date"),
            Self::BiologicalSource => Some("Biological source"),
            Self::SoftwareVersion => Some("Software Version"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::LotNumber => Some("The label includes the lot number."),
            Self::ManufacturedDate => Some("The label includes the manufacture date."),
            Self::SerialNumber => Some("The label includes the serial number."),
            Self::ExpirationDate => Some("The label includes the expiration date."),
            Self::BiologicalSource => Some("The label includes the biological source identifier."),
            Self::SoftwareVersion => Some("The label includes the software version."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::LotNumber => None,
            Self::ManufacturedDate => None,
            Self::SerialNumber => None,
            Self::ExpirationDate => None,
            Self::BiologicalSource => None,
            Self::SoftwareVersion => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::LotNumber => 0,
            Self::ManufacturedDate => 0,
            Self::SerialNumber => 0,
            Self::ExpirationDate => 0,
            Self::BiologicalSource => 0,
            Self::SoftwareVersion => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
