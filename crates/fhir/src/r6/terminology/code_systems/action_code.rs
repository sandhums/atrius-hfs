// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Action Code
//!
//! Provides examples of actions to be performed.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ActionCode {
    SendMessage,
    CollectInformation,
    PrescribeMedication,
    RecommendImmunization,
    OrderService,
    ProposeDiagnosis,
    RecordDetectedIssue,
    RecordInference,
    ReportFlag,
}

impl ActionCode {
    pub const URL: &'static str = "http://hl7.org/fhir/action-code";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::SendMessage => "send-message",
            Self::CollectInformation => "collect-information",
            Self::PrescribeMedication => "prescribe-medication",
            Self::RecommendImmunization => "recommend-immunization",
            Self::OrderService => "order-service",
            Self::ProposeDiagnosis => "propose-diagnosis",
            Self::RecordDetectedIssue => "record-detected-issue",
            Self::RecordInference => "record-inference",
            Self::ReportFlag => "report-flag",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "send-message" => Self::SendMessage,
            "collect-information" => Self::CollectInformation,
            "prescribe-medication" => Self::PrescribeMedication,
            "recommend-immunization" => Self::RecommendImmunization,
            "order-service" => Self::OrderService,
            "propose-diagnosis" => Self::ProposeDiagnosis,
            "record-detected-issue" => Self::RecordDetectedIssue,
            "record-inference" => Self::RecordInference,
            "report-flag" => Self::ReportFlag,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::SendMessage => Some("Send a message"),
            Self::CollectInformation => Some("Collect information"),
            Self::PrescribeMedication => Some("Prescribe a medication"),
            Self::RecommendImmunization => Some("Recommend an immunization"),
            Self::OrderService => Some("Order a service"),
            Self::ProposeDiagnosis => Some("Propose a diagnosis"),
            Self::RecordDetectedIssue => Some("Record a detected issue"),
            Self::RecordInference => Some("Record an inference"),
            Self::ReportFlag => Some("Report a flag"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::SendMessage => Some(
                "The action indicates that a particular message should be sent to a participant in the process.",
            ),
            Self::CollectInformation => Some(
                "The action indicates that information should be collected from a participant in the process.",
            ),
            Self::PrescribeMedication => Some(
                "The action indicates that a particular medication should be prescribed to the patient.",
            ),
            Self::RecommendImmunization => {
                Some("The action indicates that a particular immunization should be performed.")
            }
            Self::OrderService => {
                Some("The action indicates that a particular service should be provided.")
            }
            Self::ProposeDiagnosis => {
                Some("The action indicates that a particular diagnosis should be proposed.")
            }
            Self::RecordDetectedIssue => {
                Some("The action indicates that a particular detected issue should be recorded.")
            }
            Self::RecordInference => {
                Some("The action indicates that a particular inference should be recorded.")
            }
            Self::ReportFlag => {
                Some("The action indicates that a particular flag should be reported.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::SendMessage => None,
            Self::CollectInformation => None,
            Self::PrescribeMedication => None,
            Self::RecommendImmunization => None,
            Self::OrderService => None,
            Self::ProposeDiagnosis => None,
            Self::RecordDetectedIssue => None,
            Self::RecordInference => None,
            Self::ReportFlag => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::SendMessage => 0,
            Self::CollectInformation => 0,
            Self::PrescribeMedication => 0,
            Self::RecommendImmunization => 0,
            Self::OrderService => 0,
            Self::ProposeDiagnosis => 0,
            Self::RecordDetectedIssue => 0,
            Self::RecordInference => 0,
            Self::ReportFlag => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
