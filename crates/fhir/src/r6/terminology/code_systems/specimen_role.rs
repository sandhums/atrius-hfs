// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Specimen Role
//!
//! Codes providing the combined status of the specimen.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum SpecimenRole {
    B,
    C,
    E,
    F,
    O,
    P,
    Q,
    R,
    V,
}

impl SpecimenRole {
    pub const URL: &'static str = "http://hl7.org/fhir/specimen-role";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::B => "b",
            Self::C => "c",
            Self::E => "e",
            Self::F => "f",
            Self::O => "o",
            Self::P => "p",
            Self::Q => "q",
            Self::R => "r",
            Self::V => "v",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "b" => Self::B,
            "c" => Self::C,
            "e" => Self::E,
            "f" => Self::F,
            "o" => Self::O,
            "p" => Self::P,
            "q" => Self::Q,
            "r" => Self::R,
            "v" => Self::V,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::B => Some("Blind Sample"),
            Self::C => Some("Calibrator"),
            Self::E => Some("Electronic QC"),
            Self::F => Some("Filler Organization Proficiency"),
            Self::O => Some("Operator Proficiency"),
            Self::P => Some("Patient"),
            Self::Q => Some("Control specimen"),
            Self::R => Some("Replicate (of patient sample as a control)"),
            Self::V => Some("Verifying Calibrator"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::B => Some(
                "Used to test the validity of the measurement process, where the composition of the sample is unknown except to the person submitting it.",
            ),
            Self::C => Some("Used for initial setting of calibration of the instrument."),
            Self::E => {
                Some("Used with manufactured reference providing signals that simulate QC results.")
            }
            Self::F => Some(
                "Specimen used for testing proficiency of the organization performing the testing (Filler).",
            ),
            Self::O => Some("Specimen used for testing Operator Proficiency."),
            Self::P => Some("Used for any patient sample."),
            Self::Q => {
                Some("Used when specimen is the control specimen (either positive or negative).")
            }
            Self::R => Some("Used when a patient sample is re-run as a control for a repeat test."),
            Self::V => Some("Used for periodic calibration checks."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::B => None,
            Self::C => None,
            Self::E => None,
            Self::F => None,
            Self::O => None,
            Self::P => None,
            Self::Q => None,
            Self::R => None,
            Self::V => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::B => 0,
            Self::C => 0,
            Self::E => 0,
            Self::F => 0,
            Self::O => 0,
            Self::P => 0,
            Self::Q => 0,
            Self::R => 0,
            Self::V => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
