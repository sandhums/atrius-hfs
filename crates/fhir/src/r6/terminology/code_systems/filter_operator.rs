// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Filter Operator
//!
//! The kind of operation to perform as a part of a property based filter.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum FilterOperator {
    Code,
    IsA,
    DescendentOf,
    IsNotA,
    Regex,
    In_,
    NotIn,
    Generalizes,
    ChildOf,
    DescendentLeaf,
    Exists,
}

impl FilterOperator {
    pub const URL: &'static str = "http://hl7.org/fhir/filter-operator";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Code => "=",
            Self::IsA => "is-a",
            Self::DescendentOf => "descendent-of",
            Self::IsNotA => "is-not-a",
            Self::Regex => "regex",
            Self::In_ => "in",
            Self::NotIn => "not-in",
            Self::Generalizes => "generalizes",
            Self::ChildOf => "child-of",
            Self::DescendentLeaf => "descendent-leaf",
            Self::Exists => "exists",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "=" => Self::Code,
            "is-a" => Self::IsA,
            "descendent-of" => Self::DescendentOf,
            "is-not-a" => Self::IsNotA,
            "regex" => Self::Regex,
            "in" => Self::In_,
            "not-in" => Self::NotIn,
            "generalizes" => Self::Generalizes,
            "child-of" => Self::ChildOf,
            "descendent-leaf" => Self::DescendentLeaf,
            "exists" => Self::Exists,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Code => Some("Equals"),
            Self::IsA => Some("Is A (by subsumption)"),
            Self::DescendentOf => Some("Descendent Of (by subsumption)"),
            Self::IsNotA => Some("Not (Is A) (by subsumption)"),
            Self::Regex => Some("Regular Expression"),
            Self::In_ => Some("In Set"),
            Self::NotIn => Some("Not in Set"),
            Self::Generalizes => Some("Generalizes (by Subsumption)"),
            Self::ChildOf => Some("Child Of"),
            Self::DescendentLeaf => Some("Descendent Leaf"),
            Self::Exists => Some("Exists"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Code => Some("The specified property of the code equals the provided value."),
            Self::IsA => Some(
                "Includes all concept ids that have a transitive is-a relationship with the concept Id provided as the value, including the provided concept itself (include descendant codes and self).",
            ),
            Self::DescendentOf => Some(
                "Includes all concept ids that have a transitive is-a relationship with the concept Id provided as the value, excluding the provided concept itself (i.e. include descendant codes only).",
            ),
            Self::IsNotA => Some(
                "The specified property of the code does not have an is-a relationship with the provided value.",
            ),
            Self::Regex => Some(
                "The specified property of the code  matches the regex specified in the provided value.",
            ),
            Self::In_ => Some(
                "The specified property of the code is in the set of codes or concepts specified in the provided value (comma-separated list).",
            ),
            Self::NotIn => Some(
                "The specified property of the code is not in the set of codes or concepts specified in the provided value (comma-separated list).",
            ),
            Self::Generalizes => Some(
                "Includes all concept ids that have a transitive is-a relationship from the concept Id provided as the value, including the provided concept itself (i.e. include ancestor codes and self).",
            ),
            Self::ChildOf => Some(
                "Only concepts with a direct hierarchical relationship to the index code and no other concepts. This does not include the index code in the output.",
            ),
            Self::DescendentLeaf => Some(
                "Includes concept ids that have a transitive is-a relationship with the concept Id provided as the value, but which do not have any concept ids with transitive is-a relationships with themselves.",
            ),
            Self::Exists => Some(
                "The specified property of the code has at least one value (if the specified value is true; if the specified value is false, then matches when the specified property of the code has no values).",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Code => None,
            Self::IsA => None,
            Self::DescendentOf => None,
            Self::IsNotA => None,
            Self::Regex => None,
            Self::In_ => None,
            Self::NotIn => None,
            Self::Generalizes => None,
            Self::ChildOf => None,
            Self::DescendentLeaf => None,
            Self::Exists => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Code => 0,
            Self::IsA => 0,
            Self::DescendentOf => 0,
            Self::IsNotA => 0,
            Self::Regex => 0,
            Self::In_ => 0,
            Self::NotIn => 0,
            Self::Generalizes => 0,
            Self::ChildOf => 0,
            Self::DescendentLeaf => 0,
            Self::Exists => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
