// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Nutrition Intake Status Reason Codes
//!
//! Nutrition Intake Status Reason Codes

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum NutritionIntakeStatusReason {
    IncompleteData,
    UnableToProvideCare,
    RequestClarification,
    Allergy,
    SuspectedIntolerance,
    ScheduledForSurgery,
    ProductNotAvailable,
}

impl NutritionIntakeStatusReason {
    pub const URL: &'static str = "http://hl7.org/fhir/CodeSystem/nutritionintake-status-reason";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::IncompleteData => "incomplete-data",
            Self::UnableToProvideCare => "unable-to-provide-care",
            Self::RequestClarification => "request-clarification",
            Self::Allergy => "allergy",
            Self::SuspectedIntolerance => "suspected-intolerance",
            Self::ScheduledForSurgery => "scheduled-for-surgery",
            Self::ProductNotAvailable => "product-not-available",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "incomplete-data" => Self::IncompleteData,
            "unable-to-provide-care" => Self::UnableToProvideCare,
            "request-clarification" => Self::RequestClarification,
            "allergy" => Self::Allergy,
            "suspected-intolerance" => Self::SuspectedIntolerance,
            "scheduled-for-surgery" => Self::ScheduledForSurgery,
            "product-not-available" => Self::ProductNotAvailable,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::IncompleteData => Some("Incomplete Data"),
            Self::UnableToProvideCare => Some("Unable to provide care"),
            Self::RequestClarification => Some("Request requires clarification"),
            Self::Allergy => Some("Allergy"),
            Self::SuspectedIntolerance => Some("Suspected intolerance"),
            Self::ScheduledForSurgery => Some("Patient/subject scheduled for surgery"),
            Self::ProductNotAvailable => Some("Product not available"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::IncompleteData => Some(
                "Data needed to safely act on the intake which was expected to become available independent of the intake is not yet available.",
            ),
            Self::UnableToProvideCare => Some(
                "Fulfiller not able to provide appropriate care associated with fulfilling the intake.",
            ),
            Self::RequestClarification => {
                Some("Clarification is required before the intake can be acted upon.")
            }
            Self::Allergy => Some(
                "The patient/subject is believed to be allergic to a substance that is part of the therapy and the therapy is being temporarily withdrawn to confirm.",
            ),
            Self::SuspectedIntolerance => Some(
                "The patient/subject is believed to have an intolerance to a substance that is part of the therapy and the therapy is being temporarily withdrawn to confirm.",
            ),
            Self::ScheduledForSurgery => Some(
                "Patient/subject is receiving surgery and/or the subject is scheduled to be admitted for surgery in the near future. The intake will be resumed when the subject has sufficiently recovered from the surgery.",
            ),
            Self::ProductNotAvailable => Some("Cannot supply product."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::IncompleteData => None,
            Self::UnableToProvideCare => None,
            Self::RequestClarification => None,
            Self::Allergy => None,
            Self::SuspectedIntolerance => None,
            Self::ScheduledForSurgery => None,
            Self::ProductNotAvailable => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::IncompleteData => 0,
            Self::UnableToProvideCare => 0,
            Self::RequestClarification => 0,
            Self::Allergy => 0,
            Self::SuspectedIntolerance => 0,
            Self::ScheduledForSurgery => 0,
            Self::ProductNotAvailable => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
