// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Detected Issue Management Code
//!
//! Example code system illustrating the type action (management) required.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DetectedIssueManagementCode {
    InterventionRequired,
    ReviewSuggested,
    PotentialRisk,
}

impl DetectedIssueManagementCode {
    pub const URL: &'static str = "http://hl7.org/fhir/detectedissue-management-code";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::InterventionRequired => "intervention-required",
            Self::ReviewSuggested => "review-suggested",
            Self::PotentialRisk => "potential-risk",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "intervention-required" => Self::InterventionRequired,
            "review-suggested" => Self::ReviewSuggested,
            "potential-risk" => Self::PotentialRisk,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::InterventionRequired => Some("Professional Intervention Required"),
            Self::ReviewSuggested => Some("Professional Review Suggested"),
            Self::PotentialRisk => Some("Potential Risk"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::InterventionRequired => {
                Some("Indicates the issue requires professional intervention.")
            }
            Self::ReviewSuggested => {
                Some("Indicates professional review of the issue is suggested.")
            }
            Self::PotentialRisk => Some("Indicates the issue represents a potential risk."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::InterventionRequired => None,
            Self::ReviewSuggested => None,
            Self::PotentialRisk => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::InterventionRequired => 0,
            Self::ReviewSuggested => 0,
            Self::PotentialRisk => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
