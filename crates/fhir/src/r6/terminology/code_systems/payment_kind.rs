// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Payment Kind Codes
//!
//! This value set contains codes for the type of workflow from which payments arise.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum PaymentKindCodes {
    Deposit,
    PeriodicPayment,
    Online,
    Kiosk,
}

impl PaymentKindCodes {
    pub const URL: &'static str = "http://hl7.org/fhir/payment-kind";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Deposit => "deposit",
            Self::PeriodicPayment => "periodic-payment",
            Self::Online => "online",
            Self::Kiosk => "kiosk",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "deposit" => Self::Deposit,
            "periodic-payment" => Self::PeriodicPayment,
            "online" => Self::Online,
            "kiosk" => Self::Kiosk,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Deposit => Some("Deposit on Account"),
            Self::PeriodicPayment => Some("Periodic Payment"),
            Self::Online => Some("Online Bill Payment"),
            Self::Kiosk => Some("Kiosk Payment"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Deposit => Some(
                "The payment or adjustment is to an indicated account not to a specific charge.",
            ),
            Self::PeriodicPayment => Some(
                "The payment is one of a set of previously agreed payments, for example in fullfilment of a payment plan.",
            ),
            Self::Online => Some(
                "Payment, full or partial, of an invoice or statement provided to the payment issuer.",
            ),
            Self::Kiosk => Some("Payment made at an authorized Kiosk."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Deposit => None,
            Self::PeriodicPayment => None,
            Self::Online => None,
            Self::Kiosk => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Deposit => 0,
            Self::PeriodicPayment => 0,
            Self::Online => 0,
            Self::Kiosk => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
