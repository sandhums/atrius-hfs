// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! RejectionCriterion
//!
//! Criterion for rejection of the specimen by laboratory.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum RejectionCriterion {
    Hemolized,
    Insufficient,
    Broken,
    Clotted,
    WrongTemperature,
}

impl RejectionCriterion {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/rejection-criteria";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Hemolized => "hemolized",
            Self::Insufficient => "insufficient",
            Self::Broken => "broken",
            Self::Clotted => "clotted",
            Self::WrongTemperature => "wrong-temperature",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "hemolized" => Self::Hemolized,
            "insufficient" => Self::Insufficient,
            "broken" => Self::Broken,
            "clotted" => Self::Clotted,
            "wrong-temperature" => Self::WrongTemperature,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Hemolized => Some("hemolized specimen"),
            Self::Insufficient => Some("insufficient specimen volume"),
            Self::Broken => Some("broken specimen container"),
            Self::Clotted => Some("specimen clotted"),
            Self::WrongTemperature => Some("specimen temperature inappropriate"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Hemolized => Some("blood specimen hemolized."),
            Self::Insufficient => Some("insufficient quantity of specimen."),
            Self::Broken => Some("specimen container broken."),
            Self::Clotted => Some("specimen clotted."),
            Self::WrongTemperature => Some("specimen temperature inappropriate."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Hemolized => None,
            Self::Insufficient => None,
            Self::Broken => None,
            Self::Clotted => None,
            Self::WrongTemperature => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Hemolized => 0,
            Self::Insufficient => 0,
            Self::Broken => 0,
            Self::Clotted => 0,
            Self::WrongTemperature => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
