// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Coverage CoPay Type Codes
//!
//! This codeset includes Coverage Copayment Type codes.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum CoverageCoPayTypeCodes {
    Gpvisit,
    Spvisit,
    Emergency,
    Inpthosp,
    Televisit,
    Urgentcare,
    Copaypct,
    Copay,
    Deductible,
    Maxoutofpocket,
}

impl CoverageCoPayTypeCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/coverage-copay-type";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Gpvisit => "gpvisit",
            Self::Spvisit => "spvisit",
            Self::Emergency => "emergency",
            Self::Inpthosp => "inpthosp",
            Self::Televisit => "televisit",
            Self::Urgentcare => "urgentcare",
            Self::Copaypct => "copaypct",
            Self::Copay => "copay",
            Self::Deductible => "deductible",
            Self::Maxoutofpocket => "maxoutofpocket",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "gpvisit" => Self::Gpvisit,
            "spvisit" => Self::Spvisit,
            "emergency" => Self::Emergency,
            "inpthosp" => Self::Inpthosp,
            "televisit" => Self::Televisit,
            "urgentcare" => Self::Urgentcare,
            "copaypct" => Self::Copaypct,
            "copay" => Self::Copay,
            "deductible" => Self::Deductible,
            "maxoutofpocket" => Self::Maxoutofpocket,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Gpvisit => Some("GP Office Visit"),
            Self::Spvisit => Some("Specialist Office Visit"),
            Self::Emergency => Some("Emergency"),
            Self::Inpthosp => Some("Inpatient Hospital"),
            Self::Televisit => Some("Tele-visit"),
            Self::Urgentcare => Some("Urgent Care"),
            Self::Copaypct => Some("Copay Percentage"),
            Self::Copay => Some("Copay Amount"),
            Self::Deductible => Some("Deductible"),
            Self::Maxoutofpocket => Some("Maximum out of pocket"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Gpvisit => Some("An office visit for a general practitioner of a discipline."),
            Self::Spvisit => Some("An office visit for a specialist practitioner of a discipline"),
            Self::Emergency => Some("An episode in an emergency department."),
            Self::Inpthosp => Some("An episode of an Inpatient hospital stay."),
            Self::Televisit => Some(
                "A visit held where the patient is remote relative to the practitioner, e.g. by phone, computer or video conference.",
            ),
            Self::Urgentcare => {
                Some("A visit to an urgent care facility - typically a community care clinic.")
            }
            Self::Copaypct => Some(
                "A standard percentage applied to all classes or service or product not otherwise specified.",
            ),
            Self::Copay => Some(
                "A standard fixed currency amount applied to all classes or service or product not otherwise specified.",
            ),
            Self::Deductible => Some(
                "The accumulated amount of patient payment before the coverage begins to pay for services.",
            ),
            Self::Maxoutofpocket => Some(
                "The maximum amout of payment for services which a patient, or family, is expected to incur - typically annually.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Gpvisit => None,
            Self::Spvisit => None,
            Self::Emergency => None,
            Self::Inpthosp => None,
            Self::Televisit => None,
            Self::Urgentcare => None,
            Self::Copaypct => None,
            Self::Copay => None,
            Self::Deductible => None,
            Self::Maxoutofpocket => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Gpvisit => 0,
            Self::Spvisit => 0,
            Self::Emergency => 0,
            Self::Inpthosp => 0,
            Self::Televisit => 0,
            Self::Urgentcare => 0,
            Self::Copaypct => 0,
            Self::Copay => 0,
            Self::Deductible => 0,
            Self::Maxoutofpocket => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
