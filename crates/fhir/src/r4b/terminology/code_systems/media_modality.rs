// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Media SubType
//!
//! Detailed information about the type of the image - its kind, purpose, or the kind of equipment used to generate it.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MediaSubType {
    Diagram,
    Fax,
    Scan,
    Retina,
    Fingerprint,
    Iris,
    Palm,
    Face,
}

impl MediaSubType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/media-modality";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Diagram => "diagram",
            Self::Fax => "fax",
            Self::Scan => "scan",
            Self::Retina => "retina",
            Self::Fingerprint => "fingerprint",
            Self::Iris => "iris",
            Self::Palm => "palm",
            Self::Face => "face",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "diagram" => Self::Diagram,
            "fax" => Self::Fax,
            "scan" => Self::Scan,
            "retina" => Self::Retina,
            "fingerprint" => Self::Fingerprint,
            "iris" => Self::Iris,
            "palm" => Self::Palm,
            "face" => Self::Face,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Diagram => Some("Diagram"),
            Self::Fax => Some("Fax"),
            Self::Scan => Some("Scanned Document"),
            Self::Retina => Some("Retina Scan"),
            Self::Fingerprint => Some("Fingerprint"),
            Self::Iris => Some("Iris Scan"),
            Self::Palm => Some("Palm Scan"),
            Self::Face => Some("Face Scan"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Diagram => Some("A diagram. Often used in diagnostic reports"),
            Self::Fax => Some("A digital record of a fax document"),
            Self::Scan => Some(
                "A digital scan of a document. This is reserved for when there is not enough metadata to create a document reference",
            ),
            Self::Retina => Some("A retinal image used for identification purposes"),
            Self::Fingerprint => Some("A finger print scan used for identification purposes"),
            Self::Iris => Some("An iris scan used for identification purposes"),
            Self::Palm => Some("A palm scan used for identification purposes"),
            Self::Face => Some("A face scan used for identification purposes"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Diagram => None,
            Self::Fax => None,
            Self::Scan => None,
            Self::Retina => None,
            Self::Fingerprint => None,
            Self::Iris => None,
            Self::Palm => None,
            Self::Face => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Diagram => 0,
            Self::Fax => 0,
            Self::Scan => 0,
            Self::Retina => 0,
            Self::Fingerprint => 0,
            Self::Iris => 0,
            Self::Palm => 0,
            Self::Face => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
