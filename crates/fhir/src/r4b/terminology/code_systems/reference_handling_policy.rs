// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! ReferenceHandlingPolicy
//!
//! A set of flags that defines how references are supported.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ReferenceHandlingPolicy {
    Literal,
    Logical,
    Resolves,
    Enforced,
    Local,
}

impl ReferenceHandlingPolicy {
    pub const URL: &'static str = "http://hl7.org/fhir/reference-handling-policy";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Literal => "literal",
            Self::Logical => "logical",
            Self::Resolves => "resolves",
            Self::Enforced => "enforced",
            Self::Local => "local",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "literal" => Self::Literal,
            "logical" => Self::Logical,
            "resolves" => Self::Resolves,
            "enforced" => Self::Enforced,
            "local" => Self::Local,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Literal => Some("Literal References"),
            Self::Logical => Some("Logical References"),
            Self::Resolves => Some("Resolves References"),
            Self::Enforced => Some("Reference Integrity Enforced"),
            Self::Local => Some("Local References Only"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Literal => Some(
                "The server supports and populates Literal references (i.e. using Reference.reference) where they are known (this code does not guarantee that all references are literal; see \'enforced\').",
            ),
            Self::Logical => {
                Some("The server allows logical references (i.e. using Reference.identifier).")
            }
            Self::Resolves => Some(
                "The server will attempt to resolve logical references to literal references - i.e. converting Reference.identifier to Reference.reference (if resolution fails, the server may still accept resources; see logical).",
            ),
            Self::Enforced => Some(
                "The server enforces that references have integrity - e.g. it ensures that references can always be resolved. This is typically the case for clinical record systems, but often not the case for middleware/proxy systems.",
            ),
            Self::Local => {
                Some("The server does not support references that point to other servers.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Literal => None,
            Self::Logical => None,
            Self::Resolves => None,
            Self::Enforced => None,
            Self::Local => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Literal => 0,
            Self::Logical => 0,
            Self::Resolves => 0,
            Self::Enforced => 0,
            Self::Local => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
