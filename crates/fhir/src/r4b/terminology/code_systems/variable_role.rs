// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! EvidenceVariableRole
//!
//! The role that the assertion variable plays.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum EvidenceVariableRole {
    Population,
    Subpopulation,
    Exposure,
    ReferenceExposure,
    MeasuredVariable,
    Confounder,
}

impl EvidenceVariableRole {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/variable-role";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Population => "population",
            Self::Subpopulation => "subpopulation",
            Self::Exposure => "exposure",
            Self::ReferenceExposure => "referenceExposure",
            Self::MeasuredVariable => "measuredVariable",
            Self::Confounder => "confounder",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "population" => Self::Population,
            "subpopulation" => Self::Subpopulation,
            "exposure" => Self::Exposure,
            "referenceExposure" => Self::ReferenceExposure,
            "measuredVariable" => Self::MeasuredVariable,
            "confounder" => Self::Confounder,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Population => Some("population"),
            Self::Subpopulation => Some("subpopulation"),
            Self::Exposure => Some("exposure"),
            Self::ReferenceExposure => Some("reference exposure"),
            Self::MeasuredVariable => Some("measured variable"),
            Self::Confounder => Some("confounder"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Population => Some("variable represents a population."),
            Self::Subpopulation => Some("variable represents a subpopulation."),
            Self::Exposure => Some("variable represents an exposure."),
            Self::ReferenceExposure => Some("variable represents a reference exposure."),
            Self::MeasuredVariable => Some("variable represents a measured variable."),
            Self::Confounder => Some("variable represents a confounder."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Population => None,
            Self::Subpopulation => None,
            Self::Exposure => None,
            Self::ReferenceExposure => None,
            Self::MeasuredVariable => None,
            Self::Confounder => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Population => 0,
            Self::Subpopulation => 0,
            Self::Exposure => 0,
            Self::ReferenceExposure => 0,
            Self::MeasuredVariable => 0,
            Self::Confounder => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
