// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! CommunicationTopic
//!
//! Codes describing the purpose or content of the communication.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum CommunicationTopic {
    PrescriptionRefillRequest,
    ProgressUpdate,
    ReportLabs,
    AppointmentReminder,
    PhoneConsult,
    SummaryReport,
}

impl CommunicationTopic {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/communication-topic";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::PrescriptionRefillRequest => "prescription-refill-request",
            Self::ProgressUpdate => "progress-update",
            Self::ReportLabs => "report-labs",
            Self::AppointmentReminder => "appointment-reminder",
            Self::PhoneConsult => "phone-consult",
            Self::SummaryReport => "summary-report",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "prescription-refill-request" => Self::PrescriptionRefillRequest,
            "progress-update" => Self::ProgressUpdate,
            "report-labs" => Self::ReportLabs,
            "appointment-reminder" => Self::AppointmentReminder,
            "phone-consult" => Self::PhoneConsult,
            "summary-report" => Self::SummaryReport,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::PrescriptionRefillRequest => Some("Prescription Refill Request"),
            Self::ProgressUpdate => Some("Progress Update"),
            Self::ReportLabs => Some("Report Labs"),
            Self::AppointmentReminder => Some("Appointment Reminder"),
            Self::PhoneConsult => Some("Phone Consult"),
            Self::SummaryReport => Some("Summary Report"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::PrescriptionRefillRequest => Some(
                "The purpose or content of the communication is a prescription refill request.",
            ),
            Self::ProgressUpdate => {
                Some("The purpose or content of the communication is a progress update.")
            }
            Self::ReportLabs => {
                Some("The purpose or content of the communication is to report labs.")
            }
            Self::AppointmentReminder => {
                Some("The purpose or content of the communication is an appointment reminder.")
            }
            Self::PhoneConsult => {
                Some("The purpose or content of the communication is a phone consult.")
            }
            Self::SummaryReport => {
                Some("The purpose or content of the communication is a summary report.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::PrescriptionRefillRequest => None,
            Self::ProgressUpdate => None,
            Self::ReportLabs => None,
            Self::AppointmentReminder => None,
            Self::PhoneConsult => None,
            Self::SummaryReport => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::PrescriptionRefillRequest => 0,
            Self::ProgressUpdate => 0,
            Self::ReportLabs => 0,
            Self::AppointmentReminder => 0,
            Self::PhoneConsult => 0,
            Self::SummaryReport => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
