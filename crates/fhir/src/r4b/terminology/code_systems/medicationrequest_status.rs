// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! MedicationRequestStatusCodes

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MedicationRequestStatusCodes {
    Active,
    OnHold,
    Cancelled,
    Completed,
    EnteredInError,
    Stopped,
    Draft,
    Unknown,
}

impl MedicationRequestStatusCodes {
    pub const URL: &'static str = "http://hl7.org/fhir/CodeSystem/medicationrequest-status";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Active => "active",
            Self::OnHold => "on-hold",
            Self::Cancelled => "cancelled",
            Self::Completed => "completed",
            Self::EnteredInError => "entered-in-error",
            Self::Stopped => "stopped",
            Self::Draft => "draft",
            Self::Unknown => "unknown",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "active" => Self::Active,
            "on-hold" => Self::OnHold,
            "cancelled" => Self::Cancelled,
            "completed" => Self::Completed,
            "entered-in-error" => Self::EnteredInError,
            "stopped" => Self::Stopped,
            "draft" => Self::Draft,
            "unknown" => Self::Unknown,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Active => Some("Active"),
            Self::OnHold => Some("On Hold"),
            Self::Cancelled => Some("Cancelled"),
            Self::Completed => Some("Completed"),
            Self::EnteredInError => Some("Entered in Error"),
            Self::Stopped => Some("Stopped"),
            Self::Draft => Some("Draft"),
            Self::Unknown => Some("Unknown"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Active => Some(
                "The prescription is \'actionable\', but not all actions that are implied by it have occurred yet.",
            ),
            Self::OnHold => Some(
                "Actions implied by the prescription are to be temporarily halted, but are expected to continue later.  May also be called \'suspended\'.",
            ),
            Self::Cancelled => {
                Some("The prescription has been withdrawn before any administrations have occurred")
            }
            Self::Completed => {
                Some("All actions that are implied by the prescription have occurred.")
            }
            Self::EnteredInError => Some(
                "Some of the actions that are implied by the medication request may have occurred.  For example, the medication may have been dispensed and the patient may have taken some of the medication.  Clinical decision support systems should take this status into account",
            ),
            Self::Stopped => Some(
                "Actions implied by the prescription are to be permanently halted, before all of the administrations occurred. This should not be used if the original order was entered in error",
            ),
            Self::Draft => Some(
                "The prescription is not yet \'actionable\', e.g. it is a work in progress, requires sign-off, verification\u{a0}or\u{a0}needs to be run through decision support process.",
            ),
            Self::Unknown => Some(
                "The authoring/source system does not know which of the status values currently applies for this observation. Note: This concept is not to be used for \'other\' - one of the listed statuses is presumed to apply, but the authoring/source system does not know which.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Active => None,
            Self::OnHold => None,
            Self::Cancelled => None,
            Self::Completed => None,
            Self::EnteredInError => None,
            Self::Stopped => None,
            Self::Draft => None,
            Self::Unknown => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Active => 0,
            Self::OnHold => 0,
            Self::Cancelled => 0,
            Self::Completed => 0,
            Self::EnteredInError => 0,
            Self::Stopped => 0,
            Self::Draft => 0,
            Self::Unknown => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
