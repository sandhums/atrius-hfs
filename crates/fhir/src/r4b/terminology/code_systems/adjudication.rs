// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Adjudication Codes
//!
//! This value set includes a smattering of Adjudication codes.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum AdjudicationCodes {
    Submitted,
    Copay,
    Eligible,
    Deductible,
    Unallocdeduct,
    Eligpercent,
    Tax,
    Benefit,
}

impl AdjudicationCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/adjudication";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Submitted => "submitted",
            Self::Copay => "copay",
            Self::Eligible => "eligible",
            Self::Deductible => "deductible",
            Self::Unallocdeduct => "unallocdeduct",
            Self::Eligpercent => "eligpercent",
            Self::Tax => "tax",
            Self::Benefit => "benefit",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "submitted" => Self::Submitted,
            "copay" => Self::Copay,
            "eligible" => Self::Eligible,
            "deductible" => Self::Deductible,
            "unallocdeduct" => Self::Unallocdeduct,
            "eligpercent" => Self::Eligpercent,
            "tax" => Self::Tax,
            "benefit" => Self::Benefit,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Submitted => Some("Submitted Amount"),
            Self::Copay => Some("CoPay"),
            Self::Eligible => Some("Eligible Amount"),
            Self::Deductible => Some("Deductible"),
            Self::Unallocdeduct => Some("Unallocated Deductible"),
            Self::Eligpercent => Some("Eligible %"),
            Self::Tax => Some("Tax"),
            Self::Benefit => Some("Benefit Amount"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Submitted => {
                Some("The total submitted amount for the claim or group or line item.")
            }
            Self::Copay => Some("Patient Co-Payment"),
            Self::Eligible => Some("Amount of the change which is considered for adjudication."),
            Self::Deductible => {
                Some("Amount deducted from the eligible amount prior to adjudication.")
            }
            Self::Unallocdeduct => {
                Some("The amount of deductible which could not allocated to other line items.")
            }
            Self::Eligpercent => Some("Eligible Percentage."),
            Self::Tax => Some("The amount of tax."),
            Self::Benefit => Some("Amount payable under the coverage"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Submitted => None,
            Self::Copay => None,
            Self::Eligible => None,
            Self::Deductible => None,
            Self::Unallocdeduct => None,
            Self::Eligpercent => None,
            Self::Tax => None,
            Self::Benefit => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Submitted => 0,
            Self::Copay => 0,
            Self::Eligible => 0,
            Self::Deductible => 0,
            Self::Unallocdeduct => 0,
            Self::Eligpercent => 0,
            Self::Tax => 0,
            Self::Benefit => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
