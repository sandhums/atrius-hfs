// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! FHIRDeviceStatusReason
//!
//! The availability status reason of the device.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum FHIRDeviceStatusReason {
    Online,
    Paused,
    Standby,
    Offline,
    NotReady,
    TransducDiscon,
    HwDiscon,
    Off,
}

impl FHIRDeviceStatusReason {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/device-status-reason";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Online => "online",
            Self::Paused => "paused",
            Self::Standby => "standby",
            Self::Offline => "offline",
            Self::NotReady => "not-ready",
            Self::TransducDiscon => "transduc-discon",
            Self::HwDiscon => "hw-discon",
            Self::Off => "off",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "online" => Self::Online,
            "paused" => Self::Paused,
            "standby" => Self::Standby,
            "offline" => Self::Offline,
            "not-ready" => Self::NotReady,
            "transduc-discon" => Self::TransducDiscon,
            "hw-discon" => Self::HwDiscon,
            "off" => Self::Off,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Online => Some("Online"),
            Self::Paused => Some("Paused"),
            Self::Standby => Some("Standby"),
            Self::Offline => Some("Offline"),
            Self::NotReady => Some("Not Ready"),
            Self::TransducDiscon => Some("Transducer Disconnected"),
            Self::HwDiscon => Some("Hardware Disconnected"),
            Self::Off => Some("Off"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Online => Some("The device is off."),
            Self::Paused => Some("The device is paused."),
            Self::Standby => Some("The device is ready but not actively operating."),
            Self::Offline => Some("The device is offline."),
            Self::NotReady => Some("The device is not ready."),
            Self::TransducDiscon => Some("The device transducer is disconnected."),
            Self::HwDiscon => Some("The device hardware is disconnected."),
            Self::Off => Some("The device is off."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Online => None,
            Self::Paused => None,
            Self::Standby => None,
            Self::Offline => None,
            Self::NotReady => None,
            Self::TransducDiscon => None,
            Self::HwDiscon => None,
            Self::Off => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Online => 0,
            Self::Paused => 0,
            Self::Standby => 0,
            Self::Offline => 0,
            Self::NotReady => 0,
            Self::TransducDiscon => 0,
            Self::HwDiscon => 0,
            Self::Off => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
