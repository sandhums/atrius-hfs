// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Immunization Event Origin
//!
//! This code system supports describing the source of the data when the report of the immunization event is not based on information from the person, entity or organization who administered the vaccine.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ImmunizationEventOrigin {
    Provider,
    Record,
    Recall,
    School,
    Jurisdiction,
}

impl ImmunizationEventOrigin {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/immunization-origin";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Provider => "provider",
            Self::Record => "record",
            Self::Recall => "recall",
            Self::School => "school",
            Self::Jurisdiction => "jurisdiction",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "provider" => Self::Provider,
            "record" => Self::Record,
            "recall" => Self::Recall,
            "school" => Self::School,
            "jurisdiction" => Self::Jurisdiction,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Provider => Some("Other Provider"),
            Self::Record => Some("Written Record"),
            Self::Recall => Some("Parent/Guardian/Patient Recall"),
            Self::School => Some("School Record"),
            Self::Jurisdiction => Some("Jurisdictional IIS"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Provider => {
                Some("The data for the immunization event originated with another provider.")
            }
            Self::Record => Some(
                "The data for the immunization event originated with a written record for the patient.",
            ),
            Self::Recall => Some(
                "The data for the immunization event originated from the recollection of the patient or parent/guardian of the patient.",
            ),
            Self::School => Some(
                "The data for the immunization event originated with a school record for the patient.",
            ),
            Self::Jurisdiction => Some(
                "The data for the immunization event originated with an immunization information system (IIS) or registry operating within the jurisdiction.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Provider => None,
            Self::Record => None,
            Self::Recall => None,
            Self::School => None,
            Self::Jurisdiction => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Provider => 0,
            Self::Record => 0,
            Self::Recall => 0,
            Self::School => 0,
            Self::Jurisdiction => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
