// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Example Diagnosis Type Codes
//!
//! This value set includes example Diagnosis Type codes.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ExampleDiagnosisTypeCodes {
    Admitting,
    Clinical,
    Differential,
    Discharge,
    Laboratory,
    Nursing,
    Prenatal,
    Principal,
    Radiology,
    Remote,
    Retrospective,
    Self_,
}

impl ExampleDiagnosisTypeCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/ex-diagnosistype";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Admitting => "admitting",
            Self::Clinical => "clinical",
            Self::Differential => "differential",
            Self::Discharge => "discharge",
            Self::Laboratory => "laboratory",
            Self::Nursing => "nursing",
            Self::Prenatal => "prenatal",
            Self::Principal => "principal",
            Self::Radiology => "radiology",
            Self::Remote => "remote",
            Self::Retrospective => "retrospective",
            Self::Self_ => "self",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "admitting" => Self::Admitting,
            "clinical" => Self::Clinical,
            "differential" => Self::Differential,
            "discharge" => Self::Discharge,
            "laboratory" => Self::Laboratory,
            "nursing" => Self::Nursing,
            "prenatal" => Self::Prenatal,
            "principal" => Self::Principal,
            "radiology" => Self::Radiology,
            "remote" => Self::Remote,
            "retrospective" => Self::Retrospective,
            "self" => Self::Self_,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Admitting => Some("Admitting Diagnosis"),
            Self::Clinical => Some("Clinical Diagnosis"),
            Self::Differential => Some("Differential Diagnosis"),
            Self::Discharge => Some("Discharge Diagnosis"),
            Self::Laboratory => Some("Laboratory Diagnosis"),
            Self::Nursing => Some("Nursing Diagnosis"),
            Self::Prenatal => Some("Prenatal Diagnosis"),
            Self::Principal => Some("Principal Diagnosis"),
            Self::Radiology => Some("Radiology Diagnosis"),
            Self::Remote => Some("Remote Diagnosis"),
            Self::Retrospective => Some("Retrospective Diagnosis"),
            Self::Self_ => Some("Self Diagnosis"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Admitting => Some(
                "The diagnosis given as the reason why the patient was admitted to the hospital.",
            ),
            Self::Clinical => Some(
                "A diagnosis made on the basis of medical signs and patient-reported symptoms, rather than diagnostic tests.",
            ),
            Self::Differential => Some(
                "One of a set of the possible diagnoses that could be connected to the signs, symptoms, and lab findings.",
            ),
            Self::Discharge => {
                Some("The diagnosis given when the patient is discharged from the hospital.")
            }
            Self::Laboratory => Some(
                "A diagnosis based significantly on laboratory reports or test results, rather than the physical examination of the patient.",
            ),
            Self::Nursing => Some(
                "A diagnosis which identifies people\'s responses to situations in their lives, such as a readiness to change or a willingness to accept assistance.",
            ),
            Self::Prenatal => Some("A diagnosis determined prior to birth."),
            Self::Principal => Some(
                "The single medical diagnosis that is most relevant to the patient\'s chief complaint or need for treatment.",
            ),
            Self::Radiology => {
                Some("A diagnosis based primarily on the results from medical imaging studies.")
            }
            Self::Remote => Some("A diagnosis determined using telemedicine techniques."),
            Self::Retrospective => Some(
                "The labeling of an illness in a specific historical event using modern knowledge, methods and disease classifications.",
            ),
            Self::Self_ => Some("A diagnosis determined by the patient."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Admitting => None,
            Self::Clinical => None,
            Self::Differential => None,
            Self::Discharge => None,
            Self::Laboratory => None,
            Self::Nursing => None,
            Self::Prenatal => None,
            Self::Principal => None,
            Self::Radiology => None,
            Self::Remote => None,
            Self::Retrospective => None,
            Self::Self_ => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Admitting => 0,
            Self::Clinical => 0,
            Self::Differential => 0,
            Self::Discharge => 0,
            Self::Laboratory => 0,
            Self::Nursing => 0,
            Self::Prenatal => 0,
            Self::Principal => 0,
            Self::Radiology => 0,
            Self::Remote => 0,
            Self::Retrospective => 0,
            Self::Self_ => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
