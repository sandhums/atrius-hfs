// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! GoalAchievementStatus
//!
//! Describes the progression, or lack thereof, towards the goal against the target.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum GoalAchievementStatus {
    InProgress,
    Improving,
    Worsening,
    NoChange,
    Achieved,
    Sustaining,
    NotAchieved,
    NoProgress,
    NotAttainable,
}

impl GoalAchievementStatus {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/goal-achievement";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::InProgress => "in-progress",
            Self::Improving => "improving",
            Self::Worsening => "worsening",
            Self::NoChange => "no-change",
            Self::Achieved => "achieved",
            Self::Sustaining => "sustaining",
            Self::NotAchieved => "not-achieved",
            Self::NoProgress => "no-progress",
            Self::NotAttainable => "not-attainable",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "in-progress" => Self::InProgress,
            "improving" => Self::Improving,
            "worsening" => Self::Worsening,
            "no-change" => Self::NoChange,
            "achieved" => Self::Achieved,
            "sustaining" => Self::Sustaining,
            "not-achieved" => Self::NotAchieved,
            "no-progress" => Self::NoProgress,
            "not-attainable" => Self::NotAttainable,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::InProgress => Some("In Progress"),
            Self::Improving => Some("Improving"),
            Self::Worsening => Some("Worsening"),
            Self::NoChange => Some("No Change"),
            Self::Achieved => Some("Achieved"),
            Self::Sustaining => Some("Sustaining"),
            Self::NotAchieved => Some("Not Achieved"),
            Self::NoProgress => Some("No Progress"),
            Self::NotAttainable => Some("Not Attainable"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::InProgress => Some(
                "The goal is being sought but has not yet been reached. (Also applies if the goal was reached in the past but there has been regression and the goal is again being sought).",
            ),
            Self::Improving => Some("The goal is being sought, and is progressing."),
            Self::Worsening => Some("The goal is being sought, but is regressing."),
            Self::NoChange => Some("The goal is being sought, but the trend is flat."),
            Self::Achieved => Some("The goal has been met."),
            Self::Sustaining => Some(
                "The goal has been met, but ongoing activity is needed to sustain the goal objective.",
            ),
            Self::NotAchieved => Some(
                "The goal has not been met and there might or might not have been progress towards target.",
            ),
            Self::NoProgress => {
                Some("The goal has not been met and little to no progress towards target.")
            }
            Self::NotAttainable => Some("The goal is not possible to be met."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::InProgress => None,
            Self::Improving => Some("in-progress"),
            Self::Worsening => Some("in-progress"),
            Self::NoChange => Some("in-progress"),
            Self::Achieved => None,
            Self::Sustaining => Some("achieved"),
            Self::NotAchieved => None,
            Self::NoProgress => Some("not-achieved"),
            Self::NotAttainable => Some("not-achieved"),
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::InProgress => 0,
            Self::Improving => 1,
            Self::Worsening => 1,
            Self::NoChange => 1,
            Self::Achieved => 0,
            Self::Sustaining => 1,
            Self::NotAchieved => 0,
            Self::NoProgress => 1,
            Self::NotAttainable => 1,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
