// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! SynthesisType
//!
//! Types of combining results from a body of evidence (eg. summary data meta-analysis).

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum SynthesisType {
    StdMA,
    IPDMA,
    IndirectNMA,
    CombinedNMA,
    Range,
    Classification,
    NotApplicable,
}

impl SynthesisType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/synthesis-type";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::StdMA => "std-MA",
            Self::IPDMA => "IPD-MA",
            Self::IndirectNMA => "indirect-NMA",
            Self::CombinedNMA => "combined-NMA",
            Self::Range => "range",
            Self::Classification => "classification",
            Self::NotApplicable => "NotApplicable",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "std-MA" => Self::StdMA,
            "IPD-MA" => Self::IPDMA,
            "indirect-NMA" => Self::IndirectNMA,
            "combined-NMA" => Self::CombinedNMA,
            "range" => Self::Range,
            "classification" => Self::Classification,
            "NotApplicable" => Self::NotApplicable,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::StdMA => Some("summary data meta-analysis"),
            Self::IPDMA => Some("individual patient data meta-analysis"),
            Self::IndirectNMA => Some("indirect network meta-analysis"),
            Self::CombinedNMA => Some("combined direct plus indirect network meta-analysis"),
            Self::Range => Some("range of results"),
            Self::Classification => Some("classifcation of results"),
            Self::NotApplicable => Some("not applicable"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::StdMA => Some(
                "A meta-analysis of the summary data of estimates from individual studies or data sets.",
            ),
            Self::IPDMA => Some(
                "A meta-analysis of the individual participant data from individual studies or data sets.",
            ),
            Self::IndirectNMA => Some(
                "An indirect meta-analysis derived from 2 or more direct comparisons in a network meta-analysis.",
            ),
            Self::CombinedNMA => Some(
                "An composite meta-analysis derived from direct comparisons and indirect comparisons in a network meta-analysis.",
            ),
            Self::Range => Some("A range of results across a body of evidence."),
            Self::Classification => Some(
                "An approach describing a body of evidence by categorically classifying individual studies (eg 3 studies showed beneft and 2 studied found no effect).",
            ),
            Self::NotApplicable => Some(
                "Not applicable because the evidence is not from a synthesis but from a single study. Used fo explicitly state that it\'s not a synthesis.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::StdMA => None,
            Self::IPDMA => None,
            Self::IndirectNMA => None,
            Self::CombinedNMA => None,
            Self::Range => None,
            Self::Classification => None,
            Self::NotApplicable => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::StdMA => 0,
            Self::IPDMA => 0,
            Self::IndirectNMA => 0,
            Self::CombinedNMA => 0,
            Self::Range => 0,
            Self::Classification => 0,
            Self::NotApplicable => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
