// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! GroupMeasure
//!
//! Possible group measure aggregates (E.g. Mean, Median).

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum GroupMeasure {
    Mean,
    Median,
    MeanOfMean,
    MeanOfMedian,
    MedianOfMean,
    MedianOfMedian,
}

impl GroupMeasure {
    pub const URL: &'static str = "http://hl7.org/fhir/group-measure";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Mean => "mean",
            Self::Median => "median",
            Self::MeanOfMean => "mean-of-mean",
            Self::MeanOfMedian => "mean-of-median",
            Self::MedianOfMean => "median-of-mean",
            Self::MedianOfMedian => "median-of-median",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "mean" => Self::Mean,
            "median" => Self::Median,
            "mean-of-mean" => Self::MeanOfMean,
            "mean-of-median" => Self::MeanOfMedian,
            "median-of-mean" => Self::MedianOfMean,
            "median-of-median" => Self::MedianOfMedian,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Mean => Some("Mean"),
            Self::Median => Some("Median"),
            Self::MeanOfMean => Some("Mean of Study Means"),
            Self::MeanOfMedian => Some("Mean of Study Medins"),
            Self::MedianOfMean => Some("Median of Study Means"),
            Self::MedianOfMedian => Some("Median of Study Medians"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Mean => Some("Aggregated using Mean of participant values."),
            Self::Median => Some("Aggregated using Median of participant values."),
            Self::MeanOfMean => Some("Aggregated using Mean of study mean values."),
            Self::MeanOfMedian => Some("Aggregated using Mean of study median values."),
            Self::MedianOfMean => Some("Aggregated using Median of study mean values."),
            Self::MedianOfMedian => Some("Aggregated using Median of study median values."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Mean => None,
            Self::Median => None,
            Self::MeanOfMean => None,
            Self::MeanOfMedian => None,
            Self::MedianOfMean => None,
            Self::MedianOfMedian => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Mean => 0,
            Self::Median => 0,
            Self::MeanOfMean => 0,
            Self::MeanOfMedian => 0,
            Self::MedianOfMean => 0,
            Self::MedianOfMedian => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
