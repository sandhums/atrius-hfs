// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! FHIRRestfulInteractions
//!
//! The set of interactions defined by the RESTful part of the FHIR specification.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum FHIRRestfulInteractions {
    Read,
    Vread,
    Update,
    Patch,
    Delete,
    History,
    HistoryInstance,
    HistoryType,
    HistorySystem,
    Create,
    Search,
    SearchType,
    SearchSystem,
    Capabilities,
    Transaction,
    Batch,
    Operation,
}

impl FHIRRestfulInteractions {
    pub const URL: &'static str = "http://hl7.org/fhir/restful-interaction";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Read => "read",
            Self::Vread => "vread",
            Self::Update => "update",
            Self::Patch => "patch",
            Self::Delete => "delete",
            Self::History => "history",
            Self::HistoryInstance => "history-instance",
            Self::HistoryType => "history-type",
            Self::HistorySystem => "history-system",
            Self::Create => "create",
            Self::Search => "search",
            Self::SearchType => "search-type",
            Self::SearchSystem => "search-system",
            Self::Capabilities => "capabilities",
            Self::Transaction => "transaction",
            Self::Batch => "batch",
            Self::Operation => "operation",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "read" => Self::Read,
            "vread" => Self::Vread,
            "update" => Self::Update,
            "patch" => Self::Patch,
            "delete" => Self::Delete,
            "history" => Self::History,
            "history-instance" => Self::HistoryInstance,
            "history-type" => Self::HistoryType,
            "history-system" => Self::HistorySystem,
            "create" => Self::Create,
            "search" => Self::Search,
            "search-type" => Self::SearchType,
            "search-system" => Self::SearchSystem,
            "capabilities" => Self::Capabilities,
            "transaction" => Self::Transaction,
            "batch" => Self::Batch,
            "operation" => Self::Operation,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Read => Some("read"),
            Self::Vread => Some("vread"),
            Self::Update => Some("update"),
            Self::Patch => Some("patch"),
            Self::Delete => Some("delete"),
            Self::History => Some("history"),
            Self::HistoryInstance => Some("history-instance"),
            Self::HistoryType => Some("history-type"),
            Self::HistorySystem => Some("history-system"),
            Self::Create => Some("create"),
            Self::Search => Some("search"),
            Self::SearchType => Some("search-type"),
            Self::SearchSystem => Some("search-system"),
            Self::Capabilities => Some("capabilities"),
            Self::Transaction => Some("transaction"),
            Self::Batch => Some("batch"),
            Self::Operation => Some("operation"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Read => Some("Read the current state of the resource."),
            Self::Vread => Some("Read the state of a specific version of the resource."),
            Self::Update => {
                Some("Update an existing resource by its id (or create it if it is new).")
            }
            Self::Patch => Some("Update an existing resource by posting a set of changes to it."),
            Self::Delete => Some("Delete a resource."),
            Self::History => Some(
                "Retrieve the change history for a particular resource, type of resource, or the entire system.",
            ),
            Self::HistoryInstance => Some("Retrieve the change history for a particular resource."),
            Self::HistoryType => {
                Some("Retrieve the change history for all resources of a particular type.")
            }
            Self::HistorySystem => {
                Some("Retrieve the change history for all resources on a system.")
            }
            Self::Create => Some("Create a new resource with a server assigned id."),
            Self::Search => {
                Some("Search a resource type or all resources based on some filter criteria.")
            }
            Self::SearchType => {
                Some("Search all resources of the specified type based on some filter criteria.")
            }
            Self::SearchSystem => Some("Search all resources based on some filter criteria."),
            Self::Capabilities => Some("Get a Capability Statement for the system."),
            Self::Transaction => {
                Some("Update, create or delete a set of resources as a single transaction.")
            }
            Self::Batch => {
                Some("perform a set of a separate interactions in a single http operation")
            }
            Self::Operation => Some("Perform an operation as defined by an OperationDefinition."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Read => None,
            Self::Vread => None,
            Self::Update => None,
            Self::Patch => None,
            Self::Delete => None,
            Self::History => None,
            Self::HistoryInstance => Some("history"),
            Self::HistoryType => Some("history"),
            Self::HistorySystem => Some("history"),
            Self::Create => None,
            Self::Search => None,
            Self::SearchType => Some("search"),
            Self::SearchSystem => Some("search"),
            Self::Capabilities => None,
            Self::Transaction => None,
            Self::Batch => None,
            Self::Operation => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Read => 0,
            Self::Vread => 0,
            Self::Update => 0,
            Self::Patch => 0,
            Self::Delete => 0,
            Self::History => 0,
            Self::HistoryInstance => 1,
            Self::HistoryType => 1,
            Self::HistorySystem => 1,
            Self::Create => 0,
            Self::Search => 0,
            Self::SearchType => 1,
            Self::SearchSystem => 1,
            Self::Capabilities => 0,
            Self::Transaction => 0,
            Self::Batch => 0,
            Self::Operation => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
