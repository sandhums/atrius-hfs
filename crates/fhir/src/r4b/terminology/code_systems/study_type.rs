// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! StudyType
//!
//! The type of study the evidence was derived from.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum StudyType {
    RCT,
    CCT,
    Cohort,
    CaseControl,
    Series,
    CaseReport,
    Mixed,
}

impl StudyType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/study-type";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::RCT => "RCT",
            Self::CCT => "CCT",
            Self::Cohort => "cohort",
            Self::CaseControl => "case-control",
            Self::Series => "series",
            Self::CaseReport => "case-report",
            Self::Mixed => "mixed",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "RCT" => Self::RCT,
            "CCT" => Self::CCT,
            "cohort" => Self::Cohort,
            "case-control" => Self::CaseControl,
            "series" => Self::Series,
            "case-report" => Self::CaseReport,
            "mixed" => Self::Mixed,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::RCT => Some("randomized trial"),
            Self::CCT => Some("controlled trial (non-randomized)"),
            Self::Cohort => Some("comparative cohort study"),
            Self::CaseControl => Some("case-control study"),
            Self::Series => Some("uncontrolled cohort or case series"),
            Self::CaseReport => Some("case report"),
            Self::Mixed => Some("mixed methods"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::RCT => Some("randomized controlled trial."),
            Self::CCT => Some("controlled (but not randomized) trial."),
            Self::Cohort => Some("observational study comparing cohorts."),
            Self::CaseControl => Some("case-control study."),
            Self::Series => Some("uncontrolled cohort or case series."),
            Self::CaseReport => Some("a single case report."),
            Self::Mixed => Some("a combination of 1 or more types of studies."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::RCT => None,
            Self::CCT => None,
            Self::Cohort => None,
            Self::CaseControl => None,
            Self::Series => None,
            Self::CaseReport => None,
            Self::Mixed => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::RCT => 0,
            Self::CCT => 0,
            Self::Cohort => 0,
            Self::CaseControl => 0,
            Self::Series => 0,
            Self::CaseReport => 0,
            Self::Mixed => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
