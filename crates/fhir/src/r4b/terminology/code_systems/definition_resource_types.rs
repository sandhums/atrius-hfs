// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! DefinitionResourceType
//!
//! A list of all the definition resource types defined in this version of the FHIR specification.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DefinitionResourceType {
    ActivityDefinition,
    EventDefinition,
    Measure,
    OperationDefinition,
    PlanDefinition,
    Questionnaire,
}

impl DefinitionResourceType {
    pub const URL: &'static str = "http://hl7.org/fhir/definition-resource-types";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::ActivityDefinition => "ActivityDefinition",
            Self::EventDefinition => "EventDefinition",
            Self::Measure => "Measure",
            Self::OperationDefinition => "OperationDefinition",
            Self::PlanDefinition => "PlanDefinition",
            Self::Questionnaire => "Questionnaire",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "ActivityDefinition" => Self::ActivityDefinition,
            "EventDefinition" => Self::EventDefinition,
            "Measure" => Self::Measure,
            "OperationDefinition" => Self::OperationDefinition,
            "PlanDefinition" => Self::PlanDefinition,
            "Questionnaire" => Self::Questionnaire,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::ActivityDefinition => Some("ActivityDefinition"),
            Self::EventDefinition => Some("EventDefinition"),
            Self::Measure => Some("Measure"),
            Self::OperationDefinition => Some("OperationDefinition"),
            Self::PlanDefinition => Some("PlanDefinition"),
            Self::Questionnaire => Some("Questionnaire"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::ActivityDefinition => Some(
                "This resource allows for the definition of some activity to be performed, independent of a particular patient, practitioner, or other performance context.",
            ),
            Self::EventDefinition => Some(
                "The EventDefinition resource provides a reusable description of when a particular event can occur.",
            ),
            Self::Measure => {
                Some("The Measure resource provides the definition of a quality measure.")
            }
            Self::OperationDefinition => Some(
                "A formal computable definition of an operation (on the RESTful interface) or a named query (using the search interaction).",
            ),
            Self::PlanDefinition => Some(
                "This resource allows for the definition of various types of plans as a sharable, consumable, and executable artifact. The resource is general enough to support the description of a broad range of clinical artifacts such as clinical decision support rules, order sets and protocols.",
            ),
            Self::Questionnaire => Some(
                "A structured set of questions intended to guide the collection of answers from end-users. Questionnaires provide detailed control over order, presentation, phraseology and grouping to allow coherent, consistent data collection.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::ActivityDefinition => None,
            Self::EventDefinition => None,
            Self::Measure => None,
            Self::OperationDefinition => None,
            Self::PlanDefinition => None,
            Self::Questionnaire => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::ActivityDefinition => 0,
            Self::EventDefinition => 0,
            Self::Measure => 0,
            Self::OperationDefinition => 0,
            Self::PlanDefinition => 0,
            Self::Questionnaire => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
