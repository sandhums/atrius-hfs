// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! TestScriptRequestMethodCode
//!
//! The allowable request method or HTTP operation codes.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum TestScriptRequestMethodCode {
    Delete,
    Get,
    Options,
    Patch,
    Post,
    Put,
    Head,
}

impl TestScriptRequestMethodCode {
    pub const URL: &'static str = "http://hl7.org/fhir/http-operations";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Delete => "delete",
            Self::Get => "get",
            Self::Options => "options",
            Self::Patch => "patch",
            Self::Post => "post",
            Self::Put => "put",
            Self::Head => "head",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "delete" => Self::Delete,
            "get" => Self::Get,
            "options" => Self::Options,
            "patch" => Self::Patch,
            "post" => Self::Post,
            "put" => Self::Put,
            "head" => Self::Head,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Delete => Some("DELETE"),
            Self::Get => Some("GET"),
            Self::Options => Some("OPTIONS"),
            Self::Patch => Some("PATCH"),
            Self::Post => Some("POST"),
            Self::Put => Some("PUT"),
            Self::Head => Some("HEAD"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Delete => Some("HTTP DELETE operation."),
            Self::Get => Some("HTTP GET operation."),
            Self::Options => Some("HTTP OPTIONS operation."),
            Self::Patch => Some("HTTP PATCH operation."),
            Self::Post => Some("HTTP POST operation."),
            Self::Put => Some("HTTP PUT operation."),
            Self::Head => Some("HTTP HEAD operation."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Delete => None,
            Self::Get => None,
            Self::Options => None,
            Self::Patch => None,
            Self::Post => None,
            Self::Put => None,
            Self::Head => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Delete => 0,
            Self::Get => 0,
            Self::Options => 0,
            Self::Patch => 0,
            Self::Post => 0,
            Self::Put => 0,
            Self::Head => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
