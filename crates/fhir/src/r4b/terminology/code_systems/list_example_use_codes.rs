// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Example Use Codes for List
//!
//! Example use codes for the List resource - typical kinds of use.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ExampleUseCodesForList {
    Alerts,
    Adverserxns,
    Allergies,
    Medications,
    Problems,
    Worklist,
    Waiting,
    Protocols,
    Plans,
}

impl ExampleUseCodesForList {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/list-example-use-codes";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Alerts => "alerts",
            Self::Adverserxns => "adverserxns",
            Self::Allergies => "allergies",
            Self::Medications => "medications",
            Self::Problems => "problems",
            Self::Worklist => "worklist",
            Self::Waiting => "waiting",
            Self::Protocols => "protocols",
            Self::Plans => "plans",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "alerts" => Self::Alerts,
            "adverserxns" => Self::Adverserxns,
            "allergies" => Self::Allergies,
            "medications" => Self::Medications,
            "problems" => Self::Problems,
            "worklist" => Self::Worklist,
            "waiting" => Self::Waiting,
            "protocols" => Self::Protocols,
            "plans" => Self::Plans,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Alerts => Some("Alerts"),
            Self::Adverserxns => Some("Adverse Reactions"),
            Self::Allergies => Some("Allergies"),
            Self::Medications => Some("Medication List"),
            Self::Problems => Some("Problem List"),
            Self::Worklist => Some("Worklist"),
            Self::Waiting => Some("Waiting List"),
            Self::Protocols => Some("Protocols"),
            Self::Plans => Some("Care Plans"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Alerts => Some("A list of alerts for the patient."),
            Self::Adverserxns => Some("A list of part adverse reactions."),
            Self::Allergies => Some("A list of Allergies for the patient."),
            Self::Medications => Some("A list of medication statements for the patient."),
            Self::Problems => Some(
                "A list of problems that the patient is known of have (or have had in the past).",
            ),
            Self::Worklist => Some(
                "A list of items that constitute a set of work to be performed (typically this code would be specialized for more specific uses, such as a ward round list).",
            ),
            Self::Waiting => Some(
                "A list of items waiting for an event (perhaps a surgical patient waiting list).",
            ),
            Self::Protocols => Some("A set of protocols to be followed."),
            Self::Plans => Some("A set of care plans that apply in a particular context of care."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Alerts => None,
            Self::Adverserxns => None,
            Self::Allergies => None,
            Self::Medications => None,
            Self::Problems => None,
            Self::Worklist => None,
            Self::Waiting => None,
            Self::Protocols => None,
            Self::Plans => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Alerts => 0,
            Self::Adverserxns => 0,
            Self::Allergies => 0,
            Self::Medications => 0,
            Self::Problems => 0,
            Self::Worklist => 0,
            Self::Waiting => 0,
            Self::Protocols => 0,
            Self::Plans => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
