// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! ContributorSummarySource
//!
//! Used to code the producer or rule for creating the display string.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ContributorSummarySource {
    PublisherData,
    ArticleCopy,
    CitationManager,
    Custom,
}

impl ContributorSummarySource {
    pub const URL: &'static str =
        "http://terminology.hl7.org/CodeSystem/contributor-summary-source";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::PublisherData => "publisher-data",
            Self::ArticleCopy => "article-copy",
            Self::CitationManager => "citation-manager",
            Self::Custom => "custom",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "publisher-data" => Self::PublisherData,
            "article-copy" => Self::ArticleCopy,
            "citation-manager" => Self::CitationManager,
            "custom" => Self::Custom,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::PublisherData => Some("Publisher provided"),
            Self::ArticleCopy => Some("Copied from article"),
            Self::CitationManager => Some("Reported by citation manager"),
            Self::Custom => Some("custom format"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::PublisherData => Some("Data copied by machine from publisher data."),
            Self::ArticleCopy => Some("Data copied by human from article text."),
            Self::CitationManager => Some("Data copied by machine from citation manager data."),
            Self::Custom => Some("Custom format (may be described in text note)."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::PublisherData => None,
            Self::ArticleCopy => None,
            Self::CitationManager => None,
            Self::Custom => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::PublisherData => 0,
            Self::ArticleCopy => 0,
            Self::CitationManager => 0,
            Self::Custom => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
