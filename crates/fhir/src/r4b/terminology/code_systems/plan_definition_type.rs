// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! PlanDefinitionType
//!
//! The type of PlanDefinition.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum PlanDefinitionType {
    OrderSet,
    ClinicalProtocol,
    EcaRule,
    WorkflowDefinition,
}

impl PlanDefinitionType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/plan-definition-type";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::OrderSet => "order-set",
            Self::ClinicalProtocol => "clinical-protocol",
            Self::EcaRule => "eca-rule",
            Self::WorkflowDefinition => "workflow-definition",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "order-set" => Self::OrderSet,
            "clinical-protocol" => Self::ClinicalProtocol,
            "eca-rule" => Self::EcaRule,
            "workflow-definition" => Self::WorkflowDefinition,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::OrderSet => Some("Order Set"),
            Self::ClinicalProtocol => Some("Clinical Protocol"),
            Self::EcaRule => Some("ECA Rule"),
            Self::WorkflowDefinition => Some("Workflow Definition"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::OrderSet => Some(
                "A pre-defined and approved group of orders related to a particular clinical condition (e.g. hypertension treatment and monitoring) or stage of care (e.g. hospital admission to Coronary Care Unit). An order set is used as a checklist for the clinician when managing a patient with a specific condition. It is a structured collection of orders relevant to that condition and presented to the clinician in a computerized provider order entry (CPOE) system.",
            ),
            Self::ClinicalProtocol => Some(
                "Defines a desired/typical sequence of clinical activities including preconditions, triggers and temporal relationships.",
            ),
            Self::EcaRule => Some(
                "A decision support rule of the form [on Event] if Condition then Action. It is intended to be a shareable, computable definition of actions that should be taken whenever some condition is met in response to a particular event or events.",
            ),
            Self::WorkflowDefinition => Some(
                "Defines the steps for a group of one or more systems in an event flow process along with the step constraints, sequence, pre-conditions and decision points to complete a particular objective.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::OrderSet => None,
            Self::ClinicalProtocol => None,
            Self::EcaRule => None,
            Self::WorkflowDefinition => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::OrderSet => 0,
            Self::ClinicalProtocol => 0,
            Self::EcaRule => 0,
            Self::WorkflowDefinition => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
