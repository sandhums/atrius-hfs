// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Observation Reference Range Meaning Codes
//!
//! This value set defines a set of codes that can be used to indicate the meaning/use of a reference range.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ObservationReferenceRangeMeaningCodes {
    Type_,
    Normal,
    Recommended,
    Treatment,
    Therapeutic,
    Pre,
    Post,
    Endocrine,
    PrePuberty,
    Follicular,
    Midcycle,
    Luteal,
    Postmenopausal,
}

impl ObservationReferenceRangeMeaningCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/referencerange-meaning";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Type_ => "type",
            Self::Normal => "normal",
            Self::Recommended => "recommended",
            Self::Treatment => "treatment",
            Self::Therapeutic => "therapeutic",
            Self::Pre => "pre",
            Self::Post => "post",
            Self::Endocrine => "endocrine",
            Self::PrePuberty => "pre-puberty",
            Self::Follicular => "follicular",
            Self::Midcycle => "midcycle",
            Self::Luteal => "luteal",
            Self::Postmenopausal => "postmenopausal",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "type" => Self::Type_,
            "normal" => Self::Normal,
            "recommended" => Self::Recommended,
            "treatment" => Self::Treatment,
            "therapeutic" => Self::Therapeutic,
            "pre" => Self::Pre,
            "post" => Self::Post,
            "endocrine" => Self::Endocrine,
            "pre-puberty" => Self::PrePuberty,
            "follicular" => Self::Follicular,
            "midcycle" => Self::Midcycle,
            "luteal" => Self::Luteal,
            "postmenopausal" => Self::Postmenopausal,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Type_ => Some("Type"),
            Self::Normal => Some("Normal Range"),
            Self::Recommended => Some("Recommended Range"),
            Self::Treatment => Some("Treatment Range"),
            Self::Therapeutic => Some("Therapeutic Desired Level"),
            Self::Pre => Some("Pre Therapeutic Desired Level"),
            Self::Post => Some("Post Therapeutic Desired Level"),
            Self::Endocrine => Some("Endocrine"),
            Self::PrePuberty => Some("Pre-Puberty"),
            Self::Follicular => Some("Follicular Stage"),
            Self::Midcycle => Some("MidCycle"),
            Self::Luteal => Some("Luteal"),
            Self::Postmenopausal => Some("Post-Menopause"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Type_ => Some("General types of reference range."),
            Self::Normal => Some(
                "Values expected for a normal member of the relevant control population being measured. Typically each results producer such as a laboratory has specific normal ranges and they are usually defined as within two standard deviations from the mean and account for 95.45% of this population.",
            ),
            Self::Recommended => {
                Some("The range that is recommended by a relevant professional body.")
            }
            Self::Treatment => Some("The range at which treatment would/should be considered."),
            Self::Therapeutic => Some("The optimal range for best therapeutic outcomes."),
            Self::Pre => Some(
                "The optimal range for best therapeutic outcomes for a specimen taken immediately before administration.",
            ),
            Self::Post => Some(
                "The optimal range for best therapeutic outcomes for a specimen taken immediately after administration.",
            ),
            Self::Endocrine => Some("Endocrine related states that change the expected value."),
            Self::PrePuberty => Some("An expected range in an individual prior to puberty."),
            Self::Follicular => {
                Some("An expected range in an individual during the follicular stage of the cycle.")
            }
            Self::Midcycle => {
                Some("An expected range in an individual during the midcycle stage of the cycle.")
            }
            Self::Luteal => {
                Some("An expected range in an individual during the luteal stage of the cycle.")
            }
            Self::Postmenopausal => Some("An expected range in an individual post-menopause."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Type_ => None,
            Self::Normal => Some("type"),
            Self::Recommended => Some("type"),
            Self::Treatment => Some("type"),
            Self::Therapeutic => Some("type"),
            Self::Pre => Some("therapeutic"),
            Self::Post => Some("therapeutic"),
            Self::Endocrine => None,
            Self::PrePuberty => Some("endocrine"),
            Self::Follicular => Some("endocrine"),
            Self::Midcycle => Some("endocrine"),
            Self::Luteal => Some("endocrine"),
            Self::Postmenopausal => Some("endocrine"),
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Type_ => 0,
            Self::Normal => 1,
            Self::Recommended => 1,
            Self::Treatment => 1,
            Self::Therapeutic => 1,
            Self::Pre => 2,
            Self::Post => 2,
            Self::Endocrine => 0,
            Self::PrePuberty => 1,
            Self::Follicular => 1,
            Self::Midcycle => 1,
            Self::Luteal => 1,
            Self::Postmenopausal => 1,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
