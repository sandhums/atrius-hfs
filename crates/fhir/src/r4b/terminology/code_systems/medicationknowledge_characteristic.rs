// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! MedicationKnowledgeCharacteristicCodes

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MedicationKnowledgeCharacteristicCodes {
    Imprintcd,
    Size,
    Shape,
    Color,
    Coating,
    Scoring,
    Logo,
}

impl MedicationKnowledgeCharacteristicCodes {
    pub const URL: &'static str =
        "http://terminology.hl7.org/CodeSystem/medicationknowledge-characteristic";
    pub const VERSION: &'static str = "4.3.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Imprintcd => "imprintcd",
            Self::Size => "size",
            Self::Shape => "shape",
            Self::Color => "color",
            Self::Coating => "coating",
            Self::Scoring => "scoring",
            Self::Logo => "logo",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "imprintcd" => Self::Imprintcd,
            "size" => Self::Size,
            "shape" => Self::Shape,
            "color" => Self::Color,
            "coating" => Self::Coating,
            "scoring" => Self::Scoring,
            "logo" => Self::Logo,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Imprintcd => Some("Imprint Code"),
            Self::Size => Some("Size"),
            Self::Shape => Some("Shape"),
            Self::Color => Some("Color"),
            Self::Coating => Some("Coating"),
            Self::Scoring => Some("Scoring"),
            Self::Logo => Some("Logo"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Imprintcd => Some("Identyifying marks on product"),
            Self::Size => Some("Description of size of the product"),
            Self::Shape => Some("Description of the shape of the product"),
            Self::Color => Some("Description of the color of the product"),
            Self::Coating => Some("Description of the coating of the product"),
            Self::Scoring => Some("Description of the scoring of the product"),
            Self::Logo => Some("Description of the Logo of the product"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Imprintcd => None,
            Self::Size => None,
            Self::Shape => None,
            Self::Color => None,
            Self::Coating => None,
            Self::Scoring => None,
            Self::Logo => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Imprintcd => 0,
            Self::Size => 0,
            Self::Shape => 0,
            Self::Color => 0,
            Self::Coating => 0,
            Self::Scoring => 0,
            Self::Logo => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
