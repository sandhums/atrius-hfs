// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! ISO210892017HealthRecordLifecycleEvents
//!
//! Attached is vocabulary for the 27 record lifecycle events, as per ISO TS 21089-2017, Health Informatics - Trusted End-to-End Information Flows, Section 3, Terms and Definitions (2017, at ISO Central Secretariat, passed ballot and ready for publication).  This will also be included in the FHIR EHR Record Lifecycle Event Implementation Guide, balloted and (to be) published with FHIR STU-3.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ISO210892017HealthRecordLifecycleEvents {
    Access,
    Hold,
    Amend,
    Archive,
    Attest,
    Decrypt,
    Deidentify,
    Deprecate,
    Destroy,
    Disclose,
    Encrypt,
    Extract,
    Link,
    Merge,
    Originate,
    Pseudonymize,
    Reactivate,
    Receive,
    Reidentify,
    Unhold,
    Report,
    Restore,
    Transform,
    Transmit,
    Unlink,
    Unmerge,
    Verify,
}

impl ISO210892017HealthRecordLifecycleEvents {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/iso-21089-lifecycle";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Access => "access",
            Self::Hold => "hold",
            Self::Amend => "amend",
            Self::Archive => "archive",
            Self::Attest => "attest",
            Self::Decrypt => "decrypt",
            Self::Deidentify => "deidentify",
            Self::Deprecate => "deprecate",
            Self::Destroy => "destroy",
            Self::Disclose => "disclose",
            Self::Encrypt => "encrypt",
            Self::Extract => "extract",
            Self::Link => "link",
            Self::Merge => "merge",
            Self::Originate => "originate",
            Self::Pseudonymize => "pseudonymize",
            Self::Reactivate => "reactivate",
            Self::Receive => "receive",
            Self::Reidentify => "reidentify",
            Self::Unhold => "unhold",
            Self::Report => "report",
            Self::Restore => "restore",
            Self::Transform => "transform",
            Self::Transmit => "transmit",
            Self::Unlink => "unlink",
            Self::Unmerge => "unmerge",
            Self::Verify => "verify",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "access" => Self::Access,
            "hold" => Self::Hold,
            "amend" => Self::Amend,
            "archive" => Self::Archive,
            "attest" => Self::Attest,
            "decrypt" => Self::Decrypt,
            "deidentify" => Self::Deidentify,
            "deprecate" => Self::Deprecate,
            "destroy" => Self::Destroy,
            "disclose" => Self::Disclose,
            "encrypt" => Self::Encrypt,
            "extract" => Self::Extract,
            "link" => Self::Link,
            "merge" => Self::Merge,
            "originate" => Self::Originate,
            "pseudonymize" => Self::Pseudonymize,
            "reactivate" => Self::Reactivate,
            "receive" => Self::Receive,
            "reidentify" => Self::Reidentify,
            "unhold" => Self::Unhold,
            "report" => Self::Report,
            "restore" => Self::Restore,
            "transform" => Self::Transform,
            "transmit" => Self::Transmit,
            "unlink" => Self::Unlink,
            "unmerge" => Self::Unmerge,
            "verify" => Self::Verify,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Access => Some("Access/View Record Lifecycle Event"),
            Self::Hold => Some("Add Legal Hold Record Lifecycle Event"),
            Self::Amend => Some("Amend (Update) Record Lifecycle Event"),
            Self::Archive => Some("Archive Record Lifecycle Event"),
            Self::Attest => Some("Attest Record Lifecycle Event"),
            Self::Decrypt => Some("Decrypt Record Lifecycle Event"),
            Self::Deidentify => Some("De-Identify (Anononymize) Record Lifecycle Event"),
            Self::Deprecate => Some("Deprecate Record Lifecycle Event"),
            Self::Destroy => Some("Destroy/Delete Record Lifecycle Event"),
            Self::Disclose => Some("Disclose Record Lifecycle Event"),
            Self::Encrypt => Some("Encrypt Record Lifecycle Event"),
            Self::Extract => Some("Extract Record Lifecycle Event"),
            Self::Link => Some("Link Record Lifecycle Event"),
            Self::Merge => Some("Merge Record Lifecycle Event"),
            Self::Originate => Some("Originate/Retain Record Lifecycle Event"),
            Self::Pseudonymize => Some("Pseudonymize Record Lifecycle Event"),
            Self::Reactivate => Some("Re-activate Record Lifecycle Event"),
            Self::Receive => Some("Receive/Retain Record Lifecycle Event"),
            Self::Reidentify => Some("Re-identify Record Lifecycle Event"),
            Self::Unhold => Some("Remove Legal Hold Record Lifecycle Event"),
            Self::Report => Some("Report (Output) Record Lifecycle Event"),
            Self::Restore => Some("Restore Record Lifecycle Event"),
            Self::Transform => Some("Transform/Translate Record Lifecycle Event"),
            Self::Transmit => Some("Transmit Record Lifecycle Event"),
            Self::Unlink => Some("Unlink Record Lifecycle Event"),
            Self::Unmerge => Some("Unmerge Record Lifecycle Event"),
            Self::Verify => Some("Verify Record Lifecycle Event"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Access => Some(
                "Occurs when an agent causes the system to obtain and open a record entry for inspection or review.",
            ),
            Self::Hold => Some(
                "Occurs when an agent causes the system to tag or otherwise indicate special access management and suspension of record entry deletion/destruction, if deemed relevant to a lawsuit or which are reasonably anticipated to be relevant or to fulfill organizational policy under the legal doctrine of \u{201c}duty to preserve\u{201d}.",
            ),
            Self::Amend => Some(
                "Occurs when an agent makes any change to record entry content currently residing in storage considered permanent (persistent).",
            ),
            Self::Archive => Some(
                "Occurs when an agent causes the system to create and move archive artifacts containing record entry content, typically to long-term offline storage.",
            ),
            Self::Attest => Some(
                "Occurs when an agent causes the system to capture the agent\u{2019}s digital signature (or equivalent indication) during formal validation of record entry content.",
            ),
            Self::Decrypt => Some(
                "Occurs when an agent causes the system to decode record entry content from a cipher.",
            ),
            Self::Deidentify => Some(
                "Occurs when an agent causes the system to scrub record entry content to reduce the association between a set of identifying data and the data subject in a way that might or might not be reversible.",
            ),
            Self::Deprecate => Some(
                "Occurs when an agent causes the system to tag record entry(ies) as obsolete, erroneous or untrustworthy, to warn against its future use.",
            ),
            Self::Destroy => Some(
                "Occurs when an agent causes the system to permanently erase record entry content from the system.",
            ),
            Self::Disclose => Some(
                "Occurs when an agent causes the system to release, transfer, provision access to, or otherwise divulge record entry content.",
            ),
            Self::Encrypt => Some(
                "Occurs when an agent causes the system to encode record entry content in a cipher.",
            ),
            Self::Extract => Some(
                "Occurs when an agent causes the system to selectively pull out a subset of record entry content, based on explicit criteria.",
            ),
            Self::Link => {
                Some("Occurs when an agent causes the system to connect related record entries.")
            }
            Self::Merge => Some(
                "Occurs when an agent causes the system to combine or join content from two or more record entries, resulting in a single logical record entry.",
            ),
            Self::Originate => Some(
                "Occurs when an agent causes the system to: a) initiate capture of potential record content, and b) incorporate that content into the storage considered a permanent part of the health record.",
            ),
            Self::Pseudonymize => Some(
                "Occurs when an agent causes the system to remove record entry content to reduce the association between a set of identifying data and the data subject in a way that may be reversible.",
            ),
            Self::Reactivate => Some(
                "Occurs when an agent causes the system to recreate or restore full status to record entries previously deleted or deprecated.",
            ),
            Self::Receive => Some(
                "Occurs when an agent causes the system to a) initiate capture of data content from elsewhere, and b) incorporate that content into the storage considered a permanent part of the health record.",
            ),
            Self::Reidentify => Some(
                "Occurs when an agent causes the system to restore information to data that allows identification of information source and/or information subject.",
            ),
            Self::Unhold => Some(
                "Occurs when an agent causes the system to remove a tag or other cues for special access management had required to fulfill organizational policy under the legal doctrine of \u{201c}duty to preserve\u{201d}.",
            ),
            Self::Report => Some(
                "Occurs when an agent causes the system to produce and deliver record entry content in a particular form and manner.",
            ),
            Self::Restore => Some(
                "Occurs when an agent causes the system to recreate record entries and their content from a previous created archive artefact.",
            ),
            Self::Transform => Some(
                "Occurs when an agent causes the system to change the form, language or code system used to represent record entry content.",
            ),
            Self::Transmit => Some(
                "Occurs when an agent causes the system to send record entry content from one (EHR/PHR/other) system to another.",
            ),
            Self::Unlink => Some(
                "Occurs when an agent causes the system to disconnect two or more record entries previously connected, rendering them separate (disconnected) again.",
            ),
            Self::Unmerge => Some(
                "Occurs when an agent causes the system to reverse a previous record entry merge operation, rendering them separate again.",
            ),
            Self::Verify => Some(
                "Occurs when an agent causes the system to confirm compliance of data or data objects with regulations, requirements, specifications, or other imposed conditions based on organizational policy.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Access => None,
            Self::Hold => None,
            Self::Amend => None,
            Self::Archive => None,
            Self::Attest => None,
            Self::Decrypt => None,
            Self::Deidentify => None,
            Self::Deprecate => None,
            Self::Destroy => None,
            Self::Disclose => None,
            Self::Encrypt => None,
            Self::Extract => None,
            Self::Link => None,
            Self::Merge => None,
            Self::Originate => None,
            Self::Pseudonymize => None,
            Self::Reactivate => None,
            Self::Receive => None,
            Self::Reidentify => None,
            Self::Unhold => None,
            Self::Report => None,
            Self::Restore => None,
            Self::Transform => None,
            Self::Transmit => None,
            Self::Unlink => None,
            Self::Unmerge => None,
            Self::Verify => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Access => 0,
            Self::Hold => 0,
            Self::Amend => 0,
            Self::Archive => 0,
            Self::Attest => 0,
            Self::Decrypt => 0,
            Self::Deidentify => 0,
            Self::Deprecate => 0,
            Self::Destroy => 0,
            Self::Disclose => 0,
            Self::Encrypt => 0,
            Self::Extract => 0,
            Self::Link => 0,
            Self::Merge => 0,
            Self::Originate => 0,
            Self::Pseudonymize => 0,
            Self::Reactivate => 0,
            Self::Receive => 0,
            Self::Reidentify => 0,
            Self::Unhold => 0,
            Self::Report => 0,
            Self::Restore => 0,
            Self::Transform => 0,
            Self::Transmit => 0,
            Self::Unlink => 0,
            Self::Unmerge => 0,
            Self::Verify => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: None,
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
