// AUTO-GENERATED by atrius-fhir-generator (R5 Identifier)
use crate::r5::*;

/// FHIR Identifier type
///
/// Identifier Type: An identifier - identifies some entity uniquely and
/// unambiguously. Typically this is used for business identifiers.
///
/// ## Purpose
/// Need to be able to identify things with confidence and be sure that the identification is not subject to misinterpretation.
///
/// ## Type: Complex-type type
/// Base type: http://hl7.org/fhir/StructureDefinition/DataType
///
/// ## Status: active
/// FHIR Version: 5.0.0
///
/// See: [Identifier](http://hl7.org/fhir/StructureDefinition/Identifier)
#[derive(Debug, Clone, PartialEq, FhirSerde, FhirPath, Default)]
#[fhir_resource(summary_fields = "r#use,r#type,system,value,period,assigner")]
pub struct Identifier {
    /// Unique id for inter-element referencing
    ///
    /// Unique id for the element within a resource (for internal references). This
    /// may be any string value that does not contain spaces.
    ///
    /// ## Cardinality: Optional (0..1)
    ///
    /// ## Conditions
    /// Used when: ele-1
    pub id: Option<String>,
    /// Additional content defined by implementations
    ///
    /// May be used to represent additional information that is not part of the basic
    /// definition of the element. To make the use of extensions safe and managable,
    /// there is a strict set of governance applied to the definition and use of
    /// extensions. Though any implementer can define an extension, there is a set of
    /// requirements that SHALL be met as part of the definition of the extension.
    ///
    /// ## Implementation Notes
    /// There can be no stigma associated with the use of extensions by any
    /// application, project, or standard - regardless of the institution or
    /// jurisdiction that uses or defines the extensions. The use of extensions is
    /// what allows the FHIR specification to retain a core level of simplicity for
    /// everyone.
    ///
    /// ## Cardinality: Optional, Multiple (0..*)
    ///
    /// ## Constraints
    /// - **ele-1**: All FHIR elements must have a @value or children (error)
    ///   Expression: `hasValue() or (children().count() > id.count())`
    /// - **ext-1**: Must have either extensions or value[x], not both (error)
    ///   Expression: `extension.exists() != value.exists()`
    ///
    /// ## Aliases
    /// extensions, user content
    pub extension: Option<Vec<Extension>>,
    /// usual | official | temp | secondary | old (If known)
    ///
    /// The purpose of this identifier.
    ///
    /// ## Requirements
    /// Allows the appropriate identifier for a particular context of use to be
    /// selected from among a set of identifiers.
    ///
    /// ## Implementation Notes
    /// Applications can assume that an identifier is permanent unless it explicitly
    /// says that it is temporary.
    ///
    /// ## Cardinality: Optional (0..1)
    ///
    /// ## Special Semantics
    /// - Modifier element - This is labeled as "Is Modifier" because applications should not mistake a temporary id for a permanent one.
    /// - Included in summary
    ///
    /// ## Constraints
    /// - **ele-1**: All FHIR elements must have a @value or children (error)
    ///   Expression: `hasValue() or (children().count() > id.count())`
    ///
    /// ## Binding
    /// - **Strength**: required
    /// - **Description**: Identifies the purpose for this identifier, if known .
    /// - **ValueSet**: http://hl7.org/fhir/ValueSet/identifier-use|5.0.0
    #[fhir_serde(rename = "use")]
    pub r#use: Option<Code>,
    /// Description of identifier
    ///
    /// A coded type for the identifier that can be used to determine which
    /// identifier to use for a specific purpose.
    ///
    /// ## Requirements
    /// Allows users to make use of identifiers when the identifier system is not
    /// known.
    ///
    /// ## Implementation Notes
    /// This element deals only with general categories of identifiers. It SHOULD not
    /// be used for codes that correspond 1..1 with the Identifier.system. Some
    /// identifiers may fall into multiple categories due to common usage. Where the
    /// system is known, a type is unnecessary because the type is always part of the
    /// system definition. However systems often need to handle identifiers where the
    /// system is not known. There is not a 1:1 relationship between type and system,
    /// since many different systems have the same type.
    ///
    /// ## Cardinality: Optional (0..1)
    ///
    /// ## Special Semantics
    /// - Included in summary
    ///
    /// ## Constraints
    /// - **ele-1**: All FHIR elements must have a @value or children (error)
    ///   Expression: `hasValue() or (children().count() > id.count())`
    ///
    /// ## Binding
    /// - **Strength**: extensible
    /// - **Description**: A coded type for an identifier that can be used to determine which identifier to use for a specific purpose.
    /// - **ValueSet**: http://hl7.org/fhir/ValueSet/identifier-type
    #[fhir_serde(rename = "type")]
    pub r#type: Option<CodeableConcept>,
    /// The namespace for the identifier value
    ///
    /// Establishes the namespace for the value - that is, an absolute URL that
    /// describes a set values that are unique.
    ///
    /// ## Requirements
    /// There are many sets of identifiers. To perform matching of two identifiers,
    /// we need to know what set we're dealing with. The system identifies a
    /// particular set of unique identifiers.
    ///
    /// ## Implementation Notes
    /// Identifier.system is always case sensitive.
    ///
    /// ## Cardinality: Optional (0..1)
    ///
    /// ## Special Semantics
    /// - Included in summary
    ///
    /// ## Constraints
    /// - **ele-1**: All FHIR elements must have a @value or children (error)
    ///   Expression: `hasValue() or (children().count() > id.count())`
    ///
    /// ## Examples
    /// - General: None
    pub system: Option<Uri>,
    /// The value that is unique
    ///
    /// The portion of the identifier typically relevant to the user and which is
    /// unique within the context of the system.
    ///
    /// ## Implementation Notes
    /// If the value is a full URI, then the system SHALL be urn:ietf:rfc:3986. The
    /// value's primary purpose is computational mapping. As a result, it may be
    /// normalized for comparison purposes (e.g. removing non-significant whitespace,
    /// dashes, etc.) A value formatted for human display can be conveyed using the
    /// [http://hl7.org/fhir/StructureDefinition/rendered-value](http://hl7.org/fhir/extensions/StructureDefinition-rendered-value.html)).
    /// Identifier.value is to be treated as case sensitive unless knowledge of the
    /// Identifier.system allows the processer to be confident that
    /// non-case-sensitive processing is safe.
    ///
    /// ## Cardinality: Optional (0..1)
    ///
    /// ## Special Semantics
    /// - Included in summary
    ///
    /// ## Constraints
    /// - **ele-1**: All FHIR elements must have a @value or children (error)
    ///   Expression: `hasValue() or (children().count() > id.count())`
    ///
    /// ## Examples
    /// - General: None
    ///
    /// ## Conditions
    /// Used when: ident-1
    pub value: Option<String>,
    /// Time period when id is/was valid for use
    ///
    /// Time period during which identifier is/was valid for use.
    ///
    /// ## Cardinality: Optional (0..1)
    ///
    /// ## Special Semantics
    /// - Included in summary
    ///
    /// ## Constraints
    /// - **ele-1**: All FHIR elements must have a @value or children (error)
    ///   Expression: `hasValue() or (children().count() > id.count())`
    pub period: Option<Period>,
    /// Organization that issued id (may be just text)
    ///
    /// Organization that issued/manages the identifier.
    ///
    /// ## Implementation Notes
    /// The Identifier.assigner may omit the .reference element and only contain a
    /// .display element reflecting the name or other textual information about the
    /// assigning organization.
    ///
    /// ## Cardinality: Optional (0..1)
    ///
    /// ## Special Semantics
    /// - Included in summary
    ///
    /// ## Constraints
    /// - **ele-1**: All FHIR elements must have a @value or children (error)
    ///   Expression: `hasValue() or (children().count() > id.count())`
    pub assigner: Option<Box<Reference>>,
}
