// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Subscription Notification Type
//!
//! The type of notification represented by the status message.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum SubscriptionNotificationType {
    Handshake,
    Heartbeat,
    EventNotification,
    QueryStatus,
    QueryEvent,
}

impl SubscriptionNotificationType {
    pub const URL: &'static str = "http://hl7.org/fhir/subscription-notification-type";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Handshake => "handshake",
            Self::Heartbeat => "heartbeat",
            Self::EventNotification => "event-notification",
            Self::QueryStatus => "query-status",
            Self::QueryEvent => "query-event",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "handshake" => Self::Handshake,
            "heartbeat" => Self::Heartbeat,
            "event-notification" => Self::EventNotification,
            "query-status" => Self::QueryStatus,
            "query-event" => Self::QueryEvent,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Handshake => Some("Handshake"),
            Self::Heartbeat => Some("Heartbeat"),
            Self::EventNotification => Some("Event Notification"),
            Self::QueryStatus => Some("Query Status"),
            Self::QueryEvent => Some("Query Event"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Handshake => Some(
                "The status was generated as part of the setup or verification of a communications channel.",
            ),
            Self::Heartbeat => Some(
                "The status was generated to perform a heartbeat notification to the subscriber.",
            ),
            Self::EventNotification => {
                Some("The status was generated for an event to the subscriber.")
            }
            Self::QueryStatus => {
                Some("The status was generated in response to a status query/request.")
            }
            Self::QueryEvent => {
                Some("The status was generated in response to an event query/request.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Handshake => None,
            Self::Heartbeat => None,
            Self::EventNotification => None,
            Self::QueryStatus => None,
            Self::QueryEvent => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Handshake => 0,
            Self::Heartbeat => 0,
            Self::EventNotification => 0,
            Self::QueryStatus => 0,
            Self::QueryEvent => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
