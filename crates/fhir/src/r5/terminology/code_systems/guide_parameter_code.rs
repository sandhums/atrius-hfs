// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Guide Parameter Code
//!
//! GuideParameterCode

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum GuideParameterCode {
    Apply,
    PathResource,
    PathPages,
    PathTxCache,
    ExpansionParameter,
    RuleBrokenLinks,
    GenerateXml,
    GenerateJson,
    GenerateTurtle,
    HtmlTemplate,
}

impl GuideParameterCode {
    pub const URL: &'static str = "http://hl7.org/fhir/guide-parameter-code";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Apply => "apply",
            Self::PathResource => "path-resource",
            Self::PathPages => "path-pages",
            Self::PathTxCache => "path-tx-cache",
            Self::ExpansionParameter => "expansion-parameter",
            Self::RuleBrokenLinks => "rule-broken-links",
            Self::GenerateXml => "generate-xml",
            Self::GenerateJson => "generate-json",
            Self::GenerateTurtle => "generate-turtle",
            Self::HtmlTemplate => "html-template",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "apply" => Self::Apply,
            "path-resource" => Self::PathResource,
            "path-pages" => Self::PathPages,
            "path-tx-cache" => Self::PathTxCache,
            "expansion-parameter" => Self::ExpansionParameter,
            "rule-broken-links" => Self::RuleBrokenLinks,
            "generate-xml" => Self::GenerateXml,
            "generate-json" => Self::GenerateJson,
            "generate-turtle" => Self::GenerateTurtle,
            "html-template" => Self::HtmlTemplate,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Apply => Some("Apply Metadata Value"),
            Self::PathResource => Some("Resource Path"),
            Self::PathPages => Some("Pages Path"),
            Self::PathTxCache => Some("Terminology Cache Path"),
            Self::ExpansionParameter => Some("Expansion Profile"),
            Self::RuleBrokenLinks => Some("Broken Links Rule"),
            Self::GenerateXml => Some("Generate XML"),
            Self::GenerateJson => Some("Generate JSON"),
            Self::GenerateTurtle => Some("Generate Turtle"),
            Self::HtmlTemplate => Some("HTML Template"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Apply => Some(
                "If the value of this string 0..* parameter is one of the metadata fields then all conformance resources will have any specified [Resource].[field] overwritten with the ImplementationGuide.[field], where field is one of: version, date, status, publisher, contact, copyright, experimental, jurisdiction, useContext.",
            ),
            Self::PathResource => Some(
                "The value of this string 0..* parameter is a subfolder of the build context\'s location that is to be scanned to load resources. Scope is (if present) a particular resource type.",
            ),
            Self::PathPages => Some(
                "The value of this string 0..1 parameter is a subfolder of the build context\'s location that contains files that are part of the html content processed by the builder.",
            ),
            Self::PathTxCache => Some(
                "The value of this string 0..1 parameter is a subfolder of the build context\'s location that is used as the terminology cache. If this is not present, the terminology cache is on the local system, not under version control.",
            ),
            Self::ExpansionParameter => Some(
                "The value of this string 0..* parameter is a parameter (name=value) when expanding value sets for this implementation guide. This is particularly used to specify the versions of published terminologies such as SNOMED CT.",
            ),
            Self::RuleBrokenLinks => Some(
                "The value of this string 0..1 parameter is either \"warning\" or \"error\" (default = \"error\"). If the value is \"warning\" then IG build tools allow the IG to be considered successfully build even when there is no internal broken links.",
            ),
            Self::GenerateXml => Some(
                "The value of this boolean 0..1 parameter specifies whether the IG publisher creates examples in XML format. If not present, the Publication Tool decides whether to generate XML.",
            ),
            Self::GenerateJson => Some(
                "The value of this boolean 0..1 parameter specifies whether the IG publisher creates examples in JSON format. If not present, the Publication Tool decides whether to generate JSON.",
            ),
            Self::GenerateTurtle => Some(
                "The value of this boolean 0..1 parameter specifies whether the IG publisher creates examples in Turtle format. If not present, the Publication Tool decides whether to generate Turtle.",
            ),
            Self::HtmlTemplate => Some(
                "The value of this string singleton parameter is the name of the file to use as the builder template for each generated page (see templating).",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Apply => None,
            Self::PathResource => None,
            Self::PathPages => None,
            Self::PathTxCache => None,
            Self::ExpansionParameter => None,
            Self::RuleBrokenLinks => None,
            Self::GenerateXml => None,
            Self::GenerateJson => None,
            Self::GenerateTurtle => None,
            Self::HtmlTemplate => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Apply => 0,
            Self::PathResource => 0,
            Self::PathPages => 0,
            Self::PathTxCache => 0,
            Self::ExpansionParameter => 0,
            Self::RuleBrokenLinks => 0,
            Self::GenerateXml => 0,
            Self::GenerateJson => 0,
            Self::GenerateTurtle => 0,
            Self::HtmlTemplate => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
