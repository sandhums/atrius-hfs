// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Permission Rule Combining
//!
//! Codes identifying the rule combining. See XACML Combining algorithms  http://docs.oasis-open.org/xacml/3.0/xacml-3.0-core-spec-cos01-en.html

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum PermissionRuleCombining {
    DenyOverrides,
    PermitOverrides,
    OrderedDenyOverrides,
    OrderedPermitOverrides,
    DenyUnlessPermit,
    PermitUnlessDeny,
}

impl PermissionRuleCombining {
    pub const URL: &'static str = "http://hl7.org/fhir/permission-rule-combining";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::DenyOverrides => "deny-overrides",
            Self::PermitOverrides => "permit-overrides",
            Self::OrderedDenyOverrides => "ordered-deny-overrides",
            Self::OrderedPermitOverrides => "ordered-permit-overrides",
            Self::DenyUnlessPermit => "deny-unless-permit",
            Self::PermitUnlessDeny => "permit-unless-deny",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "deny-overrides" => Self::DenyOverrides,
            "permit-overrides" => Self::PermitOverrides,
            "ordered-deny-overrides" => Self::OrderedDenyOverrides,
            "ordered-permit-overrides" => Self::OrderedPermitOverrides,
            "deny-unless-permit" => Self::DenyUnlessPermit,
            "permit-unless-deny" => Self::PermitUnlessDeny,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::DenyOverrides => Some("Deny-overrides"),
            Self::PermitOverrides => Some("Permit-overrides"),
            Self::OrderedDenyOverrides => Some("Ordered-deny-overrides"),
            Self::OrderedPermitOverrides => Some("Ordered-permit-overrides"),
            Self::DenyUnlessPermit => Some("Deny-unless-permit"),
            Self::PermitUnlessDeny => Some("Permit-unless-deny"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::DenyOverrides => Some(
                "The deny overrides combining algorithm is intended for those cases where a deny decision should have priority over a permit decision.",
            ),
            Self::PermitOverrides => Some(
                "The permit overrides combining algorithm is intended for those cases where a permit decision should have priority over a deny decision.",
            ),
            Self::OrderedDenyOverrides => Some(
                "The behavior of this algorithm is identical to that of the \u{201c}Deny-overrides\u{201d} rule-combining algorithm with one exception.  The order in which the collection of rules is evaluated SHALL match the order as listed in the permission.",
            ),
            Self::OrderedPermitOverrides => Some(
                "The behavior of this algorithm is identical to that of the \u{201c}Permit-overrides\u{201d} rule-combining algorithm with one exception.  The order in which the collection of rules is evaluated SHALL match the order as listed in the permission.",
            ),
            Self::DenyUnlessPermit => Some(
                "The \u{201c}Deny-unless-permit\u{201d} combining algorithm is intended for those cases where a permit decision should have priority over a deny decision, and an \u{201c}Indeterminate\u{201d} or \u{201c}NotApplicable\u{201d} must never be the result. It is particularly useful at the top level in a policy structure to ensure that a PDP will always return a definite \u{201c}Permit\u{201d} or \u{201c}Deny\u{201d} result.",
            ),
            Self::PermitUnlessDeny => Some(
                "The \u{201c}Permit-unless-deny\u{201d} combining algorithm is intended for those cases where a deny decision should have priority over a permit decision, and an \u{201c}Indeterminate\u{201d} or \u{201c}NotApplicable\u{201d} must never be the result. It is particularly useful at the top level in a policy structure to ensure that a PDP will always return a definite \u{201c}Permit\u{201d} or \u{201c}Deny\u{201d} result. This algorithm has the following behavior.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::DenyOverrides => None,
            Self::PermitOverrides => None,
            Self::OrderedDenyOverrides => None,
            Self::OrderedPermitOverrides => None,
            Self::DenyUnlessPermit => None,
            Self::PermitUnlessDeny => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::DenyOverrides => 0,
            Self::PermitOverrides => 0,
            Self::OrderedDenyOverrides => 0,
            Self::OrderedPermitOverrides => 0,
            Self::DenyUnlessPermit => 0,
            Self::PermitUnlessDeny => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
