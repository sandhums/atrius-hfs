// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Device Usage Adherence Reason
//!
//! A coded concept indicating the reason for the usage of the device.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceUsageAdherenceReason {
    Lost,
    Stolen,
    Prescribed,
    Broken,
    Burned,
    Forgot,
}

impl DeviceUsageAdherenceReason {
    pub const URL: &'static str = "http://hl7.org/fhir/deviceusage-adherence-reason";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Lost => "lost",
            Self::Stolen => "stolen",
            Self::Prescribed => "prescribed",
            Self::Broken => "broken",
            Self::Burned => "burned",
            Self::Forgot => "forgot",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "lost" => Self::Lost,
            "stolen" => Self::Stolen,
            "prescribed" => Self::Prescribed,
            "broken" => Self::Broken,
            "burned" => Self::Burned,
            "forgot" => Self::Forgot,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Lost => Some("Lost"),
            Self::Stolen => Some("Stolen"),
            Self::Prescribed => Some("Prescribed"),
            Self::Broken => Some("Broken"),
            Self::Burned => Some("Burned"),
            Self::Forgot => Some("Forgot"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Lost => Some("The device is lost."),
            Self::Stolen => Some("The device is stolen."),
            Self::Prescribed => Some("The device is prescribed."),
            Self::Broken => Some("The device is broken."),
            Self::Burned => Some("The device is burned."),
            Self::Forgot => Some("The device is forgotten."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Lost => None,
            Self::Stolen => None,
            Self::Prescribed => None,
            Self::Broken => None,
            Self::Burned => None,
            Self::Forgot => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Lost => 0,
            Self::Stolen => 0,
            Self::Prescribed => 0,
            Self::Broken => 0,
            Self::Burned => 0,
            Self::Forgot => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
