// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Focus Characteristic Code
//!
//! Evidence focus characteristic code.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum FocusCharacteristicCode {
    Citation,
    ClinicalOutcomesObserved,
    Population,
    Exposure,
    Comparator,
    Outcome,
    MedicationExposures,
    StudyType,
}

impl FocusCharacteristicCode {
    pub const URL: &'static str = "http://hl7.org/fhir/focus-characteristic-code";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Citation => "citation",
            Self::ClinicalOutcomesObserved => "clinical-outcomes-observed",
            Self::Population => "population",
            Self::Exposure => "exposure",
            Self::Comparator => "comparator",
            Self::Outcome => "outcome",
            Self::MedicationExposures => "medication-exposures",
            Self::StudyType => "study-type",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "citation" => Self::Citation,
            "clinical-outcomes-observed" => Self::ClinicalOutcomesObserved,
            "population" => Self::Population,
            "exposure" => Self::Exposure,
            "comparator" => Self::Comparator,
            "outcome" => Self::Outcome,
            "medication-exposures" => Self::MedicationExposures,
            "study-type" => Self::StudyType,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Citation => Some("Citation"),
            Self::ClinicalOutcomesObserved => Some("Observed outcomes are clinical outcomes"),
            Self::Population => Some("Population"),
            Self::Exposure => Some("Exposure"),
            Self::Comparator => Some("Comparator"),
            Self::Outcome => Some("Outcome"),
            Self::MedicationExposures => Some("Medication exposures"),
            Self::StudyType => Some("Study type"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Citation => Some("Used to reference a specific article."),
            Self::ClinicalOutcomesObserved => Some(
                "Used to denote a focus on clinical outcomes, ie evidence variable in role of outcome (measured variable) as observed is considered a \"clinical outcome\" (patient-important outcome such as mortality, symptoms, function or quality of life).",
            ),
            Self::Population => Some("The population of interest."),
            Self::Exposure => Some("The exposure of interest, such as an intervention."),
            Self::Comparator => Some("The comparator (intervention or control state) of interest."),
            Self::Outcome => Some("the outcome of interest."),
            Self::MedicationExposures => Some(
                "Any medication exposures. A subset of exposures or interventions that are medications.",
            ),
            Self::StudyType => {
                Some("Type of research study, such as randomized trial or case-control study.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Citation => None,
            Self::ClinicalOutcomesObserved => None,
            Self::Population => None,
            Self::Exposure => None,
            Self::Comparator => None,
            Self::Outcome => None,
            Self::MedicationExposures => None,
            Self::StudyType => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Citation => 0,
            Self::ClinicalOutcomesObserved => 0,
            Self::Population => 0,
            Self::Exposure => 0,
            Self::Comparator => 0,
            Self::Outcome => 0,
            Self::MedicationExposures => 0,
            Self::StudyType => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
