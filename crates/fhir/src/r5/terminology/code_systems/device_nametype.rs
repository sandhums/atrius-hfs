// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Device Name Type
//!
//! The type of name the device is referred by.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceNameType {
    RegisteredName,
    UserFriendlyName,
    PatientReportedName,
}

impl DeviceNameType {
    pub const URL: &'static str = "http://hl7.org/fhir/device-nametype";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::RegisteredName => "registered-name",
            Self::UserFriendlyName => "user-friendly-name",
            Self::PatientReportedName => "patient-reported-name",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "registered-name" => Self::RegisteredName,
            "user-friendly-name" => Self::UserFriendlyName,
            "patient-reported-name" => Self::PatientReportedName,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::RegisteredName => Some("Registered name"),
            Self::UserFriendlyName => Some("User Friendly name"),
            Self::PatientReportedName => Some("Patient Reported name"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::RegisteredName => Some(
                "The term assigned to a medical device by the entity who registers or submits information about it to a jurisdiction or its databases. This may be considered the manufacturer assigned name (e.g., brand name assigned by the labeler or manufacturer in US, or device name assigned by the manufacturer in EU) and may also be synonymous with proprietary name or trade name of the device.",
            ),
            Self::UserFriendlyName => Some(
                "The term that generically describes the device by a name as assigned by the manufacturer that is recognized by lay person.  This common or generic name may be printed on the package it came in or some combination of that name with the model number, serial number, or other attribute that makes the name easy to understand for the user of that device. It is often exposed in communicating devices transport protocols. It is provided to help users identify the device when reported in discovery operations.",
            ),
            Self::PatientReportedName => Some(
                "the term used by the patient associated with the device when describing the device, for example \'knee implant\', when documented as a self-reported device.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::RegisteredName => None,
            Self::UserFriendlyName => None,
            Self::PatientReportedName => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::RegisteredName => 0,
            Self::UserFriendlyName => 0,
            Self::PatientReportedName => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
