// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Evidence Certainty Type
//!
//! The aspect of quality, confidence, or certainty.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum EvidenceCertaintyType {
    Overall,
    RiskOfBias,
    Inconsistency,
    Indirectness,
    Imprecision,
    PublicationBias,
    DoseResponseGradient,
    PlausibleConfounding,
    LargeEffect,
}

impl EvidenceCertaintyType {
    pub const URL: &'static str = "http://hl7.org/fhir/certainty-type";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Overall => "Overall",
            Self::RiskOfBias => "RiskOfBias",
            Self::Inconsistency => "Inconsistency",
            Self::Indirectness => "Indirectness",
            Self::Imprecision => "Imprecision",
            Self::PublicationBias => "PublicationBias",
            Self::DoseResponseGradient => "DoseResponseGradient",
            Self::PlausibleConfounding => "PlausibleConfounding",
            Self::LargeEffect => "LargeEffect",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "Overall" => Self::Overall,
            "RiskOfBias" => Self::RiskOfBias,
            "Inconsistency" => Self::Inconsistency,
            "Indirectness" => Self::Indirectness,
            "Imprecision" => Self::Imprecision,
            "PublicationBias" => Self::PublicationBias,
            "DoseResponseGradient" => Self::DoseResponseGradient,
            "PlausibleConfounding" => Self::PlausibleConfounding,
            "LargeEffect" => Self::LargeEffect,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Overall => Some("Overall certainty"),
            Self::RiskOfBias => Some("Risk of bias"),
            Self::Inconsistency => Some("Inconsistency"),
            Self::Indirectness => Some("Indirectness"),
            Self::Imprecision => Some("Imprecision"),
            Self::PublicationBias => Some("Publication bias"),
            Self::DoseResponseGradient => Some("Dose response gradient"),
            Self::PlausibleConfounding => Some("Plausible confounding"),
            Self::LargeEffect => Some("Large effect"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Overall => Some("Overall certainty of evidence (quality of evidence)."),
            Self::RiskOfBias => Some("methodologic concerns reducing internal validity."),
            Self::Inconsistency => {
                Some("concerns that findings are not similar enough to support certainty.")
            }
            Self::Indirectness => Some("concerns reducing external validity."),
            Self::Imprecision => Some("fuzzy or wide variability."),
            Self::PublicationBias => Some(
                "likelihood that what is published misrepresents what is available to publish.",
            ),
            Self::DoseResponseGradient => {
                Some("higher certainty due to dose response relationship.")
            }
            Self::PlausibleConfounding => {
                Some("higher certainty due to risk of bias in opposite direction.")
            }
            Self::LargeEffect => Some("higher certainty due to large effect size."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Overall => None,
            Self::RiskOfBias => None,
            Self::Inconsistency => None,
            Self::Indirectness => None,
            Self::Imprecision => None,
            Self::PublicationBias => None,
            Self::DoseResponseGradient => None,
            Self::PlausibleConfounding => None,
            Self::LargeEffect => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Overall => 0,
            Self::RiskOfBias => 0,
            Self::Inconsistency => 0,
            Self::Indirectness => 0,
            Self::Imprecision => 0,
            Self::PublicationBias => 0,
            Self::DoseResponseGradient => 0,
            Self::PlausibleConfounding => 0,
            Self::LargeEffect => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
