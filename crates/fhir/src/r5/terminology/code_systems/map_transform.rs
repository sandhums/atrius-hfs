// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Structure Map Transform
//!
//! How data is copied/created.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum StructureMapTransform {
    Create,
    Copy,
    Truncate,
    Escape,
    Cast,
    Append,
    Translate,
    Reference,
    DateOp,
    Uuid,
    Pointer,
    Evaluate,
    Cc,
    C,
    Qty,
    Id,
    Cp,
}

impl StructureMapTransform {
    pub const URL: &'static str = "http://hl7.org/fhir/map-transform";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Create => "create",
            Self::Copy => "copy",
            Self::Truncate => "truncate",
            Self::Escape => "escape",
            Self::Cast => "cast",
            Self::Append => "append",
            Self::Translate => "translate",
            Self::Reference => "reference",
            Self::DateOp => "dateOp",
            Self::Uuid => "uuid",
            Self::Pointer => "pointer",
            Self::Evaluate => "evaluate",
            Self::Cc => "cc",
            Self::C => "c",
            Self::Qty => "qty",
            Self::Id => "id",
            Self::Cp => "cp",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "create" => Self::Create,
            "copy" => Self::Copy,
            "truncate" => Self::Truncate,
            "escape" => Self::Escape,
            "cast" => Self::Cast,
            "append" => Self::Append,
            "translate" => Self::Translate,
            "reference" => Self::Reference,
            "dateOp" => Self::DateOp,
            "uuid" => Self::Uuid,
            "pointer" => Self::Pointer,
            "evaluate" => Self::Evaluate,
            "cc" => Self::Cc,
            "c" => Self::C,
            "qty" => Self::Qty,
            "id" => Self::Id,
            "cp" => Self::Cp,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Create => Some("create"),
            Self::Copy => Some("copy"),
            Self::Truncate => Some("truncate"),
            Self::Escape => Some("escape"),
            Self::Cast => Some("cast"),
            Self::Append => Some("append"),
            Self::Translate => Some("translate"),
            Self::Reference => Some("reference"),
            Self::DateOp => Some("dateOp"),
            Self::Uuid => Some("uuid"),
            Self::Pointer => Some("pointer"),
            Self::Evaluate => Some("evaluate"),
            Self::Cc => Some("cc"),
            Self::C => Some("c"),
            Self::Qty => Some("qty"),
            Self::Id => Some("id"),
            Self::Cp => Some("cp"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Create => Some(
                "create(type : string) - type is passed through to the application on the standard API, and must be known by it.",
            ),
            Self::Copy => Some("copy(source)."),
            Self::Truncate => Some("truncate(source, length) - source must be stringy type."),
            Self::Escape => Some(
                "escape(source, fmt1, fmt2) - change source from one kind of escaping to another (plain, java, xml, json). note that this is for when the string itself is escaped.",
            ),
            Self::Cast => Some(
                "cast(source, type?) - cast (convert) source from one type to another. Target type can be left as implicit if there is one and only one target type known. The default namespace for the type is \'FHIR\' (see [FHIRPath type specifiers](http://hl7.org/fhirpath/N1/#is-type-specifier))",
            ),
            Self::Append => Some("append(source...) - source is element or string."),
            Self::Translate => Some("translate(source, uri_of_map) - use the translate operation."),
            Self::Reference => Some(
                "reference(source : object) - return a string that references the provided tree properly.",
            ),
            Self::DateOp => Some("Perform a date operation. *Parameters to be documented*."),
            Self::Uuid => Some("Generate a random UUID (in lowercase). No Parameters."),
            Self::Pointer => Some(
                "Return the appropriate string to put in a reference that refers to the resource provided as a parameter.",
            ),
            Self::Evaluate => {
                Some("Execute the supplied FHIRPath expression and use the value returned by that.")
            }
            Self::Cc => {
                Some("Create a CodeableConcept. Parameters = (text) or (system. Code[, display]).")
            }
            Self::C => Some("Create a Coding. Parameters = (system. Code[, display])."),
            Self::Qty => Some(
                "Create a quantity. Parameters = (text) or (value, unit, [system, code]) where text is the natural representation e.g. [comparator]value[space]unit.",
            ),
            Self::Id => Some(
                "Create an identifier. Parameters = (system, value[, type]) where type is a code from the identifier type value set.",
            ),
            Self::Cp => Some(
                "Create a contact details. Parameters = (value) or (system, value). If no system is provided, the system should be inferred from the content of the value.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Create => None,
            Self::Copy => None,
            Self::Truncate => None,
            Self::Escape => None,
            Self::Cast => None,
            Self::Append => None,
            Self::Translate => None,
            Self::Reference => None,
            Self::DateOp => None,
            Self::Uuid => None,
            Self::Pointer => None,
            Self::Evaluate => None,
            Self::Cc => None,
            Self::C => None,
            Self::Qty => None,
            Self::Id => None,
            Self::Cp => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Create => 0,
            Self::Copy => 0,
            Self::Truncate => 0,
            Self::Escape => 0,
            Self::Cast => 0,
            Self::Append => 0,
            Self::Translate => 0,
            Self::Reference => 0,
            Self::DateOp => 0,
            Self::Uuid => 0,
            Self::Pointer => 0,
            Self::Evaluate => 0,
            Self::Cc => 0,
            Self::C => 0,
            Self::Qty => 0,
            Self::Id => 0,
            Self::Cp => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
