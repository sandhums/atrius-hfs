// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! SearchParamType
//!
//! Data types allowed to be used for search parameters.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum SearchParamType {
    Number,
    Date,
    String,
    Token,
    Reference,
    Composite,
    Quantity,
    Uri,
    Special,
}

impl SearchParamType {
    pub const URL: &'static str = "http://hl7.org/fhir/search-param-type";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Number => "number",
            Self::Date => "date",
            Self::String => "string",
            Self::Token => "token",
            Self::Reference => "reference",
            Self::Composite => "composite",
            Self::Quantity => "quantity",
            Self::Uri => "uri",
            Self::Special => "special",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "number" => Self::Number,
            "date" => Self::Date,
            "string" => Self::String,
            "token" => Self::Token,
            "reference" => Self::Reference,
            "composite" => Self::Composite,
            "quantity" => Self::Quantity,
            "uri" => Self::Uri,
            "special" => Self::Special,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Number => Some("Number"),
            Self::Date => Some("Date/DateTime"),
            Self::String => Some("String"),
            Self::Token => Some("Token"),
            Self::Reference => Some("Reference"),
            Self::Composite => Some("Composite"),
            Self::Quantity => Some("Quantity"),
            Self::Uri => Some("URI"),
            Self::Special => Some("Special"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Number => {
                Some("Search parameter SHALL be a number (a whole number, or a decimal).")
            }
            Self::Date => Some(
                "Search parameter is on a date/time. The date format is the standard XML format, though other formats may be supported.",
            ),
            Self::String => Some(
                "Search parameter is a simple string, like a name part. Search is case-insensitive and accent-insensitive. May match just the start of a string. String parameters may contain spaces.",
            ),
            Self::Token => Some(
                "Search parameter on a coded element or identifier. May be used to search through the text, display, code and code/codesystem (for codes) and label, system and key (for identifier). Its value is either a string or a pair of namespace and value, separated by a \"|\", depending on the modifier used.",
            ),
            Self::Reference => Some("A reference to another resource (Reference or canonical)."),
            Self::Composite => {
                Some("A composite search parameter that combines a search on two values together.")
            }
            Self::Quantity => Some("A search parameter that searches on a quantity."),
            Self::Uri => Some("A search parameter that searches on a URI (RFC 3986)."),
            Self::Special => Some(
                "Special logic applies to this parameter per the description of the search parameter.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Number => None,
            Self::Date => None,
            Self::String => None,
            Self::Token => None,
            Self::Reference => None,
            Self::Composite => None,
            Self::Quantity => None,
            Self::Uri => None,
            Self::Special => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Number => 0,
            Self::Date => 0,
            Self::String => 0,
            Self::Token => 0,
            Self::Reference => 0,
            Self::Composite => 0,
            Self::Quantity => 0,
            Self::Uri => 0,
            Self::Special => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
