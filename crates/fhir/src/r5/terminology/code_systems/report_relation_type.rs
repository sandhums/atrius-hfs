// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Report Relationship Type
//!
//! The type of relationship between reports.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ReportRelationshipType {
    Replaces,
    Amends,
    Appends,
    Transforms,
    ReplacedWith,
    AmendedWith,
    AppendedWith,
    TransformedWith,
}

impl ReportRelationshipType {
    pub const URL: &'static str = "http://hl7.org/fhir/report-relation-type";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Replaces => "replaces",
            Self::Amends => "amends",
            Self::Appends => "appends",
            Self::Transforms => "transforms",
            Self::ReplacedWith => "replacedWith",
            Self::AmendedWith => "amendedWith",
            Self::AppendedWith => "appendedWith",
            Self::TransformedWith => "transformedWith",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "replaces" => Self::Replaces,
            "amends" => Self::Amends,
            "appends" => Self::Appends,
            "transforms" => Self::Transforms,
            "replacedWith" => Self::ReplacedWith,
            "amendedWith" => Self::AmendedWith,
            "appendedWith" => Self::AppendedWith,
            "transformedWith" => Self::TransformedWith,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Replaces => Some("Replaces"),
            Self::Amends => Some("Amends"),
            Self::Appends => Some("Appends"),
            Self::Transforms => Some("Transforms"),
            Self::ReplacedWith => Some("Replaced With"),
            Self::AmendedWith => Some("Amended With"),
            Self::AppendedWith => Some("Appended With"),
            Self::TransformedWith => Some("Transformed With"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Replaces => Some("This document replaces or supersedes the target document."),
            Self::Amends => Some(
                "This document notes corrections or changes to replace or supersede parts of the target document.",
            ),
            Self::Appends => {
                Some("This document adds additional information to the target document.")
            }
            Self::Transforms => Some(
                "This document was generated by transforming the target document (eg format or language conversion).",
            ),
            Self::ReplacedWith => Some("This document was."),
            Self::AmendedWith => Some("This document was."),
            Self::AppendedWith => Some("This document was."),
            Self::TransformedWith => Some("This document was."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Replaces => None,
            Self::Amends => None,
            Self::Appends => None,
            Self::Transforms => None,
            Self::ReplacedWith => None,
            Self::AmendedWith => None,
            Self::AppendedWith => None,
            Self::TransformedWith => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Replaces => 0,
            Self::Amends => 0,
            Self::Appends => 0,
            Self::Transforms => 0,
            Self::ReplacedWith => 0,
            Self::AmendedWith => 0,
            Self::AppendedWith => 0,
            Self::TransformedWith => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
