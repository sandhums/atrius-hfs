// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Characteristic Combination
//!
//! Logical grouping of characteristics.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum CharacteristicCombination {
    AllOf,
    AnyOf,
    AtLeast,
    AtMost,
    Statistical,
    NetEffect,
    Dataset,
}

impl CharacteristicCombination {
    pub const URL: &'static str = "http://hl7.org/fhir/characteristic-combination";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::AllOf => "all-of",
            Self::AnyOf => "any-of",
            Self::AtLeast => "at-least",
            Self::AtMost => "at-most",
            Self::Statistical => "statistical",
            Self::NetEffect => "net-effect",
            Self::Dataset => "dataset",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "all-of" => Self::AllOf,
            "any-of" => Self::AnyOf,
            "at-least" => Self::AtLeast,
            "at-most" => Self::AtMost,
            "statistical" => Self::Statistical,
            "net-effect" => Self::NetEffect,
            "dataset" => Self::Dataset,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::AllOf => Some("All of"),
            Self::AnyOf => Some("Any of"),
            Self::AtLeast => Some("At least"),
            Self::AtMost => Some("At most"),
            Self::Statistical => Some("Statistical"),
            Self::NetEffect => Some("Net effect"),
            Self::Dataset => Some("Dataset"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::AllOf => Some("Combine characteristics with AND."),
            Self::AnyOf => Some("Combine characteristics with OR."),
            Self::AtLeast => {
                Some("Meet at least the threshold number of characteristics for definition.")
            }
            Self::AtMost => {
                Some("Meet at most the threshold number of characteristics for definition.")
            }
            Self::Statistical => Some(
                "Combine characteristics statistically. Use method to specify the statistical method.",
            ),
            Self::NetEffect => {
                Some("Combine characteristics by addition of benefits and subtraction of harms.")
            }
            Self::Dataset => Some("Combine characteristics as a collection used as the dataset."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::AllOf => None,
            Self::AnyOf => None,
            Self::AtLeast => None,
            Self::AtMost => None,
            Self::Statistical => None,
            Self::NetEffect => None,
            Self::Dataset => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::AllOf => 0,
            Self::AnyOf => 0,
            Self::AtLeast => 0,
            Self::AtMost => 0,
            Self::Statistical => 0,
            Self::NetEffect => 0,
            Self::Dataset => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
