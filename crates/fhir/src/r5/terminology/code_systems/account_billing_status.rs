// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Example Account Billing Statuses
//!
//! Indicates whether the account is available to be used for billing purposes.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum AccountBillingStatus {
    Open,
    CarecompleteNotbilled,
    Billing,
    ClosedBaddebt,
    ClosedVoided,
    ClosedCompleted,
    ClosedCombined,
}

impl AccountBillingStatus {
    pub const URL: &'static str = "http://hl7.org/fhir/account-billing-status";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Open => "open",
            Self::CarecompleteNotbilled => "carecomplete-notbilled",
            Self::Billing => "billing",
            Self::ClosedBaddebt => "closed-baddebt",
            Self::ClosedVoided => "closed-voided",
            Self::ClosedCompleted => "closed-completed",
            Self::ClosedCombined => "closed-combined",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "open" => Self::Open,
            "carecomplete-notbilled" => Self::CarecompleteNotbilled,
            "billing" => Self::Billing,
            "closed-baddebt" => Self::ClosedBaddebt,
            "closed-voided" => Self::ClosedVoided,
            "closed-completed" => Self::ClosedCompleted,
            "closed-combined" => Self::ClosedCombined,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Open => Some("Open"),
            Self::CarecompleteNotbilled => Some("CareComplete/Not Billed"),
            Self::Billing => Some("Billing"),
            Self::ClosedBaddebt => Some("Closed-Bad Debt"),
            Self::ClosedVoided => Some("Closed-Voided"),
            Self::ClosedCompleted => Some("Closed-Completed"),
            Self::ClosedCombined => Some("Closed-Combined"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Open => {
                Some("The account is open for charging transactions (account.status is active)")
            }
            Self::CarecompleteNotbilled => Some(
                "The account.status is still active and may have charges recorded against it (only for events in the servicePeriod), however the encounters associated are completed. (Also known as Discharged not billed) This BillingStatus is often not used in ongoing accounts. (account.status is active)",
            ),
            Self::Billing => Some(
                "Indicates that all transactions are recorded and the finance system can perform the billing process, including preparing insurance claims, scrubbing charges, invoicing etc. During this time any new charges will not be included in the current billing run/cycle. (account.status is active)",
            ),
            Self::ClosedBaddebt => Some(
                "The balance of this debt has not been able to be recovered, and the organization has decided not to persue debt recovery. (account.status is in-active)",
            ),
            Self::ClosedVoided => Some(
                "The account was not created in error, however the organization has decided that it will not be charging any transactions associated. (account.status is i n-active)",
            ),
            Self::ClosedCompleted => Some(
                "The account is closed and all charges are processed and accounted for. (account.status is i n-active)",
            ),
            Self::ClosedCombined => Some(
                "This account has been merged into another account, all charged have been migrated. This account should no longer be used, and will not be billed. (account.status is i n-active)",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Open => None,
            Self::CarecompleteNotbilled => None,
            Self::Billing => None,
            Self::ClosedBaddebt => None,
            Self::ClosedVoided => None,
            Self::ClosedCompleted => None,
            Self::ClosedCombined => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Open => 0,
            Self::CarecompleteNotbilled => 0,
            Self::Billing => 0,
            Self::ClosedBaddebt => 0,
            Self::ClosedVoided => 0,
            Self::ClosedCompleted => 0,
            Self::ClosedCombined => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
