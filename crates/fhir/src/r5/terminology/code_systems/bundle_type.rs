// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Bundle Type
//!
//! Indicates the purpose of a bundle - how it is intended to be used.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum BundleType {
    Document,
    Message,
    Transaction,
    TransactionResponse,
    Batch,
    BatchResponse,
    History,
    Searchset,
    Collection,
    SubscriptionNotification,
}

impl BundleType {
    pub const URL: &'static str = "http://hl7.org/fhir/bundle-type";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Document => "document",
            Self::Message => "message",
            Self::Transaction => "transaction",
            Self::TransactionResponse => "transaction-response",
            Self::Batch => "batch",
            Self::BatchResponse => "batch-response",
            Self::History => "history",
            Self::Searchset => "searchset",
            Self::Collection => "collection",
            Self::SubscriptionNotification => "subscription-notification",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "document" => Self::Document,
            "message" => Self::Message,
            "transaction" => Self::Transaction,
            "transaction-response" => Self::TransactionResponse,
            "batch" => Self::Batch,
            "batch-response" => Self::BatchResponse,
            "history" => Self::History,
            "searchset" => Self::Searchset,
            "collection" => Self::Collection,
            "subscription-notification" => Self::SubscriptionNotification,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Document => Some("Document"),
            Self::Message => Some("Message"),
            Self::Transaction => Some("Transaction"),
            Self::TransactionResponse => Some("Transaction Response"),
            Self::Batch => Some("Batch"),
            Self::BatchResponse => Some("Batch Response"),
            Self::History => Some("History List"),
            Self::Searchset => Some("Search Results"),
            Self::Collection => Some("Collection"),
            Self::SubscriptionNotification => Some("Subscription Notification"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Document => {
                Some("The bundle is a document. The first resource is a Composition.")
            }
            Self::Message => {
                Some("The bundle is a message. The first resource is a MessageHeader.")
            }
            Self::Transaction => Some(
                "The bundle is a transaction - intended to be processed by a server as an atomic commit.",
            ),
            Self::TransactionResponse => Some(
                "The bundle is a transaction response. Because the response is a transaction response, the transaction has succeeded, and all responses are error free.",
            ),
            Self::Batch => Some(
                "The bundle is a set of actions - intended to be processed by a server as a group of independent actions.",
            ),
            Self::BatchResponse => Some(
                "The bundle is a batch response. Note that as a batch, some responses may indicate failure and others success.",
            ),
            Self::History => {
                Some("The bundle is a list of resources from a history interaction on a server.")
            }
            Self::Searchset => Some(
                "The bundle is a list of resources returned as a result of a search/query interaction, operation, or message.",
            ),
            Self::Collection => Some(
                "The bundle is a set of resources collected into a single package for ease of distribution that imposes no processing obligations or behavioral rules beyond persistence.",
            ),
            Self::SubscriptionNotification => Some(
                "The bundle has been generated by a Subscription to communicate information to a client.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Document => None,
            Self::Message => None,
            Self::Transaction => None,
            Self::TransactionResponse => None,
            Self::Batch => None,
            Self::BatchResponse => None,
            Self::History => None,
            Self::Searchset => None,
            Self::Collection => None,
            Self::SubscriptionNotification => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Document => 0,
            Self::Message => 0,
            Self::Transaction => 0,
            Self::TransactionResponse => 0,
            Self::Batch => 0,
            Self::BatchResponse => 0,
            Self::History => 0,
            Self::Searchset => 0,
            Self::Collection => 0,
            Self::SubscriptionNotification => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
