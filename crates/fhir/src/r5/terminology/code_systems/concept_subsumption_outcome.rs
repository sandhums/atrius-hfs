// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Concept Subsumption Outcome code system
//!
//! Codes indicating the results of a subsumption check between codes. In the context of this CodeSystem, subsumption is defined in the FHIR specification under Resource Types - CodeSystem.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ConceptSubsumptionOutcome {
    Equivalent,
    Subsumes,
    SubsumedBy,
    NotSubsumed,
}

impl ConceptSubsumptionOutcome {
    pub const URL: &'static str = "http://hl7.org/fhir/concept-subsumption-outcome";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Equivalent => "equivalent",
            Self::Subsumes => "subsumes",
            Self::SubsumedBy => "subsumed-by",
            Self::NotSubsumed => "not-subsumed",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "equivalent" => Self::Equivalent,
            "subsumes" => Self::Subsumes,
            "subsumed-by" => Self::SubsumedBy,
            "not-subsumed" => Self::NotSubsumed,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Equivalent => Some("Equivalent"),
            Self::Subsumes => Some("Subsumes"),
            Self::SubsumedBy => Some("Subsumed-By"),
            Self::NotSubsumed => Some("Not-Subsumed"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Equivalent => Some("A equivalent to B if A subsumes B and B subsumes A"),
            Self::Subsumes => {
                Some("A subsumes B if there is a subsumption relationship between A and B")
            }
            Self::SubsumedBy => Some("A subsumed by B if B subsumes A"),
            Self::NotSubsumed => Some("Neither A subsumes B nor B subsumes A"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Equivalent => None,
            Self::Subsumes => None,
            Self::SubsumedBy => None,
            Self::NotSubsumed => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Equivalent => 0,
            Self::Subsumes => 0,
            Self::SubsumedBy => 0,
            Self::NotSubsumed => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
