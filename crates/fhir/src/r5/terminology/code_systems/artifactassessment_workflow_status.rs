// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Artifact Assessment Workflow Status
//!
//! Possible values for the workflow status of the comment or assessment, typically used to coordinate workflow around the process of accepting and rejecting changes and comments on the artifact.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ArtifactAssessmentWorkflowStatus {
    Submitted,
    Triaged,
    WaitingForInput,
    ResolvedNoChange,
    ResolvedChangeRequired,
    Deferred,
    Duplicate,
    Applied,
    Published,
    EnteredInError,
}

impl ArtifactAssessmentWorkflowStatus {
    pub const URL: &'static str = "http://hl7.org/fhir/artifactassessment-workflow-status";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Submitted => "submitted",
            Self::Triaged => "triaged",
            Self::WaitingForInput => "waiting-for-input",
            Self::ResolvedNoChange => "resolved-no-change",
            Self::ResolvedChangeRequired => "resolved-change-required",
            Self::Deferred => "deferred",
            Self::Duplicate => "duplicate",
            Self::Applied => "applied",
            Self::Published => "published",
            Self::EnteredInError => "entered-in-error",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "submitted" => Self::Submitted,
            "triaged" => Self::Triaged,
            "waiting-for-input" => Self::WaitingForInput,
            "resolved-no-change" => Self::ResolvedNoChange,
            "resolved-change-required" => Self::ResolvedChangeRequired,
            "deferred" => Self::Deferred,
            "duplicate" => Self::Duplicate,
            "applied" => Self::Applied,
            "published" => Self::Published,
            "entered-in-error" => Self::EnteredInError,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Submitted => Some("Submitted"),
            Self::Triaged => Some("Triaged"),
            Self::WaitingForInput => Some("Waiting for Input"),
            Self::ResolvedNoChange => Some("Resolved - No Change"),
            Self::ResolvedChangeRequired => Some("Resolved - Change Required"),
            Self::Deferred => Some("Deferred"),
            Self::Duplicate => Some("Duplicate"),
            Self::Applied => Some("Applied"),
            Self::Published => Some("Published"),
            Self::EnteredInError => Some("Entered in Error"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Submitted => Some(
                "The comment has been submitted, but the responsible party has not yet been determined, or the responsible party has not yet determined the next steps to be taken.",
            ),
            Self::Triaged => Some(
                "The comment has been triaged, meaning the responsible party has been determined and next steps have been identified to address the comment.",
            ),
            Self::WaitingForInput => Some(
                "The comment is waiting for input from a specific party before next steps can be taken.",
            ),
            Self::ResolvedNoChange => {
                Some("The comment has been resolved and no changes resulted from the resolution")
            }
            Self::ResolvedChangeRequired => Some(
                "The comment has been resolved and changes are required to address the comment",
            ),
            Self::Deferred => Some(
                "The comment is acceptable, but resolution of the comment and application of any associated changes have been deferred",
            ),
            Self::Duplicate => {
                Some("The comment is a duplicate of another comment already received")
            }
            Self::Applied => {
                Some("The comment is resolved and any necessary changes have been applied")
            }
            Self::Published => Some(
                "The necessary changes to the artifact have been published in a new version of the artifact",
            ),
            Self::EnteredInError => Some("The assessment was entered in error"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Submitted => None,
            Self::Triaged => None,
            Self::WaitingForInput => None,
            Self::ResolvedNoChange => None,
            Self::ResolvedChangeRequired => None,
            Self::Deferred => None,
            Self::Duplicate => None,
            Self::Applied => None,
            Self::Published => None,
            Self::EnteredInError => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Submitted => 0,
            Self::Triaged => 0,
            Self::WaitingForInput => 0,
            Self::ResolvedNoChange => 0,
            Self::ResolvedChangeRequired => 0,
            Self::Deferred => 0,
            Self::Duplicate => 0,
            Self::Applied => 0,
            Self::Published => 0,
            Self::EnteredInError => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
