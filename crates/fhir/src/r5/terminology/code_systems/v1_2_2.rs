// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! ETSI Signature Type Codes
//!
//! The ETSI TS 101 733 V2.2.1 (2013-04) - Electronic Signatures and Infrastructures (ESI) - defines a set of Commitment Types (Purpose of Signature). ETSI TS 101 903 V1.2.2 defines vocabulary identifiers for these Commitment Types. Digital Signature Purposes, an indication of the reason an entity signs a document. This is included in the signed information and can be used when determining accountability for various actions concerning the document.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ETSISignatureTypeCodes {
    ProofOfOrigin,
    ProofOfReceipt,
    ProofOfDelivery,
    ProofOfSender,
    ProofOfapproval,
    ProofOfCreation,
}

impl ETSISignatureTypeCodes {
    pub const URL: &'static str = "http://uri.etsi.org/01903/v1.2.2";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::ProofOfOrigin => "ProofOfOrigin",
            Self::ProofOfReceipt => "ProofOfReceipt",
            Self::ProofOfDelivery => "ProofOfDelivery",
            Self::ProofOfSender => "ProofOfSender",
            Self::ProofOfapproval => "ProofOfapproval",
            Self::ProofOfCreation => "ProofOfCreation",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "ProofOfOrigin" => Self::ProofOfOrigin,
            "ProofOfReceipt" => Self::ProofOfReceipt,
            "ProofOfDelivery" => Self::ProofOfDelivery,
            "ProofOfSender" => Self::ProofOfSender,
            "ProofOfapproval" => Self::ProofOfapproval,
            "ProofOfCreation" => Self::ProofOfCreation,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::ProofOfOrigin => Some("Proof of origin"),
            Self::ProofOfReceipt => Some("Proof of receipt"),
            Self::ProofOfDelivery => Some("Proof of delivery"),
            Self::ProofOfSender => Some("Proof of sender"),
            Self::ProofOfapproval => Some("Proof of approval"),
            Self::ProofOfCreation => Some("Proof of creation"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::ProofOfOrigin => Some(
                "indicates that the signer recognizes to have created, approved and sent the signed data object.",
            ),
            Self::ProofOfReceipt => Some(
                "indicates that signer recognizes to have received the content of the signed data object.",
            ),
            Self::ProofOfDelivery => Some(
                "indicates that the TSP providing that indication has delivered a signed data object in a local store accessible to the recipient of the signed data object.",
            ),
            Self::ProofOfSender => Some(
                "indicates that the entity providing that indication has sent the signed data object (but not necessarily created it).",
            ),
            Self::ProofOfapproval => Some(
                "indicates that the signer has approved the content of the signed data object.",
            ),
            Self::ProofOfCreation => Some(
                "indicates that the signer has created the signed data object (but not necessarily approved, nor sent it).",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::ProofOfOrigin => None,
            Self::ProofOfReceipt => None,
            Self::ProofOfDelivery => None,
            Self::ProofOfSender => None,
            Self::ProofOfapproval => None,
            Self::ProofOfCreation => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::ProofOfOrigin => 0,
            Self::ProofOfReceipt => 0,
            Self::ProofOfDelivery => 0,
            Self::ProofOfSender => 0,
            Self::ProofOfapproval => 0,
            Self::ProofOfCreation => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
