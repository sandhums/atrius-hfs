// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Additional Binding Purpose Codes
//!
//! Additional Binding Purpose

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum AdditionalBindingPurposeCodes {
    Maximum,
    Minimum,
    Required,
    Extensible,
    Candidate,
    Current,
    Preferred,
    Ui,
    Starter,
    Component,
}

impl AdditionalBindingPurposeCodes {
    pub const URL: &'static str = "http://hl7.org/fhir/CodeSystem/additional-binding-purpose";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Maximum => "maximum",
            Self::Minimum => "minimum",
            Self::Required => "required",
            Self::Extensible => "extensible",
            Self::Candidate => "candidate",
            Self::Current => "current",
            Self::Preferred => "preferred",
            Self::Ui => "ui",
            Self::Starter => "starter",
            Self::Component => "component",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "maximum" => Self::Maximum,
            "minimum" => Self::Minimum,
            "required" => Self::Required,
            "extensible" => Self::Extensible,
            "candidate" => Self::Candidate,
            "current" => Self::Current,
            "preferred" => Self::Preferred,
            "ui" => Self::Ui,
            "starter" => Self::Starter,
            "component" => Self::Component,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Maximum => Some("Maximum Binding"),
            Self::Minimum => Some("Minimum Binding"),
            Self::Required => Some("Required Binding"),
            Self::Extensible => Some("Conformance Binding"),
            Self::Candidate => Some("Candidate Binding"),
            Self::Current => Some("Current Binding"),
            Self::Preferred => Some("Preferred Binding"),
            Self::Ui => Some("UI Suggested Binding"),
            Self::Starter => Some("Starter Binding"),
            Self::Component => Some("Component Binding"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Maximum => Some(
                "A required binding, for use when the binding strength is \'extensible\' or \'preferred\'",
            ),
            Self::Minimum => Some(
                "The minimum allowable value set - any conformant system SHALL support all these codes",
            ),
            Self::Required => Some(
                "This value set is used as a required binding (in addition to the base binding (not a replacement), usually in a particular usage context)",
            ),
            Self::Extensible => Some(
                "This value set is used as an extensible binding (in addition to the base binding (not a replacement), usually in a particular usage context)",
            ),
            Self::Candidate => Some(
                "This value set is a candidate to substitute for the overall conformance value set in some situations; usually these are defined in the documentation",
            ),
            Self::Current => Some(
                "New records are required to use this value set, but legacy records may use other codes. The definition of \'new record\' is difficult, since systems often create new records based on pre-existing data. Usually \'current\' bindings are mandated by an external authority that makes clear rules around this",
            ),
            Self::Preferred => Some(
                "This is the value set that is preferred in a given context (documentation should explain why)",
            ),
            Self::Ui => Some(
                "This value set is provided for user look up in a given context. Typically, these valuesets only include a subset of codes relevant for input in a context",
            ),
            Self::Starter => Some(
                "This value set is a good set of codes to start with when designing your system",
            ),
            Self::Component => Some(
                "This value set is a component of the base value set. Usually this is called out so that documentation can be written about a portion of the value set",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Maximum => None,
            Self::Minimum => None,
            Self::Required => None,
            Self::Extensible => None,
            Self::Candidate => None,
            Self::Current => None,
            Self::Preferred => None,
            Self::Ui => None,
            Self::Starter => None,
            Self::Component => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Maximum => 0,
            Self::Minimum => 0,
            Self::Required => 0,
            Self::Extensible => 0,
            Self::Candidate => 0,
            Self::Current => 0,
            Self::Preferred => 0,
            Self::Ui => 0,
            Self::Starter => 0,
            Self::Component => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
