// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Evidence Variable Handling
//!
//! The handling of the variable in statistical analysis for exposures or outcomes (E.g. Dichotomous, Continuous, Descriptive).

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum EvidenceVariableHandling {
    Continuous,
    Dichotomous,
    Ordinal,
    Polychotomous,
}

impl EvidenceVariableHandling {
    pub const URL: &'static str = "http://hl7.org/fhir/variable-handling";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Continuous => "continuous",
            Self::Dichotomous => "dichotomous",
            Self::Ordinal => "ordinal",
            Self::Polychotomous => "polychotomous",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "continuous" => Self::Continuous,
            "dichotomous" => Self::Dichotomous,
            "ordinal" => Self::Ordinal,
            "polychotomous" => Self::Polychotomous,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Continuous => Some("continuous variable"),
            Self::Dichotomous => Some("dichotomous variable"),
            Self::Ordinal => Some("ordinal variable"),
            Self::Polychotomous => Some("polychotomous variable"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Continuous => Some(
                "A continuous variable is one for which, within the limits the variable ranges, any value is possible (from STATO http://purl.obolibrary.org/obo/STATO_0000251).",
            ),
            Self::Dichotomous => Some(
                "A dichotomous variable is a categorical variable which is defined to have only 2 categories or possible values (from STATO http://purl.obolibrary.org/obo/STATO_0000090).",
            ),
            Self::Ordinal => Some(
                "An ordinal variable is a categorical variable where the discrete possible values are ordered or correspond to an implicit ranking (from STATO http://purl.obolibrary.org/obo/STATO_0000228).",
            ),
            Self::Polychotomous => Some(
                "A polychotomous variable is a categorical variable which is defined to have minimally 2 categories or possible values. (from STATO  http://purl.obolibrary.org/obo/STATO_0000087).  Suggestion to limit code use to situations when neither dichotomous nor ordinal variables apply.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Continuous => None,
            Self::Dichotomous => None,
            Self::Ordinal => None,
            Self::Polychotomous => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Continuous => 0,
            Self::Dichotomous => 0,
            Self::Ordinal => 0,
            Self::Polychotomous => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
