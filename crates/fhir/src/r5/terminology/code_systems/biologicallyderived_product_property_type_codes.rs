// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Biologically Derived Product Property Type Codes
//!
//! Biologically Derived Product Property Type Code.  This code system http://hl7.org/fhir/biologicallyderived-product-property-type-codes is a subset of the codes defined in http://terminology.hl7.org/CodeSystem/ib

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum BiologicallyDerivedProductPropertyTypeCodes {
    Collectiontype,
    Aborhd,
    Singleeuropeancode,
    Redcellantigen,
}

impl BiologicallyDerivedProductPropertyTypeCodes {
    pub const URL: &'static str =
        "http://hl7.org/fhir/biologicallyderived-product-property-type-codes";
    pub const VERSION: &'static str = "5.0.0";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "example";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Collectiontype => "collectiontype",
            Self::Aborhd => "aborhd",
            Self::Singleeuropeancode => "singleeuropeancode",
            Self::Redcellantigen => "redcellantigen",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "collectiontype" => Self::Collectiontype,
            "aborhd" => Self::Aborhd,
            "singleeuropeancode" => Self::Singleeuropeancode,
            "redcellantigen" => Self::Redcellantigen,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Collectiontype => Some("Collection Type"),
            Self::Aborhd => Some("ABORhD"),
            Self::Singleeuropeancode => Some("Single European Code"),
            Self::Redcellantigen => Some("Red Cell Antigen"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Collectiontype => Some("See https://www.isbt128.org/uri/CollectionType"),
            Self::Aborhd => Some("See https://www.isbt128.org/uri/ABORhD"),
            Self::Singleeuropeancode => Some("See https://www.isbt128.org/uri/SingleEuropeanCode"),
            Self::Redcellantigen => Some("See https://www.isbt128.org/uri/RedCellAntigen"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Collectiontype => None,
            Self::Aborhd => None,
            Self::Singleeuropeancode => None,
            Self::Redcellantigen => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Collectiontype => 0,
            Self::Aborhd => 0,
            Self::Singleeuropeancode => 0,
            Self::Redcellantigen => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
