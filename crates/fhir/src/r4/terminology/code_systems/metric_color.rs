// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! DeviceMetricColor
//!
//! Describes the typical color of representation.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceMetricColor {
    Black,
    Red,
    Green,
    Yellow,
    Blue,
    Magenta,
    Cyan,
    White,
}

impl DeviceMetricColor {
    pub const URL: &'static str = "http://hl7.org/fhir/metric-color";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Black => "black",
            Self::Red => "red",
            Self::Green => "green",
            Self::Yellow => "yellow",
            Self::Blue => "blue",
            Self::Magenta => "magenta",
            Self::Cyan => "cyan",
            Self::White => "white",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "black" => Self::Black,
            "red" => Self::Red,
            "green" => Self::Green,
            "yellow" => Self::Yellow,
            "blue" => Self::Blue,
            "magenta" => Self::Magenta,
            "cyan" => Self::Cyan,
            "white" => Self::White,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Black => Some("Color Black"),
            Self::Red => Some("Color Red"),
            Self::Green => Some("Color Green"),
            Self::Yellow => Some("Color Yellow"),
            Self::Blue => Some("Color Blue"),
            Self::Magenta => Some("Color Magenta"),
            Self::Cyan => Some("Color Cyan"),
            Self::White => Some("Color White"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Black => Some("Color for representation - black."),
            Self::Red => Some("Color for representation - red."),
            Self::Green => Some("Color for representation - green."),
            Self::Yellow => Some("Color for representation - yellow."),
            Self::Blue => Some("Color for representation - blue."),
            Self::Magenta => Some("Color for representation - magenta."),
            Self::Cyan => Some("Color for representation - cyan."),
            Self::White => Some("Color for representation - white."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Black => None,
            Self::Red => None,
            Self::Green => None,
            Self::Yellow => None,
            Self::Blue => None,
            Self::Magenta => None,
            Self::Cyan => None,
            Self::White => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Black => 0,
            Self::Red => 0,
            Self::Green => 0,
            Self::Yellow => 0,
            Self::Blue => 0,
            Self::Magenta => 0,
            Self::Cyan => 0,
            Self::White => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
