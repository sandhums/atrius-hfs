// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! RelatedArtifactType
//!
//! The type of relationship to the related artifact.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum RelatedArtifactType {
    Documentation,
    Justification,
    Citation,
    Predecessor,
    Successor,
    DerivedFrom,
    DependsOn,
    ComposedOf,
}

impl RelatedArtifactType {
    pub const URL: &'static str = "http://hl7.org/fhir/related-artifact-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Documentation => "documentation",
            Self::Justification => "justification",
            Self::Citation => "citation",
            Self::Predecessor => "predecessor",
            Self::Successor => "successor",
            Self::DerivedFrom => "derived-from",
            Self::DependsOn => "depends-on",
            Self::ComposedOf => "composed-of",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "documentation" => Self::Documentation,
            "justification" => Self::Justification,
            "citation" => Self::Citation,
            "predecessor" => Self::Predecessor,
            "successor" => Self::Successor,
            "derived-from" => Self::DerivedFrom,
            "depends-on" => Self::DependsOn,
            "composed-of" => Self::ComposedOf,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Documentation => Some("Documentation"),
            Self::Justification => Some("Justification"),
            Self::Citation => Some("Citation"),
            Self::Predecessor => Some("Predecessor"),
            Self::Successor => Some("Successor"),
            Self::DerivedFrom => Some("Derived From"),
            Self::DependsOn => Some("Depends On"),
            Self::ComposedOf => Some("Composed Of"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Documentation => Some(
                "Additional documentation for the knowledge resource. This would include additional instructions on usage as well as additional information on clinical context or appropriateness.",
            ),
            Self::Justification => Some(
                "A summary of the justification for the knowledge resource including supporting evidence, relevant guidelines, or other clinically important information. This information is intended to provide a way to make the justification for the knowledge resource available to the consumer of interventions or results produced by the knowledge resource.",
            ),
            Self::Citation => Some(
                "Bibliographic citation for papers, references, or other relevant material for the knowledge resource. This is intended to allow for citation of related material, but that was not necessarily specifically prepared in connection with this knowledge resource.",
            ),
            Self::Predecessor => Some("The previous version of the knowledge resource."),
            Self::Successor => Some("The next version of the knowledge resource."),
            Self::DerivedFrom => Some(
                "The knowledge resource is derived from the related artifact. This is intended to capture the relationship in which a particular knowledge resource is based on the content of another artifact, but is modified to capture either a different set of overall requirements, or a more specific set of requirements such as those involved in a particular institution or clinical setting.",
            ),
            Self::DependsOn => {
                Some("The knowledge resource depends on the given related artifact.")
            }
            Self::ComposedOf => {
                Some("The knowledge resource is composed of the given related artifact.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Documentation => None,
            Self::Justification => None,
            Self::Citation => None,
            Self::Predecessor => None,
            Self::Successor => None,
            Self::DerivedFrom => None,
            Self::DependsOn => None,
            Self::ComposedOf => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Documentation => 0,
            Self::Justification => 0,
            Self::Citation => 0,
            Self::Predecessor => 0,
            Self::Successor => 0,
            Self::DerivedFrom => 0,
            Self::DependsOn => 0,
            Self::ComposedOf => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
