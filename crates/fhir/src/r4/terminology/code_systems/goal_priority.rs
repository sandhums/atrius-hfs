// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Goal priority
//!
//! Indicates the level of importance associated with reaching or sustaining a goal.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum GoalPriority {
    HighPriority,
    MediumPriority,
    LowPriority,
}

impl GoalPriority {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/goal-priority";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::HighPriority => "high-priority",
            Self::MediumPriority => "medium-priority",
            Self::LowPriority => "low-priority",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "high-priority" => Self::HighPriority,
            "medium-priority" => Self::MediumPriority,
            "low-priority" => Self::LowPriority,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::HighPriority => Some("High Priority"),
            Self::MediumPriority => Some("Medium Priority"),
            Self::LowPriority => Some("Low Priority"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::HighPriority => Some(
                "Indicates that the goal is of considerable importance and should be a primary focus of care delivery.",
            ),
            Self::MediumPriority => Some(
                "Indicates that the goal has a reasonable degree of importance and that concrete action should be taken towards the goal.  Attainment is not as critical as high-priority goals.",
            ),
            Self::LowPriority => Some(
                "The goal is desirable but is not sufficiently important to devote significant resources to.  Achievement of the goal may be sought when incidental to achieving other goals.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::HighPriority => None,
            Self::MediumPriority => None,
            Self::LowPriority => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::HighPriority => 0,
            Self::MediumPriority => 0,
            Self::LowPriority => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
