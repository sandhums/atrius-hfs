// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! MatchGrade
//!
//! A Master Patient Index (MPI) assessment of whether a candidate patient record is a match or not.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MatchGrade {
    Certain,
    Probable,
    Possible,
    CertainlyNot,
}

impl MatchGrade {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/match-grade";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Certain => "certain",
            Self::Probable => "probable",
            Self::Possible => "possible",
            Self::CertainlyNot => "certainly-not",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "certain" => Self::Certain,
            "probable" => Self::Probable,
            "possible" => Self::Possible,
            "certainly-not" => Self::CertainlyNot,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Certain => Some("Certain Match"),
            Self::Probable => Some("Probable Match"),
            Self::Possible => Some("Possible Match"),
            Self::CertainlyNot => Some("Certainly Not a Match"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Certain => Some(
                "This record meets the matching criteria to be automatically considered as a full match.",
            ),
            Self::Probable => Some(
                "This record is a close match, but not a certain match. Additional review (e.g. by a human) may be required before using this as a match.",
            ),
            Self::Possible => Some(
                "This record may be a matching one. Additional review (e.g. by a human) SHOULD be performed before using this as a match.",
            ),
            Self::CertainlyNot => Some(
                "This record is known not to be a match. Note that usually non-matching records are not returned, but in some cases records previously or likely considered as a match may specifically be negated by the matching engine.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Certain => None,
            Self::Probable => None,
            Self::Possible => None,
            Self::CertainlyNot => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Certain => 0,
            Self::Probable => 0,
            Self::Possible => 0,
            Self::CertainlyNot => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
