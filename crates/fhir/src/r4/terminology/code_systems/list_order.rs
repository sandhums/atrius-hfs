// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! List Order Codes
//!
//! Base values for the order of the items in a list resource.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ListOrderCodes {
    User,
    System,
    EventDate,
    EntryDate,
    Priority,
    Alphabetic,
    Category,
    Patient,
}

impl ListOrderCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/list-order";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::User => "user",
            Self::System => "system",
            Self::EventDate => "event-date",
            Self::EntryDate => "entry-date",
            Self::Priority => "priority",
            Self::Alphabetic => "alphabetic",
            Self::Category => "category",
            Self::Patient => "patient",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "user" => Self::User,
            "system" => Self::System,
            "event-date" => Self::EventDate,
            "entry-date" => Self::EntryDate,
            "priority" => Self::Priority,
            "alphabetic" => Self::Alphabetic,
            "category" => Self::Category,
            "patient" => Self::Patient,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::User => Some("Sorted by User"),
            Self::System => Some("Sorted by System"),
            Self::EventDate => Some("Sorted by Event Date"),
            Self::EntryDate => Some("Sorted by Item Date"),
            Self::Priority => Some("Sorted by Priority"),
            Self::Alphabetic => Some("Sorted Alphabetically"),
            Self::Category => Some("Sorted by Category"),
            Self::Patient => Some("Sorted by Patient"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::User => {
                Some("The list was sorted by a user. The criteria the user used are not specified.")
            }
            Self::System => Some(
                "The list was sorted by the system. The criteria the user used are not specified; define additional codes to specify a particular order (or use other defined codes).",
            ),
            Self::EventDate => Some(
                "The list is sorted by the data of the event. This can be used when the list has items which are dates with past or future events.",
            ),
            Self::EntryDate => Some(
                "The list is sorted by the date the item was added to the list. Note that the date added to the list is not explicit in the list itself.",
            ),
            Self::Priority => Some(
                "The list is sorted by priority. The exact method in which priority has been determined is not specified.",
            ),
            Self::Alphabetic => Some(
                "The list is sorted alphabetically by an unspecified property of the items in the list.",
            ),
            Self::Category => Some(
                "The list is sorted categorically by an unspecified property of the items in the list.",
            ),
            Self::Patient => {
                Some("The list is sorted by patient, with items for each patient grouped together.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::User => None,
            Self::System => None,
            Self::EventDate => None,
            Self::EntryDate => None,
            Self::Priority => None,
            Self::Alphabetic => None,
            Self::Category => None,
            Self::Patient => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::User => 0,
            Self::System => 0,
            Self::EventDate => 0,
            Self::EntryDate => 0,
            Self::Priority => 0,
            Self::Alphabetic => 0,
            Self::Category => 0,
            Self::Patient => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
