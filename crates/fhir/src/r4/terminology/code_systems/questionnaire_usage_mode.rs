// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! QuestionnaireItemUsageMode
//!
//! Identifies the modes of usage of a questionnaire that should enable a particular questionnaire item.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum QuestionnaireItemUsageMode {
    CaptureDisplay,
    Capture,
    Display,
    DisplayNonEmpty,
    CaptureDisplayNonEmpty,
}

impl QuestionnaireItemUsageMode {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/questionnaire-usage-mode";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::CaptureDisplay => "capture-display",
            Self::Capture => "capture",
            Self::Display => "display",
            Self::DisplayNonEmpty => "display-non-empty",
            Self::CaptureDisplayNonEmpty => "capture-display-non-empty",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "capture-display" => Self::CaptureDisplay,
            "capture" => Self::Capture,
            "display" => Self::Display,
            "display-non-empty" => Self::DisplayNonEmpty,
            "capture-display-non-empty" => Self::CaptureDisplayNonEmpty,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::CaptureDisplay => Some("Capture & Display"),
            Self::Capture => Some("Capture Only"),
            Self::Display => Some("Display Only"),
            Self::DisplayNonEmpty => Some("Display when Answered"),
            Self::CaptureDisplayNonEmpty => Some("Capture or, if answered, Display"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::CaptureDisplay => Some("Render the item regardless of usage mode."),
            Self::Capture => Some("Render the item only when capturing data."),
            Self::Display => Some("Render the item only when displaying a completed form."),
            Self::DisplayNonEmpty => Some(
                "Render the item only when displaying a completed form and the item has been answered (or has child items that have been answered).",
            ),
            Self::CaptureDisplayNonEmpty => Some(
                "Render the item when capturing data or when displaying a completed form and the item has been answered (or has child items that have been answered).",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::CaptureDisplay => None,
            Self::Capture => None,
            Self::Display => None,
            Self::DisplayNonEmpty => None,
            Self::CaptureDisplayNonEmpty => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::CaptureDisplay => 0,
            Self::Capture => 0,
            Self::Display => 0,
            Self::DisplayNonEmpty => 0,
            Self::CaptureDisplayNonEmpty => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
