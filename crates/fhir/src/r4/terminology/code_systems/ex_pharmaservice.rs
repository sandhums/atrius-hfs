// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Example Pharmacy Service Codes
//!
//! This value set includes a smattering of Pharmacy Service codes.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ExamplePharmacyServiceCodes {
    Smokecess,
    Flushot,
    Drugcost,
    Markup,
    Dispensefee,
    Compoundfee,
}

impl ExamplePharmacyServiceCodes {
    pub const URL: &'static str = "http://hl7.org/fhir/ex-pharmaservice";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Smokecess => "smokecess",
            Self::Flushot => "flushot",
            Self::Drugcost => "drugcost",
            Self::Markup => "markup",
            Self::Dispensefee => "dispensefee",
            Self::Compoundfee => "compoundfee",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "smokecess" => Self::Smokecess,
            "flushot" => Self::Flushot,
            "drugcost" => Self::Drugcost,
            "markup" => Self::Markup,
            "dispensefee" => Self::Dispensefee,
            "compoundfee" => Self::Compoundfee,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Smokecess => Some("Smoking cessation"),
            Self::Flushot => Some("Flu Shot"),
            Self::Drugcost => Some("Drug Cost"),
            Self::Markup => Some("Markup"),
            Self::Dispensefee => Some("Dispense Fee"),
            Self::Compoundfee => Some("Compounding Fee"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Smokecess => Some("Smoking cessation"),
            Self::Flushot => Some("Flu Shot"),
            Self::Drugcost => Some("The wholesale price of the medication."),
            Self::Markup => Some("The additional cost assessed on the drug."),
            Self::Dispensefee => {
                Some("The professional fee charged for dispensing the product or service.")
            }
            Self::Compoundfee => {
                Some("The professional fee charged for compounding the medication.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Smokecess => None,
            Self::Flushot => None,
            Self::Drugcost => None,
            Self::Markup => None,
            Self::Dispensefee => None,
            Self::Compoundfee => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Smokecess => 0,
            Self::Flushot => 0,
            Self::Drugcost => 0,
            Self::Markup => 0,
            Self::Dispensefee => 0,
            Self::Compoundfee => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
