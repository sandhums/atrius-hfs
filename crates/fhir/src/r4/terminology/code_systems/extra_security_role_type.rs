// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! SecurityRoleType
//!
//! This CodeSystem contains Additional FHIR-defined Security Role types not defined elsewhere

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum SecurityRoleType {
    Authserver,
    Datacollector,
    Dataprocessor,
    Datasubject,
    Humanuser,
}

impl SecurityRoleType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/extra-security-role-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Authserver => "authserver",
            Self::Datacollector => "datacollector",
            Self::Dataprocessor => "dataprocessor",
            Self::Datasubject => "datasubject",
            Self::Humanuser => "humanuser",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "authserver" => Self::Authserver,
            "datacollector" => Self::Datacollector,
            "dataprocessor" => Self::Dataprocessor,
            "datasubject" => Self::Datasubject,
            "humanuser" => Self::Humanuser,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Authserver => Some("authorization server"),
            Self::Datacollector => Some("data collector"),
            Self::Dataprocessor => Some("data processor"),
            Self::Datasubject => Some("data subject"),
            Self::Humanuser => Some("human user"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Authserver => Some(
                "An entity providing authorization services to enable the electronic sharing of health-related information based on resource owner\'s preapproved permissions. For example, an UMA Authorization Server[UMA]",
            ),
            Self::Datacollector => Some(
                "An entity that collects information over which the data subject may have certain rights under policy or law to control that information\'s management and distribution by data collectors, including the right to access, retrieve, distribute, or delete that information. ",
            ),
            Self::Dataprocessor => Some(
                "An entity that processes collected information over which the data subject may have certain rights under policy or law to control that information\'s management and distribution by data processors, including the right to access, retrieve, distribute, or delete that information.",
            ),
            Self::Datasubject => Some(
                "A person whose personal information is collected or processed, and who may have certain rights under policy or law to control that information\'s management and distribution by data collectors or processors, including the right to access, retrieve, distribute, or delete that information.",
            ),
            Self::Humanuser => Some("The human user that has participated."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Authserver => None,
            Self::Datacollector => None,
            Self::Dataprocessor => None,
            Self::Datasubject => None,
            Self::Humanuser => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Authserver => 0,
            Self::Datacollector => 0,
            Self::Dataprocessor => 0,
            Self::Datasubject => 0,
            Self::Humanuser => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
