// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Example Message Reason Codes
//!
//! Example Message Reasons. These are the set of codes that might be used an updating an encounter using admin-update.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ExampleMessageReasonCodes {
    Admit,
    Discharge,
    Absent,
    Return,
    Moved,
    Edit,
}

impl ExampleMessageReasonCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/message-reasons-encounter";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Admit => "admit",
            Self::Discharge => "discharge",
            Self::Absent => "absent",
            Self::Return => "return",
            Self::Moved => "moved",
            Self::Edit => "edit",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "admit" => Self::Admit,
            "discharge" => Self::Discharge,
            "absent" => Self::Absent,
            "return" => Self::Return,
            "moved" => Self::Moved,
            "edit" => Self::Edit,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Admit => Some("Admit"),
            Self::Discharge => Some("Discharge"),
            Self::Absent => Some("Absent"),
            Self::Return => Some("Returned"),
            Self::Moved => Some("Moved"),
            Self::Edit => Some("Edit"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Admit => Some("The patient has been admitted."),
            Self::Discharge => Some("The patient has been discharged."),
            Self::Absent => Some("The patient has temporarily left the institution."),
            Self::Return => Some("The patient has returned from a temporary absence."),
            Self::Moved => Some("The patient has been moved to a new location."),
            Self::Edit => {
                Some("Encounter details have been updated (e.g. to correct a coding error).")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Admit => None,
            Self::Discharge => None,
            Self::Absent => None,
            Self::Return => None,
            Self::Moved => None,
            Self::Edit => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Admit => 0,
            Self::Discharge => 0,
            Self::Absent => 0,
            Self::Return => 0,
            Self::Moved => 0,
            Self::Edit => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
