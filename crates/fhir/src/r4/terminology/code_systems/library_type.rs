// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! LibraryType
//!
//! The type of knowledge asset this library contains.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum LibraryType {
    LogicLibrary,
    ModelDefinition,
    AssetCollection,
    ModuleDefinition,
}

impl LibraryType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/library-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::LogicLibrary => "logic-library",
            Self::ModelDefinition => "model-definition",
            Self::AssetCollection => "asset-collection",
            Self::ModuleDefinition => "module-definition",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "logic-library" => Self::LogicLibrary,
            "model-definition" => Self::ModelDefinition,
            "asset-collection" => Self::AssetCollection,
            "module-definition" => Self::ModuleDefinition,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::LogicLibrary => Some("Logic Library"),
            Self::ModelDefinition => Some("Model Definition"),
            Self::AssetCollection => Some("Asset Collection"),
            Self::ModuleDefinition => Some("Module Definition"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::LogicLibrary => {
                Some("The resource is a shareable library of formalized knowledge.")
            }
            Self::ModelDefinition => Some("The resource is a definition of an information model."),
            Self::AssetCollection => Some("The resource is a collection of knowledge assets."),
            Self::ModuleDefinition => Some(
                "The resource defines the dependencies, parameters, and data requirements for a particular module or evaluation context.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::LogicLibrary => None,
            Self::ModelDefinition => None,
            Self::AssetCollection => None,
            Self::ModuleDefinition => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::LogicLibrary => 0,
            Self::ModelDefinition => 0,
            Self::AssetCollection => 0,
            Self::ModuleDefinition => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
