// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Enteral Formula Additive Type Code
//!
//! EnteralFormulaAdditiveType: Codes for the type of modular component such as protein, carbohydrate or fiber to be provided in addition to or mixed with the base formula. This value set is provided as a suggestive example.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum EnteralFormulaAdditiveTypeCode {
    Lipid,
    Protein,
    Carbohydrate,
    Fiber,
    Water,
}

impl EnteralFormulaAdditiveTypeCode {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/entformula-additive";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Lipid => "lipid",
            Self::Protein => "protein",
            Self::Carbohydrate => "carbohydrate",
            Self::Fiber => "fiber",
            Self::Water => "water",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "lipid" => Self::Lipid,
            "protein" => Self::Protein,
            "carbohydrate" => Self::Carbohydrate,
            "fiber" => Self::Fiber,
            "water" => Self::Water,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Lipid => Some("Lipid"),
            Self::Protein => Some("Protein"),
            Self::Carbohydrate => Some("Carbohydrate"),
            Self::Fiber => Some("Fiber"),
            Self::Water => Some("Water"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Lipid => Some("Modular lipid enteral formula component"),
            Self::Protein => Some("Modular protein enteral formula component"),
            Self::Carbohydrate => Some("Modular carbohydrate enteral formula component"),
            Self::Fiber => Some("Modular fiber enteral formula component"),
            Self::Water => Some("Added water"),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Lipid => None,
            Self::Protein => None,
            Self::Carbohydrate => None,
            Self::Fiber => None,
            Self::Water => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Lipid => 0,
            Self::Protein => 0,
            Self::Carbohydrate => 0,
            Self::Fiber => 0,
            Self::Water => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
