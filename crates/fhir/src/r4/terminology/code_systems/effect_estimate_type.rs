// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! EffectEstimateType
//!
//! Whether the effect estimate is an absolute effect estimate (absolute difference) or a relative effect estimate (relative difference), and the specific type of effect estimate (eg relative risk or median difference).

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum EffectEstimateType {
    RelativeRR,
    RelativeOR,
    RelativeHR,
    AbsoluteARD,
    AbsoluteMeanDiff,
    AbsoluteSMD,
    AbsoluteMedianDiff,
}

impl EffectEstimateType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/effect-estimate-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::RelativeRR => "relative-RR",
            Self::RelativeOR => "relative-OR",
            Self::RelativeHR => "relative-HR",
            Self::AbsoluteARD => "absolute-ARD",
            Self::AbsoluteMeanDiff => "absolute-MeanDiff",
            Self::AbsoluteSMD => "absolute-SMD",
            Self::AbsoluteMedianDiff => "absolute-MedianDiff",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "relative-RR" => Self::RelativeRR,
            "relative-OR" => Self::RelativeOR,
            "relative-HR" => Self::RelativeHR,
            "absolute-ARD" => Self::AbsoluteARD,
            "absolute-MeanDiff" => Self::AbsoluteMeanDiff,
            "absolute-SMD" => Self::AbsoluteSMD,
            "absolute-MedianDiff" => Self::AbsoluteMedianDiff,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::RelativeRR => Some("relative risk"),
            Self::RelativeOR => Some("odds ratio"),
            Self::RelativeHR => Some("hazard ratio"),
            Self::AbsoluteARD => Some("absolute risk difference"),
            Self::AbsoluteMeanDiff => Some("mean difference"),
            Self::AbsoluteSMD => Some("standardized mean difference"),
            Self::AbsoluteMedianDiff => Some("median difference"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::RelativeRR => Some("relative risk (a type of relative effect estimate)."),
            Self::RelativeOR => Some("odds ratio (a type of relative effect estimate)."),
            Self::RelativeHR => Some("hazard ratio (a type of relative effect estimate)."),
            Self::AbsoluteARD => {
                Some("absolute risk difference (a type of absolute effect estimate).")
            }
            Self::AbsoluteMeanDiff => Some("mean difference (a type of absolute effect estimate)."),
            Self::AbsoluteSMD => {
                Some("standardized mean difference (a type of absolute effect estimate).")
            }
            Self::AbsoluteMedianDiff => {
                Some("median difference (a type of absolute effect estimate).")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::RelativeRR => None,
            Self::RelativeOR => None,
            Self::RelativeHR => None,
            Self::AbsoluteARD => None,
            Self::AbsoluteMeanDiff => None,
            Self::AbsoluteSMD => None,
            Self::AbsoluteMedianDiff => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::RelativeRR => 0,
            Self::RelativeOR => 0,
            Self::RelativeHR => 0,
            Self::AbsoluteARD => 0,
            Self::AbsoluteMeanDiff => 0,
            Self::AbsoluteSMD => 0,
            Self::AbsoluteMedianDiff => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
