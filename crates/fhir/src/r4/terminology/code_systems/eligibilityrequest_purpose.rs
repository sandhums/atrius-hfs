// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! EligibilityRequestPurpose
//!
//! A code specifying the types of information being requested.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum EligibilityRequestPurpose {
    AuthRequirements,
    Benefits,
    Discovery,
    Validation,
}

impl EligibilityRequestPurpose {
    pub const URL: &'static str = "http://hl7.org/fhir/eligibilityrequest-purpose";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::AuthRequirements => "auth-requirements",
            Self::Benefits => "benefits",
            Self::Discovery => "discovery",
            Self::Validation => "validation",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "auth-requirements" => Self::AuthRequirements,
            "benefits" => Self::Benefits,
            "discovery" => Self::Discovery,
            "validation" => Self::Validation,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::AuthRequirements => Some("Coverage auth-requirements"),
            Self::Benefits => Some("Coverage benefits"),
            Self::Discovery => Some("Coverage Discovery"),
            Self::Validation => Some("Coverage Validation"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::AuthRequirements => Some(
                "The prior authorization requirements for the listed, or discovered if specified, converages for the categories of service and/or specifed biling codes are requested.",
            ),
            Self::Benefits => Some(
                "The plan benefits and optionally benefits consumed  for the listed, or discovered if specified, converages are requested.",
            ),
            Self::Discovery => Some(
                "The insurer is requested to report on any coverages which they are aware of in addition to any specifed.",
            ),
            Self::Validation => {
                Some("A check that the specified coverages are in-force is requested.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::AuthRequirements => None,
            Self::Benefits => None,
            Self::Discovery => None,
            Self::Validation => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::AuthRequirements => 0,
            Self::Benefits => 0,
            Self::Discovery => 0,
            Self::Validation => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
