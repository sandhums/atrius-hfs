// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Financial Task Input Type Codes
//!
//! This value set includes Financial Task Input Type codes.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum FinancialTaskInputTypeCodes {
    Include,
    Exclude,
    Origresponse,
    Reference,
    Item,
    Period,
    Status,
}

impl FinancialTaskInputTypeCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/financialtaskinputtype";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Include => "include",
            Self::Exclude => "exclude",
            Self::Origresponse => "origresponse",
            Self::Reference => "reference",
            Self::Item => "item",
            Self::Period => "period",
            Self::Status => "status",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "include" => Self::Include,
            "exclude" => Self::Exclude,
            "origresponse" => Self::Origresponse,
            "reference" => Self::Reference,
            "item" => Self::Item,
            "period" => Self::Period,
            "status" => Self::Status,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Include => Some("Include"),
            Self::Exclude => Some("Exclude"),
            Self::Origresponse => Some("Original Response"),
            Self::Reference => Some("Reference Number"),
            Self::Item => Some("Item Number"),
            Self::Period => Some("Period"),
            Self::Status => Some("Status code"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Include => Some("The name of a resource to include in a selection."),
            Self::Exclude => Some("The name of a resource to not include in a selection."),
            Self::Origresponse => Some(
                "A reference to the response resource to the original processing of a resource.",
            ),
            Self::Reference => {
                Some("A reference value which must be quoted to authorize an action.")
            }
            Self::Item => Some("The sequence number associated with an item for reprocessing."),
            Self::Period => Some("The reference period for the action being requested."),
            Self::Status => Some(
                "The processing status from a check on the processing status of a resource such as the adjudication of a claim.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Include => None,
            Self::Exclude => None,
            Self::Origresponse => None,
            Self::Reference => None,
            Self::Item => None,
            Self::Period => None,
            Self::Status => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Include => 0,
            Self::Exclude => 0,
            Self::Origresponse => 0,
            Self::Reference => 0,
            Self::Item => 0,
            Self::Period => 0,
            Self::Status => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
