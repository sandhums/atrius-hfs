// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! RiskEstimateType
//!
//! Whether the risk estimate is dichotomous, continuous or qualitative and the specific type of risk estimate (eg proportion or median).

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum RiskEstimateType {
    Proportion,
    DerivedProportion,
    Mean,
    Median,
    Count,
    Descriptive,
}

impl RiskEstimateType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/risk-estimate-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Proportion => "proportion",
            Self::DerivedProportion => "derivedProportion",
            Self::Mean => "mean",
            Self::Median => "median",
            Self::Count => "count",
            Self::Descriptive => "descriptive",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "proportion" => Self::Proportion,
            "derivedProportion" => Self::DerivedProportion,
            "mean" => Self::Mean,
            "median" => Self::Median,
            "count" => Self::Count,
            "descriptive" => Self::Descriptive,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Proportion => Some("proportion"),
            Self::DerivedProportion => Some("derivedProportion"),
            Self::Mean => Some("mean"),
            Self::Median => Some("median"),
            Self::Count => Some("count"),
            Self::Descriptive => Some("descriptive"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Proportion => Some(
                "dichotomous measure (present or absent) reported as a ratio compared to the denominator of 1 (A percentage is a proportion with denominator of 100).",
            ),
            Self::DerivedProportion => Some(
                "A special use case where the proportion is derived from a formula rather than derived from summary evidence.",
            ),
            Self::Mean => Some("continuous numerical measure reported as an average."),
            Self::Median => {
                Some("continuous numerical measure reported as the middle of the range.")
            }
            Self::Count => Some("descriptive measure reported as total number of items."),
            Self::Descriptive => Some("descriptive measure reported as narrative."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Proportion => None,
            Self::DerivedProportion => None,
            Self::Mean => None,
            Self::Median => None,
            Self::Count => None,
            Self::Descriptive => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Proportion => 0,
            Self::DerivedProportion => 0,
            Self::Mean => 0,
            Self::Median => 0,
            Self::Count => 0,
            Self::Descriptive => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
