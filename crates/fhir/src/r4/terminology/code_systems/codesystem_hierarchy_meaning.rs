// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! CodeSystemHierarchyMeaning
//!
//! The meaning of the hierarchy of concepts in a code system.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum CodeSystemHierarchyMeaning {
    GroupedBy,
    IsA,
    PartOf,
    ClassifiedWith,
}

impl CodeSystemHierarchyMeaning {
    pub const URL: &'static str = "http://hl7.org/fhir/codesystem-hierarchy-meaning";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::GroupedBy => "grouped-by",
            Self::IsA => "is-a",
            Self::PartOf => "part-of",
            Self::ClassifiedWith => "classified-with",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "grouped-by" => Self::GroupedBy,
            "is-a" => Self::IsA,
            "part-of" => Self::PartOf,
            "classified-with" => Self::ClassifiedWith,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::GroupedBy => Some("Grouped By"),
            Self::IsA => Some("Is-A"),
            Self::PartOf => Some("Part Of"),
            Self::ClassifiedWith => Some("Classified With"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::GroupedBy => Some(
                "No particular relationship between the concepts can be assumed, except what can be determined by inspection of the definitions of the elements (possible reasons to use this: importing from a source where this is not defined, or where various parts of the hierarchy have different meanings).",
            ),
            Self::IsA => Some(
                "A hierarchy where the child concepts have an IS-A relationship with the parents - that is, all the properties of the parent are also true for its child concepts. Not that is-a is a property of the concepts, so additional subsumption relationships may be defined using properties or the [subsumes](extension-codesystem-subsumes.html) extension.",
            ),
            Self::PartOf => Some(
                "Child elements list the individual parts of a composite whole (e.g. body site).",
            ),
            Self::ClassifiedWith => Some(
                "Child concepts in the hierarchy may have only one parent, and there is a presumption that the code system is a \"closed world\" meaning all things must be in the hierarchy. This results in concepts such as \"not otherwise classified.\".",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::GroupedBy => None,
            Self::IsA => None,
            Self::PartOf => None,
            Self::ClassifiedWith => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::GroupedBy => 0,
            Self::IsA => 0,
            Self::PartOf => 0,
            Self::ClassifiedWith => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
