// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Risk Probability
//!
//! Codes representing the likelihood of a particular outcome in a risk assessment.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum RiskProbability {
    Negligible,
    Low,
    Moderate,
    High,
    Certain,
}

impl RiskProbability {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/risk-probability";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Negligible => "negligible",
            Self::Low => "low",
            Self::Moderate => "moderate",
            Self::High => "high",
            Self::Certain => "certain",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "negligible" => Self::Negligible,
            "low" => Self::Low,
            "moderate" => Self::Moderate,
            "high" => Self::High,
            "certain" => Self::Certain,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Negligible => Some("Negligible likelihood"),
            Self::Low => Some("Low likelihood"),
            Self::Moderate => Some("Moderate likelihood"),
            Self::High => Some("High likelihood"),
            Self::Certain => Some("Certain"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Negligible => Some("The specified outcome is exceptionally unlikely."),
            Self::Low => Some("The specified outcome is possible but unlikely."),
            Self::Moderate => {
                Some("The specified outcome has a reasonable likelihood of occurrence.")
            }
            Self::High => Some("The specified outcome is more likely to occur than not."),
            Self::Certain => Some("The specified outcome is effectively guaranteed."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Negligible => None,
            Self::Low => None,
            Self::Moderate => None,
            Self::High => None,
            Self::Certain => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Negligible => 0,
            Self::Low => 0,
            Self::Moderate => 0,
            Self::High => 0,
            Self::Certain => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
