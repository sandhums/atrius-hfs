// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! ResearchStudyPrimaryPurposeType
//!
//! Codes for the main intent of the study.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ResearchStudyPrimaryPurposeType {
    Treatment,
    Prevention,
    Diagnostic,
    SupportiveCare,
    Screening,
    HealthServicesResearch,
    BasicScience,
    DeviceFeasibility,
}

impl ResearchStudyPrimaryPurposeType {
    pub const URL: &'static str =
        "http://terminology.hl7.org/CodeSystem/research-study-prim-purp-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Treatment => "treatment",
            Self::Prevention => "prevention",
            Self::Diagnostic => "diagnostic",
            Self::SupportiveCare => "supportive-care",
            Self::Screening => "screening",
            Self::HealthServicesResearch => "health-services-research",
            Self::BasicScience => "basic-science",
            Self::DeviceFeasibility => "device-feasibility",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "treatment" => Self::Treatment,
            "prevention" => Self::Prevention,
            "diagnostic" => Self::Diagnostic,
            "supportive-care" => Self::SupportiveCare,
            "screening" => Self::Screening,
            "health-services-research" => Self::HealthServicesResearch,
            "basic-science" => Self::BasicScience,
            "device-feasibility" => Self::DeviceFeasibility,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Treatment => Some("Treatment"),
            Self::Prevention => Some("Prevention"),
            Self::Diagnostic => Some("Diagnostic"),
            Self::SupportiveCare => Some("Supportive Care"),
            Self::Screening => Some("Screening"),
            Self::HealthServicesResearch => Some("Health Services Research"),
            Self::BasicScience => Some("Basic Science"),
            Self::DeviceFeasibility => Some("Device Feasibility"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Treatment => Some(
                "One or more interventions are being evaluated for treating a disease, syndrome, or condition.",
            ),
            Self::Prevention => Some(
                "One or more interventions are being assessed for preventing the development of a specific disease or health condition.",
            ),
            Self::Diagnostic => Some(
                "One or more interventions are being evaluated for identifying a disease or health condition.",
            ),
            Self::SupportiveCare => Some(
                "One or more interventions are evaluated for maximizing comfort, minimizing side effects, or mitigating against a decline in the participant\'s health or function.",
            ),
            Self::Screening => Some(
                "One or more interventions are assessed or examined for identifying a condition, or risk factors for a condition, in people who are not yet known to have the condition or risk factor.",
            ),
            Self::HealthServicesResearch => Some(
                "One or more interventions for evaluating the delivery, processes, management, organization, or financing of healthcare.",
            ),
            Self::BasicScience => Some(
                "One or more interventions for examining the basic mechanism of action (for example, physiology or biomechanics of an intervention).",
            ),
            Self::DeviceFeasibility => Some(
                "An intervention of a device product is being evaluated to determine the feasibility of the product or to test a prototype device and not health outcomes. Such studies are conducted to confirm the design and operating specifications of a device before beginning a full clinical trial.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Treatment => None,
            Self::Prevention => None,
            Self::Diagnostic => None,
            Self::SupportiveCare => None,
            Self::Screening => None,
            Self::HealthServicesResearch => None,
            Self::BasicScience => None,
            Self::DeviceFeasibility => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Treatment => 0,
            Self::Prevention => 0,
            Self::Diagnostic => 0,
            Self::SupportiveCare => 0,
            Self::Screening => 0,
            Self::HealthServicesResearch => 0,
            Self::BasicScience => 0,
            Self::DeviceFeasibility => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
