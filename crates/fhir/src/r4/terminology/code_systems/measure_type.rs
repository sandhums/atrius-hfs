// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! MeasureType
//!
//! The type of measure (includes codes from 2.16.840.1.113883.1.11.20368).

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MeasureType {
    Process,
    Outcome,
    Structure,
    PatientReportedOutcome,
    Composite,
}

impl MeasureType {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/measure-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Process => "process",
            Self::Outcome => "outcome",
            Self::Structure => "structure",
            Self::PatientReportedOutcome => "patient-reported-outcome",
            Self::Composite => "composite",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "process" => Self::Process,
            "outcome" => Self::Outcome,
            "structure" => Self::Structure,
            "patient-reported-outcome" => Self::PatientReportedOutcome,
            "composite" => Self::Composite,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Process => Some("Process"),
            Self::Outcome => Some("Outcome"),
            Self::Structure => Some("Structure"),
            Self::PatientReportedOutcome => Some("Patient Reported Outcome"),
            Self::Composite => Some("Composite"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Process => Some(
                "A measure which focuses on a process which leads to a certain outcome, meaning that a scientific basis exists for believing that the process, when executed well, will increase the probability of achieving a desired outcome.",
            ),
            Self::Outcome => Some(
                "A measure that indicates the result of the performance (or non-performance) of a function or process.",
            ),
            Self::Structure => Some(
                "A measure that focuses on a health care provider\'s capacity, systems, and processes to provide high-quality care.",
            ),
            Self::PatientReportedOutcome => Some(
                "A measure that focuses on patient-reported information such as patient engagement or patient experience measures.",
            ),
            Self::Composite => Some(
                "A measure that combines multiple component measures in to a single quality measure.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Process => None,
            Self::Outcome => None,
            Self::Structure => None,
            Self::PatientReportedOutcome => None,
            Self::Composite => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Process => 0,
            Self::Outcome => 0,
            Self::Structure => 0,
            Self::PatientReportedOutcome => 0,
            Self::Composite => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
