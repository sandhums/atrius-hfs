// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Substance Category Codes
//!
//! Substance category codes

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum SubstanceCategoryCodes {
    Allergen,
    Biological,
    Body,
    Chemical,
    Food,
    Drug,
    Material,
}

impl SubstanceCategoryCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/substance-category";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Allergen => "allergen",
            Self::Biological => "biological",
            Self::Body => "body",
            Self::Chemical => "chemical",
            Self::Food => "food",
            Self::Drug => "drug",
            Self::Material => "material",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "allergen" => Self::Allergen,
            "biological" => Self::Biological,
            "body" => Self::Body,
            "chemical" => Self::Chemical,
            "food" => Self::Food,
            "drug" => Self::Drug,
            "material" => Self::Material,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Allergen => Some("Allergen"),
            Self::Biological => Some("Biological Substance"),
            Self::Body => Some("Body Substance"),
            Self::Chemical => Some("Chemical"),
            Self::Food => Some("Dietary Substance"),
            Self::Drug => Some("Drug or Medicament"),
            Self::Material => Some("Material"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Allergen => Some("A substance that causes an allergic reaction."),
            Self::Biological => {
                Some("A substance that is produced by or extracted from a biological source.")
            }
            Self::Body => Some(
                "A substance that comes directly from a human or an animal (e.g. blood, urine, feces, tears, etc.).",
            ),
            Self::Chemical => Some(
                "Any organic or inorganic substance of a particular molecular identity, including -- (i) any combination of such substances occurring in whole or in part as a result of a chemical reaction or occurring in nature and (ii) any element or uncombined radical (http://www.epa.gov/opptintr/import-export/pubs/importguide.pdf).",
            ),
            Self::Food => {
                Some("A food, dietary ingredient, or dietary supplement for human or animal.")
            }
            Self::Drug => Some(
                "A substance intended for use in the diagnosis, cure, mitigation, treatment, or prevention of disease in man or other animals (Federal Food Drug and Cosmetic Act).",
            ),
            Self::Material => Some(
                "A finished product which is not normally ingested, absorbed or injected (e.g. steel, iron, wood, plastic and paper).",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Allergen => None,
            Self::Biological => None,
            Self::Body => None,
            Self::Chemical => None,
            Self::Food => None,
            Self::Drug => None,
            Self::Material => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Allergen => 0,
            Self::Biological => 0,
            Self::Body => 0,
            Self::Chemical => 0,
            Self::Food => 0,
            Self::Drug => 0,
            Self::Material => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
