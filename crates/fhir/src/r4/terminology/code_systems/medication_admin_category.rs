// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Medication administration  category  codes
//!
//! MedicationAdministration Category Codes

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MedicationAdministrationCategoryCodes {
    Inpatient,
    Outpatient,
    Community,
}

impl MedicationAdministrationCategoryCodes {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/medication-admin-category";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Inpatient => "inpatient",
            Self::Outpatient => "outpatient",
            Self::Community => "community",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "inpatient" => Self::Inpatient,
            "outpatient" => Self::Outpatient,
            "community" => Self::Community,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Inpatient => Some("Inpatient"),
            Self::Outpatient => Some("Outpatient"),
            Self::Community => Some("Community"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Inpatient => {
                Some("Includes administrations in an inpatient or acute care setting")
            }
            Self::Outpatient => Some(
                "Includes administrations in an outpatient setting (for example, Emergency Department, Outpatient Clinic, Outpatient Surgery, Doctor\'s office)",
            ),
            Self::Community => Some(
                "Includes administrations by the patient in their home (this would include long term care or nursing homes, hospices, etc.)",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Inpatient => None,
            Self::Outpatient => None,
            Self::Community => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Inpatient => 0,
            Self::Outpatient => 0,
            Self::Community => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
