// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! DeviceMetricCalibrationState
//!
//! Describes the state of a metric calibration.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceMetricCalibrationState {
    NotCalibrated,
    CalibrationRequired,
    Calibrated,
    Unspecified,
}

impl DeviceMetricCalibrationState {
    pub const URL: &'static str = "http://hl7.org/fhir/metric-calibration-state";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::NotCalibrated => "not-calibrated",
            Self::CalibrationRequired => "calibration-required",
            Self::Calibrated => "calibrated",
            Self::Unspecified => "unspecified",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "not-calibrated" => Self::NotCalibrated,
            "calibration-required" => Self::CalibrationRequired,
            "calibrated" => Self::Calibrated,
            "unspecified" => Self::Unspecified,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::NotCalibrated => Some("Not Calibrated"),
            Self::CalibrationRequired => Some("Calibration Required"),
            Self::Calibrated => Some("Calibrated"),
            Self::Unspecified => Some("Unspecified"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::NotCalibrated => Some("The metric has not been calibrated."),
            Self::CalibrationRequired => Some("The metric needs to be calibrated."),
            Self::Calibrated => Some("The metric has been calibrated."),
            Self::Unspecified => Some("The state of calibration of this metric is unspecified."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::NotCalibrated => None,
            Self::CalibrationRequired => None,
            Self::Calibrated => None,
            Self::Unspecified => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::NotCalibrated => 0,
            Self::CalibrationRequired => 0,
            Self::Calibrated => 0,
            Self::Unspecified => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
