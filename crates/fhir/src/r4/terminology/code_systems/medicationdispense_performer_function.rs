// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Medication dispense  performer  function  codes
//!
//! MedicationDispense Performer Function Codes

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MedicationDispensePerformerFunctionCodes {
    Dataenterer,
    Packager,
    Checker,
    Finalchecker,
}

impl MedicationDispensePerformerFunctionCodes {
    pub const URL: &'static str =
        "http://terminology.hl7.org/CodeSystem/medicationdispense-performer-function";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Dataenterer => "dataenterer",
            Self::Packager => "packager",
            Self::Checker => "checker",
            Self::Finalchecker => "finalchecker",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "dataenterer" => Self::Dataenterer,
            "packager" => Self::Packager,
            "checker" => Self::Checker,
            "finalchecker" => Self::Finalchecker,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Dataenterer => Some("Data Enterer"),
            Self::Packager => Some("Packager"),
            Self::Checker => Some("Checker"),
            Self::Finalchecker => Some("Final Checker"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Dataenterer => Some("Recorded the details of the request"),
            Self::Packager => Some("Prepared the medication."),
            Self::Checker => Some("Performed initial quality assurance on the prepared medication"),
            Self::Finalchecker => Some(
                "Performed the final quality assurance on the prepared medication against the request. Typically, this is a pharmacist function.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Dataenterer => None,
            Self::Packager => None,
            Self::Checker => None,
            Self::Finalchecker => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Dataenterer => 0,
            Self::Packager => 0,
            Self::Checker => 0,
            Self::Finalchecker => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
