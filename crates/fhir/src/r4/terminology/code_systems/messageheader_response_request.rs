// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! messageheader-response-request
//!
//! HL7-defined table of codes which identify conditions under which acknowledgments are required to be returned in response to a message.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MessageheaderResponseRequest {
    Always,
    OnError,
    Never,
    OnSuccess,
}

impl MessageheaderResponseRequest {
    pub const URL: &'static str = "http://hl7.org/fhir/messageheader-response-request";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Always => "always",
            Self::OnError => "on-error",
            Self::Never => "never",
            Self::OnSuccess => "on-success",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "always" => Self::Always,
            "on-error" => Self::OnError,
            "never" => Self::Never,
            "on-success" => Self::OnSuccess,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Always => Some("Always"),
            Self::OnError => Some("Error/reject conditions only"),
            Self::Never => Some("Never"),
            Self::OnSuccess => Some("Successful completion only"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Always => Some("initiator expects a response for this message."),
            Self::OnError => Some("initiator expects a response only if in error."),
            Self::Never => Some("initiator does not expect a response."),
            Self::OnSuccess => Some("initiator expects a response only if successful."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Always => None,
            Self::OnError => None,
            Self::Never => None,
            Self::OnSuccess => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Always => 0,
            Self::OnError => 0,
            Self::Never => 0,
            Self::OnSuccess => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
