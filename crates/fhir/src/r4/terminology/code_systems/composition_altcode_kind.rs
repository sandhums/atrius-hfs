// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! AlternativeCodeKind
//!
//! Indicates the type of use for which the code is defined.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum AlternativeCodeKind {
    Alternate,
    Deprecated,
    CaseInsensitive,
    CaseSensitive,
    Expression,
}

impl AlternativeCodeKind {
    pub const URL: &'static str = "http://terminology.hl7.org/CodeSystem/composition-altcode-kind";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Alternate => "alternate",
            Self::Deprecated => "deprecated",
            Self::CaseInsensitive => "case-insensitive",
            Self::CaseSensitive => "case-sensitive",
            Self::Expression => "expression",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "alternate" => Self::Alternate,
            "deprecated" => Self::Deprecated,
            "case-insensitive" => Self::CaseInsensitive,
            "case-sensitive" => Self::CaseSensitive,
            "expression" => Self::Expression,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Alternate => Some("Alternate Code"),
            Self::Deprecated => Some("Deprecated"),
            Self::CaseInsensitive => Some("Case Insensitive"),
            Self::CaseSensitive => Some("Case Sensitive"),
            Self::Expression => Some("Expression"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Alternate => Some(
                "The code is an alternative code that can be used in any of the circumstances that the primary code can be used.",
            ),
            Self::Deprecated => {
                Some("The code should no longer be used, but was used in the past.")
            }
            Self::CaseInsensitive => Some(
                "The code is an alternative to be used when a case insensitive code is required (when the primary codes are case sensitive).",
            ),
            Self::CaseSensitive => Some(
                "The code is an alternative to be used when a case sensitive code is required (when the primary codes are case insensitive).",
            ),
            Self::Expression => Some(
                "The code is an alternative for the primary code that is built using the expression grammar defined by the code system.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Alternate => None,
            Self::Deprecated => None,
            Self::CaseInsensitive => None,
            Self::CaseSensitive => None,
            Self::Expression => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Alternate => 0,
            Self::Deprecated => 0,
            Self::CaseInsensitive => 0,
            Self::CaseSensitive => 0,
            Self::Expression => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
