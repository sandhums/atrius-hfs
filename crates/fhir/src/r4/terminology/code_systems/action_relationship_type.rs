// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! ActionRelationshipType
//!
//! Defines the types of relationships between actions.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum ActionRelationshipType {
    BeforeStart,
    Before,
    BeforeEnd,
    ConcurrentWithStart,
    Concurrent,
    ConcurrentWithEnd,
    AfterStart,
    After,
    AfterEnd,
}

impl ActionRelationshipType {
    pub const URL: &'static str = "http://hl7.org/fhir/action-relationship-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::BeforeStart => "before-start",
            Self::Before => "before",
            Self::BeforeEnd => "before-end",
            Self::ConcurrentWithStart => "concurrent-with-start",
            Self::Concurrent => "concurrent",
            Self::ConcurrentWithEnd => "concurrent-with-end",
            Self::AfterStart => "after-start",
            Self::After => "after",
            Self::AfterEnd => "after-end",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "before-start" => Self::BeforeStart,
            "before" => Self::Before,
            "before-end" => Self::BeforeEnd,
            "concurrent-with-start" => Self::ConcurrentWithStart,
            "concurrent" => Self::Concurrent,
            "concurrent-with-end" => Self::ConcurrentWithEnd,
            "after-start" => Self::AfterStart,
            "after" => Self::After,
            "after-end" => Self::AfterEnd,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::BeforeStart => Some("Before Start"),
            Self::Before => Some("Before"),
            Self::BeforeEnd => Some("Before End"),
            Self::ConcurrentWithStart => Some("Concurrent With Start"),
            Self::Concurrent => Some("Concurrent"),
            Self::ConcurrentWithEnd => Some("Concurrent With End"),
            Self::AfterStart => Some("After Start"),
            Self::After => Some("After"),
            Self::AfterEnd => Some("After End"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::BeforeStart => {
                Some("The action must be performed before the start of the related action.")
            }
            Self::Before => Some("The action must be performed before the related action."),
            Self::BeforeEnd => {
                Some("The action must be performed before the end of the related action.")
            }
            Self::ConcurrentWithStart => Some(
                "The action must be performed concurrent with the start of the related action.",
            ),
            Self::Concurrent => {
                Some("The action must be performed concurrent with the related action.")
            }
            Self::ConcurrentWithEnd => {
                Some("The action must be performed concurrent with the end of the related action.")
            }
            Self::AfterStart => {
                Some("The action must be performed after the start of the related action.")
            }
            Self::After => Some("The action must be performed after the related action."),
            Self::AfterEnd => {
                Some("The action must be performed after the end of the related action.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::BeforeStart => None,
            Self::Before => None,
            Self::BeforeEnd => None,
            Self::ConcurrentWithStart => None,
            Self::Concurrent => None,
            Self::ConcurrentWithEnd => None,
            Self::AfterStart => None,
            Self::After => None,
            Self::AfterEnd => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::BeforeStart => 0,
            Self::Before => 0,
            Self::BeforeEnd => 0,
            Self::ConcurrentWithStart => 0,
            Self::Concurrent => 0,
            Self::ConcurrentWithEnd => 0,
            Self::AfterStart => 0,
            Self::After => 0,
            Self::AfterEnd => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
