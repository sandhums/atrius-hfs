// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! TriggerType
//!
//! The type of trigger.

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum TriggerType {
    NamedEvent,
    Periodic,
    DataChanged,
    DataAdded,
    DataModified,
    DataRemoved,
    DataAccessed,
    DataAccessEnded,
}

impl TriggerType {
    pub const URL: &'static str = "http://hl7.org/fhir/trigger-type";
    pub const VERSION: &'static str = "4.0.1";
    pub const STATUS: &'static str = "draft";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::NamedEvent => "named-event",
            Self::Periodic => "periodic",
            Self::DataChanged => "data-changed",
            Self::DataAdded => "data-added",
            Self::DataModified => "data-modified",
            Self::DataRemoved => "data-removed",
            Self::DataAccessed => "data-accessed",
            Self::DataAccessEnded => "data-access-ended",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "named-event" => Self::NamedEvent,
            "periodic" => Self::Periodic,
            "data-changed" => Self::DataChanged,
            "data-added" => Self::DataAdded,
            "data-modified" => Self::DataModified,
            "data-removed" => Self::DataRemoved,
            "data-accessed" => Self::DataAccessed,
            "data-access-ended" => Self::DataAccessEnded,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::NamedEvent => Some("Named Event"),
            Self::Periodic => Some("Periodic"),
            Self::DataChanged => Some("Data Changed"),
            Self::DataAdded => Some("Data Added"),
            Self::DataModified => Some("Data Updated"),
            Self::DataRemoved => Some("Data Removed"),
            Self::DataAccessed => Some("Data Accessed"),
            Self::DataAccessEnded => Some("Data Access Ended"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::NamedEvent => Some(
                "The trigger occurs in response to a specific named event, and no other information about the trigger is specified. Named events are completely pre-coordinated, and the formal semantics of the trigger are not provided.",
            ),
            Self::Periodic => Some(
                "The trigger occurs at a specific time or periodically as described by a timing or schedule. A periodic event cannot have any data elements, but may have a name assigned as a shorthand for the event.",
            ),
            Self::DataChanged => Some(
                "The trigger occurs whenever data of a particular type is changed in any way, either added, modified, or removed.",
            ),
            Self::DataAdded => {
                Some("The trigger occurs whenever data of a particular type is added.")
            }
            Self::DataModified => {
                Some("The trigger occurs whenever data of a particular type is modified.")
            }
            Self::DataRemoved => {
                Some("The trigger occurs whenever data of a particular type is removed.")
            }
            Self::DataAccessed => {
                Some("The trigger occurs whenever data of a particular type is accessed.")
            }
            Self::DataAccessEnded => Some(
                "The trigger occurs whenever access to data of a particular type is completed.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::NamedEvent => None,
            Self::Periodic => None,
            Self::DataChanged => None,
            Self::DataAdded => Some("data-changed"),
            Self::DataModified => Some("data-changed"),
            Self::DataRemoved => Some("data-changed"),
            Self::DataAccessed => None,
            Self::DataAccessEnded => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::NamedEvent => 0,
            Self::Periodic => 0,
            Self::DataChanged => 0,
            Self::DataAdded => 1,
            Self::DataModified => 1,
            Self::DataRemoved => 1,
            Self::DataAccessed => 0,
            Self::DataAccessEnded => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
