// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! DeviceAssociation Relationship Status Codes
//!
//! Codes that describe the lifecycle state of a DeviceAssociation relationship.

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceAssociationRelationshipStatusCodes {
    Planned,
    Active,
    Suspended,
    Ended,
    Removed,
    Abandoned,
    Unknown,
}

impl DeviceAssociationRelationshipStatusCodes {
    pub const URL: &'static str = "http://hl7.org/fhir/deviceassociation-relationship-status";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Planned => "planned",
            Self::Active => "active",
            Self::Suspended => "suspended",
            Self::Ended => "ended",
            Self::Removed => "removed",
            Self::Abandoned => "abandoned",
            Self::Unknown => "unknown",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "planned" => Self::Planned,
            "active" => Self::Active,
            "suspended" => Self::Suspended,
            "ended" => Self::Ended,
            "removed" => Self::Removed,
            "abandoned" => Self::Abandoned,
            "unknown" => Self::Unknown,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Planned => Some("Planned"),
            Self::Active => Some("Active"),
            Self::Suspended => Some("Suspended"),
            Self::Ended => Some("Ended"),
            Self::Removed => Some("Removed"),
            Self::Abandoned => Some("Abandoned"),
            Self::Unknown => Some("Unknown"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Planned => {
                Some("The relationship is intended or scheduled but is not yet active.")
            }
            Self::Active => Some("The relationship is currently active."),
            Self::Suspended => Some("The relationship is temporarily inactive and may resume."),
            Self::Ended => Some("The relationship has concluded."),
            Self::Removed => Some(
                "The relationship ended because the device was removed from the subject, location, or context.",
            ),
            Self::Abandoned => Some(
                "The relationship has been left in place but is no longer intended to be active.",
            ),
            Self::Unknown => Some("The relationship status is not known."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Planned => None,
            Self::Active => None,
            Self::Suspended => None,
            Self::Ended => None,
            Self::Removed => None,
            Self::Abandoned => None,
            Self::Unknown => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Planned => 0,
            Self::Active => 0,
            Self::Suspended => 0,
            Self::Ended => 0,
            Self::Removed => 0,
            Self::Abandoned => 0,
            Self::Unknown => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
