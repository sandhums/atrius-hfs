// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Polymer Geometry
//!
//! Polymer geometry, e.g. linear, branched, cross-linked.

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum PolymerGeometryExample {
    Linear,
    Branched,
    CrossLinked,
    Network,
    Dendritic,
    Star,
    Comb,
    Brush,
}

impl PolymerGeometryExample {
    pub const URL: &'static str = "http://hl7.org/fhir/polymer-geometry";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Linear => "linear",
            Self::Branched => "branched",
            Self::CrossLinked => "cross-linked",
            Self::Network => "network",
            Self::Dendritic => "dendritic",
            Self::Star => "star",
            Self::Comb => "comb",
            Self::Brush => "brush",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "linear" => Self::Linear,
            "branched" => Self::Branched,
            "cross-linked" => Self::CrossLinked,
            "network" => Self::Network,
            "dendritic" => Self::Dendritic,
            "star" => Self::Star,
            "comb" => Self::Comb,
            "brush" => Self::Brush,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Linear => Some("Linear"),
            Self::Branched => Some("Branched"),
            Self::CrossLinked => Some("Cross-linked"),
            Self::Network => Some("Network"),
            Self::Dendritic => Some("Dendritic"),
            Self::Star => Some("Star"),
            Self::Comb => Some("Comb"),
            Self::Brush => Some("Brush"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Linear => Some("Linear polymer geometry."),
            Self::Branched => Some("Branched polymer geometry."),
            Self::CrossLinked => Some("Cross-linked polymer network."),
            Self::Network => Some("Network polymer."),
            Self::Dendritic => Some("Dendritic polymer."),
            Self::Star => Some("Star polymer geometry."),
            Self::Comb => Some("Comb polymer geometry."),
            Self::Brush => Some("Brush polymer geometry."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Linear => None,
            Self::Branched => None,
            Self::CrossLinked => None,
            Self::Network => None,
            Self::Dendritic => None,
            Self::Star => None,
            Self::Comb => None,
            Self::Brush => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Linear => 0,
            Self::Branched => 0,
            Self::CrossLinked => 0,
            Self::Network => 0,
            Self::Dendritic => 0,
            Self::Star => 0,
            Self::Comb => 0,
            Self::Brush => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
