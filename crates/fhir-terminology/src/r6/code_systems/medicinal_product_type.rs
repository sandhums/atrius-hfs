// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Medicinal Product Type
//!
//! Overall defining type of this Medicinal Product.

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum MedicinalProductTypeExample {
    MedicinalProduct,
    InvestigationalProduct,
}

impl MedicinalProductTypeExample {
    pub const URL: &'static str = "http://hl7.org/fhir/medicinal-product-type";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::MedicinalProduct => "MedicinalProduct",
            Self::InvestigationalProduct => "InvestigationalProduct",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "MedicinalProduct" => Self::MedicinalProduct,
            "InvestigationalProduct" => Self::InvestigationalProduct,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::MedicinalProduct => Some("Medicinal Product"),
            Self::InvestigationalProduct => Some("Investigational Medicinal Product"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::MedicinalProduct => Some(
                "Any substance or combination of substances that may be administered to human beings (or animals) for treating or preventing disease, or with the intent to make a medical diagnosis or to restore, correct or modify physiological functions. Its regulatory status is authorized/marketed for commercial use.",
            ),
            Self::InvestigationalProduct => Some(
                "A medicinal product which is being tested or used as a reference, including as a placebo, in a clinical trial. Its regulatory status is under evaluation in a clinical trial; might or might not have marketing authorization.",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::MedicinalProduct => None,
            Self::InvestigationalProduct => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::MedicinalProduct => 0,
            Self::InvestigationalProduct => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
