// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Device Warnings
//!
//! Hazards of using a device.

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceWarningsExample {
    Flammable,
    ElectricalShockRisk,
    DoNotSubmerge,
    TemperatureLimit,
    RequiresTraining,
    BatteryHandling,
}

impl DeviceWarningsExample {
    pub const URL: &'static str = "http://hl7.org/fhir/devicedefinition-warnings-example";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Flammable => "flammable",
            Self::ElectricalShockRisk => "electrical-shock-risk",
            Self::DoNotSubmerge => "do-not-submerge",
            Self::TemperatureLimit => "temperature-limit",
            Self::RequiresTraining => "requires-training",
            Self::BatteryHandling => "battery-handling",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "flammable" => Self::Flammable,
            "electrical-shock-risk" => Self::ElectricalShockRisk,
            "do-not-submerge" => Self::DoNotSubmerge,
            "temperature-limit" => Self::TemperatureLimit,
            "requires-training" => Self::RequiresTraining,
            "battery-handling" => Self::BatteryHandling,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Flammable => Some("Flammable"),
            Self::ElectricalShockRisk => Some("Electrical Shock Risk"),
            Self::DoNotSubmerge => Some("Do Not Submerge"),
            Self::TemperatureLimit => Some("Temperature Limit"),
            Self::RequiresTraining => Some("Requires Training"),
            Self::BatteryHandling => Some("Battery Handling"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Flammable => Some("Flammable - keep away from heat/open flame."),
            Self::ElectricalShockRisk => {
                Some("Risk of electrical shock - follow grounding/earthing instructions.")
            }
            Self::DoNotSubmerge => Some("Do not immerse / keep dry."),
            Self::TemperatureLimit => Some("Temperature limits must be observed."),
            Self::RequiresTraining => Some("Operator training required."),
            Self::BatteryHandling => Some("Battery handling / charging instructions."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Flammable => None,
            Self::ElectricalShockRisk => None,
            Self::DoNotSubmerge => None,
            Self::TemperatureLimit => None,
            Self::RequiresTraining => None,
            Self::BatteryHandling => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Flammable => 0,
            Self::ElectricalShockRisk => 0,
            Self::DoNotSubmerge => 0,
            Self::TemperatureLimit => 0,
            Self::RequiresTraining => 0,
            Self::BatteryHandling => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
