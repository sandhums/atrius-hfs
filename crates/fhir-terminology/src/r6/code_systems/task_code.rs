// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Task Codes
//!
//! Codes indicating the type of action that is expected to be performed

use super::super::super::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum TaskCode {
    Approve,
    Fulfill,
    Instantiate,
    Abort,
    Replace,
    Change,
    Suspend,
    Resume,
    DataRequest,
}

impl TaskCode {
    pub const URL: &'static str = "http://hl7.org/fhir/CodeSystem/task-code";
    pub const VERSION: &'static str = "6.0.0-ballot3";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Approve => "approve",
            Self::Fulfill => "fulfill",
            Self::Instantiate => "instantiate",
            Self::Abort => "abort",
            Self::Replace => "replace",
            Self::Change => "change",
            Self::Suspend => "suspend",
            Self::Resume => "resume",
            Self::DataRequest => "data-request",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "approve" => Self::Approve,
            "fulfill" => Self::Fulfill,
            "instantiate" => Self::Instantiate,
            "abort" => Self::Abort,
            "replace" => Self::Replace,
            "change" => Self::Change,
            "suspend" => Self::Suspend,
            "resume" => Self::Resume,
            "data-request" => Self::DataRequest,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Approve => Some("Activate/approve the focal resource"),
            Self::Fulfill => Some("Fulfill the focal request"),
            Self::Instantiate => Some("Instantiate the focal definition"),
            Self::Abort => Some("Mark the focal resource as no longer active"),
            Self::Replace => Some("Replace the focal resource with the input resource"),
            Self::Change => Some("Change the focal resource"),
            Self::Suspend => Some("Suspend the focal resource"),
            Self::Resume => Some("Re-activate the focal resource"),
            Self::DataRequest => Some("data request task"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Approve => Some(
                "Take what actions are needed to transition the focus resource from \'draft\' to \'active\' or \'in-progress\', as appropriate for the resource type.  This may involve additing additional content, approval, validation, etc.",
            ),
            Self::Fulfill => Some(
                "Act to perform the actions described in the focus request.  This might result in a \'more assertive\' request (order for a plan or proposal, filler order for a placer order), but is intend to eventually result in events.  The degree of fulfillment requested might be limited by Task.restriction.",
            ),
            Self::Instantiate => Some(
                "Act to perform the actions defined in the focus definition resource (ActivityDefinition, PlanDefinition, Questionnaire, etc.)  For PlanDefinition or ActivityDefinition, this might result in a \'more assertive\' request (order for a plan or proposal, filler order for a placer order), but is intend to eventually result in events.  For Questionnaire, this would result in a QuestionnaireResponse - and possibly resources constructed using data extracted from the response.  The degree of fulfillment requested might be limited by Task.restriction.",
            ),
            Self::Abort => Some(
                "Abort, cancel or withdraw the focal resource, as appropriate for the type of resource.",
            ),
            Self::Replace => Some("Replace the focal resource with the specified input resource"),
            Self::Change => Some(
                "Update the focal resource of the owning system to reflect the content specified as the Task.focus",
            ),
            Self::Suspend => Some(
                "Transition the focal resource from \'active\' or \'in-progress\' to \'suspended\'",
            ),
            Self::Resume => Some(
                "Transition the focal resource from \'suspended\' to \'active\' or \'in-progress\' as appropriate for the resource type.",
            ),
            Self::DataRequest => Some(
                "A type of Task asking the Task.owner to deliver the data corresponding to the characteristics defined by the Task.inputs to the Task.requester",
            ),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Approve => None,
            Self::Fulfill => None,
            Self::Instantiate => None,
            Self::Abort => None,
            Self::Replace => None,
            Self::Change => None,
            Self::Suspend => None,
            Self::Resume => None,
            Self::DataRequest => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Approve => 0,
            Self::Fulfill => 0,
            Self::Instantiate => 0,
            Self::Abort => 0,
            Self::Replace => 0,
            Self::Change => 0,
            Self::Suspend => 0,
            Self::Resume => 0,
            Self::DataRequest => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
