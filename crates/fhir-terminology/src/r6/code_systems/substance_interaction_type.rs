// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Substance Interaction Type
//!
//! The kind of interaction between a substance and its target, e.g. binding, inhibition, agonism.

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum SubstanceInteractionTypeExample {
    Binding,
    Inhibition,
    Activation,
    Agonist,
    Antagonist,
    Substrate,
    AllostericModulator,
}

impl SubstanceInteractionTypeExample {
    pub const URL: &'static str = "http://hl7.org/fhir/substance-interaction-type";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Binding => "binding",
            Self::Inhibition => "inhibition",
            Self::Activation => "activation",
            Self::Agonist => "agonist",
            Self::Antagonist => "antagonist",
            Self::Substrate => "substrate",
            Self::AllostericModulator => "allosteric-modulator",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "binding" => Self::Binding,
            "inhibition" => Self::Inhibition,
            "activation" => Self::Activation,
            "agonist" => Self::Agonist,
            "antagonist" => Self::Antagonist,
            "substrate" => Self::Substrate,
            "allosteric-modulator" => Self::AllostericModulator,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Binding => Some("Binding"),
            Self::Inhibition => Some("Inhibition"),
            Self::Activation => Some("Activation"),
            Self::Agonist => Some("Agonist"),
            Self::Antagonist => Some("Antagonist"),
            Self::Substrate => Some("Substrate"),
            Self::AllostericModulator => Some("Allosteric modulator"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Binding => Some("The substance binds to its target."),
            Self::Inhibition => Some("The substance inhibits the activity of its target."),
            Self::Activation => Some("The substance activates its target."),
            Self::Agonist => Some("The substance acts as an agonist of its target."),
            Self::Antagonist => Some("The substance acts as an antagonist of its target."),
            Self::Substrate => Some("The substance is a substrate of its target."),
            Self::AllostericModulator => {
                Some("The substance acts as an allosteric modulator of its target.")
            }
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Binding => None,
            Self::Inhibition => None,
            Self::Activation => None,
            Self::Agonist => None,
            Self::Antagonist => None,
            Self::Substrate => None,
            Self::AllostericModulator => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Binding => 0,
            Self::Inhibition => 0,
            Self::Activation => 0,
            Self::Agonist => 0,
            Self::Antagonist => 0,
            Self::Substrate => 0,
            Self::AllostericModulator => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
