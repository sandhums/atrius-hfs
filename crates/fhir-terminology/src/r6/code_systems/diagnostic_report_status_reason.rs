// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Diagnostic Report Status Reason Codes
//!
//! Codes indicating why a diagnostic report is in its current status.

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DiagnosticReportStatusReasonCodes {
    PatientMismatch,
    SampleMismatch,
    InvalidCalibration,
    ReagentIssue,
    Duplicate,
}

impl DiagnosticReportStatusReasonCodes {
    pub const URL: &'static str = "http://hl7.org/fhir/CodeSystem/diagnostic-report-status-reason";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::PatientMismatch => "patient-mismatch",
            Self::SampleMismatch => "sample-mismatch",
            Self::InvalidCalibration => "invalid-calibration",
            Self::ReagentIssue => "reagent-issue",
            Self::Duplicate => "duplicate",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "patient-mismatch" => Self::PatientMismatch,
            "sample-mismatch" => Self::SampleMismatch,
            "invalid-calibration" => Self::InvalidCalibration,
            "reagent-issue" => Self::ReagentIssue,
            "duplicate" => Self::Duplicate,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::PatientMismatch => Some("Patient Mismatch"),
            Self::SampleMismatch => Some("Sample Mismatch"),
            Self::InvalidCalibration => Some("Invalid Calibration"),
            Self::ReagentIssue => Some("Reagent issue"),
            Self::Duplicate => Some("Duplicate"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::PatientMismatch => Some(
                "The patient identity or subject context did not match the expected patient or subject.",
            ),
            Self::SampleMismatch => Some(
                "The specimen, sample, or referenced result did not match the expected subject, order, container, or report.",
            ),
            Self::InvalidCalibration => Some(
                "The device, instrument, or analyzer calibration used for referenced results was invalid, expired, or otherwise unreliable.",
            ),
            Self::ReagentIssue => Some(
                "An incorrect, invalid, expired, recalled, or otherwise unsuitable reagent or reagent lot was used or identified for referenced results.",
            ),
            Self::Duplicate => Some("The diagnostic report duplicates another diagnostic report."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::PatientMismatch => None,
            Self::SampleMismatch => None,
            Self::InvalidCalibration => None,
            Self::ReagentIssue => None,
            Self::Duplicate => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::PatientMismatch => 0,
            Self::SampleMismatch => 0,
            Self::InvalidCalibration => 0,
            Self::ReagentIssue => 0,
            Self::Duplicate => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
