// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Copolymer Connectivity
//!
//! Describes the copolymer sequence type (polymer connectivity).

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum CopolymerConnectivityExample {
    Random,
    Alternating,
    Block,
    Graft,
    Statistical,
    Periodic,
    Mixed,
    Cross,
}

impl CopolymerConnectivityExample {
    pub const URL: &'static str = "http://hl7.org/fhir/copolymer-connectivity";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Random => "random",
            Self::Alternating => "alternating",
            Self::Block => "block",
            Self::Graft => "graft",
            Self::Statistical => "statistical",
            Self::Periodic => "periodic",
            Self::Mixed => "mixed",
            Self::Cross => "cross",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "random" => Self::Random,
            "alternating" => Self::Alternating,
            "block" => Self::Block,
            "graft" => Self::Graft,
            "statistical" => Self::Statistical,
            "periodic" => Self::Periodic,
            "mixed" => Self::Mixed,
            "cross" => Self::Cross,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Random => Some("Random"),
            Self::Alternating => Some("Alternating"),
            Self::Block => Some("Block"),
            Self::Graft => Some("Graft"),
            Self::Statistical => Some("Statistical"),
            Self::Periodic => Some("Periodic"),
            Self::Mixed => Some("Mixed"),
            Self::Cross => Some("Cross"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Random => Some("Monomers arranged randomly."),
            Self::Alternating => Some("Monomers arranged in alternating order."),
            Self::Block => Some("Block copolymer."),
            Self::Graft => Some("Graft copolymer."),
            Self::Statistical => Some("Statistical copolymer."),
            Self::Periodic => Some("Periodic copolymer."),
            Self::Mixed => Some("Mixed connectivity copolymer."),
            Self::Cross => Some("Crosslinked copolymer."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Random => None,
            Self::Alternating => None,
            Self::Block => None,
            Self::Graft => None,
            Self::Statistical => None,
            Self::Periodic => None,
            Self::Mixed => None,
            Self::Cross => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Random => 0,
            Self::Alternating => 0,
            Self::Block => 0,
            Self::Graft => 0,
            Self::Statistical => 0,
            Self::Periodic => 0,
            Self::Mixed => 0,
            Self::Cross => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
