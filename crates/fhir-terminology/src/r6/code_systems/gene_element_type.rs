// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! Gene Element Type
//!
//! The functional role of a DNA element within a gene therapy construct.

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum GeneElementTypeExample {
    Promoter,
    Enhancer,
    Silencer,
    Terminator,
    Repressor,
    CodingSequence,
    Intron,
    Ribozyme,
}

impl GeneElementTypeExample {
    pub const URL: &'static str = "http://hl7.org/fhir/gene-element-type";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = true;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::Promoter => "promoter",
            Self::Enhancer => "enhancer",
            Self::Silencer => "silencer",
            Self::Terminator => "terminator",
            Self::Repressor => "repressor",
            Self::CodingSequence => "coding-sequence",
            Self::Intron => "intron",
            Self::Ribozyme => "ribozyme",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "promoter" => Self::Promoter,
            "enhancer" => Self::Enhancer,
            "silencer" => Self::Silencer,
            "terminator" => Self::Terminator,
            "repressor" => Self::Repressor,
            "coding-sequence" => Self::CodingSequence,
            "intron" => Self::Intron,
            "ribozyme" => Self::Ribozyme,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::Promoter => Some("Promoter"),
            Self::Enhancer => Some("Enhancer"),
            Self::Silencer => Some("Silencer"),
            Self::Terminator => Some("Terminator"),
            Self::Repressor => Some("Repressor binding site"),
            Self::CodingSequence => Some("Coding sequence (CDS)"),
            Self::Intron => Some("Intron"),
            Self::Ribozyme => Some("Ribozyme"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::Promoter => {
                Some("A DNA sequence that initiates and regulates transcription of a gene.")
            }
            Self::Enhancer => Some("A DNA sequence that increases the rate of transcription."),
            Self::Silencer => Some("A DNA sequence that reduces or prevents transcription."),
            Self::Terminator => Some("A DNA sequence that signals the end of transcription."),
            Self::Repressor => {
                Some("A DNA sequence to which a repressor protein binds to inhibit transcription.")
            }
            Self::CodingSequence => Some("The portion of a gene that encodes a protein."),
            Self::Intron => Some(
                "A non-coding sequence within a gene that is spliced out during mRNA processing.",
            ),
            Self::Ribozyme => Some("A catalytic RNA element."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::Promoter => None,
            Self::Enhancer => None,
            Self::Silencer => None,
            Self::Terminator => None,
            Self::Repressor => None,
            Self::CodingSequence => None,
            Self::Intron => None,
            Self::Ribozyme => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::Promoter => 0,
            Self::Enhancer => 0,
            Self::Silencer => 0,
            Self::Terminator => 0,
            Self::Repressor => 0,
            Self::CodingSequence => 0,
            Self::Intron => 0,
            Self::Ribozyme => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
