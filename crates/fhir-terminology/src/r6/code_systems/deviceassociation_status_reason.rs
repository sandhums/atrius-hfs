// @generated by fhir-valueset-gen
// DO NOT EDIT MANUALLY

//! DeviceAssociation Status Reason Codes
//!
//! DeviceAssociation Status Reason Codes

use helios_fhir::r6::{CodeableConcept, Coding};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[allow(non_camel_case_types)]
pub enum DeviceAssociationStatusReason {
    LostToFollowUp,
    RecordArchived,
    Superseded,
    Duplicate,
    PatientDeceased,
    DeviceRetired,
    AssociationEnded,
    Unknown,
}

impl DeviceAssociationStatusReason {
    pub const URL: &'static str = "http://hl7.org/fhir/deviceassociation-status-reason";
    pub const VERSION: &'static str = "6.0.0-ballot4";
    pub const STATUS: &'static str = "active";
    pub const CONTENT: &'static str = "complete";
    pub const EXPERIMENTAL: bool = false;
    pub const CASE_SENSITIVE: bool = true;
    pub const HAS_CONCEPTS: bool = true;

    pub fn as_code(self) -> &'static str {
        match self {
            Self::LostToFollowUp => "lost-to-follow-up",
            Self::RecordArchived => "record-archived",
            Self::Superseded => "superseded",
            Self::Duplicate => "duplicate",
            Self::PatientDeceased => "patient-deceased",
            Self::DeviceRetired => "device-retired",
            Self::AssociationEnded => "association-ended",
            Self::Unknown => "unknown",
        }
    }

    pub fn try_from_code(code: &str) -> Option<Self> {
        Some(match code {
            "lost-to-follow-up" => Self::LostToFollowUp,
            "record-archived" => Self::RecordArchived,
            "superseded" => Self::Superseded,
            "duplicate" => Self::Duplicate,
            "patient-deceased" => Self::PatientDeceased,
            "device-retired" => Self::DeviceRetired,
            "association-ended" => Self::AssociationEnded,
            "unknown" => Self::Unknown,
            _ => return None,
        })
    }

    pub fn display(self) -> Option<&'static str> {
        match self {
            Self::LostToFollowUp => Some("Lost To Follow-Up"),
            Self::RecordArchived => Some("Record Archived"),
            Self::Superseded => Some("Superseded"),
            Self::Duplicate => Some("Duplicate"),
            Self::PatientDeceased => Some("Patient Deceased"),
            Self::DeviceRetired => Some("Device Retired"),
            Self::AssociationEnded => Some("Association Ended"),
            Self::Unknown => Some("Unknown"),
        }
    }

    pub fn definition(self) -> Option<&'static str> {
        match self {
            Self::LostToFollowUp => Some(
                "The relationship status changed because follow-up with the subject or responsible party was lost.",
            ),
            Self::RecordArchived => {
                Some("The relationship status changed because the association record was archived.")
            }
            Self::Superseded => Some(
                "The relationship status changed because the association was replaced by a newer association record.",
            ),
            Self::Duplicate => Some(
                "The relationship status changed because the association record was identified as a duplicate.",
            ),
            Self::PatientDeceased => Some(
                "The relationship status changed because the patient associated with the device is deceased.",
            ),
            Self::DeviceRetired => {
                Some("The relationship status changed because the device was retired from service.")
            }
            Self::AssociationEnded => {
                Some("The relationship status changed because the association ended.")
            }
            Self::Unknown => Some("The reason for the relationship status is not known."),
        }
    }

    pub fn parent_code(self) -> Option<&'static str> {
        match self {
            Self::LostToFollowUp => None,
            Self::RecordArchived => None,
            Self::Superseded => None,
            Self::Duplicate => None,
            Self::PatientDeceased => None,
            Self::DeviceRetired => None,
            Self::AssociationEnded => None,
            Self::Unknown => None,
        }
    }

    pub fn level(self) -> u16 {
        match self {
            Self::LostToFollowUp => 0,
            Self::RecordArchived => 0,
            Self::Superseded => 0,
            Self::Duplicate => 0,
            Self::PatientDeceased => 0,
            Self::DeviceRetired => 0,
            Self::AssociationEnded => 0,
            Self::Unknown => 0,
        }
    }
    pub fn to_coding(self) -> Coding {
        Coding {
            id: None,
            extension: None,
            system: Some(Self::URL.to_string().into()),
            version: Some(Self::VERSION.to_string().into()),
            code: Some(self.as_code().to_string().into()),
            display: self.display().map(|d| d.to_string().into()),
            user_selected: None,
        }
    }

    pub fn to_codeable_concept(self) -> CodeableConcept {
        let coding = self.to_coding();
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: self.display().map(|d| d.to_string().into()),
        }
    }

    pub fn matches_coding(self, coding: &Coding) -> bool {
        let system = coding.system.as_ref().and_then(|e| e.value.as_deref());
        let code = coding.code.as_ref().and_then(|e| e.value.as_deref());
        system == Some(Self::URL) && code == Some(self.as_code())
    }

    pub fn matches_codeable_concept(self, cc: &CodeableConcept) -> bool {
        cc.coding
            .as_ref()
            .map(|v| {
                v.iter().any(|c| {
                    let system = c.system.as_ref().and_then(|e| e.value.as_deref());
                    let code = c.code.as_ref().and_then(|e| e.value.as_deref());
                    system == Some(Self::URL) && code == Some(self.as_code())
                })
            })
            .unwrap_or(false)
    }
}
