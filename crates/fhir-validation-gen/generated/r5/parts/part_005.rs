// @generated by fhir-validation-gen
// FHIR version: FHIR R5
// shard parts/part_005.rs

static CLAIM_RESPONSE_ITEM_DETAIL_SUB_DETAIL_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.item.detail.subDetail".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseItemDetailSubDetail {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.traceNumber[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_ITEM_DETAIL_SUB_DETAIL_INVARIANTS.as_slice(), evaluator, "ClaimResponse.item.detail.subDetail"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.item.detail.subDetail.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.item.detail.subDetail.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.modifierExtension"));
            }
        }
        if let Some(values) = &self.trace_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.item.detail.subDetail.traceNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.traceNumber"));
            }
        }
        if let Some(values) = &self.note_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.item.detail.subDetail.noteNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.noteNumber"));
            }
        }
        if let Some(values) = &self.adjudication {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.item.detail.subDetail.adjudication", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.adjudication"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.traceNumber[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseItemDetailSubDetail {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.traceNumber[{idx}]")));
            }
        }
        issues
    }
    }

static CLAIM_RESPONSE_ADD_ITEM_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.addItem".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_ADD_ITEM_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.revenue".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/ex-revenue-center".to_string(),
        binding_name: Some("RevenueCenter".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.productOrService".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-uscls".to_string(),
        binding_name: Some("ServiceProduct".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.productOrServiceEnd".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-uscls".to_string(),
        binding_name: Some("ServiceProduct".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.modifier".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/claim-modifiers".to_string(),
        binding_name: Some("Modifiers".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.programCode".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/ex-program-code".to_string(),
        binding_name: Some("ProgramCode".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.location[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-place".to_string(),
        binding_name: Some("ServicePlace".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["CodeableConcept".to_string()]),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseAddItem {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_ADD_ITEM_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.traceNumber[{idx}]")));
            }
        }
        if let Some(values) = &self.provider {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.provider[{idx}]")));
            }
        }
        if let Some(values) = &self.request {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.request[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.net"));
        }
        if let Some(values) = &self.body_site {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.bodySite[{idx}]")));
            }
        }
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.detail[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.serviced {
            match choice {
                ClaimResponseAddItemServiced::Date(_value) => {
                }
                ClaimResponseAddItemServiced::Period(_value) => {
                }
            }
        }
        if let Some(choice) = &self.location {
            match choice {
                ClaimResponseAddItemLocation::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "ClaimResponse.addItem.location[x]", "http://hl7.org/fhir/ValueSet/service-place", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/service-place", value));
                    issues.extend(child_issues);
                }
                ClaimResponseAddItemLocation::Address(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.location[x]"));
                }
                ClaimResponseAddItemLocation::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.location[x]"));
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_ADD_ITEM_INVARIANTS.as_slice(), evaluator, "ClaimResponse.addItem"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.modifierExtension"));
            }
        }
        if let Some(values) = &self.item_sequence {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.itemSequence", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.itemSequence"));
            }
        }
        if let Some(values) = &self.detail_sequence {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detailSequence", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.detailSequence"));
            }
        }
        if let Some(values) = &self.subdetail_sequence {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.subdetailSequence", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.subdetailSequence"));
            }
        }
        if let Some(values) = &self.trace_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.traceNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.traceNumber"));
            }
        }
        if let Some(values) = &self.provider {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.provider", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.provider"));
            }
        }
        if let Some(values) = &self.request {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.request", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.request"));
            }
        }
        if let Some(values) = &self.modifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.modifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.modifier"));
            }
        }
        if let Some(values) = &self.program_code {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.programCode", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.programCode"));
            }
        }
        if let Some(values) = &self.body_site {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.bodySite", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.bodySite"));
            }
        }
        if let Some(values) = &self.note_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.noteNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.noteNumber"));
            }
        }
        if let Some(values) = &self.adjudication {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.adjudication", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.adjudication"));
            }
        }
        if let Some(values) = &self.detail {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("addItem.detail"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.traceNumber[{idx}]")));
            }
        }
        if let Some(values) = &self.provider {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.provider[{idx}]")));
            }
        }
        if let Some(value) = &self.revenue {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.revenue"));
        }
        if let Some(value) = &self.product_or_service {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.productOrService"));
        }
        if let Some(value) = &self.product_or_service_end {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.productOrServiceEnd"));
        }
        if let Some(values) = &self.request {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.request[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.modifier[{idx}]")));
            }
        }
        if let Some(values) = &self.program_code {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.programCode[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.net"));
        }
        if let Some(values) = &self.body_site {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.bodySite[{idx}]")));
            }
        }
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.detail[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.serviced {
            match choice {
                ClaimResponseAddItemServiced::Date(_value) => {
                }
                ClaimResponseAddItemServiced::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.serviced[x]"));
                }
            }
        }
        if let Some(choice) = &self.location {
            match choice {
                ClaimResponseAddItemLocation::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.location[x]"));
                }
                ClaimResponseAddItemLocation::Address(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.location[x]"));
                }
                ClaimResponseAddItemLocation::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.location[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseAddItem {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_ADD_ITEM_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.traceNumber[{idx}]")));
            }
        }
        if let Some(values) = &self.provider {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.provider[{idx}]")));
            }
        }
        if let Some(values) = &self.request {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.request[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "addItem.net"));
        }
        if let Some(values) = &self.body_site {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.bodySite[{idx}]")));
            }
        }
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("addItem.detail[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.serviced {
            match choice {
                ClaimResponseAddItemServiced::Date(_value) => {
                }
                ClaimResponseAddItemServiced::Period(_value) => {
                }
            }
        }
        if let Some(choice) = &self.location {
            match choice {
                ClaimResponseAddItemLocation::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "ClaimResponse.addItem.location[x]", "http://hl7.org/fhir/ValueSet/service-place", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/service-place", value)).await;
                    issues.extend(child_issues);
                }
                ClaimResponseAddItemLocation::Address(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.location[x]"));
                }
                ClaimResponseAddItemLocation::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "addItem.location[x]"));
                }
            }
        }
        issues
    }
    }

static CLAIM_RESPONSE_ADD_ITEM_BODY_SITE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.addItem.bodySite".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_ADD_ITEM_BODY_SITE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.bodySite.site".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/tooth".to_string(),
        binding_name: Some("OralSites".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.bodySite.subSite".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/surface".to_string(),
        binding_name: Some("Surface".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseAddItemBodySite {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_ADD_ITEM_BODY_SITE_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_ADD_ITEM_BODY_SITE_INVARIANTS.as_slice(), evaluator, "ClaimResponse.addItem.bodySite"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.bodySite.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("bodySite.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.bodySite.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("bodySite.modifierExtension"));
            }
        }
        if let Some(values) = &self.site {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.bodySite.site", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("bodySite.site"));
            }
        }
        if let Some(values) = &self.sub_site {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.bodySite.subSite", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("bodySite.subSite"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("bodySite.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("bodySite.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.site {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("bodySite.site[{idx}]")));
            }
        }
        if let Some(values) = &self.sub_site {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("bodySite.subSite[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseAddItemBodySite {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_ADD_ITEM_BODY_SITE_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CLAIM_RESPONSE_ADD_ITEM_DETAIL_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.addItem.detail".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_ADD_ITEM_DETAIL_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.revenue".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/ex-revenue-center".to_string(),
        binding_name: Some("RevenueCenter".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.productOrService".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-uscls".to_string(),
        binding_name: Some("ServiceProduct".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.productOrServiceEnd".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-uscls".to_string(),
        binding_name: Some("ServiceProduct".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.modifier".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/claim-modifiers".to_string(),
        binding_name: Some("Modifiers".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseAddItemDetail {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_ADD_ITEM_DETAIL_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.traceNumber[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.net"));
        }
        if let Some(values) = &self.sub_detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.subDetail[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_ADD_ITEM_DETAIL_INVARIANTS.as_slice(), evaluator, "ClaimResponse.addItem.detail"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.modifierExtension"));
            }
        }
        if let Some(values) = &self.trace_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.traceNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.traceNumber"));
            }
        }
        if let Some(values) = &self.modifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.modifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.modifier"));
            }
        }
        if let Some(values) = &self.note_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.noteNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.noteNumber"));
            }
        }
        if let Some(values) = &self.adjudication {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.adjudication", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.adjudication"));
            }
        }
        if let Some(values) = &self.sub_detail {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.subDetail", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.subDetail"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.traceNumber[{idx}]")));
            }
        }
        if let Some(value) = &self.revenue {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.revenue"));
        }
        if let Some(value) = &self.product_or_service {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.productOrService"));
        }
        if let Some(value) = &self.product_or_service_end {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.productOrServiceEnd"));
        }
        if let Some(values) = &self.modifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.modifier[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.net"));
        }
        if let Some(values) = &self.sub_detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.subDetail[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseAddItemDetail {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_ADD_ITEM_DETAIL_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.traceNumber[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "detail.net"));
        }
        if let Some(values) = &self.sub_detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.subDetail[{idx}]")));
            }
        }
        issues
    }
    }

static CLAIM_RESPONSE_ADD_ITEM_DETAIL_SUB_DETAIL_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.addItem.detail.subDetail".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_ADD_ITEM_DETAIL_SUB_DETAIL_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.subDetail.revenue".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/ex-revenue-center".to_string(),
        binding_name: Some("RevenueCenter".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.subDetail.productOrService".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-uscls".to_string(),
        binding_name: Some("ServiceProduct".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.subDetail.productOrServiceEnd".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-uscls".to_string(),
        binding_name: Some("ServiceProduct".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.addItem.detail.subDetail.modifier".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/claim-modifiers".to_string(),
        binding_name: Some("Modifiers".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseAddItemDetailSubDetail {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_ADD_ITEM_DETAIL_SUB_DETAIL_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.traceNumber[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.net"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_ADD_ITEM_DETAIL_SUB_DETAIL_INVARIANTS.as_slice(), evaluator, "ClaimResponse.addItem.detail.subDetail"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.subDetail.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.subDetail.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.modifierExtension"));
            }
        }
        if let Some(values) = &self.trace_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.subDetail.traceNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.traceNumber"));
            }
        }
        if let Some(values) = &self.modifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.subDetail.modifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.modifier"));
            }
        }
        if let Some(values) = &self.note_number {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.subDetail.noteNumber", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.noteNumber"));
            }
        }
        if let Some(values) = &self.adjudication {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.addItem.detail.subDetail.adjudication", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("subDetail.adjudication"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.traceNumber[{idx}]")));
            }
        }
        if let Some(value) = &self.revenue {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.revenue"));
        }
        if let Some(value) = &self.product_or_service {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.productOrService"));
        }
        if let Some(value) = &self.product_or_service_end {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.productOrServiceEnd"));
        }
        if let Some(values) = &self.modifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.modifier[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.net"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseAddItemDetailSubDetail {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_ADD_ITEM_DETAIL_SUB_DETAIL_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.trace_number {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("subDetail.traceNumber[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.quantity"));
        }
        if let Some(value) = &self.unit_price {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.unitPrice"));
        }
        if let Some(value) = &self.tax {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.tax"));
        }
        if let Some(value) = &self.net {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "subDetail.net"));
        }
        issues
    }
    }

static CLAIM_RESPONSE_TOTAL_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.total".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_TOTAL_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.total.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adjudication".to_string(),
        binding_name: Some("Adjudication".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseTotal {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_TOTAL_BINDINGS.as_slice(), terminology));

        let child_issues = self.amount.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "total.amount"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_TOTAL_INVARIANTS.as_slice(), evaluator, "ClaimResponse.total"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.total.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("total.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.total.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("total.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("total.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("total.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.category.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "total.category"));
        let child_issues = self.amount.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "total.amount"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseTotal {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_TOTAL_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.amount.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "total.amount"));
        issues
    }
    }

static CLAIM_RESPONSE_PAYMENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.payment".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_PAYMENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.payment.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/ex-paymenttype".to_string(),
        binding_name: Some("PaymentType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.payment.adjustmentReason".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/payment-adjustment-reason".to_string(),
        binding_name: Some("PaymentAdjustmentReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponsePayment {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_PAYMENT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.adjustment {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "payment.adjustment"));
        }
        let child_issues = self.amount.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "payment.amount"));
        if let Some(value) = &self.identifier {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "payment.identifier"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_PAYMENT_INVARIANTS.as_slice(), evaluator, "ClaimResponse.payment"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.payment.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("payment.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.payment.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("payment.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("payment.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("payment.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "payment.type"));
        if let Some(value) = &self.adjustment {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "payment.adjustment"));
        }
        if let Some(value) = &self.adjustment_reason {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "payment.adjustmentReason"));
        }
        let child_issues = self.amount.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "payment.amount"));
        if let Some(value) = &self.identifier {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "payment.identifier"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponsePayment {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_PAYMENT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.adjustment {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "payment.adjustment"));
        }
        let child_issues = self.amount.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "payment.amount"));
        if let Some(value) = &self.identifier {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "payment.identifier"));
        }
        issues
    }
    }

static CLAIM_RESPONSE_PROCESS_NOTE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.processNote".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_PROCESS_NOTE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.processNote.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/note-type".to_string(),
        binding_name: Some("NoteType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.processNote.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseProcessNote {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_PROCESS_NOTE_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_PROCESS_NOTE_INVARIANTS.as_slice(), evaluator, "ClaimResponse.processNote"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.processNote.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("processNote.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.processNote.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("processNote.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("processNote.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("processNote.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "processNote.type"));
        }
        if let Some(value) = &self.language {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "processNote.language"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseProcessNote {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_PROCESS_NOTE_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CLAIM_RESPONSE_INSURANCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.insurance".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseInsurance {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.coverage.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "insurance.coverage"));
        if let Some(value) = &self.claim_response {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "insurance.claimResponse"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_INSURANCE_INVARIANTS.as_slice(), evaluator, "ClaimResponse.insurance"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.insurance.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("insurance.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.insurance.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("insurance.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("insurance.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("insurance.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.coverage.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "insurance.coverage"));
        if let Some(value) = &self.claim_response {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "insurance.claimResponse"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseInsurance {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.coverage.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "insurance.coverage"));
        if let Some(value) = &self.claim_response {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "insurance.claimResponse"));
        }
        issues
    }
    }

static CLAIM_RESPONSE_ERROR_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClaimResponse.error".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLAIM_RESPONSE_ERROR_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClaimResponse.error.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adjudication-error".to_string(),
        binding_name: Some("AdjudicationError".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClaimResponseError {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLAIM_RESPONSE_ERROR_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLAIM_RESPONSE_ERROR_INVARIANTS.as_slice(), evaluator, "ClaimResponse.error"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.error.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("error.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.error.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("error.modifierExtension"));
            }
        }
        if let Some(values) = &self.expression {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClaimResponse.error.expression", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("error.expression"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("error.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("error.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "error.code"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClaimResponseError {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLAIM_RESPONSE_ERROR_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CLINICAL_IMPRESSION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalImpression".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalImpression".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalImpression".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalImpression".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "ClinicalImpression".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalImpression".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_IMPRESSION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalImpression.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalImpression.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/event-status|5.0.0".to_string(),
        binding_name: Some("ClinicalImpressionStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalImpression.statusReason".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/clinicalimpression-status-reason".to_string(),
        binding_name: Some("ClinicalImpressionStatusReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalImpression.changePattern".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/clinicalimpression-change-pattern".to_string(),
        binding_name: Some("ClinicalImpressionChangePattern".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalImpression.prognosisCodeableConcept".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/clinicalimpression-prognosis".to_string(),
        binding_name: Some("ClinicalImpressionPrognosis".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalImpression {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_IMPRESSION_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.identifier[{idx}]")));
            }
        }
        let child_issues = self.subject.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.subject"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.encounter"));
        }
        if let Some(value) = &self.performer {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.performer"));
        }
        if let Some(value) = &self.previous {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.previous"));
        }
        if let Some(values) = &self.problem {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.problem[{idx}]")));
            }
        }
        if let Some(values) = &self.finding {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.finding[{idx}]")));
            }
        }
        if let Some(values) = &self.prognosis_reference {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.prognosisReference[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_info {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.supportingInfo[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.effective {
            match choice {
                ClinicalImpressionEffective::DateTime(_value) => {
                }
                ClinicalImpressionEffective::Period(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_IMPRESSION_INVARIANTS.as_slice(), evaluator, "ClinicalImpression"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.identifier"));
            }
        }
        if let Some(values) = &self.problem {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.problem", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.problem"));
            }
        }
        if let Some(values) = &self.protocol {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.protocol", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.protocol"));
            }
        }
        if let Some(values) = &self.finding {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.finding", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.finding"));
            }
        }
        if let Some(values) = &self.prognosis_codeable_concept {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.prognosisCodeableConcept", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.prognosisCodeableConcept"));
            }
        }
        if let Some(values) = &self.prognosis_reference {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.prognosisReference", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.prognosisReference"));
            }
        }
        if let Some(values) = &self.supporting_info {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.supportingInfo", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.supportingInfo"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalImpression.note"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.status_reason {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.statusReason"));
        }
        let child_issues = self.subject.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.subject"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.encounter"));
        }
        if let Some(value) = &self.performer {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.performer"));
        }
        if let Some(value) = &self.previous {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.previous"));
        }
        if let Some(values) = &self.problem {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.problem[{idx}]")));
            }
        }
        if let Some(value) = &self.change_pattern {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.changePattern"));
        }
        if let Some(values) = &self.finding {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.finding[{idx}]")));
            }
        }
        if let Some(values) = &self.prognosis_codeable_concept {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.prognosisCodeableConcept[{idx}]")));
            }
        }
        if let Some(values) = &self.prognosis_reference {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.prognosisReference[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_info {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.supportingInfo[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.note[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.effective {
            match choice {
                ClinicalImpressionEffective::DateTime(_value) => {
                }
                ClinicalImpressionEffective::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.effective[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalImpression {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_IMPRESSION_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.identifier[{idx}]")));
            }
        }
        let child_issues = self.subject.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.subject"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.encounter"));
        }
        if let Some(value) = &self.performer {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.performer"));
        }
        if let Some(value) = &self.previous {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalImpression.previous"));
        }
        if let Some(values) = &self.problem {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.problem[{idx}]")));
            }
        }
        if let Some(values) = &self.finding {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.finding[{idx}]")));
            }
        }
        if let Some(values) = &self.prognosis_reference {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.prognosisReference[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_info {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.supportingInfo[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalImpression.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.effective {
            match choice {
                ClinicalImpressionEffective::DateTime(_value) => {
                }
                ClinicalImpressionEffective::Period(_value) => {
                }
            }
        }
        issues
    }
    }

static CLINICAL_IMPRESSION_FINDING_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalImpression.finding".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_IMPRESSION_FINDING_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalImpression.finding.item".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/condition-code".to_string(),
        binding_name: Some("ConditionKind".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalImpressionFinding {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_IMPRESSION_FINDING_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_IMPRESSION_FINDING_INVARIANTS.as_slice(), evaluator, "ClinicalImpression.finding"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.finding.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("finding.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalImpression.finding.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("finding.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("finding.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("finding.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.item {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "finding.item"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalImpressionFinding {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_IMPRESSION_FINDING_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CLINICAL_USE_DEFINITION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "cud-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition".to_string(),
        expression: "(ClinicalUseDefinition.indication.count() + ClinicalUseDefinition.contraindication.count() + ClinicalUseDefinition.interaction.count() + ClinicalUseDefinition.undesirableEffect.count() + ClinicalUseDefinition.warning.count())  < 2".to_string(),
        human: "Indication, Contraindication, Interaction, UndesirableEffect and Warning cannot be used in the same instance".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "ClinicalUseDefinition".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/clinical-use-definition-type|5.0.0".to_string(),
        binding_name: Some("ClinicalUseDefinitionType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/clinical-use-definition-category".to_string(),
        binding_name: Some("ClinicalUseDefinitionCategory".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/publication-status".to_string(),
        binding_name: Some("PublicationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinition {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.subject {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.subject[{idx}]")));
            }
        }
        if let Some(value) = &self.contraindication {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.contraindication"));
        }
        if let Some(value) = &self.indication {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.indication"));
        }
        if let Some(value) = &self.interaction {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.interaction"));
        }
        if let Some(values) = &self.population {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.population[{idx}]")));
            }
        }
        if let Some(value) = &self.undesirable_effect {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.undesirableEffect"));
        }
        if let Some(value) = &self.warning {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.warning"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.identifier"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.category"));
            }
        }
        if let Some(values) = &self.subject {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.subject", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.subject"));
            }
        }
        if let Some(values) = &self.population {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.population", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.population"));
            }
        }
        if let Some(values) = &self.library {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.library", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ClinicalUseDefinition.library"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.category[{idx}]")));
            }
        }
        if let Some(values) = &self.subject {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.subject[{idx}]")));
            }
        }
        if let Some(value) = &self.status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.status"));
        }
        if let Some(value) = &self.contraindication {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.contraindication"));
        }
        if let Some(value) = &self.indication {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.indication"));
        }
        if let Some(value) = &self.interaction {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.interaction"));
        }
        if let Some(values) = &self.population {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.population[{idx}]")));
            }
        }
        if let Some(value) = &self.undesirable_effect {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.undesirableEffect"));
        }
        if let Some(value) = &self.warning {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.warning"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinition {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.subject {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.subject[{idx}]")));
            }
        }
        if let Some(value) = &self.contraindication {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.contraindication"));
        }
        if let Some(value) = &self.indication {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.indication"));
        }
        if let Some(value) = &self.interaction {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.interaction"));
        }
        if let Some(values) = &self.population {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.population[{idx}]")));
            }
        }
        if let Some(value) = &self.undesirable_effect {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.undesirableEffect"));
        }
        if let Some(value) = &self.warning {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ClinicalUseDefinition.warning"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ClinicalUseDefinition.contained[{idx}]")));
            }
        }
        issues
    }
    }

static CLINICAL_USE_DEFINITION_CONTRAINDICATION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition.contraindication".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_CONTRAINDICATION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.contraindication.diseaseSymptomProcedure".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/disease-symptom-procedure".to_string(),
        binding_name: Some("DiseaseSymptomProcedure".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.contraindication.diseaseStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/disease-status".to_string(),
        binding_name: Some("DiseaseStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.contraindication.comorbidity".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/disease-symptom-procedure".to_string(),
        binding_name: Some("DiseaseSymptomProcedure".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinitionContraindication {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_CONTRAINDICATION_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.indication {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.indication[{idx}]")));
            }
        }
        if let Some(value) = &self.applicability {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "contraindication.applicability"));
        }
        if let Some(values) = &self.other_therapy {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.otherTherapy[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_CONTRAINDICATION_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition.contraindication"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contraindication.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("contraindication.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contraindication.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("contraindication.modifierExtension"));
            }
        }
        if let Some(values) = &self.comorbidity {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contraindication.comorbidity", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("contraindication.comorbidity"));
            }
        }
        if let Some(values) = &self.indication {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contraindication.indication", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("contraindication.indication"));
            }
        }
        if let Some(values) = &self.other_therapy {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contraindication.otherTherapy", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("contraindication.otherTherapy"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.disease_symptom_procedure {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "contraindication.diseaseSymptomProcedure"));
        }
        if let Some(value) = &self.disease_status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "contraindication.diseaseStatus"));
        }
        if let Some(values) = &self.comorbidity {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.comorbidity[{idx}]")));
            }
        }
        if let Some(values) = &self.indication {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.indication[{idx}]")));
            }
        }
        if let Some(value) = &self.applicability {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "contraindication.applicability"));
        }
        if let Some(values) = &self.other_therapy {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.otherTherapy[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinitionContraindication {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_CONTRAINDICATION_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.indication {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.indication[{idx}]")));
            }
        }
        if let Some(value) = &self.applicability {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "contraindication.applicability"));
        }
        if let Some(values) = &self.other_therapy {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contraindication.otherTherapy[{idx}]")));
            }
        }
        issues
    }
    }

static CLINICAL_USE_DEFINITION_CONTRAINDICATION_OTHER_THERAPY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition.contraindication.otherTherapy".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_CONTRAINDICATION_OTHER_THERAPY_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.contraindication.otherTherapy.relationshipType".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/therapy-relationship-type".to_string(),
        binding_name: Some("TherapyRelationshipType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.contraindication.otherTherapy.treatment".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/therapy".to_string(),
        binding_name: Some("Therapy".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinitionContraindicationOtherTherapy {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_CONTRAINDICATION_OTHER_THERAPY_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_CONTRAINDICATION_OTHER_THERAPY_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition.contraindication.otherTherapy"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contraindication.otherTherapy.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("otherTherapy.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.contraindication.otherTherapy.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("otherTherapy.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("otherTherapy.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("otherTherapy.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.relationship_type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "otherTherapy.relationshipType"));
        let child_issues = self.treatment.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "otherTherapy.treatment"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinitionContraindicationOtherTherapy {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_CONTRAINDICATION_OTHER_THERAPY_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CLINICAL_USE_DEFINITION_INDICATION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition.indication".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_INDICATION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.indication.diseaseSymptomProcedure".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/disease-symptom-procedure".to_string(),
        binding_name: Some("DiseaseSymptomProcedure".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.indication.diseaseStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/disease-status".to_string(),
        binding_name: Some("DiseaseStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.indication.comorbidity".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/disease-symptom-procedure".to_string(),
        binding_name: Some("DiseaseSymptomProcedure".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.indication.intendedEffect".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/product-intended-use".to_string(),
        binding_name: Some("ProductIntendedUse".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinitionIndication {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_INDICATION_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.undesirable_effect {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("indication.undesirableEffect[{idx}]")));
            }
        }
        if let Some(value) = &self.applicability {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "indication.applicability"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.duration {
            match choice {
                ClinicalUseDefinitionIndicationDuration::Range(_value) => {
                }
                ClinicalUseDefinitionIndicationDuration::String(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_INDICATION_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition.indication"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.indication.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("indication.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.indication.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("indication.modifierExtension"));
            }
        }
        if let Some(values) = &self.comorbidity {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.indication.comorbidity", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("indication.comorbidity"));
            }
        }
        if let Some(values) = &self.undesirable_effect {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.indication.undesirableEffect", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("indication.undesirableEffect"));
            }
        }
        if let Some(values) = &self.other_therapy {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.indication.otherTherapy", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("indication.otherTherapy"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("indication.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("indication.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.disease_symptom_procedure {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "indication.diseaseSymptomProcedure"));
        }
        if let Some(value) = &self.disease_status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "indication.diseaseStatus"));
        }
        if let Some(values) = &self.comorbidity {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("indication.comorbidity[{idx}]")));
            }
        }
        if let Some(value) = &self.intended_effect {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "indication.intendedEffect"));
        }
        if let Some(values) = &self.undesirable_effect {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("indication.undesirableEffect[{idx}]")));
            }
        }
        if let Some(value) = &self.applicability {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "indication.applicability"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.duration {
            match choice {
                ClinicalUseDefinitionIndicationDuration::Range(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "indication.duration[x]"));
                }
                ClinicalUseDefinitionIndicationDuration::String(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinitionIndication {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_INDICATION_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.undesirable_effect {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("indication.undesirableEffect[{idx}]")));
            }
        }
        if let Some(value) = &self.applicability {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "indication.applicability"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.duration {
            match choice {
                ClinicalUseDefinitionIndicationDuration::Range(_value) => {
                }
                ClinicalUseDefinitionIndicationDuration::String(_value) => {
                }
            }
        }
        issues
    }
    }

static CLINICAL_USE_DEFINITION_INTERACTION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition.interaction".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_INTERACTION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.interaction.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/interaction-type".to_string(),
        binding_name: Some("InteractionType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.interaction.effect".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/interaction-effect".to_string(),
        binding_name: Some("InteractionEffect".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.interaction.incidence".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/interaction-incidence".to_string(),
        binding_name: Some("UndesirableEffectSymptom".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.interaction.management".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/interaction-management".to_string(),
        binding_name: Some("InteractionManagement".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinitionInteraction {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_INTERACTION_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.interactant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interaction.interactant[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_INTERACTION_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition.interaction"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.interaction.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("interaction.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.interaction.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("interaction.modifierExtension"));
            }
        }
        if let Some(values) = &self.interactant {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.interaction.interactant", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("interaction.interactant"));
            }
        }
        if let Some(values) = &self.management {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.interaction.management", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("interaction.management"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interaction.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interaction.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.interactant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interaction.interactant[{idx}]")));
            }
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "interaction.type"));
        }
        if let Some(value) = &self.effect {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "interaction.effect"));
        }
        if let Some(value) = &self.incidence {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "interaction.incidence"));
        }
        if let Some(values) = &self.management {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interaction.management[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinitionInteraction {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_INTERACTION_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.interactant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interaction.interactant[{idx}]")));
            }
        }
        issues
    }
    }

static CLINICAL_USE_DEFINITION_INTERACTION_INTERACTANT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition.interaction.interactant".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_INTERACTION_INTERACTANT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.interaction.interactant.item[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/interactant".to_string(),
        binding_name: Some("Interactant".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["CodeableConcept".to_string()]),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinitionInteractionInteractant {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_INTERACTION_INTERACTANT_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                ClinicalUseDefinitionInteractionInteractantItem::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "interactant.item[x]"));
                }
                ClinicalUseDefinitionInteractionInteractantItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "ClinicalUseDefinition.interaction.interactant.item[x]", "http://hl7.org/fhir/ValueSet/interactant", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/interactant", value));
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_INTERACTION_INTERACTANT_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition.interaction.interactant"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.interaction.interactant.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("interactant.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.interaction.interactant.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("interactant.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interactant.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("interactant.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                ClinicalUseDefinitionInteractionInteractantItem::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "interactant.item[x]"));
                }
                ClinicalUseDefinitionInteractionInteractantItem::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "interactant.item[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinitionInteractionInteractant {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_INTERACTION_INTERACTANT_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                ClinicalUseDefinitionInteractionInteractantItem::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "interactant.item[x]"));
                }
                ClinicalUseDefinitionInteractionInteractantItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "ClinicalUseDefinition.interaction.interactant.item[x]", "http://hl7.org/fhir/ValueSet/interactant", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/interactant", value)).await;
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }
    }

static CLINICAL_USE_DEFINITION_UNDESIRABLE_EFFECT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition.undesirableEffect".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_UNDESIRABLE_EFFECT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.undesirableEffect.symptomConditionEffect".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/undesirable-effect-symptom".to_string(),
        binding_name: Some("UndesirableEffectSymptom".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.undesirableEffect.classification".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/undesirable-effect-classification".to_string(),
        binding_name: Some("UndesirableEffectClassification".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.undesirableEffect.frequencyOfOccurrence".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/undesirable-effect-frequency".to_string(),
        binding_name: Some("UndesirablEffectFrequency".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinitionUndesirableEffect {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_UNDESIRABLE_EFFECT_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_UNDESIRABLE_EFFECT_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition.undesirableEffect"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.undesirableEffect.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("undesirableEffect.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.undesirableEffect.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("undesirableEffect.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("undesirableEffect.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("undesirableEffect.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.symptom_condition_effect {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "undesirableEffect.symptomConditionEffect"));
        }
        if let Some(value) = &self.classification {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "undesirableEffect.classification"));
        }
        if let Some(value) = &self.frequency_of_occurrence {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "undesirableEffect.frequencyOfOccurrence"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinitionUndesirableEffect {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_UNDESIRABLE_EFFECT_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CLINICAL_USE_DEFINITION_WARNING_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ClinicalUseDefinition.warning".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CLINICAL_USE_DEFINITION_WARNING_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ClinicalUseDefinition.warning.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/warning-type".to_string(),
        binding_name: Some("WarningType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ClinicalUseDefinitionWarning {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CLINICAL_USE_DEFINITION_WARNING_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CLINICAL_USE_DEFINITION_WARNING_INVARIANTS.as_slice(), evaluator, "ClinicalUseDefinition.warning"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.warning.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("warning.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ClinicalUseDefinition.warning.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("warning.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("warning.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("warning.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.code {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "warning.code"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ClinicalUseDefinitionWarning {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CLINICAL_USE_DEFINITION_WARNING_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CODE_SYSTEM_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "cnl-0".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "CodeSystem".to_string(),
        expression: "name.exists() implies name.matches('^[A-Z]([A-Za-z0-9_]){1,254}$')".to_string(),
        human: "Name should be usable as an identifier for the module by machine processing applications such as code generation".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "csd-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem".to_string(),
        expression: "concept.exists() implies concept.code.combine(%resource.concept.descendants().concept.code).isDistinct()".to_string(),
        human: "Within a code system definition, all the codes SHALL be unique".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "csd-2".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "CodeSystem".to_string(),
        expression: "concept.concept.exists() implies hierarchyMeaning.exists()".to_string(),
        human: "If there is an explicit hierarchy, a hierarchyMeaning should be provided".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "csd-3".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "CodeSystem".to_string(),
        expression: "concept.where(property.code = 'parent' or property.code = 'child').exists() implies hierarchyMeaning.exists()".to_string(),
        human: "If there is an implicit hierarchy, a hierarchyMeaning should be provided".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "csd-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem".to_string(),
        expression: "CodeSystem.content = 'supplement' implies CodeSystem.supplements.exists()".to_string(),
        human: "If the code system content = supplement, it must nominate what it's a supplement for".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "CodeSystem".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CODE_SYSTEM_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "CodeSystem.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.versionAlgorithm[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/version-algorithm".to_string(),
        binding_name: None,
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["string".to_string(), "Coding".to_string()]),
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/publication-status|5.0.0".to_string(),
        binding_name: Some("PublicationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.jurisdiction".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/jurisdiction".to_string(),
        binding_name: Some("Jurisdiction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.topic".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/definition-topic".to_string(),
        binding_name: Some("DefinitionTopic".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.hierarchyMeaning".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/codesystem-hierarchy-meaning|5.0.0".to_string(),
        binding_name: Some("CodeSystemHierarchyMeaning".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.content".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/codesystem-content-mode|5.0.0".to_string(),
        binding_name: Some("CodeSystemContentMode".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CodeSystem {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CODE_SYSTEM_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.related_artifact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.relatedArtifact[{idx}]")));
            }
        }
        if let Some(values) = &self.filter {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.filter[{idx}]")));
            }
        }
        if let Some(values) = &self.property {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.property[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.version_algorithm {
            match choice {
                CodeSystemVersionAlgorithm::String(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "CodeSystem.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_primitive_value_binding(&binding_ctx, value.value.as_deref(), |value| helios_fhir::r5::terminology::validate_code("http://hl7.org/fhir/ValueSet/version-algorithm", value));
                    issues.extend(child_issues);
                }
                CodeSystemVersionAlgorithm::Coding(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "CodeSystem.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_coding_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_coding("http://hl7.org/fhir/ValueSet/version-algorithm", value));
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CODE_SYSTEM_INVARIANTS.as_slice(), evaluator, "CodeSystem"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.identifier"));
            }
        }
        if let Some(values) = &self.contact {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.contact", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.contact"));
            }
        }
        if let Some(values) = &self.use_context {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.useContext", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.useContext"));
            }
        }
        if let Some(values) = &self.jurisdiction {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.jurisdiction", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.jurisdiction"));
            }
        }
        if let Some(values) = &self.topic {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.topic", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.topic"));
            }
        }
        if let Some(values) = &self.author {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.author", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.author"));
            }
        }
        if let Some(values) = &self.editor {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.editor", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.editor"));
            }
        }
        if let Some(values) = &self.reviewer {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.reviewer", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.reviewer"));
            }
        }
        if let Some(values) = &self.endorser {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.endorser", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.endorser"));
            }
        }
        if let Some(values) = &self.related_artifact {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.relatedArtifact", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.relatedArtifact"));
            }
        }
        if let Some(values) = &self.filter {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.filter", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.filter"));
            }
        }
        if let Some(values) = &self.property {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.property", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.property"));
            }
        }
        if let Some(values) = &self.concept {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CodeSystem.concept"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.contact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.contact[{idx}]")));
            }
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.jurisdiction {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.jurisdiction[{idx}]")));
            }
        }
        if let Some(value) = &self.effective_period {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.effectivePeriod"));
        }
        if let Some(values) = &self.topic {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.topic[{idx}]")));
            }
        }
        if let Some(values) = &self.author {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.author[{idx}]")));
            }
        }
        if let Some(values) = &self.editor {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.editor[{idx}]")));
            }
        }
        if let Some(values) = &self.reviewer {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.reviewer[{idx}]")));
            }
        }
        if let Some(values) = &self.endorser {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.endorser[{idx}]")));
            }
        }
        if let Some(values) = &self.related_artifact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.relatedArtifact[{idx}]")));
            }
        }
        if let Some(values) = &self.filter {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.filter[{idx}]")));
            }
        }
        if let Some(values) = &self.property {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.property[{idx}]")));
            }
        }
        if let Some(values) = &self.concept {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.concept[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.version_algorithm {
            match choice {
                CodeSystemVersionAlgorithm::String(_value) => {
                }
                CodeSystemVersionAlgorithm::Coding(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.versionAlgorithm[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CodeSystem {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CODE_SYSTEM_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CodeSystem.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.related_artifact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.relatedArtifact[{idx}]")));
            }
        }
        if let Some(values) = &self.filter {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.filter[{idx}]")));
            }
        }
        if let Some(values) = &self.property {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.property[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CodeSystem.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.version_algorithm {
            match choice {
                CodeSystemVersionAlgorithm::String(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "CodeSystem.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_primitive_value_binding_async(&binding_ctx, value.value.as_deref(), |value| helios_fhir::r5::terminology::validate_code("http://hl7.org/fhir/ValueSet/version-algorithm", value)).await;
                    issues.extend(child_issues);
                }
                CodeSystemVersionAlgorithm::Coding(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "CodeSystem.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_coding_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_coding("http://hl7.org/fhir/ValueSet/version-algorithm", value)).await;
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }
    }

static CODE_SYSTEM_FILTER_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem.filter".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CODE_SYSTEM_FILTER_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "CodeSystem.filter.operator".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/filter-operator|5.0.0".to_string(),
        binding_name: Some("FilterOperator".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CodeSystemFilter {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CODE_SYSTEM_FILTER_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CODE_SYSTEM_FILTER_INVARIANTS.as_slice(), evaluator, "CodeSystem.filter"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.filter.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("filter.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.filter.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("filter.modifierExtension"));
            }
        }
        if let Some(values) = &self.operator {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.filter.operator", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("filter.operator"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("filter.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("filter.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CodeSystemFilter {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CODE_SYSTEM_FILTER_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CODE_SYSTEM_PROPERTY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem.property".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CODE_SYSTEM_PROPERTY_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "CodeSystem.property.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/concept-property-type|5.0.0".to_string(),
        binding_name: Some("PropertyType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CodeSystemProperty {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CODE_SYSTEM_PROPERTY_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CODE_SYSTEM_PROPERTY_INVARIANTS.as_slice(), evaluator, "CodeSystem.property"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.property.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("property.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.property.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("property.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("property.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("property.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CodeSystemProperty {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CODE_SYSTEM_PROPERTY_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CODE_SYSTEM_CONCEPT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem.concept".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CodeSystemConcept {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.designation {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("concept.designation[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CODE_SYSTEM_CONCEPT_INVARIANTS.as_slice(), evaluator, "CodeSystem.concept"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("concept.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("concept.modifierExtension"));
            }
        }
        if let Some(values) = &self.designation {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.designation", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("concept.designation"));
            }
        }
        if let Some(values) = &self.property {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.property", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("concept.property"));
            }
        }
        if let Some(values) = &self.concept {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.concept", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("concept.concept"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("concept.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("concept.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.designation {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("concept.designation[{idx}]")));
            }
        }
        if let Some(values) = &self.property {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("concept.property[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CodeSystemConcept {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.designation {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("concept.designation[{idx}]")));
            }
        }
        issues
    }
    }

static CODE_SYSTEM_CONCEPT_DESIGNATION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "csd-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem.concept.designation".to_string(),
        expression: "additionalUse.exists() implies use.exists()".to_string(),
        human: "Must have a value for concept.designation.use if concept.designation.additionalUse is present".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem.concept.designation".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static CODE_SYSTEM_CONCEPT_DESIGNATION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "CodeSystem.concept.designation.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.concept.designation.use".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/designation-use".to_string(),
        binding_name: Some("ConceptDesignationUse".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CodeSystem.concept.designation.additionalUse".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/designation-use".to_string(),
        binding_name: Some("ConceptDesignationUse".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CodeSystemConceptDesignation {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, CODE_SYSTEM_CONCEPT_DESIGNATION_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CODE_SYSTEM_CONCEPT_DESIGNATION_INVARIANTS.as_slice(), evaluator, "CodeSystem.concept.designation"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.designation.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("designation.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.designation.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("designation.modifierExtension"));
            }
        }
        if let Some(values) = &self.additional_use {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.designation.additionalUse", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("designation.additionalUse"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("designation.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("designation.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.r#use {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "designation.use"));
        }
        if let Some(values) = &self.additional_use {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("designation.additionalUse[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CodeSystemConceptDesignation {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, CODE_SYSTEM_CONCEPT_DESIGNATION_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static CODE_SYSTEM_CONCEPT_PROPERTY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CodeSystem.concept.property".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CodeSystemConceptProperty {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                CodeSystemConceptPropertyValue::Code(_value) => {
                }
                CodeSystemConceptPropertyValue::Coding(_value) => {
                }
                CodeSystemConceptPropertyValue::String(_value) => {
                }
                CodeSystemConceptPropertyValue::Integer(_value) => {
                }
                CodeSystemConceptPropertyValue::Boolean(_value) => {
                }
                CodeSystemConceptPropertyValue::DateTime(_value) => {
                }
                CodeSystemConceptPropertyValue::Decimal(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, CODE_SYSTEM_CONCEPT_PROPERTY_INVARIANTS.as_slice(), evaluator, "CodeSystem.concept.property"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.property.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("property.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CodeSystem.concept.property.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("property.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("property.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("property.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                CodeSystemConceptPropertyValue::Code(_value) => {
                }
                CodeSystemConceptPropertyValue::Coding(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                CodeSystemConceptPropertyValue::String(_value) => {
                }
                CodeSystemConceptPropertyValue::Integer(_value) => {
                }
                CodeSystemConceptPropertyValue::Boolean(_value) => {
                }
                CodeSystemConceptPropertyValue::DateTime(_value) => {
                }
                CodeSystemConceptPropertyValue::Decimal(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CodeSystemConceptProperty {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                CodeSystemConceptPropertyValue::Code(_value) => {
                }
                CodeSystemConceptPropertyValue::Coding(_value) => {
                }
                CodeSystemConceptPropertyValue::String(_value) => {
                }
                CodeSystemConceptPropertyValue::Integer(_value) => {
                }
                CodeSystemConceptPropertyValue::Boolean(_value) => {
                }
                CodeSystemConceptPropertyValue::DateTime(_value) => {
                }
                CodeSystemConceptPropertyValue::Decimal(_value) => {
                }
            }
        }
        issues
    }
    }

static COMMUNICATION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Communication".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Communication".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Communication".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Communication".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "Communication".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Communication".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMMUNICATION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Communication.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Communication.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/event-status|5.0.0".to_string(),
        binding_name: Some("CommunicationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Communication.statusReason".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/communication-not-done-reason".to_string(),
        binding_name: Some("CommunicationNotDoneReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Communication.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/communication-category".to_string(),
        binding_name: Some("CommunicationCategory".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Communication.priority".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/request-priority|5.0.0".to_string(),
        binding_name: Some("CommunicationPriority".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Communication.medium".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://terminology.hl7.org/ValueSet/v3-ParticipationMode".to_string(),
        binding_name: Some("CommunicationMedium".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Communication.topic".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/communication-topic".to_string(),
        binding_name: Some("CommunicationTopic".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Communication.reason".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/clinical-findings".to_string(),
        binding_name: Some("CommunicationReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for Communication {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMMUNICATION_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.part_of {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.partOf[{idx}]")));
            }
        }
        if let Some(values) = &self.in_response_to {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.inResponseTo[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.subject"));
        }
        if let Some(values) = &self.about {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.about[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.encounter"));
        }
        if let Some(values) = &self.recipient {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.recipient[{idx}]")));
            }
        }
        if let Some(value) = &self.sender {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.sender"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMMUNICATION_INVARIANTS.as_slice(), evaluator, "Communication"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.identifier"));
            }
        }
        if let Some(values) = &self.instantiates_canonical {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.instantiatesCanonical", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.instantiatesCanonical"));
            }
        }
        if let Some(values) = &self.instantiates_uri {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.instantiatesUri", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.instantiatesUri"));
            }
        }
        if let Some(values) = &self.based_on {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.basedOn", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.basedOn"));
            }
        }
        if let Some(values) = &self.part_of {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.partOf", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.partOf"));
            }
        }
        if let Some(values) = &self.in_response_to {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.inResponseTo", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.inResponseTo"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.category"));
            }
        }
        if let Some(values) = &self.medium {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.medium", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.medium"));
            }
        }
        if let Some(values) = &self.about {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.about", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.about"));
            }
        }
        if let Some(values) = &self.recipient {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.recipient", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.recipient"));
            }
        }
        if let Some(values) = &self.reason {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.reason", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.reason"));
            }
        }
        if let Some(values) = &self.payload {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.payload", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.payload"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Communication.note"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.part_of {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.partOf[{idx}]")));
            }
        }
        if let Some(values) = &self.in_response_to {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.inResponseTo[{idx}]")));
            }
        }
        if let Some(value) = &self.status_reason {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.statusReason"));
        }
        if let Some(values) = &self.category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.category[{idx}]")));
            }
        }
        if let Some(values) = &self.medium {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.medium[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.subject"));
        }
        if let Some(value) = &self.topic {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.topic"));
        }
        if let Some(values) = &self.about {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.about[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.encounter"));
        }
        if let Some(values) = &self.recipient {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.recipient[{idx}]")));
            }
        }
        if let Some(value) = &self.sender {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.sender"));
        }
        if let Some(values) = &self.reason {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.reason[{idx}]")));
            }
        }
        if let Some(values) = &self.payload {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.payload[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.note[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for Communication {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMMUNICATION_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.part_of {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.partOf[{idx}]")));
            }
        }
        if let Some(values) = &self.in_response_to {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.inResponseTo[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.subject"));
        }
        if let Some(values) = &self.about {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.about[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.encounter"));
        }
        if let Some(values) = &self.recipient {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.recipient[{idx}]")));
            }
        }
        if let Some(value) = &self.sender {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Communication.sender"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Communication.contained[{idx}]")));
            }
        }
        issues
    }
    }

static COMMUNICATION_PAYLOAD_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Communication.payload".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CommunicationPayload {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.content {
            match choice {
                CommunicationPayloadContent::Attachment(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationPayloadContent::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationPayloadContent::CodeableConcept(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMMUNICATION_PAYLOAD_INVARIANTS.as_slice(), evaluator, "Communication.payload"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.payload.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("payload.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Communication.payload.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("payload.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("payload.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("payload.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.content {
            match choice {
                CommunicationPayloadContent::Attachment(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationPayloadContent::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationPayloadContent::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CommunicationPayload {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.content {
            match choice {
                CommunicationPayloadContent::Attachment(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationPayloadContent::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationPayloadContent::CodeableConcept(_value) => {
                }
            }
        }
        issues
    }
    }

static COMMUNICATION_REQUEST_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CommunicationRequest".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CommunicationRequest".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CommunicationRequest".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CommunicationRequest".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "CommunicationRequest".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CommunicationRequest".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMMUNICATION_REQUEST_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/request-status|5.0.0".to_string(),
        binding_name: Some("CommunicationRequestStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.statusReason".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/communication-request-status-reason".to_string(),
        binding_name: Some("CommunicationRequestStatusReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.intent".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/request-intent|5.0.0".to_string(),
        binding_name: Some("CommunicationRequestIntent".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/communication-category".to_string(),
        binding_name: Some("CommunicationCategory".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.priority".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/request-priority|5.0.0".to_string(),
        binding_name: Some("CommunicationPriority".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.medium".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://terminology.hl7.org/ValueSet/v3-ParticipationMode".to_string(),
        binding_name: Some("CommunicationMedium".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CommunicationRequest.reason".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://terminology.hl7.org/ValueSet/v3-ActReason".to_string(),
        binding_name: Some("CommunicationReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CommunicationRequest {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMMUNICATION_REQUEST_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.replaces {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.replaces[{idx}]")));
            }
        }
        if let Some(value) = &self.group_identifier {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.groupIdentifier"));
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.subject"));
        }
        if let Some(values) = &self.about {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.about[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.encounter"));
        }
        if let Some(value) = &self.requester {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.requester"));
        }
        if let Some(values) = &self.recipient {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.recipient[{idx}]")));
            }
        }
        if let Some(values) = &self.information_provider {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.informationProvider[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurrence {
            match choice {
                CommunicationRequestOccurrence::DateTime(_value) => {
                }
                CommunicationRequestOccurrence::Period(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMMUNICATION_REQUEST_INVARIANTS.as_slice(), evaluator, "CommunicationRequest"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.identifier"));
            }
        }
        if let Some(values) = &self.based_on {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.basedOn", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.basedOn"));
            }
        }
        if let Some(values) = &self.replaces {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.replaces", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.replaces"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.category"));
            }
        }
        if let Some(values) = &self.medium {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.medium", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.medium"));
            }
        }
        if let Some(values) = &self.about {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.about", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.about"));
            }
        }
        if let Some(values) = &self.payload {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.payload", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.payload"));
            }
        }
        if let Some(values) = &self.recipient {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.recipient", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.recipient"));
            }
        }
        if let Some(values) = &self.information_provider {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.informationProvider", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.informationProvider"));
            }
        }
        if let Some(values) = &self.reason {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.reason", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.reason"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CommunicationRequest.note"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.replaces {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.replaces[{idx}]")));
            }
        }
        if let Some(value) = &self.group_identifier {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.groupIdentifier"));
        }
        if let Some(value) = &self.status_reason {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.statusReason"));
        }
        if let Some(values) = &self.category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.category[{idx}]")));
            }
        }
        if let Some(values) = &self.medium {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.medium[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.subject"));
        }
        if let Some(values) = &self.about {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.about[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.encounter"));
        }
        if let Some(values) = &self.payload {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.payload[{idx}]")));
            }
        }
        if let Some(value) = &self.requester {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.requester"));
        }
        if let Some(values) = &self.recipient {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.recipient[{idx}]")));
            }
        }
        if let Some(values) = &self.information_provider {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.informationProvider[{idx}]")));
            }
        }
        if let Some(values) = &self.reason {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.reason[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.note[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurrence {
            match choice {
                CommunicationRequestOccurrence::DateTime(_value) => {
                }
                CommunicationRequestOccurrence::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.occurrence[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CommunicationRequest {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMMUNICATION_REQUEST_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.replaces {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.replaces[{idx}]")));
            }
        }
        if let Some(value) = &self.group_identifier {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.groupIdentifier"));
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.subject"));
        }
        if let Some(values) = &self.about {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.about[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.encounter"));
        }
        if let Some(value) = &self.requester {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CommunicationRequest.requester"));
        }
        if let Some(values) = &self.recipient {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.recipient[{idx}]")));
            }
        }
        if let Some(values) = &self.information_provider {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.informationProvider[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CommunicationRequest.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurrence {
            match choice {
                CommunicationRequestOccurrence::DateTime(_value) => {
                }
                CommunicationRequestOccurrence::Period(_value) => {
                }
            }
        }
        issues
    }
    }

static COMMUNICATION_REQUEST_PAYLOAD_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CommunicationRequest.payload".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CommunicationRequestPayload {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.content {
            match choice {
                CommunicationRequestPayloadContent::Attachment(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationRequestPayloadContent::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationRequestPayloadContent::CodeableConcept(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMMUNICATION_REQUEST_PAYLOAD_INVARIANTS.as_slice(), evaluator, "CommunicationRequest.payload"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.payload.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("payload.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CommunicationRequest.payload.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("payload.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("payload.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("payload.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.content {
            match choice {
                CommunicationRequestPayloadContent::Attachment(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationRequestPayloadContent::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationRequestPayloadContent::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CommunicationRequestPayload {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.content {
            match choice {
                CommunicationRequestPayloadContent::Attachment(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationRequestPayloadContent::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "payload.content[x]"));
                }
                CommunicationRequestPayloadContent::CodeableConcept(_value) => {
                }
            }
        }
        issues
    }
    }

static COMPARTMENT_DEFINITION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "cnl-0".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "CompartmentDefinition".to_string(),
        expression: "name.exists() implies name.matches('^[A-Z]([A-Za-z0-9_]){1,254}$')".to_string(),
        human: "Name should be usable as an identifier for the module by machine processing applications such as code generation".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CompartmentDefinition".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CompartmentDefinition".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CompartmentDefinition".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CompartmentDefinition".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "CompartmentDefinition".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CompartmentDefinition".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMPARTMENT_DEFINITION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "CompartmentDefinition.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CompartmentDefinition.versionAlgorithm[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/version-algorithm".to_string(),
        binding_name: None,
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["string".to_string(), "Coding".to_string()]),
    },
    fhir_validation_types::BindingDef {
        path: "CompartmentDefinition.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/publication-status|5.0.0".to_string(),
        binding_name: Some("PublicationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "CompartmentDefinition.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/compartment-type|5.0.0".to_string(),
        binding_name: Some("CompartmentType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CompartmentDefinition {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMPARTMENT_DEFINITION_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CompartmentDefinition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "CompartmentDefinition.text"));
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.resource {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.resource[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.version_algorithm {
            match choice {
                CompartmentDefinitionVersionAlgorithm::String(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "CompartmentDefinition.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_primitive_value_binding(&binding_ctx, value.value.as_deref(), |value| helios_fhir::r5::terminology::validate_code("http://hl7.org/fhir/ValueSet/version-algorithm", value));
                    issues.extend(child_issues);
                }
                CompartmentDefinitionVersionAlgorithm::Coding(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "CompartmentDefinition.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_coding_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_coding("http://hl7.org/fhir/ValueSet/version-algorithm", value));
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMPARTMENT_DEFINITION_INVARIANTS.as_slice(), evaluator, "CompartmentDefinition"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CompartmentDefinition.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CompartmentDefinition.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CompartmentDefinition.modifierExtension"));
            }
        }
        if let Some(values) = &self.contact {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.contact", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CompartmentDefinition.contact"));
            }
        }
        if let Some(values) = &self.use_context {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.useContext", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CompartmentDefinition.useContext"));
            }
        }
        if let Some(values) = &self.resource {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.resource", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("CompartmentDefinition.resource"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CompartmentDefinition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "CompartmentDefinition.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.contact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.contact[{idx}]")));
            }
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.resource {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.resource[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.version_algorithm {
            match choice {
                CompartmentDefinitionVersionAlgorithm::String(_value) => {
                }
                CompartmentDefinitionVersionAlgorithm::Coding(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "CompartmentDefinition.versionAlgorithm[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CompartmentDefinition {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMPARTMENT_DEFINITION_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CompartmentDefinition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "CompartmentDefinition.text"));
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.resource {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.resource[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("CompartmentDefinition.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.version_algorithm {
            match choice {
                CompartmentDefinitionVersionAlgorithm::String(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "CompartmentDefinition.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_primitive_value_binding_async(&binding_ctx, value.value.as_deref(), |value| helios_fhir::r5::terminology::validate_code("http://hl7.org/fhir/ValueSet/version-algorithm", value)).await;
                    issues.extend(child_issues);
                }
                CompartmentDefinitionVersionAlgorithm::Coding(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "CompartmentDefinition.versionAlgorithm[x]", "http://hl7.org/fhir/ValueSet/version-algorithm", fhir_validation_types::BindingStrength::Extensible, terminology);
                    let child_issues = fhir_validation::r5::validate_coding_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_coding("http://hl7.org/fhir/ValueSet/version-algorithm", value)).await;
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }
    }

static COMPARTMENT_DEFINITION_RESOURCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "CompartmentDefinition.resource".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMPARTMENT_DEFINITION_RESOURCE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "CompartmentDefinition.resource.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/resource-types|5.0.0".to_string(),
        binding_name: Some("ResourceType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CompartmentDefinitionResource {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMPARTMENT_DEFINITION_RESOURCE_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMPARTMENT_DEFINITION_RESOURCE_INVARIANTS.as_slice(), evaluator, "CompartmentDefinition.resource"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.resource.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.resource.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.modifierExtension"));
            }
        }
        if let Some(values) = &self.param {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "CompartmentDefinition.resource.param", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.param"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("resource.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("resource.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CompartmentDefinitionResource {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMPARTMENT_DEFINITION_RESOURCE_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static COMPOSITION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "Composition".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMPOSITION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Composition.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Composition.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/composition-status|5.0.0".to_string(),
        binding_name: Some("CompositionStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Composition.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/doc-typecodes".to_string(),
        binding_name: Some("DocumentType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Composition.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/referenced-item-category".to_string(),
        binding_name: Some("DocumentCategory".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for Composition {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMPOSITION_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.subject {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.subject[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.encounter"));
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.author {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.author[{idx}]")));
            }
        }
        if let Some(values) = &self.attester {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.attester[{idx}]")));
            }
        }
        if let Some(value) = &self.custodian {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.custodian"));
        }
        if let Some(values) = &self.relates_to {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.relatesTo[{idx}]")));
            }
        }
        if let Some(values) = &self.event {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.event[{idx}]")));
            }
        }
        if let Some(values) = &self.section {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.section[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMPOSITION_INVARIANTS.as_slice(), evaluator, "Composition"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.identifier"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.category"));
            }
        }
        if let Some(values) = &self.subject {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.subject", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.subject"));
            }
        }
        if let Some(values) = &self.use_context {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.useContext", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.useContext"));
            }
        }
        if let Some(values) = &self.author {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.author", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.author"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.note"));
            }
        }
        if let Some(values) = &self.attester {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.attester", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.attester"));
            }
        }
        if let Some(values) = &self.relates_to {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.relatesTo", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.relatesTo"));
            }
        }
        if let Some(values) = &self.event {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.event", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.event"));
            }
        }
        if let Some(values) = &self.section {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.section", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Composition.section"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.identifier[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "Composition.type"));
        if let Some(values) = &self.category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.category[{idx}]")));
            }
        }
        if let Some(values) = &self.subject {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.subject[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.encounter"));
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.author {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.author[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.note[{idx}]")));
            }
        }
        if let Some(values) = &self.attester {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.attester[{idx}]")));
            }
        }
        if let Some(value) = &self.custodian {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.custodian"));
        }
        if let Some(values) = &self.relates_to {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.relatesTo[{idx}]")));
            }
        }
        if let Some(values) = &self.event {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.event[{idx}]")));
            }
        }
        if let Some(values) = &self.section {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.section[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for Composition {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMPOSITION_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.subject {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.subject[{idx}]")));
            }
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.encounter"));
        }
        if let Some(values) = &self.use_context {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.useContext[{idx}]")));
            }
        }
        if let Some(values) = &self.author {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.author[{idx}]")));
            }
        }
        if let Some(values) = &self.attester {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.attester[{idx}]")));
            }
        }
        if let Some(value) = &self.custodian {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Composition.custodian"));
        }
        if let Some(values) = &self.relates_to {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.relatesTo[{idx}]")));
            }
        }
        if let Some(values) = &self.event {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.event[{idx}]")));
            }
        }
        if let Some(values) = &self.section {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.section[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Composition.contained[{idx}]")));
            }
        }
        issues
    }
    }

static COMPOSITION_ATTESTER_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition.attester".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMPOSITION_ATTESTER_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Composition.attester.mode".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/composition-attestation-mode".to_string(),
        binding_name: Some("CompositionAttestationMode".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CompositionAttester {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMPOSITION_ATTESTER_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.party {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "attester.party"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMPOSITION_ATTESTER_INVARIANTS.as_slice(), evaluator, "Composition.attester"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.attester.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("attester.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.attester.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("attester.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("attester.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("attester.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.mode.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "attester.mode"));
        if let Some(value) = &self.party {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "attester.party"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CompositionAttester {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMPOSITION_ATTESTER_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.party {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "attester.party"));
        }
        issues
    }
    }

static COMPOSITION_EVENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition.event".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMPOSITION_EVENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Composition.event.detail".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://terminology.hl7.org/ValueSet/v3-ActCode".to_string(),
        binding_name: Some("DocumentEventType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CompositionEvent {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMPOSITION_EVENT_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMPOSITION_EVENT_INVARIANTS.as_slice(), evaluator, "Composition.event"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.event.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("event.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.event.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("event.modifierExtension"));
            }
        }
        if let Some(values) = &self.detail {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.event.detail", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("event.detail"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("event.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("event.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.period {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "event.period"));
        }
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("event.detail[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CompositionEvent {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMPOSITION_EVENT_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static COMPOSITION_SECTION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "cmp-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition.section".to_string(),
        expression: "text.exists() or entry.exists() or section.exists()".to_string(),
        human: "A section must contain at least one of text, entries, or sub-sections".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "cmp-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition.section".to_string(),
        expression: "emptyReason.empty() or entry.empty()".to_string(),
        human: "A section can only have an emptyReason if it is empty".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Composition.section".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static COMPOSITION_SECTION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Composition.section.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/doc-section-codes".to_string(),
        binding_name: Some("CompositionSectionType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Composition.section.orderedBy".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/list-order".to_string(),
        binding_name: Some("SectionEntryOrder".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Composition.section.emptyReason".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/list-empty-reason".to_string(),
        binding_name: Some("SectionEmptyReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for CompositionSection {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, COMPOSITION_SECTION_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.author {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.author[{idx}]")));
            }
        }
        if let Some(value) = &self.focus {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "section.focus"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "section.text"));
        }
        if let Some(values) = &self.entry {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.entry[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, COMPOSITION_SECTION_INVARIANTS.as_slice(), evaluator, "Composition.section"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.section.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("section.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.section.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("section.modifierExtension"));
            }
        }
        if let Some(values) = &self.author {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.section.author", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("section.author"));
            }
        }
        if let Some(values) = &self.entry {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.section.entry", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("section.entry"));
            }
        }
        if let Some(values) = &self.section {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Composition.section.section", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("section.section"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.code {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "section.code"));
        }
        if let Some(values) = &self.author {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.author[{idx}]")));
            }
        }
        if let Some(value) = &self.focus {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "section.focus"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "section.text"));
        }
        if let Some(value) = &self.ordered_by {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "section.orderedBy"));
        }
        if let Some(values) = &self.entry {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.entry[{idx}]")));
            }
        }
        if let Some(value) = &self.empty_reason {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "section.emptyReason"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for CompositionSection {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, COMPOSITION_SECTION_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.author {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.author[{idx}]")));
            }
        }
        if let Some(value) = &self.focus {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "section.focus"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "section.text"));
        }
        if let Some(values) = &self.entry {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("section.entry[{idx}]")));
            }
        }
        issues
    }
    }

