// @generated by fhir-validation-gen
// FHIR version: FHIR R5
// shard parts/part_012.rs

static IMPLEMENTATION_GUIDE_GLOBAL_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.global".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static IMPLEMENTATION_GUIDE_GLOBAL_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ImplementationGuide.global.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/resource-types|5.0.0".to_string(),
        binding_name: Some("ResourceType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideGlobal {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, IMPLEMENTATION_GUIDE_GLOBAL_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_GLOBAL_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.global"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.global.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("global.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.global.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("global.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("global.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("global.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideGlobal {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, IMPLEMENTATION_GUIDE_GLOBAL_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static IMPLEMENTATION_GUIDE_DEFINITION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ig-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition".to_string(),
        expression: "resource.groupingId.all(%context.grouping.id contains $this)".to_string(),
        human: "If a resource has a groupingId, it must refer to a grouping defined in the Implementation Guide".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideDefinition {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.resource {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.resource[{idx}]")));
            }
        }
        if let Some(value) = &self.page {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "definition.page"));
        }
        if let Some(values) = &self.parameter {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.parameter[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_DEFINITION_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.definition"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("definition.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("definition.modifierExtension"));
            }
        }
        if let Some(values) = &self.grouping {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.grouping", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("definition.grouping"));
            }
        }
        if let Some(values) = &self.resource {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.resource", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("definition.resource"));
            }
        }
        if let Some(values) = &self.parameter {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.parameter", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("definition.parameter"));
            }
        }
        if let Some(values) = &self.template {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.template", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("definition.template"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.grouping {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.grouping[{idx}]")));
            }
        }
        if let Some(values) = &self.resource {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.resource[{idx}]")));
            }
        }
        if let Some(value) = &self.page {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "definition.page"));
        }
        if let Some(values) = &self.parameter {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.parameter[{idx}]")));
            }
        }
        if let Some(values) = &self.template {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.template[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideDefinition {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.resource {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.resource[{idx}]")));
            }
        }
        if let Some(value) = &self.page {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "definition.page"));
        }
        if let Some(values) = &self.parameter {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("definition.parameter[{idx}]")));
            }
        }
        issues
    }
    }

static IMPLEMENTATION_GUIDE_DEFINITION_GROUPING_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition.grouping".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideDefinitionGrouping {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_DEFINITION_GROUPING_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.definition.grouping"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.grouping.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("grouping.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.grouping.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("grouping.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("grouping.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("grouping.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideDefinitionGrouping {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static IMPLEMENTATION_GUIDE_DEFINITION_RESOURCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition.resource".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static IMPLEMENTATION_GUIDE_DEFINITION_RESOURCE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ImplementationGuide.definition.resource.fhirVersion".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/FHIR-version|5.0.0".to_string(),
        binding_name: Some("FHIRVersion".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideDefinitionResource {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, IMPLEMENTATION_GUIDE_DEFINITION_RESOURCE_BINDINGS.as_slice(), terminology));

        let child_issues = self.reference.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "resource.reference"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_DEFINITION_RESOURCE_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.definition.resource"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.resource.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.resource.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.modifierExtension"));
            }
        }
        if let Some(values) = &self.fhir_version {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.resource.fhirVersion", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.fhirVersion"));
            }
        }
        if let Some(values) = &self.profile {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.resource.profile", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.profile"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("resource.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("resource.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.reference.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "resource.reference"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideDefinitionResource {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, IMPLEMENTATION_GUIDE_DEFINITION_RESOURCE_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.reference.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "resource.reference"));
        issues
    }
    }

static IMPLEMENTATION_GUIDE_DEFINITION_PAGE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition.page".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ig-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition.page".to_string(),
        expression: "generation='generated' implies source.empty()".to_string(),
        human: "Source must be absent if 'generated' is generated".to_string(),
    },
]);

static IMPLEMENTATION_GUIDE_DEFINITION_PAGE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ImplementationGuide.definition.page.generation".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/guide-page-generation|5.0.0".to_string(),
        binding_name: Some("GuidePageGeneration".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideDefinitionPage {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, IMPLEMENTATION_GUIDE_DEFINITION_PAGE_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.source {
            match choice {
                ImplementationGuideDefinitionPageSource::Url(_value) => {
                }
                ImplementationGuideDefinitionPageSource::String(_value) => {
                }
                ImplementationGuideDefinitionPageSource::Markdown(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_DEFINITION_PAGE_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.definition.page"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.page.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("page.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.page.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("page.modifierExtension"));
            }
        }
        if let Some(values) = &self.page {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.page.page", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("page.page"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("page.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("page.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.source {
            match choice {
                ImplementationGuideDefinitionPageSource::Url(_value) => {
                }
                ImplementationGuideDefinitionPageSource::String(_value) => {
                }
                ImplementationGuideDefinitionPageSource::Markdown(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideDefinitionPage {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, IMPLEMENTATION_GUIDE_DEFINITION_PAGE_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.source {
            match choice {
                ImplementationGuideDefinitionPageSource::Url(_value) => {
                }
                ImplementationGuideDefinitionPageSource::String(_value) => {
                }
                ImplementationGuideDefinitionPageSource::Markdown(_value) => {
                }
            }
        }
        issues
    }
    }

static IMPLEMENTATION_GUIDE_DEFINITION_PARAMETER_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition.parameter".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static IMPLEMENTATION_GUIDE_DEFINITION_PARAMETER_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ImplementationGuide.definition.parameter.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/guide-parameter-code".to_string(),
        binding_name: None,
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideDefinitionParameter {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, IMPLEMENTATION_GUIDE_DEFINITION_PARAMETER_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_DEFINITION_PARAMETER_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.definition.parameter"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.parameter.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("parameter.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.parameter.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("parameter.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("parameter.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("parameter.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "parameter.code"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideDefinitionParameter {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, IMPLEMENTATION_GUIDE_DEFINITION_PARAMETER_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static IMPLEMENTATION_GUIDE_DEFINITION_TEMPLATE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.definition.template".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideDefinitionTemplate {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_DEFINITION_TEMPLATE_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.definition.template"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.template.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("template.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.definition.template.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("template.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("template.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("template.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideDefinitionTemplate {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static IMPLEMENTATION_GUIDE_MANIFEST_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.manifest".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideManifest {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_MANIFEST_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.manifest"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manifest.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manifest.modifierExtension"));
            }
        }
        if let Some(values) = &self.resource {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.resource", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manifest.resource"));
            }
        }
        if let Some(values) = &self.page {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.page", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manifest.page"));
            }
        }
        if let Some(values) = &self.image {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.image", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manifest.image"));
            }
        }
        if let Some(values) = &self.other {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.other", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manifest.other"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("manifest.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("manifest.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.resource {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("manifest.resource[{idx}]")));
            }
        }
        if let Some(values) = &self.page {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("manifest.page[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideManifest {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static IMPLEMENTATION_GUIDE_MANIFEST_RESOURCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.manifest.resource".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideManifestResource {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.reference.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "resource.reference"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_MANIFEST_RESOURCE_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.manifest.resource"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.resource.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.resource.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.modifierExtension"));
            }
        }
        if let Some(values) = &self.profile {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.resource.profile", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("resource.profile"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("resource.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("resource.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.reference.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "resource.reference"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideManifestResource {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.reference.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "resource.reference"));
        issues
    }
    }

static IMPLEMENTATION_GUIDE_MANIFEST_PAGE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ImplementationGuide.manifest.page".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ImplementationGuideManifestPage {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, IMPLEMENTATION_GUIDE_MANIFEST_PAGE_INVARIANTS.as_slice(), evaluator, "ImplementationGuide.manifest.page"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.page.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("page.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.page.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("page.modifierExtension"));
            }
        }
        if let Some(values) = &self.anchor {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ImplementationGuide.manifest.page.anchor", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("page.anchor"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("page.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("page.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ImplementationGuideManifestPage {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static INGREDIENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "Ingredient".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ing-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient".to_string(),
        expression: "Ingredient.where(allergenicIndicator=true).count() + Ingredient.substance.code.reference.count()  < 2".to_string(),
        human: "If an ingredient is noted as an allergen (allergenicIndicator) then its substance should be a code. If the substance is a SubstanceDefinition, then the allegen information should be documented in that resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INGREDIENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Ingredient.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Ingredient.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/publication-status|5.0.0".to_string(),
        binding_name: Some("PublicationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Ingredient.role".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/ingredient-role".to_string(),
        binding_name: Some("IngredientRole".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Ingredient.function".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/ingredient-function".to_string(),
        binding_name: Some("IngredientFunction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for Ingredient {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INGREDIENT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.text"));
        }
        if let Some(value) = &self.identifier {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.identifier"));
        }
        if let Some(values) = &self.r#for {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.for[{idx}]")));
            }
        }
        if let Some(values) = &self.manufacturer {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.manufacturer[{idx}]")));
            }
        }
        let child_issues = self.substance.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.substance"));

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INGREDIENT_INVARIANTS.as_slice(), evaluator, "Ingredient"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Ingredient.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Ingredient.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Ingredient.modifierExtension"));
            }
        }
        if let Some(values) = &self.r#for {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.for", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Ingredient.for"));
            }
        }
        if let Some(values) = &self.function {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.function", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Ingredient.function"));
            }
        }
        if let Some(values) = &self.manufacturer {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.manufacturer", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Ingredient.manufacturer"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.identifier {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.identifier"));
        }
        if let Some(values) = &self.r#for {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.for[{idx}]")));
            }
        }
        let child_issues = self.role.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.role"));
        if let Some(values) = &self.function {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.function[{idx}]")));
            }
        }
        if let Some(value) = &self.group {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.group"));
        }
        if let Some(values) = &self.manufacturer {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.manufacturer[{idx}]")));
            }
        }
        let child_issues = self.substance.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.substance"));

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for Ingredient {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INGREDIENT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.text"));
        }
        if let Some(value) = &self.identifier {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.identifier"));
        }
        if let Some(values) = &self.r#for {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.for[{idx}]")));
            }
        }
        if let Some(values) = &self.manufacturer {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.manufacturer[{idx}]")));
            }
        }
        let child_issues = self.substance.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "Ingredient.substance"));

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Ingredient.contained[{idx}]")));
            }
        }
        issues
    }
    }

static INGREDIENT_MANUFACTURER_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient.manufacturer".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INGREDIENT_MANUFACTURER_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Ingredient.manufacturer.role".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/ingredient-manufacturer-role|5.0.0".to_string(),
        binding_name: Some("IngredientManufacturerRole".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for IngredientManufacturer {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INGREDIENT_MANUFACTURER_BINDINGS.as_slice(), terminology));

        let child_issues = self.manufacturer.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "manufacturer.manufacturer"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INGREDIENT_MANUFACTURER_INVARIANTS.as_slice(), evaluator, "Ingredient.manufacturer"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.manufacturer.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manufacturer.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.manufacturer.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("manufacturer.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("manufacturer.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("manufacturer.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.manufacturer.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "manufacturer.manufacturer"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for IngredientManufacturer {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INGREDIENT_MANUFACTURER_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.manufacturer.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "manufacturer.manufacturer"));
        issues
    }
    }

static INGREDIENT_SUBSTANCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient.substance".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INGREDIENT_SUBSTANCE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Ingredient.substance.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/substance-codes".to_string(),
        binding_name: Some("SNOMEDCTSubstanceCodes".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for IngredientSubstance {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INGREDIENT_SUBSTANCE_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.strength {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("substance.strength[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INGREDIENT_SUBSTANCE_INVARIANTS.as_slice(), evaluator, "Ingredient.substance"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("substance.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("substance.modifierExtension"));
            }
        }
        if let Some(values) = &self.strength {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("substance.strength"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("substance.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("substance.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "substance.code"));
        if let Some(values) = &self.strength {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("substance.strength[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for IngredientSubstance {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INGREDIENT_SUBSTANCE_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.strength {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("substance.strength[{idx}]")));
            }
        }
        issues
    }
    }

static INGREDIENT_SUBSTANCE_STRENGTH_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient.substance.strength".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INGREDIENT_SUBSTANCE_STRENGTH_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Ingredient.substance.strength.country".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/country".to_string(),
        binding_name: Some("Country".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for IngredientSubstanceStrength {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INGREDIENT_SUBSTANCE_STRENGTH_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.reference_strength {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("strength.referenceStrength[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.presentation {
            match choice {
                IngredientSubstanceStrengthPresentation::Ratio(_value) => {
                }
                IngredientSubstanceStrengthPresentation::RatioRange(_value) => {
                }
                IngredientSubstanceStrengthPresentation::CodeableConcept(_value) => {
                }
                IngredientSubstanceStrengthPresentation::Quantity(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.presentation[x]"));
                }
            }
        }
        if let Some(choice) = &self.concentration {
            match choice {
                IngredientSubstanceStrengthConcentration::Ratio(_value) => {
                }
                IngredientSubstanceStrengthConcentration::RatioRange(_value) => {
                }
                IngredientSubstanceStrengthConcentration::CodeableConcept(_value) => {
                }
                IngredientSubstanceStrengthConcentration::Quantity(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.concentration[x]"));
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INGREDIENT_SUBSTANCE_STRENGTH_INVARIANTS.as_slice(), evaluator, "Ingredient.substance.strength"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("strength.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("strength.modifierExtension"));
            }
        }
        if let Some(values) = &self.country {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength.country", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("strength.country"));
            }
        }
        if let Some(values) = &self.reference_strength {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength.referenceStrength", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("strength.referenceStrength"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("strength.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("strength.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.basis {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "strength.basis"));
        }
        if let Some(values) = &self.country {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("strength.country[{idx}]")));
            }
        }
        if let Some(values) = &self.reference_strength {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("strength.referenceStrength[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.presentation {
            match choice {
                IngredientSubstanceStrengthPresentation::Ratio(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.presentation[x]"));
                }
                IngredientSubstanceStrengthPresentation::RatioRange(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.presentation[x]"));
                }
                IngredientSubstanceStrengthPresentation::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.presentation[x]"));
                }
                IngredientSubstanceStrengthPresentation::Quantity(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.presentation[x]"));
                }
            }
        }
        if let Some(choice) = &self.concentration {
            match choice {
                IngredientSubstanceStrengthConcentration::Ratio(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.concentration[x]"));
                }
                IngredientSubstanceStrengthConcentration::RatioRange(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.concentration[x]"));
                }
                IngredientSubstanceStrengthConcentration::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.concentration[x]"));
                }
                IngredientSubstanceStrengthConcentration::Quantity(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.concentration[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for IngredientSubstanceStrength {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INGREDIENT_SUBSTANCE_STRENGTH_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.reference_strength {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("strength.referenceStrength[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.presentation {
            match choice {
                IngredientSubstanceStrengthPresentation::Ratio(_value) => {
                }
                IngredientSubstanceStrengthPresentation::RatioRange(_value) => {
                }
                IngredientSubstanceStrengthPresentation::CodeableConcept(_value) => {
                }
                IngredientSubstanceStrengthPresentation::Quantity(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.presentation[x]"));
                }
            }
        }
        if let Some(choice) = &self.concentration {
            match choice {
                IngredientSubstanceStrengthConcentration::Ratio(_value) => {
                }
                IngredientSubstanceStrengthConcentration::RatioRange(_value) => {
                }
                IngredientSubstanceStrengthConcentration::CodeableConcept(_value) => {
                }
                IngredientSubstanceStrengthConcentration::Quantity(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "strength.concentration[x]"));
                }
            }
        }
        issues
    }
    }

static INGREDIENT_SUBSTANCE_STRENGTH_REFERENCE_STRENGTH_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Ingredient.substance.strength.referenceStrength".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INGREDIENT_SUBSTANCE_STRENGTH_REFERENCE_STRENGTH_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Ingredient.substance.strength.referenceStrength.substance".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/substance-codes".to_string(),
        binding_name: Some("SNOMEDCTSubstanceCodes".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Ingredient.substance.strength.referenceStrength.country".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/country".to_string(),
        binding_name: Some("Country".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for IngredientSubstanceStrengthReferenceStrength {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INGREDIENT_SUBSTANCE_STRENGTH_REFERENCE_STRENGTH_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.strength {
            match choice {
                IngredientSubstanceStrengthReferenceStrengthStrength::Ratio(_value) => {
                }
                IngredientSubstanceStrengthReferenceStrengthStrength::RatioRange(_value) => {
                }
                IngredientSubstanceStrengthReferenceStrengthStrength::Quantity(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "referenceStrength.strength[x]"));
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INGREDIENT_SUBSTANCE_STRENGTH_REFERENCE_STRENGTH_INVARIANTS.as_slice(), evaluator, "Ingredient.substance.strength.referenceStrength"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength.referenceStrength.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("referenceStrength.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength.referenceStrength.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("referenceStrength.modifierExtension"));
            }
        }
        if let Some(values) = &self.country {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Ingredient.substance.strength.referenceStrength.country", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("referenceStrength.country"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("referenceStrength.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("referenceStrength.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.substance.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "referenceStrength.substance"));
        if let Some(values) = &self.country {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("referenceStrength.country[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.strength {
            match choice {
                IngredientSubstanceStrengthReferenceStrengthStrength::Ratio(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "referenceStrength.strength[x]"));
                }
                IngredientSubstanceStrengthReferenceStrengthStrength::RatioRange(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "referenceStrength.strength[x]"));
                }
                IngredientSubstanceStrengthReferenceStrengthStrength::Quantity(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "referenceStrength.strength[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for IngredientSubstanceStrengthReferenceStrength {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INGREDIENT_SUBSTANCE_STRENGTH_REFERENCE_STRENGTH_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.strength {
            match choice {
                IngredientSubstanceStrengthReferenceStrengthStrength::Ratio(_value) => {
                }
                IngredientSubstanceStrengthReferenceStrengthStrength::RatioRange(_value) => {
                }
                IngredientSubstanceStrengthReferenceStrengthStrength::Quantity(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "referenceStrength.strength[x]"));
                }
            }
        }
        issues
    }
    }

static INSURANCE_PLAN_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "InsurancePlan".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ipn-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan".to_string(),
        expression: "(identifier.count() + name.count()) > 0".to_string(),
        human: "The organization SHALL at least have a name or an identifier, and possibly more than one".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INSURANCE_PLAN_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "InsurancePlan.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "InsurancePlan.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/publication-status|5.0.0".to_string(),
        binding_name: Some("PublicationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "InsurancePlan.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/insuranceplan-type".to_string(),
        binding_name: Some("InsurancePlanType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlan {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INSURANCE_PLAN_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.owned_by {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.ownedBy"));
        }
        if let Some(value) = &self.administered_by {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.administeredBy"));
        }
        if let Some(values) = &self.coverage_area {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.coverageArea[{idx}]")));
            }
        }
        if let Some(values) = &self.contact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.contact[{idx}]")));
            }
        }
        if let Some(values) = &self.endpoint {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.endpoint[{idx}]")));
            }
        }
        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.network[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_INVARIANTS.as_slice(), evaluator, "InsurancePlan"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.identifier"));
            }
        }
        if let Some(values) = &self.r#type {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.type", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.type"));
            }
        }
        if let Some(values) = &self.alias {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.alias", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.alias"));
            }
        }
        if let Some(values) = &self.coverage_area {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverageArea", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.coverageArea"));
            }
        }
        if let Some(values) = &self.contact {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.contact", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.contact"));
            }
        }
        if let Some(values) = &self.endpoint {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.endpoint", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.endpoint"));
            }
        }
        if let Some(values) = &self.network {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.network", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.network"));
            }
        }
        if let Some(values) = &self.coverage {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.coverage"));
            }
        }
        if let Some(values) = &self.plan {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InsurancePlan.plan"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.r#type {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.type[{idx}]")));
            }
        }
        if let Some(value) = &self.period {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.period"));
        }
        if let Some(value) = &self.owned_by {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.ownedBy"));
        }
        if let Some(value) = &self.administered_by {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.administeredBy"));
        }
        if let Some(values) = &self.coverage_area {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.coverageArea[{idx}]")));
            }
        }
        if let Some(values) = &self.contact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.contact[{idx}]")));
            }
        }
        if let Some(values) = &self.endpoint {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.endpoint[{idx}]")));
            }
        }
        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.network[{idx}]")));
            }
        }
        if let Some(values) = &self.coverage {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.coverage[{idx}]")));
            }
        }
        if let Some(values) = &self.plan {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.plan[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlan {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INSURANCE_PLAN_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.owned_by {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.ownedBy"));
        }
        if let Some(value) = &self.administered_by {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InsurancePlan.administeredBy"));
        }
        if let Some(values) = &self.coverage_area {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.coverageArea[{idx}]")));
            }
        }
        if let Some(values) = &self.contact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.contact[{idx}]")));
            }
        }
        if let Some(values) = &self.endpoint {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.endpoint[{idx}]")));
            }
        }
        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.network[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InsurancePlan.contained[{idx}]")));
            }
        }
        issues
    }
    }

static INSURANCE_PLAN_COVERAGE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.coverage".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanCoverage {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("coverage.network[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_COVERAGE_INVARIANTS.as_slice(), evaluator, "InsurancePlan.coverage"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("coverage.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("coverage.modifierExtension"));
            }
        }
        if let Some(values) = &self.network {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.network", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("coverage.network"));
            }
        }
        if let Some(values) = &self.benefit {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.benefit", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("coverage.benefit"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("coverage.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("coverage.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "coverage.type"));
        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("coverage.network[{idx}]")));
            }
        }
        if let Some(values) = &self.benefit {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("coverage.benefit[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanCoverage {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("coverage.network[{idx}]")));
            }
        }
        issues
    }
    }

static INSURANCE_PLAN_COVERAGE_BENEFIT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.coverage.benefit".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanCoverageBenefit {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_COVERAGE_BENEFIT_INVARIANTS.as_slice(), evaluator, "InsurancePlan.coverage.benefit"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.benefit.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("benefit.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.benefit.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("benefit.modifierExtension"));
            }
        }
        if let Some(values) = &self.limit {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.benefit.limit", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("benefit.limit"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "benefit.type"));
        if let Some(values) = &self.limit {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.limit[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanCoverageBenefit {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static INSURANCE_PLAN_COVERAGE_BENEFIT_LIMIT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.coverage.benefit.limit".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanCoverageBenefitLimit {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.value {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "limit.value"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_COVERAGE_BENEFIT_LIMIT_INVARIANTS.as_slice(), evaluator, "InsurancePlan.coverage.benefit.limit"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.benefit.limit.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("limit.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.coverage.benefit.limit.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("limit.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("limit.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("limit.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.value {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "limit.value"));
        }
        if let Some(value) = &self.code {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "limit.code"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanCoverageBenefitLimit {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.value {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "limit.value"));
        }
        issues
    }
    }

static INSURANCE_PLAN_PLAN_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.plan".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanPlan {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.coverage_area {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.coverageArea[{idx}]")));
            }
        }
        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.network[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_PLAN_INVARIANTS.as_slice(), evaluator, "InsurancePlan.plan"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("plan.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("plan.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("plan.identifier"));
            }
        }
        if let Some(values) = &self.coverage_area {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.coverageArea", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("plan.coverageArea"));
            }
        }
        if let Some(values) = &self.network {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.network", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("plan.network"));
            }
        }
        if let Some(values) = &self.general_cost {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.generalCost", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("plan.generalCost"));
            }
        }
        if let Some(values) = &self.specific_cost {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("plan.specificCost"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "plan.type"));
        }
        if let Some(values) = &self.coverage_area {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.coverageArea[{idx}]")));
            }
        }
        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.network[{idx}]")));
            }
        }
        if let Some(values) = &self.general_cost {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.generalCost[{idx}]")));
            }
        }
        if let Some(values) = &self.specific_cost {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.specificCost[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanPlan {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.coverage_area {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.coverageArea[{idx}]")));
            }
        }
        if let Some(values) = &self.network {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("plan.network[{idx}]")));
            }
        }
        issues
    }
    }

static INSURANCE_PLAN_PLAN_GENERAL_COST_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.plan.generalCost".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanPlanGeneralCost {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.cost {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "generalCost.cost"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_PLAN_GENERAL_COST_INVARIANTS.as_slice(), evaluator, "InsurancePlan.plan.generalCost"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.generalCost.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("generalCost.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.generalCost.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("generalCost.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("generalCost.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("generalCost.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "generalCost.type"));
        }
        if let Some(value) = &self.cost {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "generalCost.cost"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanPlanGeneralCost {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.cost {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "generalCost.cost"));
        }
        issues
    }
    }

static INSURANCE_PLAN_PLAN_SPECIFIC_COST_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.plan.specificCost".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanPlanSpecificCost {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_PLAN_SPECIFIC_COST_INVARIANTS.as_slice(), evaluator, "InsurancePlan.plan.specificCost"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("specificCost.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("specificCost.modifierExtension"));
            }
        }
        if let Some(values) = &self.benefit {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.benefit", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("specificCost.benefit"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("specificCost.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("specificCost.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.category.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "specificCost.category"));
        if let Some(values) = &self.benefit {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("specificCost.benefit[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanPlanSpecificCost {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static INSURANCE_PLAN_PLAN_SPECIFIC_COST_BENEFIT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.plan.specificCost.benefit".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanPlanSpecificCostBenefit {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.cost {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.cost[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_PLAN_SPECIFIC_COST_BENEFIT_INVARIANTS.as_slice(), evaluator, "InsurancePlan.plan.specificCost.benefit"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.benefit.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("benefit.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.benefit.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("benefit.modifierExtension"));
            }
        }
        if let Some(values) = &self.cost {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.benefit.cost", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("benefit.cost"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "benefit.type"));
        if let Some(values) = &self.cost {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.cost[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanPlanSpecificCostBenefit {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.cost {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("benefit.cost[{idx}]")));
            }
        }
        issues
    }
    }

static INSURANCE_PLAN_PLAN_SPECIFIC_COST_BENEFIT_COST_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InsurancePlan.plan.specificCost.benefit.cost".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INSURANCE_PLAN_PLAN_SPECIFIC_COST_BENEFIT_COST_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "InsurancePlan.plan.specificCost.benefit.cost.applicability".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/insuranceplan-applicability|5.0.0".to_string(),
        binding_name: Some("BenefitCostApplicability".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InsurancePlanPlanSpecificCostBenefitCost {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INSURANCE_PLAN_PLAN_SPECIFIC_COST_BENEFIT_COST_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.value {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "cost.value"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INSURANCE_PLAN_PLAN_SPECIFIC_COST_BENEFIT_COST_INVARIANTS.as_slice(), evaluator, "InsurancePlan.plan.specificCost.benefit.cost"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.benefit.cost.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("cost.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.benefit.cost.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("cost.modifierExtension"));
            }
        }
        if let Some(values) = &self.qualifiers {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InsurancePlan.plan.specificCost.benefit.cost.qualifiers", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("cost.qualifiers"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("cost.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("cost.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "cost.type"));
        if let Some(value) = &self.applicability {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "cost.applicability"));
        }
        if let Some(values) = &self.qualifiers {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("cost.qualifiers[{idx}]")));
            }
        }
        if let Some(value) = &self.value {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "cost.value"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InsurancePlanPlanSpecificCostBenefitCost {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INSURANCE_PLAN_PLAN_SPECIFIC_COST_BENEFIT_COST_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.value {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "cost.value"));
        }
        issues
    }
    }

static INVENTORY_ITEM_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "InventoryItem".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INVENTORY_ITEM_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "InventoryItem.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "InventoryItem.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/inventoryitem-status|5.0.0".to_string(),
        binding_name: Some("InventoryItemStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryItem {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INVENTORY_ITEM_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.name {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.name[{idx}]")));
            }
        }
        if let Some(value) = &self.description {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.description"));
        }
        if let Some(value) = &self.net_content {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.netContent"));
        }
        if let Some(value) = &self.product_reference {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.productReference"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_ITEM_INVARIANTS.as_slice(), evaluator, "InventoryItem"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.identifier"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.category"));
            }
        }
        if let Some(values) = &self.code {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.code", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.code"));
            }
        }
        if let Some(values) = &self.name {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.name", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.name"));
            }
        }
        if let Some(values) = &self.responsible_organization {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.responsibleOrganization", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.responsibleOrganization"));
            }
        }
        if let Some(values) = &self.inventory_status {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.inventoryStatus", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.inventoryStatus"));
            }
        }
        if let Some(values) = &self.association {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.association", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.association"));
            }
        }
        if let Some(values) = &self.characteristic {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.characteristic", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryItem.characteristic"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.category[{idx}]")));
            }
        }
        if let Some(values) = &self.code {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.code[{idx}]")));
            }
        }
        if let Some(values) = &self.name {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.name[{idx}]")));
            }
        }
        if let Some(values) = &self.responsible_organization {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.responsibleOrganization[{idx}]")));
            }
        }
        if let Some(value) = &self.description {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.description"));
        }
        if let Some(values) = &self.inventory_status {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.inventoryStatus[{idx}]")));
            }
        }
        if let Some(value) = &self.base_unit {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.baseUnit"));
        }
        if let Some(value) = &self.net_content {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.netContent"));
        }
        if let Some(values) = &self.association {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.association[{idx}]")));
            }
        }
        if let Some(values) = &self.characteristic {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.characteristic[{idx}]")));
            }
        }
        if let Some(value) = &self.instance {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.instance"));
        }
        if let Some(value) = &self.product_reference {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.productReference"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryItem {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INVENTORY_ITEM_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.name {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.name[{idx}]")));
            }
        }
        if let Some(value) = &self.description {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.description"));
        }
        if let Some(value) = &self.net_content {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.netContent"));
        }
        if let Some(value) = &self.product_reference {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryItem.productReference"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryItem.contained[{idx}]")));
            }
        }
        issues
    }
    }

static INVENTORY_ITEM_NAME_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem.name".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INVENTORY_ITEM_NAME_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "InventoryItem.name.nameType".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/inventoryitem-nametype".to_string(),
        binding_name: Some("NameType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "InventoryItem.name.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/languages|5.0.0".to_string(),
        binding_name: Some("NameLanguage".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryItemName {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INVENTORY_ITEM_NAME_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_ITEM_NAME_INVARIANTS.as_slice(), evaluator, "InventoryItem.name"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.name.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("name.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.name.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("name.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("name.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("name.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.name_type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "name.nameType"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryItemName {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INVENTORY_ITEM_NAME_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static INVENTORY_ITEM_RESPONSIBLE_ORGANIZATION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem.responsibleOrganization".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryItemResponsibleOrganization {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.organization.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "responsibleOrganization.organization"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_ITEM_RESPONSIBLE_ORGANIZATION_INVARIANTS.as_slice(), evaluator, "InventoryItem.responsibleOrganization"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.responsibleOrganization.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("responsibleOrganization.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.responsibleOrganization.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("responsibleOrganization.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("responsibleOrganization.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("responsibleOrganization.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.role.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "responsibleOrganization.role"));
        let child_issues = self.organization.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "responsibleOrganization.organization"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryItemResponsibleOrganization {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.organization.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "responsibleOrganization.organization"));
        issues
    }
    }

static INVENTORY_ITEM_DESCRIPTION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem.description".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INVENTORY_ITEM_DESCRIPTION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "InventoryItem.description.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/languages|5.0.0".to_string(),
        binding_name: Some("ItemDescriptionLanguage".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryItemDescription {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INVENTORY_ITEM_DESCRIPTION_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_ITEM_DESCRIPTION_INVARIANTS.as_slice(), evaluator, "InventoryItem.description"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.description.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("description.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.description.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("description.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("description.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("description.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryItemDescription {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INVENTORY_ITEM_DESCRIPTION_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static INVENTORY_ITEM_ASSOCIATION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem.association".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryItemAssociation {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.related_item.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "association.relatedItem"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_ITEM_ASSOCIATION_INVARIANTS.as_slice(), evaluator, "InventoryItem.association"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.association.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("association.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.association.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("association.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("association.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("association.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.association_type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "association.associationType"));
        let child_issues = self.related_item.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "association.relatedItem"));
        let child_issues = self.quantity.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "association.quantity"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryItemAssociation {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.related_item.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "association.relatedItem"));
        issues
    }
    }

static INVENTORY_ITEM_CHARACTERISTIC_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem.characteristic".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryItemCharacteristic {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                InventoryItemCharacteristicValue::String(_value) => {
                }
                InventoryItemCharacteristicValue::Integer(_value) => {
                }
                InventoryItemCharacteristicValue::Decimal(_value) => {
                }
                InventoryItemCharacteristicValue::Boolean(_value) => {
                }
                InventoryItemCharacteristicValue::Url(_value) => {
                }
                InventoryItemCharacteristicValue::DateTime(_value) => {
                }
                InventoryItemCharacteristicValue::Quantity(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Range(_value) => {
                }
                InventoryItemCharacteristicValue::Ratio(_value) => {
                }
                InventoryItemCharacteristicValue::Annotation(_value) => {
                }
                InventoryItemCharacteristicValue::Address(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Duration(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::CodeableConcept(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_ITEM_CHARACTERISTIC_INVARIANTS.as_slice(), evaluator, "InventoryItem.characteristic"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.characteristic.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("characteristic.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.characteristic.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("characteristic.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("characteristic.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("characteristic.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.characteristic_type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.characteristicType"));

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                InventoryItemCharacteristicValue::String(_value) => {
                }
                InventoryItemCharacteristicValue::Integer(_value) => {
                }
                InventoryItemCharacteristicValue::Decimal(_value) => {
                }
                InventoryItemCharacteristicValue::Boolean(_value) => {
                }
                InventoryItemCharacteristicValue::Url(_value) => {
                }
                InventoryItemCharacteristicValue::DateTime(_value) => {
                }
                InventoryItemCharacteristicValue::Quantity(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Range(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Ratio(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Annotation(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Address(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Duration(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryItemCharacteristic {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                InventoryItemCharacteristicValue::String(_value) => {
                }
                InventoryItemCharacteristicValue::Integer(_value) => {
                }
                InventoryItemCharacteristicValue::Decimal(_value) => {
                }
                InventoryItemCharacteristicValue::Boolean(_value) => {
                }
                InventoryItemCharacteristicValue::Url(_value) => {
                }
                InventoryItemCharacteristicValue::DateTime(_value) => {
                }
                InventoryItemCharacteristicValue::Quantity(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Range(_value) => {
                }
                InventoryItemCharacteristicValue::Ratio(_value) => {
                }
                InventoryItemCharacteristicValue::Annotation(_value) => {
                }
                InventoryItemCharacteristicValue::Address(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::Duration(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "characteristic.value[x]"));
                }
                InventoryItemCharacteristicValue::CodeableConcept(_value) => {
                }
            }
        }
        issues
    }
    }

static INVENTORY_ITEM_INSTANCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryItem.instance".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryItemInstance {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("instance.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "instance.subject"));
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "instance.location"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_ITEM_INSTANCE_INVARIANTS.as_slice(), evaluator, "InventoryItem.instance"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.instance.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("instance.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.instance.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("instance.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryItem.instance.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("instance.identifier"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("instance.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("instance.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("instance.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "instance.subject"));
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "instance.location"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryItemInstance {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("instance.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "instance.subject"));
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "instance.location"));
        }
        issues
    }
    }

static INVENTORY_REPORT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryReport".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryReport".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryReport".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryReport".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "InventoryReport".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryReport".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INVENTORY_REPORT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "InventoryReport.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "InventoryReport.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/inventoryreport-status|5.0.0".to_string(),
        binding_name: Some("InventoryReportStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "InventoryReport.countType".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/inventoryreport-counttype|5.0.0".to_string(),
        binding_name: Some("InventoryCountType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryReport {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INVENTORY_REPORT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.reporter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.reporter"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_REPORT_INVARIANTS.as_slice(), evaluator, "InventoryReport"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryReport.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryReport.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryReport.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryReport.identifier"));
            }
        }
        if let Some(values) = &self.inventory_listing {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.inventoryListing", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryReport.inventoryListing"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("InventoryReport.note"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.operation_type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.operationType"));
        }
        if let Some(value) = &self.operation_type_reason {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.operationTypeReason"));
        }
        if let Some(value) = &self.reporter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.reporter"));
        }
        if let Some(value) = &self.reporting_period {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.reportingPeriod"));
        }
        if let Some(values) = &self.inventory_listing {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.inventoryListing[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.note[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryReport {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INVENTORY_REPORT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.reporter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "InventoryReport.reporter"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("InventoryReport.contained[{idx}]")));
            }
        }
        issues
    }
    }

static INVENTORY_REPORT_INVENTORY_LISTING_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryReport.inventoryListing".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryReportInventoryListing {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "inventoryListing.location"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_REPORT_INVENTORY_LISTING_INVARIANTS.as_slice(), evaluator, "InventoryReport.inventoryListing"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.inventoryListing.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("inventoryListing.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.inventoryListing.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("inventoryListing.modifierExtension"));
            }
        }
        if let Some(values) = &self.item {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.inventoryListing.item", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("inventoryListing.item"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("inventoryListing.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("inventoryListing.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "inventoryListing.location"));
        }
        if let Some(value) = &self.item_status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "inventoryListing.itemStatus"));
        }
        if let Some(values) = &self.item {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("inventoryListing.item[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryReportInventoryListing {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "inventoryListing.location"));
        }
        issues
    }
    }

static INVENTORY_REPORT_INVENTORY_LISTING_ITEM_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "InventoryReport.inventoryListing.item".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InventoryReportInventoryListingItem {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.quantity.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "item.quantity"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVENTORY_REPORT_INVENTORY_LISTING_ITEM_INVARIANTS.as_slice(), evaluator, "InventoryReport.inventoryListing.item"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.inventoryListing.item.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("item.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "InventoryReport.inventoryListing.item.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("item.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("item.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("item.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.category {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "item.category"));
        }
        let child_issues = self.quantity.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "item.quantity"));
        let child_issues = self.item.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "item.item"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InventoryReportInventoryListingItem {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.quantity.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "item.quantity"));
        issues
    }
    }

static INVOICE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Invoice".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Invoice".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Invoice".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Invoice".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "Invoice".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Invoice".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static INVOICE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Invoice.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Invoice.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/invoice-status|5.0.0".to_string(),
        binding_name: Some("InvoiceStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for Invoice {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, INVOICE_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.subject"));
        }
        if let Some(value) = &self.recipient {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.recipient"));
        }
        if let Some(value) = &self.issuer {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.issuer"));
        }
        if let Some(value) = &self.account {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.account"));
        }
        if let Some(values) = &self.total_price_component {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.totalPriceComponent[{idx}]")));
            }
        }
        if let Some(value) = &self.total_net {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.totalNet"));
        }
        if let Some(value) = &self.total_gross {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.totalGross"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.period {
            match choice {
                InvoicePeriod::Date(_value) => {
                }
                InvoicePeriod::Period(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVOICE_INVARIANTS.as_slice(), evaluator, "Invoice"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.identifier"));
            }
        }
        if let Some(values) = &self.participant {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.participant", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.participant"));
            }
        }
        if let Some(values) = &self.line_item {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.lineItem", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.lineItem"));
            }
        }
        if let Some(values) = &self.total_price_component {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.totalPriceComponent", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.totalPriceComponent"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Invoice.note"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.type"));
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.subject"));
        }
        if let Some(value) = &self.recipient {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.recipient"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.participant[{idx}]")));
            }
        }
        if let Some(value) = &self.issuer {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.issuer"));
        }
        if let Some(value) = &self.account {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.account"));
        }
        if let Some(values) = &self.line_item {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.lineItem[{idx}]")));
            }
        }
        if let Some(values) = &self.total_price_component {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.totalPriceComponent[{idx}]")));
            }
        }
        if let Some(value) = &self.total_net {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.totalNet"));
        }
        if let Some(value) = &self.total_gross {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.totalGross"));
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.note[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.period {
            match choice {
                InvoicePeriod::Date(_value) => {
                }
                InvoicePeriod::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.period[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for Invoice {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, INVOICE_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.subject"));
        }
        if let Some(value) = &self.recipient {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.recipient"));
        }
        if let Some(value) = &self.issuer {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.issuer"));
        }
        if let Some(value) = &self.account {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.account"));
        }
        if let Some(values) = &self.total_price_component {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.totalPriceComponent[{idx}]")));
            }
        }
        if let Some(value) = &self.total_net {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.totalNet"));
        }
        if let Some(value) = &self.total_gross {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Invoice.totalGross"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Invoice.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.period {
            match choice {
                InvoicePeriod::Date(_value) => {
                }
                InvoicePeriod::Period(_value) => {
                }
            }
        }
        issues
    }
    }

static INVOICE_PARTICIPANT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Invoice.participant".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for InvoiceParticipant {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.actor.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, INVOICE_PARTICIPANT_INVARIANTS.as_slice(), evaluator, "Invoice.participant"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.participant.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Invoice.participant.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.role {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "participant.role"));
        }
        let child_issues = self.actor.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for InvoiceParticipant {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        let child_issues = self.actor.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }
    }

