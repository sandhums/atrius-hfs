// @generated by fhir-validation-gen
// FHIR version: FHIR R5
// shard parts/part_002.rs

static ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdministrableProductDefinition.routeOfAdministration".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdministrableProductDefinition.routeOfAdministration.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/route-codes".to_string(),
        binding_name: Some("SNOMEDCTRouteCodes".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdministrableProductDefinitionRouteOfAdministration {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.first_dose {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.firstDose"));
        }
        if let Some(value) = &self.max_single_dose {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxSingleDose"));
        }
        if let Some(value) = &self.max_dose_per_day {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxDosePerDay"));
        }
        if let Some(value) = &self.max_treatment_period {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxTreatmentPeriod"));
        }
        if let Some(values) = &self.target_species {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("routeOfAdministration.targetSpecies[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_INVARIANTS.as_slice(), evaluator, "AdministrableProductDefinition.routeOfAdministration"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("routeOfAdministration.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("routeOfAdministration.modifierExtension"));
            }
        }
        if let Some(values) = &self.target_species {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.targetSpecies", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("routeOfAdministration.targetSpecies"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("routeOfAdministration.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("routeOfAdministration.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.code"));
        if let Some(value) = &self.first_dose {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.firstDose"));
        }
        if let Some(value) = &self.max_single_dose {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxSingleDose"));
        }
        if let Some(value) = &self.max_dose_per_day {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxDosePerDay"));
        }
        if let Some(value) = &self.max_dose_per_treatment_period {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxDosePerTreatmentPeriod"));
        }
        if let Some(value) = &self.max_treatment_period {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxTreatmentPeriod"));
        }
        if let Some(values) = &self.target_species {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("routeOfAdministration.targetSpecies[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdministrableProductDefinitionRouteOfAdministration {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.first_dose {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.firstDose"));
        }
        if let Some(value) = &self.max_single_dose {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxSingleDose"));
        }
        if let Some(value) = &self.max_dose_per_day {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxDosePerDay"));
        }
        if let Some(value) = &self.max_treatment_period {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "routeOfAdministration.maxTreatmentPeriod"));
        }
        if let Some(values) = &self.target_species {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("routeOfAdministration.targetSpecies[{idx}]")));
            }
        }
        issues
    }
    }

static ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdministrableProductDefinition.routeOfAdministration.targetSpecies".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdministrableProductDefinition.routeOfAdministration.targetSpecies.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/target-species".to_string(),
        binding_name: Some("TargetSpecies".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdministrableProductDefinitionRouteOfAdministrationTargetSpecies {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_BINDINGS.as_slice(), terminology));

        if let Some(values) = &self.withdrawal_period {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("targetSpecies.withdrawalPeriod[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_INVARIANTS.as_slice(), evaluator, "AdministrableProductDefinition.routeOfAdministration.targetSpecies"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.targetSpecies.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("targetSpecies.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.targetSpecies.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("targetSpecies.modifierExtension"));
            }
        }
        if let Some(values) = &self.withdrawal_period {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.targetSpecies.withdrawalPeriod", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("targetSpecies.withdrawalPeriod"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("targetSpecies.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("targetSpecies.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "targetSpecies.code"));
        if let Some(values) = &self.withdrawal_period {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("targetSpecies.withdrawalPeriod[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdministrableProductDefinitionRouteOfAdministrationTargetSpecies {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_BINDINGS.as_slice(), terminology).await);

        if let Some(values) = &self.withdrawal_period {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("targetSpecies.withdrawalPeriod[{idx}]")));
            }
        }
        issues
    }
    }

static ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_WITHDRAWAL_PERIOD_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdministrableProductDefinition.routeOfAdministration.targetSpecies.withdrawalPeriod".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_WITHDRAWAL_PERIOD_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdministrableProductDefinition.routeOfAdministration.targetSpecies.withdrawalPeriod.tissue".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/animal-tissue-type".to_string(),
        binding_name: Some("AnimalTissueType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdministrableProductDefinitionRouteOfAdministrationTargetSpeciesWithdrawalPeriod {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_WITHDRAWAL_PERIOD_BINDINGS.as_slice(), terminology));

        let child_issues = self.value.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "withdrawalPeriod.value"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_WITHDRAWAL_PERIOD_INVARIANTS.as_slice(), evaluator, "AdministrableProductDefinition.routeOfAdministration.targetSpecies.withdrawalPeriod"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.targetSpecies.withdrawalPeriod.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("withdrawalPeriod.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdministrableProductDefinition.routeOfAdministration.targetSpecies.withdrawalPeriod.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("withdrawalPeriod.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("withdrawalPeriod.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("withdrawalPeriod.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.tissue.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "withdrawalPeriod.tissue"));
        let child_issues = self.value.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "withdrawalPeriod.value"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdministrableProductDefinitionRouteOfAdministrationTargetSpeciesWithdrawalPeriod {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADMINISTRABLE_PRODUCT_DEFINITION_ROUTE_OF_ADMINISTRATION_TARGET_SPECIES_WITHDRAWAL_PERIOD_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.value.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "withdrawalPeriod.value"));
        issues
    }
    }

static ADVERSE_EVENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "AdverseEvent".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADVERSE_EVENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-status|5.0.0".to_string(),
        binding_name: Some("AdverseEventStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.actuality".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-actuality|5.0.0".to_string(),
        binding_name: Some("AdverseEventActuality".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-category".to_string(),
        binding_name: Some("AdverseEventCategory".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-type".to_string(),
        binding_name: Some("AdverseEventType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.seriousness".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-seriousness".to_string(),
        binding_name: Some("AdverseEventSeriousness".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.outcome".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-outcome".to_string(),
        binding_name: Some("AdverseEventOutcome".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEvent {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADVERSE_EVENT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.identifier[{idx}]")));
            }
        }
        let child_issues = self.subject.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.subject"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.encounter"));
        }
        if let Some(values) = &self.resulting_effect {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.resultingEffect[{idx}]")));
            }
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.location"));
        }
        if let Some(value) = &self.recorder {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.recorder"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.study {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.study[{idx}]")));
            }
        }
        if let Some(values) = &self.contributing_factor {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.contributingFactor[{idx}]")));
            }
        }
        if let Some(values) = &self.preventive_action {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.preventiveAction[{idx}]")));
            }
        }
        if let Some(values) = &self.mitigating_action {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.mitigatingAction[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_info {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.supportingInfo[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurrence {
            match choice {
                AdverseEventOccurrence::DateTime(_value) => {
                }
                AdverseEventOccurrence::Period(_value) => {
                }
                AdverseEventOccurrence::Timing(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.occurrence[x]"));
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_INVARIANTS.as_slice(), evaluator, "AdverseEvent"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.identifier"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.category"));
            }
        }
        if let Some(values) = &self.resulting_effect {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.resultingEffect", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.resultingEffect"));
            }
        }
        if let Some(values) = &self.outcome {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.outcome", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.outcome"));
            }
        }
        if let Some(values) = &self.participant {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.participant", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.participant"));
            }
        }
        if let Some(values) = &self.study {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.study", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.study"));
            }
        }
        if let Some(values) = &self.suspect_entity {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.suspectEntity", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.suspectEntity"));
            }
        }
        if let Some(values) = &self.contributing_factor {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.contributingFactor", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.contributingFactor"));
            }
        }
        if let Some(values) = &self.preventive_action {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.preventiveAction", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.preventiveAction"));
            }
        }
        if let Some(values) = &self.mitigating_action {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.mitigatingAction", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.mitigatingAction"));
            }
        }
        if let Some(values) = &self.supporting_info {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.supportingInfo", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.supportingInfo"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AdverseEvent.note"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.category[{idx}]")));
            }
        }
        if let Some(value) = &self.code {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.code"));
        }
        let child_issues = self.subject.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.subject"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.encounter"));
        }
        if let Some(values) = &self.resulting_effect {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.resultingEffect[{idx}]")));
            }
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.location"));
        }
        if let Some(value) = &self.seriousness {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.seriousness"));
        }
        if let Some(values) = &self.outcome {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.outcome[{idx}]")));
            }
        }
        if let Some(value) = &self.recorder {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.recorder"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.study {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.study[{idx}]")));
            }
        }
        if let Some(values) = &self.suspect_entity {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.suspectEntity[{idx}]")));
            }
        }
        if let Some(values) = &self.contributing_factor {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.contributingFactor[{idx}]")));
            }
        }
        if let Some(values) = &self.preventive_action {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.preventiveAction[{idx}]")));
            }
        }
        if let Some(values) = &self.mitigating_action {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.mitigatingAction[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_info {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.supportingInfo[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.note[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurrence {
            match choice {
                AdverseEventOccurrence::DateTime(_value) => {
                }
                AdverseEventOccurrence::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.occurrence[x]"));
                }
                AdverseEventOccurrence::Timing(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.occurrence[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEvent {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADVERSE_EVENT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.identifier[{idx}]")));
            }
        }
        let child_issues = self.subject.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.subject"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.encounter"));
        }
        if let Some(values) = &self.resulting_effect {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.resultingEffect[{idx}]")));
            }
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.location"));
        }
        if let Some(value) = &self.recorder {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.recorder"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.study {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.study[{idx}]")));
            }
        }
        if let Some(values) = &self.contributing_factor {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.contributingFactor[{idx}]")));
            }
        }
        if let Some(values) = &self.preventive_action {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.preventiveAction[{idx}]")));
            }
        }
        if let Some(values) = &self.mitigating_action {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.mitigatingAction[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_info {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.supportingInfo[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AdverseEvent.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurrence {
            match choice {
                AdverseEventOccurrence::DateTime(_value) => {
                }
                AdverseEventOccurrence::Period(_value) => {
                }
                AdverseEventOccurrence::Timing(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "AdverseEvent.occurrence[x]"));
                }
            }
        }
        issues
    }
    }

static ADVERSE_EVENT_PARTICIPANT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent.participant".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADVERSE_EVENT_PARTICIPANT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.participant.function".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-participant-function".to_string(),
        binding_name: Some("AdverseEventParticipantFunction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEventParticipant {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADVERSE_EVENT_PARTICIPANT_BINDINGS.as_slice(), terminology));

        let child_issues = self.actor.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_PARTICIPANT_INVARIANTS.as_slice(), evaluator, "AdverseEvent.participant"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.participant.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.participant.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.function {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "participant.function"));
        }
        let child_issues = self.actor.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEventParticipant {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADVERSE_EVENT_PARTICIPANT_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.actor.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }
    }

static ADVERSE_EVENT_SUSPECT_ENTITY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent.suspectEntity".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEventSuspectEntity {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.causality {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "suspectEntity.causality"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.instance {
            match choice {
                AdverseEventSuspectEntityInstance::CodeableConcept(_value) => {
                }
                AdverseEventSuspectEntityInstance::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "suspectEntity.instance[x]"));
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_SUSPECT_ENTITY_INVARIANTS.as_slice(), evaluator, "AdverseEvent.suspectEntity"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.suspectEntity.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("suspectEntity.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.suspectEntity.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("suspectEntity.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("suspectEntity.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("suspectEntity.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.causality {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "suspectEntity.causality"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.instance {
            match choice {
                AdverseEventSuspectEntityInstance::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "suspectEntity.instance[x]"));
                }
                AdverseEventSuspectEntityInstance::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "suspectEntity.instance[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEventSuspectEntity {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.causality {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "suspectEntity.causality"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.instance {
            match choice {
                AdverseEventSuspectEntityInstance::CodeableConcept(_value) => {
                }
                AdverseEventSuspectEntityInstance::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "suspectEntity.instance[x]"));
                }
            }
        }
        issues
    }
    }

static ADVERSE_EVENT_SUSPECT_ENTITY_CAUSALITY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent.suspectEntity.causality".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADVERSE_EVENT_SUSPECT_ENTITY_CAUSALITY_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.suspectEntity.causality.assessmentMethod".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-causality-method".to_string(),
        binding_name: Some("AdverseEventCausalityMethod".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.suspectEntity.causality.entityRelatedness".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-causality-assess".to_string(),
        binding_name: Some("AdverseEventCausalityAssessment".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEventSuspectEntityCausality {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADVERSE_EVENT_SUSPECT_ENTITY_CAUSALITY_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.author {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "causality.author"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_SUSPECT_ENTITY_CAUSALITY_INVARIANTS.as_slice(), evaluator, "AdverseEvent.suspectEntity.causality"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.suspectEntity.causality.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("causality.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.suspectEntity.causality.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("causality.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("causality.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("causality.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.assessment_method {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "causality.assessmentMethod"));
        }
        if let Some(value) = &self.entity_relatedness {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "causality.entityRelatedness"));
        }
        if let Some(value) = &self.author {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "causality.author"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEventSuspectEntityCausality {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADVERSE_EVENT_SUSPECT_ENTITY_CAUSALITY_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.author {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "causality.author"));
        }
        issues
    }
    }

static ADVERSE_EVENT_CONTRIBUTING_FACTOR_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent.contributingFactor".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADVERSE_EVENT_CONTRIBUTING_FACTOR_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.contributingFactor.item[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-contributing-factor".to_string(),
        binding_name: Some("AdverseEventContributingFactor".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["CodeableConcept".to_string()]),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEventContributingFactor {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADVERSE_EVENT_CONTRIBUTING_FACTOR_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventContributingFactorItem::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "contributingFactor.item[x]"));
                }
                AdverseEventContributingFactorItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "AdverseEvent.contributingFactor.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-contributing-factor", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-contributing-factor", value));
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_CONTRIBUTING_FACTOR_INVARIANTS.as_slice(), evaluator, "AdverseEvent.contributingFactor"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.contributingFactor.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("contributingFactor.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.contributingFactor.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("contributingFactor.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contributingFactor.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("contributingFactor.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventContributingFactorItem::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "contributingFactor.item[x]"));
                }
                AdverseEventContributingFactorItem::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "contributingFactor.item[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEventContributingFactor {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADVERSE_EVENT_CONTRIBUTING_FACTOR_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventContributingFactorItem::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "contributingFactor.item[x]"));
                }
                AdverseEventContributingFactorItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "AdverseEvent.contributingFactor.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-contributing-factor", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-contributing-factor", value)).await;
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }
    }

static ADVERSE_EVENT_PREVENTIVE_ACTION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent.preventiveAction".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADVERSE_EVENT_PREVENTIVE_ACTION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.preventiveAction.item[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-preventive-action".to_string(),
        binding_name: Some("AdverseEventPreventiveAction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["CodeableConcept".to_string()]),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEventPreventiveAction {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADVERSE_EVENT_PREVENTIVE_ACTION_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventPreventiveActionItem::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "preventiveAction.item[x]"));
                }
                AdverseEventPreventiveActionItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "AdverseEvent.preventiveAction.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-preventive-action", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-preventive-action", value));
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_PREVENTIVE_ACTION_INVARIANTS.as_slice(), evaluator, "AdverseEvent.preventiveAction"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.preventiveAction.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("preventiveAction.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.preventiveAction.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("preventiveAction.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("preventiveAction.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("preventiveAction.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventPreventiveActionItem::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "preventiveAction.item[x]"));
                }
                AdverseEventPreventiveActionItem::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "preventiveAction.item[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEventPreventiveAction {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADVERSE_EVENT_PREVENTIVE_ACTION_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventPreventiveActionItem::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "preventiveAction.item[x]"));
                }
                AdverseEventPreventiveActionItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "AdverseEvent.preventiveAction.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-preventive-action", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-preventive-action", value)).await;
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }
    }

static ADVERSE_EVENT_MITIGATING_ACTION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent.mitigatingAction".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADVERSE_EVENT_MITIGATING_ACTION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.mitigatingAction.item[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-mitigating-action".to_string(),
        binding_name: Some("AdverseEventMitigatingAction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["CodeableConcept".to_string()]),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEventMitigatingAction {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADVERSE_EVENT_MITIGATING_ACTION_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventMitigatingActionItem::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "mitigatingAction.item[x]"));
                }
                AdverseEventMitigatingActionItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "AdverseEvent.mitigatingAction.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-mitigating-action", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-mitigating-action", value));
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_MITIGATING_ACTION_INVARIANTS.as_slice(), evaluator, "AdverseEvent.mitigatingAction"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.mitigatingAction.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("mitigatingAction.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.mitigatingAction.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("mitigatingAction.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("mitigatingAction.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("mitigatingAction.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventMitigatingActionItem::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "mitigatingAction.item[x]"));
                }
                AdverseEventMitigatingActionItem::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "mitigatingAction.item[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEventMitigatingAction {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADVERSE_EVENT_MITIGATING_ACTION_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventMitigatingActionItem::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "mitigatingAction.item[x]"));
                }
                AdverseEventMitigatingActionItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "AdverseEvent.mitigatingAction.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-mitigating-action", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-mitigating-action", value)).await;
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }
    }

static ADVERSE_EVENT_SUPPORTING_INFO_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AdverseEvent.supportingInfo".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ADVERSE_EVENT_SUPPORTING_INFO_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AdverseEvent.supportingInfo.item[x]".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/adverse-event-supporting-info".to_string(),
        binding_name: Some("AdverseEventSupportingInfo".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Choice,
        choice_type_codes: Some(vec!["CodeableConcept".to_string()]),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AdverseEventSupportingInfo {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ADVERSE_EVENT_SUPPORTING_INFO_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventSupportingInfoItem::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "supportingInfo.item[x]"));
                }
                AdverseEventSupportingInfoItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextSync::new(validator, "AdverseEvent.supportingInfo.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-supporting-info", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-supporting-info", value));
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ADVERSE_EVENT_SUPPORTING_INFO_INVARIANTS.as_slice(), evaluator, "AdverseEvent.supportingInfo"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.supportingInfo.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("supportingInfo.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AdverseEvent.supportingInfo.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("supportingInfo.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("supportingInfo.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("supportingInfo.modifierExtension[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventSupportingInfoItem::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "supportingInfo.item[x]"));
                }
                AdverseEventSupportingInfoItem::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "supportingInfo.item[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AdverseEventSupportingInfo {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ADVERSE_EVENT_SUPPORTING_INFO_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.item {
            match choice {
                AdverseEventSupportingInfoItem::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "supportingInfo.item[x]"));
                }
                AdverseEventSupportingInfoItem::CodeableConcept(value) => {
                    let binding_ctx = fhir_validation::binding::common::BindingCheckContextAsync::new(validator, "AdverseEvent.supportingInfo.item[x]", "http://hl7.org/fhir/ValueSet/adverse-event-supporting-info", fhir_validation_types::BindingStrength::Example, terminology);
                    let child_issues = fhir_validation::r5::validate_codeable_concept_binding_async(&binding_ctx, Some(value), |value| helios_fhir::r5::terminology::validate_codeable_concept("http://hl7.org/fhir/ValueSet/adverse-event-supporting-info", value)).await;
                    issues.extend(child_issues);
                }
            }
        }
        issues
    }
    }

static ALLERGY_INTOLERANCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AllergyIntolerance".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AllergyIntolerance".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AllergyIntolerance".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AllergyIntolerance".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "AllergyIntolerance".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AllergyIntolerance".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ALLERGY_INTOLERANCE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.clinicalStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/allergyintolerance-clinical|5.0.0".to_string(),
        binding_name: Some("AllergyIntoleranceClinicalStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.verificationStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/allergyintolerance-verification|5.0.0".to_string(),
        binding_name: Some("AllergyIntoleranceVerificationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/allergy-intolerance-type".to_string(),
        binding_name: Some("AllergyIntoleranceType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/allergy-intolerance-category|5.0.0".to_string(),
        binding_name: Some("AllergyIntoleranceCategory".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.criticality".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/allergy-intolerance-criticality|5.0.0".to_string(),
        binding_name: Some("AllergyIntoleranceCriticality".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/allergyintolerance-code".to_string(),
        binding_name: Some("AllergyIntoleranceCode".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AllergyIntolerance {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ALLERGY_INTOLERANCE_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.identifier[{idx}]")));
            }
        }
        let child_issues = self.patient.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.patient"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.encounter"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.reaction {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.reaction[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.onset {
            match choice {
                AllergyIntoleranceOnset::DateTime(_value) => {
                }
                AllergyIntoleranceOnset::Age(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.onset[x]"));
                }
                AllergyIntoleranceOnset::Period(_value) => {
                }
                AllergyIntoleranceOnset::Range(_value) => {
                }
                AllergyIntoleranceOnset::String(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ALLERGY_INTOLERANCE_INVARIANTS.as_slice(), evaluator, "AllergyIntolerance"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.identifier"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.category"));
            }
        }
        if let Some(values) = &self.participant {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.participant", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.participant"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.note"));
            }
        }
        if let Some(values) = &self.reaction {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.reaction", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AllergyIntolerance.reaction"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.clinical_status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.clinicalStatus"));
        }
        if let Some(value) = &self.verification_status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.verificationStatus"));
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.type"));
        }
        if let Some(value) = &self.code {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.code"));
        }
        let child_issues = self.patient.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.patient"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.encounter"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.note[{idx}]")));
            }
        }
        if let Some(values) = &self.reaction {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.reaction[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.onset {
            match choice {
                AllergyIntoleranceOnset::DateTime(_value) => {
                }
                AllergyIntoleranceOnset::Age(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.onset[x]"));
                }
                AllergyIntoleranceOnset::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.onset[x]"));
                }
                AllergyIntoleranceOnset::Range(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.onset[x]"));
                }
                AllergyIntoleranceOnset::String(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AllergyIntolerance {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ALLERGY_INTOLERANCE_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.identifier[{idx}]")));
            }
        }
        let child_issues = self.patient.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.patient"));
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.encounter"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.reaction {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.reaction[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AllergyIntolerance.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.onset {
            match choice {
                AllergyIntoleranceOnset::DateTime(_value) => {
                }
                AllergyIntoleranceOnset::Age(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "AllergyIntolerance.onset[x]"));
                }
                AllergyIntoleranceOnset::Period(_value) => {
                }
                AllergyIntoleranceOnset::Range(_value) => {
                }
                AllergyIntoleranceOnset::String(_value) => {
                }
            }
        }
        issues
    }
    }

static ALLERGY_INTOLERANCE_PARTICIPANT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AllergyIntolerance.participant".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ALLERGY_INTOLERANCE_PARTICIPANT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.participant.function".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/participation-role-type".to_string(),
        binding_name: Some("AllergyIntoleranceParticipantFunction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AllergyIntoleranceParticipant {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ALLERGY_INTOLERANCE_PARTICIPANT_BINDINGS.as_slice(), terminology));

        let child_issues = self.actor.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ALLERGY_INTOLERANCE_PARTICIPANT_INVARIANTS.as_slice(), evaluator, "AllergyIntolerance.participant"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.participant.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.participant.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.function {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "participant.function"));
        }
        let child_issues = self.actor.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AllergyIntoleranceParticipant {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ALLERGY_INTOLERANCE_PARTICIPANT_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.actor.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        issues
    }
    }

static ALLERGY_INTOLERANCE_REACTION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AllergyIntolerance.reaction".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ALLERGY_INTOLERANCE_REACTION_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.reaction.substance".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/substance-code".to_string(),
        binding_name: Some("SubstanceCode".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.reaction.manifestation".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/clinical-findings".to_string(),
        binding_name: Some("Manifestation".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.reaction.severity".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/reaction-event-severity|5.0.0".to_string(),
        binding_name: Some("AllergyIntoleranceSeverity".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AllergyIntolerance.reaction.exposureRoute".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/route-codes".to_string(),
        binding_name: Some("RouteOfAdministration".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AllergyIntoleranceReaction {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ALLERGY_INTOLERANCE_REACTION_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ALLERGY_INTOLERANCE_REACTION_INVARIANTS.as_slice(), evaluator, "AllergyIntolerance.reaction"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.reaction.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("reaction.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.reaction.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("reaction.modifierExtension"));
            }
        }
        if let Some(values) = &self.manifestation {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.reaction.manifestation", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("reaction.manifestation"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AllergyIntolerance.reaction.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("reaction.note"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("reaction.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("reaction.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.substance {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "reaction.substance"));
        }
        if let Some(values) = &self.manifestation {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("reaction.manifestation[{idx}]")));
            }
        }
        if let Some(value) = &self.exposure_route {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "reaction.exposureRoute"));
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("reaction.note[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AllergyIntoleranceReaction {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ALLERGY_INTOLERANCE_REACTION_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static APPOINTMENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "app-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "start.exists() = end.exists()".to_string(),
        human: "Either start and end are specified, or neither".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "app-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "(start.exists() and end.exists()) or (status in ('proposed' | 'cancelled' | 'waitlist'))".to_string(),
        human: "Only proposed or cancelled appointments can be missing start/end dates".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "app-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "cancellationReason.exists() implies (status='noshow' or status='cancelled')".to_string(),
        human: "Cancellation reason is only used for appointments that have been cancelled, or noshow".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "app-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "start.exists() implies start <= end".to_string(),
        human: "The start must be less than or equal to the end".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "app-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "Appointment".to_string(),
        expression: "originatingAppointment.exists().not() or recurrenceTemplate.exists().not()".to_string(),
        human: "An appointment may have an originatingAppointment or recurrenceTemplate, but not both".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "app-7".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "cancellationDate.exists() implies (status='noshow' or status='cancelled')".to_string(),
        human: "Cancellation date is only used for appointments that have been cancelled, or noshow".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "Appointment".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static APPOINTMENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Appointment.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/appointmentstatus|5.0.0".to_string(),
        binding_name: Some("AppointmentStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.cancellationReason".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/appointment-cancellation-reason".to_string(),
        binding_name: Some("cancellation-reason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.class".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://terminology.hl7.org/ValueSet/EncounterClass".to_string(),
        binding_name: Some("EncounterClass".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.serviceCategory".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-category".to_string(),
        binding_name: Some("service-category".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.serviceType".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/service-type".to_string(),
        binding_name: Some("service-type".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.specialty".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/c80-practice-codes".to_string(),
        binding_name: Some("specialty".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.appointmentType".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://terminology.hl7.org/ValueSet/v2-0276".to_string(),
        binding_name: Some("appointment-type".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.reason".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/encounter-reason".to_string(),
        binding_name: Some("ApptReason".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableReference,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.priority".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://terminology.hl7.org/ValueSet/v3-ActPriority".to_string(),
        binding_name: Some("Priority".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for Appointment {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, APPOINTMENT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.replaces {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.replaces[{idx}]")));
            }
        }
        if let Some(values) = &self.virtual_service {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.virtualService[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_information {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.supportingInformation[{idx}]")));
            }
        }
        if let Some(value) = &self.previous_appointment {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.previousAppointment"));
        }
        if let Some(value) = &self.originating_appointment {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.originatingAppointment"));
        }
        if let Some(values) = &self.slot {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.slot[{idx}]")));
            }
        }
        if let Some(values) = &self.account {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.account[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.basedOn[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.subject"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.recurrence_template {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.recurrenceTemplate[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, APPOINTMENT_INVARIANTS.as_slice(), evaluator, "Appointment"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.identifier"));
            }
        }
        if let Some(values) = &self.class {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.class", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.class"));
            }
        }
        if let Some(values) = &self.service_category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.serviceCategory", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.serviceCategory"));
            }
        }
        if let Some(values) = &self.service_type {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.serviceType", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.serviceType"));
            }
        }
        if let Some(values) = &self.specialty {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.specialty", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.specialty"));
            }
        }
        if let Some(values) = &self.reason {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.reason", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.reason"));
            }
        }
        if let Some(values) = &self.replaces {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.replaces", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.replaces"));
            }
        }
        if let Some(values) = &self.virtual_service {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.virtualService", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.virtualService"));
            }
        }
        if let Some(values) = &self.supporting_information {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.supportingInformation", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.supportingInformation"));
            }
        }
        if let Some(values) = &self.requested_period {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.requestedPeriod", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.requestedPeriod"));
            }
        }
        if let Some(values) = &self.slot {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.slot", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.slot"));
            }
        }
        if let Some(values) = &self.account {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.account", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.account"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.note"));
            }
        }
        if let Some(values) = &self.patient_instruction {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.patientInstruction", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.patientInstruction"));
            }
        }
        if let Some(values) = &self.based_on {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.basedOn", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.basedOn"));
            }
        }
        if let Some(values) = &self.participant {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.participant", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.participant"));
            }
        }
        if let Some(values) = &self.recurrence_template {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Appointment.recurrenceTemplate"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.cancellation_reason {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.cancellationReason"));
        }
        if let Some(values) = &self.class {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.class[{idx}]")));
            }
        }
        if let Some(values) = &self.service_category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.serviceCategory[{idx}]")));
            }
        }
        if let Some(values) = &self.service_type {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.serviceType[{idx}]")));
            }
        }
        if let Some(values) = &self.specialty {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.specialty[{idx}]")));
            }
        }
        if let Some(value) = &self.appointment_type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.appointmentType"));
        }
        if let Some(values) = &self.reason {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.reason[{idx}]")));
            }
        }
        if let Some(value) = &self.priority {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.priority"));
        }
        if let Some(values) = &self.replaces {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.replaces[{idx}]")));
            }
        }
        if let Some(values) = &self.virtual_service {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.virtualService[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_information {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.supportingInformation[{idx}]")));
            }
        }
        if let Some(value) = &self.previous_appointment {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.previousAppointment"));
        }
        if let Some(value) = &self.originating_appointment {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.originatingAppointment"));
        }
        if let Some(values) = &self.requested_period {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.requestedPeriod[{idx}]")));
            }
        }
        if let Some(values) = &self.slot {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.slot[{idx}]")));
            }
        }
        if let Some(values) = &self.account {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.account[{idx}]")));
            }
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.note[{idx}]")));
            }
        }
        if let Some(values) = &self.patient_instruction {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.patientInstruction[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.basedOn[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.subject"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.recurrence_template {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.recurrenceTemplate[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for Appointment {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, APPOINTMENT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.replaces {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.replaces[{idx}]")));
            }
        }
        if let Some(values) = &self.virtual_service {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.virtualService[{idx}]")));
            }
        }
        if let Some(values) = &self.supporting_information {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.supportingInformation[{idx}]")));
            }
        }
        if let Some(value) = &self.previous_appointment {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.previousAppointment"));
        }
        if let Some(value) = &self.originating_appointment {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.originatingAppointment"));
        }
        if let Some(values) = &self.slot {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.slot[{idx}]")));
            }
        }
        if let Some(values) = &self.account {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.account[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.basedOn[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Appointment.subject"));
        }
        if let Some(values) = &self.participant {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.participant[{idx}]")));
            }
        }
        if let Some(values) = &self.recurrence_template {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.recurrenceTemplate[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Appointment.contained[{idx}]")));
            }
        }
        issues
    }
    }

static APPOINTMENT_PARTICIPANT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "app-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment.participant".to_string(),
        expression: "type.exists() or actor.exists()".to_string(),
        human: "Either the type or actor on the participant SHALL be specified".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment.participant".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static APPOINTMENT_PARTICIPANT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Appointment.participant.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/encounter-participant-type".to_string(),
        binding_name: Some("ParticipantType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.participant.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/participationstatus|5.0.0".to_string(),
        binding_name: Some("ParticipationStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AppointmentParticipant {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, APPOINTMENT_PARTICIPANT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.actor {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, APPOINTMENT_PARTICIPANT_INVARIANTS.as_slice(), evaluator, "Appointment.participant"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.participant.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.participant.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.modifierExtension"));
            }
        }
        if let Some(values) = &self.r#type {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.participant.type", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("participant.type"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.r#type {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("participant.type[{idx}]")));
            }
        }
        if let Some(value) = &self.period {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "participant.period"));
        }
        if let Some(value) = &self.actor {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AppointmentParticipant {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, APPOINTMENT_PARTICIPANT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.actor {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "participant.actor"));
        }
        issues
    }
    }

static APPOINTMENT_RECURRENCE_TEMPLATE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment.recurrenceTemplate".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static APPOINTMENT_RECURRENCE_TEMPLATE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Appointment.recurrenceTemplate.timezone".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/timezones|5.0.0".to_string(),
        binding_name: Some("IANATimezone".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.recurrenceTemplate.recurrenceType".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/appointment-recurrrence-type".to_string(),
        binding_name: Some("AppointmentRecurrenceType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AppointmentRecurrenceTemplate {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, APPOINTMENT_RECURRENCE_TEMPLATE_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.monthly_template {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "recurrenceTemplate.monthlyTemplate"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, APPOINTMENT_RECURRENCE_TEMPLATE_INVARIANTS.as_slice(), evaluator, "Appointment.recurrenceTemplate"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("recurrenceTemplate.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("recurrenceTemplate.modifierExtension"));
            }
        }
        if let Some(values) = &self.occurrence_date {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.occurrenceDate", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("recurrenceTemplate.occurrenceDate"));
            }
        }
        if let Some(values) = &self.excluding_date {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.excludingDate", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("recurrenceTemplate.excludingDate"));
            }
        }
        if let Some(values) = &self.excluding_recurrence_id {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.excludingRecurrenceId", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("recurrenceTemplate.excludingRecurrenceId"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("recurrenceTemplate.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("recurrenceTemplate.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.timezone {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "recurrenceTemplate.timezone"));
        }
        let child_issues = self.recurrence_type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "recurrenceTemplate.recurrenceType"));
        if let Some(value) = &self.weekly_template {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "recurrenceTemplate.weeklyTemplate"));
        }
        if let Some(value) = &self.monthly_template {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "recurrenceTemplate.monthlyTemplate"));
        }
        if let Some(value) = &self.yearly_template {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "recurrenceTemplate.yearlyTemplate"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AppointmentRecurrenceTemplate {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, APPOINTMENT_RECURRENCE_TEMPLATE_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.monthly_template {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "recurrenceTemplate.monthlyTemplate"));
        }
        issues
    }
    }

static APPOINTMENT_RECURRENCE_TEMPLATE_WEEKLY_TEMPLATE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment.recurrenceTemplate.weeklyTemplate".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AppointmentRecurrenceTemplateWeeklyTemplate {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, APPOINTMENT_RECURRENCE_TEMPLATE_WEEKLY_TEMPLATE_INVARIANTS.as_slice(), evaluator, "Appointment.recurrenceTemplate.weeklyTemplate"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.weeklyTemplate.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("weeklyTemplate.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.weeklyTemplate.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("weeklyTemplate.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("weeklyTemplate.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("weeklyTemplate.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AppointmentRecurrenceTemplateWeeklyTemplate {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static APPOINTMENT_RECURRENCE_TEMPLATE_MONTHLY_TEMPLATE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment.recurrenceTemplate.monthlyTemplate".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static APPOINTMENT_RECURRENCE_TEMPLATE_MONTHLY_TEMPLATE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Appointment.recurrenceTemplate.monthlyTemplate.nthWeekOfMonth".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/week-of-month|5.0.0".to_string(),
        binding_name: Some("WeekOfMonth".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Appointment.recurrenceTemplate.monthlyTemplate.dayOfWeek".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/days-of-week|5.0.0".to_string(),
        binding_name: Some("DaysOfWeek".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AppointmentRecurrenceTemplateMonthlyTemplate {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, APPOINTMENT_RECURRENCE_TEMPLATE_MONTHLY_TEMPLATE_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, APPOINTMENT_RECURRENCE_TEMPLATE_MONTHLY_TEMPLATE_INVARIANTS.as_slice(), evaluator, "Appointment.recurrenceTemplate.monthlyTemplate"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.monthlyTemplate.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("monthlyTemplate.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.monthlyTemplate.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("monthlyTemplate.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("monthlyTemplate.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("monthlyTemplate.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.nth_week_of_month {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "monthlyTemplate.nthWeekOfMonth"));
        }
        if let Some(value) = &self.day_of_week {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "monthlyTemplate.dayOfWeek"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AppointmentRecurrenceTemplateMonthlyTemplate {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, APPOINTMENT_RECURRENCE_TEMPLATE_MONTHLY_TEMPLATE_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static APPOINTMENT_RECURRENCE_TEMPLATE_YEARLY_TEMPLATE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Appointment.recurrenceTemplate.yearlyTemplate".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AppointmentRecurrenceTemplateYearlyTemplate {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, APPOINTMENT_RECURRENCE_TEMPLATE_YEARLY_TEMPLATE_INVARIANTS.as_slice(), evaluator, "Appointment.recurrenceTemplate.yearlyTemplate"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.yearlyTemplate.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("yearlyTemplate.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Appointment.recurrenceTemplate.yearlyTemplate.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("yearlyTemplate.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("yearlyTemplate.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("yearlyTemplate.modifierExtension[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AppointmentRecurrenceTemplateYearlyTemplate {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);
        issues
    }
    }

static APPOINTMENT_RESPONSE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "apr-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AppointmentResponse".to_string(),
        expression: "participantType.exists() or actor.exists()".to_string(),
        human: "Either the participantType or actor must be specified".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AppointmentResponse".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AppointmentResponse".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AppointmentResponse".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AppointmentResponse".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "AppointmentResponse".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AppointmentResponse".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static APPOINTMENT_RESPONSE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AppointmentResponse.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AppointmentResponse.participantType".to_string(),
        strength: fhir_validation_types::BindingStrength::Extensible,
        value_set: "http://hl7.org/fhir/ValueSet/encounter-participant-type".to_string(),
        binding_name: Some("ParticipantType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AppointmentResponse.participantStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/appointmentresponse-status|5.0.0".to_string(),
        binding_name: Some("ParticipantStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AppointmentResponse {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, APPOINTMENT_RESPONSE_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.identifier[{idx}]")));
            }
        }
        let child_issues = self.appointment.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.appointment"));
        if let Some(value) = &self.actor {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.actor"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, APPOINTMENT_RESPONSE_INVARIANTS.as_slice(), evaluator, "AppointmentResponse"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AppointmentResponse.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AppointmentResponse.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AppointmentResponse.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AppointmentResponse.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AppointmentResponse.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AppointmentResponse.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AppointmentResponse.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AppointmentResponse.identifier"));
            }
        }
        if let Some(values) = &self.participant_type {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AppointmentResponse.participantType", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AppointmentResponse.participantType"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.identifier[{idx}]")));
            }
        }
        let child_issues = self.appointment.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.appointment"));
        if let Some(values) = &self.participant_type {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.participantType[{idx}]")));
            }
        }
        if let Some(value) = &self.actor {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.actor"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AppointmentResponse {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, APPOINTMENT_RESPONSE_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.identifier[{idx}]")));
            }
        }
        let child_issues = self.appointment.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.appointment"));
        if let Some(value) = &self.actor {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AppointmentResponse.actor"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AppointmentResponse.contained[{idx}]")));
            }
        }
        issues
    }
    }

static ARTIFACT_ASSESSMENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ArtifactAssessment".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ArtifactAssessment".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ArtifactAssessment".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ArtifactAssessment".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "ArtifactAssessment".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ArtifactAssessment".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ARTIFACT_ASSESSMENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ArtifactAssessment.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ArtifactAssessment.workflowStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/artifactassessment-workflow-status|5.0.0".to_string(),
        binding_name: Some("WorkflowStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ArtifactAssessment.disposition".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/artifactassessment-disposition|5.0.0".to_string(),
        binding_name: Some("Disposition".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ArtifactAssessment {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ARTIFACT_ASSESSMENT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.content {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.content[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.cite_as {
            match choice {
                ArtifactAssessmentCiteAs::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.citeAs[x]"));
                }
                ArtifactAssessmentCiteAs::Markdown(_value) => {
                }
            }
        }
        if let Some(choice) = &self.artifact {
            match choice {
                ArtifactAssessmentArtifact::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.artifact[x]"));
                }
                ArtifactAssessmentArtifact::Canonical(_value) => {
                }
                ArtifactAssessmentArtifact::Uri(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ARTIFACT_ASSESSMENT_INVARIANTS.as_slice(), evaluator, "ArtifactAssessment"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ArtifactAssessment.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ArtifactAssessment.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ArtifactAssessment.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ArtifactAssessment.identifier"));
            }
        }
        if let Some(values) = &self.content {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.content", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("ArtifactAssessment.content"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.content {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.content[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.cite_as {
            match choice {
                ArtifactAssessmentCiteAs::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.citeAs[x]"));
                }
                ArtifactAssessmentCiteAs::Markdown(_value) => {
                }
            }
        }
        if let Some(choice) = &self.artifact {
            match choice {
                ArtifactAssessmentArtifact::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.artifact[x]"));
                }
                ArtifactAssessmentArtifact::Canonical(_value) => {
                }
                ArtifactAssessmentArtifact::Uri(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ArtifactAssessment {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ARTIFACT_ASSESSMENT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.content {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.content[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("ArtifactAssessment.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.cite_as {
            match choice {
                ArtifactAssessmentCiteAs::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.citeAs[x]"));
                }
                ArtifactAssessmentCiteAs::Markdown(_value) => {
                }
            }
        }
        if let Some(choice) = &self.artifact {
            match choice {
                ArtifactAssessmentArtifact::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "ArtifactAssessment.artifact[x]"));
                }
                ArtifactAssessmentArtifact::Canonical(_value) => {
                }
                ArtifactAssessmentArtifact::Uri(_value) => {
                }
            }
        }
        issues
    }
    }

static ARTIFACT_ASSESSMENT_CONTENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "ArtifactAssessment.content".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static ARTIFACT_ASSESSMENT_CONTENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "ArtifactAssessment.content.informationType".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/artifactassessment-information-type|5.0.0".to_string(),
        binding_name: Some("InformationType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ArtifactAssessment.content.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/certainty-type".to_string(),
        binding_name: Some("EvidenceCertaintyType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "ArtifactAssessment.content.classifier".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/certainty-rating".to_string(),
        binding_name: Some("EvidenceCertaintyRating".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for ArtifactAssessmentContent {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, ARTIFACT_ASSESSMENT_CONTENT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "content.quantity"));
        }
        if let Some(value) = &self.author {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "content.author"));
        }
        if let Some(values) = &self.related_artifact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("content.relatedArtifact[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, ARTIFACT_ASSESSMENT_CONTENT_INVARIANTS.as_slice(), evaluator, "ArtifactAssessment.content"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.content.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("content.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.content.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("content.modifierExtension"));
            }
        }
        if let Some(values) = &self.classifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.content.classifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("content.classifier"));
            }
        }
        if let Some(values) = &self.path {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.content.path", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("content.path"));
            }
        }
        if let Some(values) = &self.related_artifact {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.content.relatedArtifact", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("content.relatedArtifact"));
            }
        }
        if let Some(values) = &self.component {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "ArtifactAssessment.content.component", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("content.component"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("content.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("content.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "content.type"));
        }
        if let Some(values) = &self.classifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("content.classifier[{idx}]")));
            }
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "content.quantity"));
        }
        if let Some(value) = &self.author {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "content.author"));
        }
        if let Some(values) = &self.related_artifact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("content.relatedArtifact[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for ArtifactAssessmentContent {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, ARTIFACT_ASSESSMENT_CONTENT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "content.quantity"));
        }
        if let Some(value) = &self.author {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "content.author"));
        }
        if let Some(values) = &self.related_artifact {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("content.relatedArtifact[{idx}]")));
            }
        }
        issues
    }
    }

static AUDIT_EVENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "AuditEvent".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static AUDIT_EVENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AuditEvent.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.category".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/audit-event-type".to_string(),
        binding_name: Some("AuditEventType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/audit-event-sub-type".to_string(),
        binding_name: Some("AuditEventSubType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.action".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/audit-event-action|5.0.0".to_string(),
        binding_name: Some("AuditEventAction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.severity".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/audit-event-severity|5.0.0".to_string(),
        binding_name: Some("AuditEventSeverity".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.authorization".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://terminology.hl7.org/ValueSet/v3-PurposeOfUse".to_string(),
        binding_name: Some("AuditPurposeOfUse".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AuditEvent {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, AUDIT_EVENT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.text"));
        }
        if let Some(value) = &self.outcome {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.outcome"));
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.basedOn[{idx}]")));
            }
        }
        if let Some(value) = &self.patient {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.patient"));
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.encounter"));
        }
        if let Some(values) = &self.agent {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.agent[{idx}]")));
            }
        }
        let child_issues = self.source.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.source"));
        if let Some(values) = &self.entity {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.entity[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurred {
            match choice {
                AuditEventOccurred::Period(_value) => {
                }
                AuditEventOccurred::DateTime(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, AUDIT_EVENT_INVARIANTS.as_slice(), evaluator, "AuditEvent"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.modifierExtension"));
            }
        }
        if let Some(values) = &self.category {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.category", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.category"));
            }
        }
        if let Some(values) = &self.authorization {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.authorization", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.authorization"));
            }
        }
        if let Some(values) = &self.based_on {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.basedOn", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.basedOn"));
            }
        }
        if let Some(values) = &self.agent {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.agent", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.agent"));
            }
        }
        if let Some(values) = &self.entity {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("AuditEvent.entity"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.category {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.category[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.code"));
        if let Some(value) = &self.outcome {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.outcome"));
        }
        if let Some(values) = &self.authorization {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.authorization[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.basedOn[{idx}]")));
            }
        }
        if let Some(value) = &self.patient {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.patient"));
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.encounter"));
        }
        if let Some(values) = &self.agent {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.agent[{idx}]")));
            }
        }
        let child_issues = self.source.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.source"));
        if let Some(values) = &self.entity {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.entity[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurred {
            match choice {
                AuditEventOccurred::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.occurred[x]"));
                }
                AuditEventOccurred::DateTime(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AuditEvent {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, AUDIT_EVENT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.text"));
        }
        if let Some(value) = &self.outcome {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.outcome"));
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.basedOn[{idx}]")));
            }
        }
        if let Some(value) = &self.patient {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.patient"));
        }
        if let Some(value) = &self.encounter {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.encounter"));
        }
        if let Some(values) = &self.agent {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.agent[{idx}]")));
            }
        }
        let child_issues = self.source.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "AuditEvent.source"));
        if let Some(values) = &self.entity {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.entity[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("AuditEvent.contained[{idx}]")));
            }
        }
        if let Some(choice) = &self.occurred {
            match choice {
                AuditEventOccurred::Period(_value) => {
                }
                AuditEventOccurred::DateTime(_value) => {
                }
            }
        }
        issues
    }
    }

static AUDIT_EVENT_OUTCOME_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent.outcome".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static AUDIT_EVENT_OUTCOME_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AuditEvent.outcome.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/audit-event-outcome".to_string(),
        binding_name: Some("AuditEventOutcome".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.outcome.detail".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/audit-event-outcome-detail".to_string(),
        binding_name: Some("AuditEventOutcomeDetail".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AuditEventOutcome {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, AUDIT_EVENT_OUTCOME_BINDINGS.as_slice(), terminology));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, AUDIT_EVENT_OUTCOME_INVARIANTS.as_slice(), evaluator, "AuditEvent.outcome"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.outcome.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("outcome.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.outcome.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("outcome.modifierExtension"));
            }
        }
        if let Some(values) = &self.detail {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.outcome.detail", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("outcome.detail"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("outcome.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("outcome.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "outcome.code"));
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("outcome.detail[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AuditEventOutcome {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, AUDIT_EVENT_OUTCOME_BINDINGS.as_slice(), terminology).await);
        issues
    }
    }

static AUDIT_EVENT_AGENT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent.agent".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static AUDIT_EVENT_AGENT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AuditEvent.agent.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/participation-role-type".to_string(),
        binding_name: Some("AuditAgentType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.agent.role".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/security-role-type".to_string(),
        binding_name: Some("AuditAgentRole".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.agent.authorization".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://terminology.hl7.org/ValueSet/v3-PurposeOfUse".to_string(),
        binding_name: Some("AuditPurposeOfUse".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AuditEventAgent {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, AUDIT_EVENT_AGENT_BINDINGS.as_slice(), terminology));

        let child_issues = self.who.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "agent.who"));
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "agent.location"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.network {
            match choice {
                AuditEventAgentNetwork::Reference(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "agent.network[x]"));
                }
                AuditEventAgentNetwork::Uri(_value) => {
                }
                AuditEventAgentNetwork::String(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, AUDIT_EVENT_AGENT_INVARIANTS.as_slice(), evaluator, "AuditEvent.agent"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.agent.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("agent.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.agent.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("agent.modifierExtension"));
            }
        }
        if let Some(values) = &self.role {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.agent.role", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("agent.role"));
            }
        }
        if let Some(values) = &self.policy {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.agent.policy", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("agent.policy"));
            }
        }
        if let Some(values) = &self.authorization {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.agent.authorization", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("agent.authorization"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("agent.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("agent.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.r#type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "agent.type"));
        }
        if let Some(values) = &self.role {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("agent.role[{idx}]")));
            }
        }
        let child_issues = self.who.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "agent.who"));
        if let Some(value) = &self.location {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "agent.location"));
        }
        if let Some(values) = &self.authorization {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("agent.authorization[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.network {
            match choice {
                AuditEventAgentNetwork::Reference(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "agent.network[x]"));
                }
                AuditEventAgentNetwork::Uri(_value) => {
                }
                AuditEventAgentNetwork::String(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AuditEventAgent {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, AUDIT_EVENT_AGENT_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.who.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "agent.who"));
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "agent.location"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.network {
            match choice {
                AuditEventAgentNetwork::Reference(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "agent.network[x]"));
                }
                AuditEventAgentNetwork::Uri(_value) => {
                }
                AuditEventAgentNetwork::String(_value) => {
                }
            }
        }
        issues
    }
    }

static AUDIT_EVENT_SOURCE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent.source".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static AUDIT_EVENT_SOURCE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AuditEvent.source.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Preferred,
        value_set: "http://hl7.org/fhir/ValueSet/security-source-type".to_string(),
        binding_name: Some("AuditEventSourceType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AuditEventSource {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, AUDIT_EVENT_SOURCE_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.site {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "source.site"));
        }
        let child_issues = self.observer.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "source.observer"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, AUDIT_EVENT_SOURCE_INVARIANTS.as_slice(), evaluator, "AuditEvent.source"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.source.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("source.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.source.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("source.modifierExtension"));
            }
        }
        if let Some(values) = &self.r#type {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.source.type", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("source.type"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("source.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("source.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.site {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "source.site"));
        }
        let child_issues = self.observer.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "source.observer"));
        if let Some(values) = &self.r#type {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("source.type[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AuditEventSource {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, AUDIT_EVENT_SOURCE_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.site {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "source.site"));
        }
        let child_issues = self.observer.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "source.observer"));
        issues
    }
    }

static AUDIT_EVENT_ENTITY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent.entity".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static AUDIT_EVENT_ENTITY_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AuditEvent.entity.role".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/object-role".to_string(),
        binding_name: Some("AuditEventEntityRole".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "AuditEvent.entity.securityLabel".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/security-label-examples".to_string(),
        binding_name: Some("SecurityLabels".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AuditEventEntity {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, AUDIT_EVENT_ENTITY_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.what {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "entity.what"));
        }
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("entity.detail[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, AUDIT_EVENT_ENTITY_INVARIANTS.as_slice(), evaluator, "AuditEvent.entity"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("entity.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("entity.modifierExtension"));
            }
        }
        if let Some(values) = &self.security_label {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity.securityLabel", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("entity.securityLabel"));
            }
        }
        if let Some(values) = &self.detail {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity.detail", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("entity.detail"));
            }
        }
        if let Some(values) = &self.agent {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity.agent", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("entity.agent"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("entity.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("entity.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.what {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "entity.what"));
        }
        if let Some(value) = &self.role {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "entity.role"));
        }
        if let Some(values) = &self.security_label {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("entity.securityLabel[{idx}]")));
            }
        }
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("entity.detail[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AuditEventEntity {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, AUDIT_EVENT_ENTITY_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.what {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "entity.what"));
        }
        if let Some(values) = &self.detail {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("entity.detail[{idx}]")));
            }
        }
        issues
    }
    }

static AUDIT_EVENT_ENTITY_DETAIL_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "AuditEvent.entity.detail".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static AUDIT_EVENT_ENTITY_DETAIL_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "AuditEvent.entity.detail.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/audit-event-type".to_string(),
        binding_name: Some("AuditEventDetailType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for AuditEventEntityDetail {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, AUDIT_EVENT_ENTITY_DETAIL_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                AuditEventEntityDetailValue::Quantity(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "detail.value[x]"));
                }
                AuditEventEntityDetailValue::CodeableConcept(_value) => {
                }
                AuditEventEntityDetailValue::String(_value) => {
                }
                AuditEventEntityDetailValue::Boolean(_value) => {
                }
                AuditEventEntityDetailValue::Integer(_value) => {
                }
                AuditEventEntityDetailValue::Range(_value) => {
                }
                AuditEventEntityDetailValue::Ratio(_value) => {
                }
                AuditEventEntityDetailValue::Time(_value) => {
                }
                AuditEventEntityDetailValue::DateTime(_value) => {
                }
                AuditEventEntityDetailValue::Period(_value) => {
                }
                AuditEventEntityDetailValue::Base64Binary(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, AUDIT_EVENT_ENTITY_DETAIL_INVARIANTS.as_slice(), evaluator, "AuditEvent.entity.detail"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity.detail.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "AuditEvent.entity.detail.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("detail.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("detail.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "detail.type"));

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                AuditEventEntityDetailValue::Quantity(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "detail.value[x]"));
                }
                AuditEventEntityDetailValue::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "detail.value[x]"));
                }
                AuditEventEntityDetailValue::String(_value) => {
                }
                AuditEventEntityDetailValue::Boolean(_value) => {
                }
                AuditEventEntityDetailValue::Integer(_value) => {
                }
                AuditEventEntityDetailValue::Range(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "detail.value[x]"));
                }
                AuditEventEntityDetailValue::Ratio(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "detail.value[x]"));
                }
                AuditEventEntityDetailValue::Time(_value) => {
                }
                AuditEventEntityDetailValue::DateTime(_value) => {
                }
                AuditEventEntityDetailValue::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "detail.value[x]"));
                }
                AuditEventEntityDetailValue::Base64Binary(_value) => {
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for AuditEventEntityDetail {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, AUDIT_EVENT_ENTITY_DETAIL_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                AuditEventEntityDetailValue::Quantity(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "detail.value[x]"));
                }
                AuditEventEntityDetailValue::CodeableConcept(_value) => {
                }
                AuditEventEntityDetailValue::String(_value) => {
                }
                AuditEventEntityDetailValue::Boolean(_value) => {
                }
                AuditEventEntityDetailValue::Integer(_value) => {
                }
                AuditEventEntityDetailValue::Range(_value) => {
                }
                AuditEventEntityDetailValue::Ratio(_value) => {
                }
                AuditEventEntityDetailValue::Time(_value) => {
                }
                AuditEventEntityDetailValue::DateTime(_value) => {
                }
                AuditEventEntityDetailValue::Period(_value) => {
                }
                AuditEventEntityDetailValue::Base64Binary(_value) => {
                }
            }
        }
        issues
    }
    }

static BASIC_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Basic".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Basic".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Basic".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Basic".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "Basic".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Basic".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static BASIC_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Basic.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Basic.code".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/basic-resource-type".to_string(),
        binding_name: Some("BasicResourceType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for Basic {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, BASIC_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.subject"));
        }
        if let Some(value) = &self.author {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.author"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, BASIC_INVARIANTS.as_slice(), evaluator, "Basic"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Basic.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Basic.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Basic.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Basic.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Basic.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Basic.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "Basic.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("Basic.identifier"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.identifier[{idx}]")));
            }
        }
        let child_issues = self.code.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "Basic.code"));
        if let Some(value) = &self.subject {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.subject"));
        }
        if let Some(value) = &self.author {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.author"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for Basic {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, BASIC_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.subject {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.subject"));
        }
        if let Some(value) = &self.author {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Basic.author"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("Basic.contained[{idx}]")));
            }
        }
        issues
    }
    }

static BINARY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "Binary".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static BINARY_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "Binary.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "Binary.contentType".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/mimetypes|5.0.0".to_string(),
        binding_name: Some("MimeType".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for Binary {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, BINARY_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Binary.meta"));
        }
        if let Some(value) = &self.security_context {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "Binary.securityContext"));
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, BINARY_INVARIANTS.as_slice(), evaluator, "Binary"));


        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Binary.meta"));
        }
        if let Some(value) = &self.security_context {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "Binary.securityContext"));
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for Binary {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, BINARY_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Binary.meta"));
        }
        if let Some(value) = &self.security_context {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "Binary.securityContext"));
        }
        issues
    }
    }

static BIOLOGICALLY_DERIVED_PRODUCT_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProduct".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProduct".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProduct".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProduct".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "BiologicallyDerivedProduct".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProduct".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static BIOLOGICALLY_DERIVED_PRODUCT_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProduct.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProduct.productCategory".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/product-category".to_string(),
        binding_name: Some("BiologicallyDerivedProductCategory".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProduct.productCode".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/biologicallyderived-productcodes".to_string(),
        binding_name: Some("BiologicallyDerivedProductCodes".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProduct.productStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/biologicallyderived-product-status".to_string(),
        binding_name: Some("BiologicallyDerivedProductStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Coding,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for BiologicallyDerivedProduct {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, BIOLOGICALLY_DERIVED_PRODUCT_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.text"));
        }
        if let Some(values) = &self.parent {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.parent[{idx}]")));
            }
        }
        if let Some(values) = &self.request {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.request[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.biological_source_event {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.biologicalSourceEvent"));
        }
        if let Some(values) = &self.processing_facility {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.processingFacility[{idx}]")));
            }
        }
        if let Some(values) = &self.property {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.property[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, BIOLOGICALLY_DERIVED_PRODUCT_INVARIANTS.as_slice(), evaluator, "BiologicallyDerivedProduct"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.modifierExtension"));
            }
        }
        if let Some(values) = &self.parent {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.parent", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.parent"));
            }
        }
        if let Some(values) = &self.request {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.request", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.request"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.identifier"));
            }
        }
        if let Some(values) = &self.processing_facility {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.processingFacility", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.processingFacility"));
            }
        }
        if let Some(values) = &self.property {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.property", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProduct.property"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.product_category {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.productCategory"));
        }
        if let Some(value) = &self.product_code {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.productCode"));
        }
        if let Some(values) = &self.parent {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.parent[{idx}]")));
            }
        }
        if let Some(values) = &self.request {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.request[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.biological_source_event {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.biologicalSourceEvent"));
        }
        if let Some(values) = &self.processing_facility {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.processingFacility[{idx}]")));
            }
        }
        if let Some(value) = &self.product_status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.productStatus"));
        }
        if let Some(value) = &self.collection {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.collection"));
        }
        if let Some(value) = &self.storage_temp_requirements {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.storageTempRequirements"));
        }
        if let Some(values) = &self.property {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.property[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for BiologicallyDerivedProduct {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, BIOLOGICALLY_DERIVED_PRODUCT_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.text"));
        }
        if let Some(values) = &self.parent {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.parent[{idx}]")));
            }
        }
        if let Some(values) = &self.request {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.request[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.identifier[{idx}]")));
            }
        }
        if let Some(value) = &self.biological_source_event {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProduct.biologicalSourceEvent"));
        }
        if let Some(values) = &self.processing_facility {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.processingFacility[{idx}]")));
            }
        }
        if let Some(values) = &self.property {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.property[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProduct.contained[{idx}]")));
            }
        }
        issues
    }
    }

static BIOLOGICALLY_DERIVED_PRODUCT_COLLECTION_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProduct.collection".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for BiologicallyDerivedProductCollection {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.collector {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "collection.collector"));
        }
        if let Some(value) = &self.source {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "collection.source"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.collected {
            match choice {
                BiologicallyDerivedProductCollectionCollected::DateTime(_value) => {
                }
                BiologicallyDerivedProductCollectionCollected::Period(_value) => {
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, BIOLOGICALLY_DERIVED_PRODUCT_COLLECTION_INVARIANTS.as_slice(), evaluator, "BiologicallyDerivedProduct.collection"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.collection.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("collection.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.collection.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("collection.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("collection.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("collection.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.collector {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "collection.collector"));
        }
        if let Some(value) = &self.source {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "collection.source"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.collected {
            match choice {
                BiologicallyDerivedProductCollectionCollected::DateTime(_value) => {
                }
                BiologicallyDerivedProductCollectionCollected::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "collection.collected[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for BiologicallyDerivedProductCollection {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        let _ = (validator, terminology);

        if let Some(value) = &self.collector {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "collection.collector"));
        }
        if let Some(value) = &self.source {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "collection.source"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.collected {
            match choice {
                BiologicallyDerivedProductCollectionCollected::DateTime(_value) => {
                }
                BiologicallyDerivedProductCollectionCollected::Period(_value) => {
                }
            }
        }
        issues
    }
    }

static BIOLOGICALLY_DERIVED_PRODUCT_PROPERTY_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProduct.property".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static BIOLOGICALLY_DERIVED_PRODUCT_PROPERTY_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProduct.property.type".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/biologicallyderived-product-property-type-codes".to_string(),
        binding_name: Some("BiologicallyDerivedProductPropertyTypeCodes".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for BiologicallyDerivedProductProperty {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, BIOLOGICALLY_DERIVED_PRODUCT_PROPERTY_BINDINGS.as_slice(), terminology));


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                BiologicallyDerivedProductPropertyValue::Boolean(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Integer(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::CodeableConcept(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Period(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Quantity(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                BiologicallyDerivedProductPropertyValue::Range(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Ratio(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::String(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Attachment(value) => {
                    let child_issues = value.validate_bindings(validator, terminology);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, BIOLOGICALLY_DERIVED_PRODUCT_PROPERTY_INVARIANTS.as_slice(), evaluator, "BiologicallyDerivedProduct.property"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.property.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("property.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProduct.property.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("property.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("property.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("property.modifierExtension[{idx}]")));
            }
        }
        let child_issues = self.r#type.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "property.type"));

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                BiologicallyDerivedProductPropertyValue::Boolean(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Integer(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::CodeableConcept(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                BiologicallyDerivedProductPropertyValue::Period(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                BiologicallyDerivedProductPropertyValue::Quantity(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                BiologicallyDerivedProductPropertyValue::Range(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                BiologicallyDerivedProductPropertyValue::Ratio(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                BiologicallyDerivedProductPropertyValue::String(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Attachment(value) => {
                    let child_issues = value.validate_invariants(validator, evaluator);
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for BiologicallyDerivedProductProperty {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, BIOLOGICALLY_DERIVED_PRODUCT_PROPERTY_BINDINGS.as_slice(), terminology).await);


        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(choice) = &self.value {
            match choice {
                BiologicallyDerivedProductPropertyValue::Boolean(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Integer(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::CodeableConcept(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Period(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Quantity(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
                BiologicallyDerivedProductPropertyValue::Range(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Ratio(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::String(_value) => {
                }
                BiologicallyDerivedProductPropertyValue::Attachment(value) => {
                    let child_issues = value.validate_bindings_async(validator, terminology).await;
                    issues.extend(validator.rebase_instance_paths(child_issues, "property.value[x]"));
                }
            }
        }
        issues
    }
    }

static BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "dom-2".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProductDispense".to_string(),
        expression: "contained.contained.empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL NOT contain nested Resources".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-3".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProductDispense".to_string(),
        expression: "contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().ofType(canonical) | %resource.descendants().ofType(uri) | %resource.descendants().ofType(url))) or descendants().where(reference = '#').exists() or descendants().where(ofType(canonical) = '#').exists() or descendants().where(ofType(canonical) = '#').exists()).not()).trace('unmatched', id).empty()".to_string(),
        human: "If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-4".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProductDispense".to_string(),
        expression: "contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-5".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProductDispense".to_string(),
        expression: "contained.meta.security.empty()".to_string(),
        human: "If a resource is contained in another resource, it SHALL NOT have a security label".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "dom-6".to_string(),
        severity: fhir_validation_types::Severity::Warning,
        path: "BiologicallyDerivedProductDispense".to_string(),
        expression: "text.`div`.exists()".to_string(),
        human: "A resource should have narrative for robust management".to_string(),
    },
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProductDispense".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProductDispense.language".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/all-languages|5.0.0".to_string(),
        binding_name: Some("Language".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProductDispense.status".to_string(),
        strength: fhir_validation_types::BindingStrength::Required,
        value_set: "http://hl7.org/fhir/ValueSet/biologicallyderivedproductdispense-status|5.0.0".to_string(),
        binding_name: Some("BiologicallyDerivedProductDispenseStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::Code,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProductDispense.originRelationshipType".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/biologicallyderivedproductdispense-origin-relationship".to_string(),
        binding_name: Some("BiologicallyDerivedProductDispenseOriginRelationship".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProductDispense.matchStatus".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/biologicallyderivedproductdispense-match-status".to_string(),
        binding_name: Some("BiologicallyDerivedProductDispenseMatchStatus".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for BiologicallyDerivedProductDispense {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_BINDINGS.as_slice(), terminology));

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.part_of {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.partOf[{idx}]")));
            }
        }
        let child_issues = self.product.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.product"));
        let child_issues = self.patient.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.patient"));
        if let Some(values) = &self.performer {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings(validator, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.performer[{idx}]")));
            }
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.location"));
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.quantity"));
        }
        if let Some(value) = &self.destination {
            let child_issues = value.validate_bindings(validator, terminology);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.destination"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings(value, terminology);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.contained[{idx}]")));
            }
        }
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_INVARIANTS.as_slice(), evaluator, "BiologicallyDerivedProductDispense"));

        if let Some(values) = &self.contained {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.contained", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.contained"));
            }
        }
        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.modifierExtension"));
            }
        }
        if let Some(values) = &self.identifier {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.identifier", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.identifier"));
            }
        }
        if let Some(values) = &self.based_on {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.basedOn", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.basedOn"));
            }
        }
        if let Some(values) = &self.part_of {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.partOf", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.partOf"));
            }
        }
        if let Some(values) = &self.performer {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.performer", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.performer"));
            }
        }
        if let Some(values) = &self.note {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.note", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("BiologicallyDerivedProductDispense.note"));
            }
        }

        if let Some(value) = &self.meta {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.text"));
        }
        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.modifierExtension[{idx}]")));
            }
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.part_of {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.partOf[{idx}]")));
            }
        }
        if let Some(value) = &self.origin_relationship_type {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.originRelationshipType"));
        }
        let child_issues = self.product.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.product"));
        let child_issues = self.patient.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.patient"));
        if let Some(value) = &self.match_status {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.matchStatus"));
        }
        if let Some(values) = &self.performer {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.performer[{idx}]")));
            }
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.location"));
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.quantity"));
        }
        if let Some(value) = &self.destination {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.destination"));
        }
        if let Some(values) = &self.note {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.note[{idx}]")));
            }
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_invariants(value, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.contained[{idx}]")));
            }
        }
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for BiologicallyDerivedProductDispense {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_BINDINGS.as_slice(), terminology).await);

        if let Some(value) = &self.meta {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.meta"));
        }
        if let Some(value) = &self.text {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.text"));
        }
        if let Some(values) = &self.identifier {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.identifier[{idx}]")));
            }
        }
        if let Some(values) = &self.based_on {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.basedOn[{idx}]")));
            }
        }
        if let Some(values) = &self.part_of {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.partOf[{idx}]")));
            }
        }
        let child_issues = self.product.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.product"));
        let child_issues = self.patient.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.patient"));
        if let Some(values) = &self.performer {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_bindings_async(validator, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.performer[{idx}]")));
            }
        }
        if let Some(value) = &self.location {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.location"));
        }
        if let Some(value) = &self.quantity {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.quantity"));
        }
        if let Some(value) = &self.destination {
            let child_issues = value.validate_bindings_async(validator, terminology).await;
            issues.extend(validator.rebase_instance_paths(child_issues, "BiologicallyDerivedProductDispense.destination"));
        }

        // Deferred recursive validation candidates.
        // These need specialized handling (for example choice enums or contained resources).
        if let Some(values) = &self.contained {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = validator.validate_r5_resource_bindings_async(value, terminology).await;
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("BiologicallyDerivedProductDispense.contained[{idx}]")));
            }
        }
        issues
    }
    }

static BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_PERFORMER_INVARIANTS: std::sync::LazyLock<Vec<fhir_validation_types::InvariantDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::InvariantDef {
        key: "ele-1".to_string(),
        severity: fhir_validation_types::Severity::Error,
        path: "BiologicallyDerivedProductDispense.performer".to_string(),
        expression: "hasValue() or (children().count() > id.count())".to_string(),
        human: "All FHIR elements must have a @value or children".to_string(),
    },
]);

static BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_PERFORMER_BINDINGS: std::sync::LazyLock<Vec<fhir_validation_types::BindingDef>> = std::sync::LazyLock::new(|| vec![
    fhir_validation_types::BindingDef {
        path: "BiologicallyDerivedProductDispense.performer.function".to_string(),
        strength: fhir_validation_types::BindingStrength::Example,
        value_set: "http://hl7.org/fhir/ValueSet/biologicallyderivedproductdispense-performer-function".to_string(),
        binding_name: Some("BiologicallyDerivedProductDispensPerformerFunction".to_string()),
        target_kind: fhir_validation_types::BindingTargetKind::CodeableConcept,
        choice_type_codes: None,
    },
]);

#[cfg(feature = "R5")]
impl fhir_validation::r5::R5Validatable for BiologicallyDerivedProductDispensePerformer {
    fn validate_bindings(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyServiceSync>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings(self, BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_PERFORMER_BINDINGS.as_slice(), terminology));

        let child_issues = self.actor.validate_bindings(validator, terminology);
        issues.extend(validator.rebase_instance_paths(child_issues, "performer.actor"));
        issues
    }

    fn validate_invariants(
        &self,
        validator: &fhir_validation::Validator,
        evaluator: &dyn fhir_validation::FhirPathEvaluator,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_invariants(self, BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_PERFORMER_INVARIANTS.as_slice(), evaluator, "BiologicallyDerivedProductDispense.performer"));

        if let Some(values) = &self.extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.performer.extension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("performer.extension"));
            }
        }
        if let Some(values) = &self.modifier_extension {
            if values.is_empty() {
                issues.push(fhir_validation::ValidationIssue::error("structure", "BiologicallyDerivedProductDispense.performer.modifierExtension", "Array cannot be empty - the property should not be present if it has no values").with_instance_path("performer.modifierExtension"));
            }
        }

        if let Some(values) = &self.extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("performer.extension[{idx}]")));
            }
        }
        if let Some(values) = &self.modifier_extension {
            for (idx, value) in values.iter().enumerate() {
                let child_issues = value.validate_invariants(validator, evaluator);
                issues.extend(validator.rebase_instance_paths(child_issues, &format!("performer.modifierExtension[{idx}]")));
            }
        }
        if let Some(value) = &self.function {
            let child_issues = value.validate_invariants(validator, evaluator);
            issues.extend(validator.rebase_instance_paths(child_issues, "performer.function"));
        }
        let child_issues = self.actor.validate_invariants(validator, evaluator);
        issues.extend(validator.rebase_instance_paths(child_issues, "performer.actor"));
        issues
    }
}

#[cfg(feature = "R5")]
#[async_trait::async_trait]
impl fhir_validation::r5::R5ValidatableAsync for BiologicallyDerivedProductDispensePerformer {
    async fn validate_bindings_async(
        &self,
        validator: &fhir_validation::Validator,
        terminology: Option<&dyn fhir_validation::TerminologyService>,
    ) -> Vec<fhir_validation::ValidationIssue> {
        let mut issues = Vec::new();
        issues.extend(validator.apply_r5_bindings_async(self, BIOLOGICALLY_DERIVED_PRODUCT_DISPENSE_PERFORMER_BINDINGS.as_slice(), terminology).await);

        let child_issues = self.actor.validate_bindings_async(validator, terminology).await;
        issues.extend(validator.rebase_instance_paths(child_issues, "performer.actor"));
        issues
    }
    }

